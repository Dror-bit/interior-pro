# Interior Pro — מסירה 2026-09-05 (הגגון קיבל את הגג של הבית)

קרא את זה קודם, ואז `CLAUDE.md`. הקודמת: `HANDOFF_2026-09-04.md`.
**אל תסמוך על הסיכום — תאמת מול הקוד החי.**

ענף `door-stabilize`. בדיקות: `cd tests` ואז
`LANG=C.UTF-8 bash run_all.sh ..` → **161 ירוקות**.
**rt51/rt52 אדומות ולא קשורות:** `tests/rt51_tmp/a (2).jpg` נכנס בטעות
ל-git. למחוק אותו מ-git ב-CMD שלו וזה ייפתר.

**הרצת הבדיקות ברקע** (כל קריאת shell מוגבלת ל-45 שניות, הריצה ~90):
`(setsid env LANG=C.UTF-8 bash run_all.sh .. > /tmp/run.log 2>&1 < /dev/null &)`
ואז לקרוא את הלוג בקריאה נפרדת. `nohup ... &` לבדו נהרג באמצע.

**מלכודת git שעלתה כאן שלוש פעמים:** `tests/dormer_manager.rb` הוא
gitignored ולא במעקב, ו-`git add` עליו מפיל את כל השורה — שלושה
קומיטים לא עברו בלי שאיש שם לב. להוסיף רק את `dormer_manager.rb`,
`roof_manager.rb`, `roof_tile_place.rb` ו-`tests/rt*.rb`.

---

## מה נסגר היום

### 1. הגגון לובש את הגג של הבית (rt109)
`DormerManager.place_tiles!`, נקרא מ-`add_dormer!` — כלומר גם בהצבה,
גם ב-Edit, גם ב-Move וגם בשתילה מחדש אחרי בניית גג. הרעפים והבר
נכנסים **לתוך תת-הקבוצה `dormer_roof`**, אז הם נוסעים עם הגגון
בהזזה ונמחקים איתו. `no_tiles: true` מחזיר גגון חשוף.
`top_skin` בוחר את הפאה העליונה **לפי מיקום ולא לפי נורמל** — החוק
ששילמנו עליו פעמיים.

### 2. רידג' קאפ על הגגון (rt110)
`place_ridge_cap!` מרים את שתי הפאות למרחב אחד (`CapFace`) ומזין
ל-`RoofManager.ridge_lines` + `build_ridge_caps!`, אז הקאפ נכנס לתוך
קבוצת הגגון. **גם בשינגלס** — הקאפ יושב מחוץ לתנאי `runs?`, כי
לשינגלס אין שדה רעפים אבל יש רכס.

### 3. הקאפ לא נגמר על מחט, וההיפ שומר את האלכסונים (rt110)
`trim_cap_lines` מושך פנימה קצה **רק אם הוא נכנס לתוך גג הבית**
(`die_in?`, לפי z מול מישור הגג ששמור על הגגון). הניסיון הראשון משך
את שני הקצוות ואכל את הקאפים על שני האלכסונים של ההיפ.
**`s` רץ לאורך `into`, לא לאורך `along`** — ראה `at_lambda`. קריאה
של `along` החזירה false בכל מקום.

### 4. אין זנב רעף בפינה האחורית (rt111)
מדרון של גגון הוא טרפז שנסגר לאפס מאחור, אז קו הסים האחרון חוצה
רק שבב. נמדד: חמישה רעפים באורך 26.87" ואחד באורך 4.00". `stub_len`
= רוחב הרידג' קאפ, ולא פחות מהרצפה של המניח.

### 5. הבר לא עובר את המטל-אדג' (rt111)
`eave_bar_slots` מקבל `u_range`; `metal_edge_u` מודד את ה-`dormer_drip`
עצמו ומקרין אותו למישור. נמדד: הבר נגמר ב-92.36 מול מטל-אדג' ב-90.80.

### 6. מחיקה סוגרת כל חור וכל קו (heal_box!)
`close_loop!` הוצא מ-`heal_roof!` כדי שאפשר יהיה להריץ אותו שוב.
`remove_dormer!` שומר את טביעת הרגל **לפני** המחיקה ואז מריץ
`heal_box!` — כל לולאה פנימית שנשארה בתוכה נסגרת, וכל קצה בלי פאות
נמחק. נמדד אצלו: שלושה גגונים מול ארבעה חורים, וארבעה קצוות עם
**אפס** פאות (מחיקת פאה לא לוקחת את הקצוות שלה).
`fix_orphan_holes.rb` בשורש מנקה מודל קיים — ואז חייב `relay_runs!`,
אחרת הפאנלים נשארים עם החור.

### 7. עריכה שומרת חומרים (rt112)
`dormer_spec` שומר **מספרים בלבד** — החומרים הגיעו מ-`place_on_roof!`.
`replace_dormer!` עוקף אותו, אז Edit החזיר גגון עם גג חשוף וקירות
לבנים. עכשיו הוא שואל את הגגון שעומד שם לפני שהוא נמחק.

### 8. קאפ לא שוקע לתוך הגג (rt110)
בצומת, קאפ מכוסה יורד `RIDGE_CAP_THICK` כדי לעבור מתחת לשכן. קאפ
מתכת מורם נושא **חצאית בגובה ההרמה שלו**, אז הרים שלו כבר נוגע בגג
ואין לו לאן לרדת. נמדד: שני האלכסונים 0.45" מתחת לפני הגג.
עכשיו הוא יורד רק כמה שיש לו רווח.

### 9. הרידג' קאפ של הרומן — חצי עיגול אמיתי (rt113)
הקשת נבנתה עם קראון של 1.42" על קאפ ברוחב 13", והחוליות הונחו 15"
כל 12" — החפיפה היא שציירה את הקו הישר לאורך. עכשיו:
הקראון = חצי הרוחב, החוליות **צמודות עם רווח של 1"** (`cap_gap`,
מתודה), בלי head lift, ובלי הליפט של 4.17" — חצי עיגול של 6.5"
מכסה את הרעף לבדו, והליפט היה רק שני קירות בין הקשת לגג.

---

## מה נשאר פתוח

1. **פס של 0.85" בין קצה הרעפים לרייק** — הפשיה (0.75) והמטל-אדג'
   (0.10) ממשיכים מעבר ללוח ויושבים נמוך יותר, אז הם נראים כפס
   מעבר לרעפים. הרעפים על רשת קבועה של 10", ואין רעף שלם שנכנס
   ל-0.85". שתי דרכים אמיתיות: להזיז את כל השדה החוצה (ואז הפס
   עובר לצד השני), או להכניס את הפשיה והמטל-אדג' פנימה.
   **הוא אמר במפורש: "עזוב תשאיר את זה ככה".** לא לגעת בלי שיבקש.
   `tests/rt114.rb` שמור כמקום ריק עם ההסבר, לא רשום ב-run_all.sh.
2. **`relay_runs!` מזין למניח גם את הפאה התחתונה** — הפילטר הוא
   `f.normal.z > 0.2` ונורמל לא אמין. הפתרון: לפי מיקום, כמו
   `top_skin` בגגון.
3. **`fetch_or_build` סומך על השם.** קומפוננט ישן במודל מנצח את
   הקוד. חותמת `build_id` על הדפיניציה.
4. **הריב נמדד בעמודה אחת ומצויר באחרת — 3⅝"**. הוא ביקש לא לגעת.
5. **rt51/rt52** — למחוק את `tests/rt51_tmp/a (2).jpg` מ-git.

---

## הצעד הבא, בסדר שהוא קבע

1. **עריכה: עומק מהפשיה / מצב מיקום לא עובד.** הוא דיווח ולא נבדק
   עדיין: אם הוא קובע בעריכה עומק מהפשיה או כל מצב אחר, זה לא
   נתפס. וכפתור ההזזה צריך ללכת לפי המצב שהוגדר — אם הוגדר 60"
   מהפשיה, הגגון יזוז **רק על הקו הזה**, וחופשי רק אם המצב הוא
   קליק חופשי. `place_mode` ו-`click_spec` ב-`dormer_manager.rb`,
   והכלים ב-`dormer_edit_tools.rb`.
2. **גגון שטוח נעלם כששמים אותו על גג ברזל ואז מחליפים לשינגלס.**
   דווח ולא נבדק. כל שאר הגגות נשארים. חשד: `replant!` /
   `harvest` מול `frame` שחוזר nil לסגנון flat.
3. **חלון בקיר הקדמי**, מספריית החלונות הקיימת.
4. **דורמר פלש**: הגג נפתח והקיר הקדמי הוא המשך של קיר הבית.
5. **גאטרס.**

### מה כבר נבדק בקוד לקראת החלון
- `WindowTool#cut_window_opening` **לא יעבוד** על קיר גגון: הוא קורא
  `start_x/start_y/end_x/end_y/thickness/height/anchor` מהקבוצה,
  ולקיר גגון אין אותם.
- מה שכן חוזר בשלמותו: `build_casement_body`, `build_arched_body`,
  `build_garden_body` — בונים סביב ראשית הקבוצה.
- הקיר הקדמי הוא תת-קבוצה `dormer_front` (`build_front_wall!`).

---

## מוקשים שנמדדו היום (לא לחזור עליהם)

- **פאה עם חור מחזירה את קודקודי החור מעורבבים עם המתאר.**
  `f.vertices` על פאה כזאת נותן פוליגון שהוא לא הגג. תמיד
  `outer_loop`. זה מה שגרם לקובץ מדידה לדווח 24 רעפים "4 אינץ'
  בחוץ" באמצע מדרון שלם, ושלח אותנו לחפש באג שלא היה.
- **מחיקת פאה לא מוחקת את הקצוות שלה.** נשארים קווים שמצוירים על
  הגג. לאסוף את הקצוות לפני, ולמחוק את מי שנשאר בלי פאות.
- **`s` רץ לאורך `into`, לא לאורך `along`** (`at_lambda`).
- **`f.normal` לא אמין** לעליון/תחתון. לפי מיקום.
- **קאפ מורם נושא חצאית בגובה ההרמה** — אין לו רווח לרדת בצומת.
- **הסטאב לא יודע `loops` / `edges` / `find_faces`**, אז מסלול
  ה-heal האמיתי לא נבדק בענן — רק אצלו.
- **`git add` על קובץ gitignored מפיל את כל השורה.**

## איך לעבוד איתו
- עברית, משפט-שניים, כמו לילד בן 5. כל פקודה בקופסה נפרדת עם
  "Ruby Console" או "CMD", **ותגיד לו במפורש מה לעשות ומה לכתוב
  לך בחזרה** — "תדביק, ואז תכתוב לי: רץ". הוא התעצבן פעמיים כי
  לא היה ברור מה הוא צריך להחזיר.
- **git רק דרך ה-CMD שלו.** commit אחרי כל צעד עובד.
- **אף פעם לא "זה עובד" מתמונה.** אם לא מדדת — תגיד שלא מדדת.
- כשהוא אומר "לא עובד" — **קובץ שמודד שכותב דוח לתיקייה**, ואתה
  קורא את הדוח בעצמך. הוא לא צריך להדביק כלום.
- **הוא מבקש דבר קטן — תעשה דבר קטן.**
- שרטוט לפני בנייה, תמונה מרונדרת עם מידות אמיתיות, ושאלה אחת
  קצרה. ASCII הוא לא מבין.
- **אל תבזבז סבבים.** היום ירדנו לרזולוציה של "מה בדיוק בולט"
  דרך חמישה קבצי מדידה שונים; שניים מהם היו מיותרים כי לא קראתי
  את `outer_loop` נכון בפעם הראשונה.

## קבצי דיבוג חדשים (gitignored)
`debug_dormer_tiles.rb` — חומר הגג, הרידג' קאפ, ומידות כל מדרון של
כל גגון, כולל איפה כל רעף יושב ב-u/v של המישור שלו.
`debug_dormer_pieces.rb` — מלאי מלא של כל חלק בתוך הגגון עם התיבה
העולמית שלו; מתחיל במלאי גגות/גגונים, אז דוח ריק אומר משהו.
`debug_heal.rb` — חורים שנשארו פתוחים, תפרים קופלנריים, וקצוות עם
פחות משתי פאות.
`debug_heal2.rb` — הפתח ש-heal היה משתמש בו מול החור האמיתי.
`debug_cap_sit.rb` — כמה כל קאפ שוקע או מרחף מעל הגג שתחתיו.
`debug_cap_shape.rb` — הסקשן של הקאפ: גובה, רוחב, וכמה חוליות ורווחים.
`debug_cap_ends.rb` — קאפ שחורג מטביעת הרגל של הגג.
`debug_panel_ends.rb` — כל חתיכת רעף שחורגת מהגג בתוכנית.
`debug_rib_span.rb` — כל ריב לאורך המדרון מול הגג שמתחתיו.
`debug_eave_strip.rb` — כמה כל דבר בגג מגיע החוצה: לוח, קרשים, שדה.
`fix_orphan_holes.rb` — **משנה את המודל.** סוגר כל חור שאין מעליו
גגון ופורס מחדש את הפאנלים. פעולה אחת, Ctrl+Z מבטל.
