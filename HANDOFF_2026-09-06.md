# Interior Pro — מסירה 2026-09-06 (הגג יושב על הקיר)

קרא את זה קודם, ואז `CLAUDE.md`. הקודמת: `HANDOFF_2026-09-05.md`.
**אל תסמוך על הסיכום — תאמת מול הקוד החי.**

ענף `door-stabilize`. קומיט אחרון של הסבב: `c91f240`.
בדיקות: `cd tests` ואז `LANG=C.UTF-8 bash run_all.sh ..` — הכל ירוק,
**rt51/rt52 אדומות מסיבה ישנה** (`tests/rt51_tmp/a (2).jpg` נכנס ל-git,
למחוק אותו משם ב-CMD שלו).

**הרצת הבדיקות ברקע כבר לא עובדת.** `setsid ... &` נהרג כשקריאת ה-shell
נגמרת (bwrap עם `--die-with-parent`). מה שכן עובד: להריץ את החבילות
בנתחים, כל נתח בקריאה נפרדת — קודם `node t*.js`, ואז `ruby rt*.rb`
בקבוצות של ~40. כל נתח נכנס בתוך 45 שניות בנוחות.

---

## מה נסגר היום

### 1. הגגון זוכר איפה הוא יושב (rt115)
`build_dormer!` כותב `place_mode` על הקבוצה ו-`dormer_spec` קורא אותו.
לפני זה גגון בנוי לא ידע כלום על המצב שלו, ולכן:
- עריכה שלחה ל-`replace_dormer!` רק מידות — בלי `setback` ובלי מצב, אז
  "עומק מהפשיה" שהוקלד בעריכה לא עשה כלום. עכשיו הוא נשלח.
- `DormerMoveTool` הוסיף `follow_click: true` תמיד ודרס כל מצב. עכשיו
  `move_spec` הולך לפי המצב השמור: רק `free` רודף אחרי הקליק.
  **גגון ישן בלי מצב שמור נשאר חופשי**, בדיוק כמו קודם.
- `click_spec` במצב `flush` בלי קיר שומר את ה-setback שקיבל במקום ליפול
  לאפס.

### 2. גגון שטוח לא נעלם יותר (rt116)
הוא דיווח על זה כהחלפת חומר. **זה לא היה קשור לחומר בכלל.**
`shed_frame` מאפס את הפיץ' ל-flat — זה מה ששטוח אומר — `build_dormer!`
שומר את האפס על הקבוצה ו-`dormer_spec` מחזיר אותו, אז כל בנייה מחדש
נפלה על `return warn_nil('the dormer roof has no pitch') if pitch <= 0.0`.
זה הפיל **גם replant, גם Edit וגם Move**. עכשיו האפס מותר רק ל-flat.
שוחזר בענן בלי המודל שלו — `/tmp/probe_flat.rb` בסבב.

### 3. חלון הגגון נפתח כולו
`dormer_dialog.rb` — הדף מודד `document.body.scrollHeight` וקורא
ל-`fit_height`, שעושה `dlg.set_size`. נקרא גם ב-`modeChanged` וגם
ב-`styleChanged`, כי שורת העומק מופיעה ונעלמת.

### 4. חור לחלון בקיר הקדמי (rt117)
צ'קבוקס "A window in the front wall" בפאנל. `window_rect(fr)` טהור:
**48×24 מקסימום, 6" קיר מכל צד, ולעולם לא פחות מ-1" מהפנים של הקירות
הצדדיים** (`clear_w - 2.0`). `punch_window!` מצייר את המלבן על הפאה
החיצונית ועל הפנימית, מוחק את שתי הפאות הקטנות כדי שהלולאות יישארו
כחורים, ומוסיף ארבע פאות רוול. נבנה **בתוך קבוצת הגגון**, אז הוא זז
ונמחק איתו ושורד עריכה ובנייה מחדש.
**הסטאב לא יודע לפצל פאות ואין לו `Face#erase!`** — החור האמיתי נבדק
רק אצלו; הבדיקה מודדת מספרים.

### 5. הגג יושב על הקיר (rt118, rt119) — הדבר הגדול של היום
לפני: תחתית האיבס ישבה **8" בתוך הקיר** (עומק הפשיה שלו). מדוד,
לא מצולם — `debug_heel2.rb`.
עכשיו: `z0 = eave_z(walls) + heel_lift(...)`, וההרמה היא **בדיוק עומק
הפשיה**. אז תחתית האיבס נוחתת על ראש הקיר, והאיבס עצמו ממלא את המרווח
שנפתח — ולכן **אין מה לסגור ולא מוסיפים כלום**.
אותו כלל בגגון: `dormer_heel` = `fascia_depth(spec)`, נכנס כצעד נוסף של
`length`. בשד מחלקים ב-`slope - pitch` ולא ב-`slope`, כי גובה הקיר של שד
גדל בקצב אחר.

**שני ניסיונות קודמים באותו סבב, שניהם נמדדו ושניהם נדחו על ידו — אל
תחזיר אותם:**
1. `overhang × slope` לבד. השאיר את קופסת האיבס 8" בתוך הקיר.
2. `overhang × slope + fascia_depth`, פלוס רצועת קיר בקבוצת הגג שסוגרת
   את הרווח. הרצועה הייתה צבועה נכון (Stucco, נמדד ב-`debug_heel3.rb`),
   אבל הוא ראה אותה מיד: *"נראה כאילו הוספת קירות נוספים מתחת לגג"*.
   הוא דרש למחוק אותה ולא להוסיף שום דבר.

**החוק שהוא קבע, במילים שלו:** *"אתה לא מגדיל כלום ולא מקטין כלום ולא
משנה שום צורה, אתה בסך הכל מוריד את הגג — אני יכול לעשות את זה ידנית
בסקצ'אפ."* rt118 נועלת בדיוק את זה: מרימים את הקירות ב-9" ומוודאים
שכל נקודה בגג זזה ב-9" בדיוק, בלי שאף חלק ישנה צורה.

---

## מה שברנו בכוונה, ואיפה זה מתועד

עשר חבילות ישנות נעולות על ה-z הישן של הגג, וחמש על החוזה הישן של הגגון.
**לא שונה בהן מספר אחד.** בכל אחת נוסף בראש stub שמכבה את העקב:

- גג (`heel_lift → 0.0`): rt15, rt17, rt85, rt86, rt88, rt90, rt91,
  rt92, rt93, rt94.
- גגון (`dormer_heel → 0.0`): rt95, rt97, rt99, rt100, rt101, rt102, rt103.

זו אותה תבנית ש-rt85 כבר השתמשה בה כדי לכבות את `abut_headroom`.
rt118 ו-rt119 נועלות את ההתנהגות החדשה.

**שני חוזים ישנים שהוחלפו בהסכמתו המפורשת:**
- "גובה שהוקלד חוזר בדיוק כמו שהוקלד" ו"האיבס לא מזיז את z_eave" של
  הגגון. הוא נשאל וענה: **"העקב נוסף למספר"** — מקלידים 33 ומקבלים 41.

---

## מוקשים שנמדדו היום

- **`roof_manager.rb` הוא CRLF ו-`dormer_*.rb` הם LF.** עריכה בפייתון
  חייבת `newline=''` ודפוסי חיפוש עם `\r\n` בקובץ של הגג. חיפוש עם `\n`
  בלבד פשוט לא מוצא כלום, בשקט.
- **`Sketchup::Face` בסטאב הוא לא Face אמיתי**: אין `erase!`, אין פיצול
  פאות, ו-`Group#erase!` לא מוציא את הקבוצה מרשימת האבות. בדיקה שסופרת
  גגונים אחרי עריכה תספור שניים.
- **פאה עם חור מחזירה קודקודים מעורבבים** — תמיד `outer_loop`. (מאתמול,
  עדיין נכון.)
- **`git add` על קובץ gitignored מפיל את כל השורה.**
- **git אצלו ממיר LF ל-CRLF בקומיט.** הוא רואה אזהרה על כל קובץ LF.
  זו הגדרת `core.autocrlf` שלו, לא משהו שהסבב הזה שינה — אבל אם קובץ
  LF יחזור מ-git עם CRLF, זה המקור.

## קבצי דיבוג חדשים (gitignored)
`debug_heel.rb` — כל חלק בגג עם ה-z הנמוך והגבוה שלו מול ראש הקיר.
`debug_heel2.rb` — **הכי שימושי.** הולך על קו הפינה העליונה החיצונית של
כל קיר, ולכל נקודה מוצא את החלק הכי נמוך בגג שעומד מעליה ואומר כמה
הפינה "בתוך" הגג. אפס = יושב בדיוק.
`debug_heel3.rb` — החומרים על כל רצועה מול החומרים של הקיר שמתחתיה.

## מה נשאר פתוח

1. **גוף החלון** — כרגע יש רק חור. `WindowTool#cut_window_opening` לא
   יעבוד על קיר גגון (הוא קורא `start_x/start_y/end_x/end_y/thickness/
   height/anchor` מהקבוצה). מה שכן חוזר בשלמותו: `build_casement_body`,
   `build_arched_body`, `build_garden_body` — בונים סביב ראשית הקבוצה.
2. **החלון ניתן לעריכה** בעורך החלונות הקיים — צעד נפרד אחרי הגוף.
3. **סופיט משופע** — העקב נמדד רק עם `boxed`. אם הוא עובר למשופע, למדוד.
4. `relay_runs!` מזין למניח גם את הפאה התחתונה (`f.normal.z > 0.2`).
   לפי מיקום, כמו `top_skin`.
5. `fetch_or_build` סומך על השם — חותמת `build_id` על הדפיניציה.
6. פס 0.85" בין קצה הרעפים לרייק — **הוא אמר להשאיר. לא לגעת.**
7. rt51/rt52.
8. דורמר פלש עם קיר הבית, וגאטרס.

## איך לעבוד איתו — מה שעלה היום
- **הוא מודד בעצמו.** אל תגיד לו "אני לא מנחש, מדדתי" בטון שמרמז שהוא
  ניחש. זה עצבן אותו בצדק.
- **כשהוא אומר משהו קצר, זה בדיוק מה שהוא מתכוון.** "תוריד את הגג" זה
  להוריד את הגג, לא לתכנן מחדש את הסופיט. בזבזתי לו היום שני סבבים
  שלמים על זה.
- **לפני שמוסיפים גיאומטריה — לשאול.** הוא דחה רצועה שלמה בשנייה.
- **רנדרינג בעברית עובד**: `pip install python-bidi`, ואז
  `from bidi.algorithm import get_display` על כל מחרוזת. בלי זה העברית
  יוצאת הפוכה, והוא לא מבין ASCII.
- שאלה אחת קצרה עם אפשרויות, ואז לבנות.
