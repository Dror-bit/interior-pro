# Interior Pro — מסירה 2026-08-31 (גג Shed — שיפוע יחיד)

קרא את זה קודם, ואז `CLAUDE.md`. הקודמת: `HANDOFF_2026-08-30.md`.
**אל תסמוך על הסיכום — תאמת מול הקוד החי.**

**כלום לא בקומיט.** ענף `door-stabilize`. הכל בעבודה, לא נבדק ב-git.
**צעד ראשון חובה: `git status` דרך CMD שלו** — אל תריץ git בעצמך.
אם הוא רוצה למחוק את הכל:
`git checkout -- roof_manager.rb toolbar.rb main.rb roof_dialog.rb`
(`roof_shed_tool.rb`, `icons/roof_shed.svg`, `tests/rt91.rb`, `tests/rt92.rb`
הם קבצים חדשים ולא יימחקו ככה.)

בדיקות: `cd tests` ואז `LANG=C.UTF-8 bash run_all.sh ..` → **142 ירוקות**
(היו 140; rt91 ו-rt92 חדשות). להעתיק לענן גם `icons/` ו-`textures/`
(בלי `textures/floors/`). **תמיד run_all.sh.**

---

## ⛔ מה עלה ביוקר היום — קרא את זה לפני שאתה נוגע בגג

### 1. הסטאב בענן **לא בונה את הגג שלו**
אין בו Edges, ולכן אין **רכס-קאפ**, אין **רעפים תלת-מימדיים**, אין
**קצוות רעף**. הגג שלו הוא `shingle` עם `ridge_cap=true` ו-`soffit=wood`.
הרוב הגדול של הצ'אט הלך על "בדקתי בענן, 142 ירוקות" — כשמה שנבדק היה
גג אחר משלו. **הבר שהוא צילם מעולם לא הופיע בסימולציה.**
לפני שאתה אומר לו "בדקתי" — תשאל את עצמך אם הדבר שהוא רואה בכלל
נבנה בסטאב. אם לא, תגיד לו את זה **מראש**.

### 2. שלוש פעמים ניחשתי מה הוא רואה בתמונה, ושלוש פעמים טעיתי
הפתרון שעבד: קובץ בדיקה שהוא מריץ אצלו (`debug_shed_edge.rb`), שמדפיס
מספרים. **המספרים סגרו כל ויכוח תוך סבב אחד.** תלך לשם מוקדם, לא אחרי
שלושה סבבי ניחוש.

### 3. תיקנתי סימפטומים, לא סיבות — והוא תפס אותי
הקיר בלט 4" מעל הגג, אז הורדתי 4". הוא ענה: *"אתה רק מוריד קצת את
המפלס אבל לא פותר את הבעיה."* צדק. הסיבה האמיתית: הקוד **העתיק** את
קו הגובה מקצה הגג לקיר שנמצא 12" משם. עכשיו הוא **מודד** את תחתית הגג
על הקו של הקיר עצמו (`wall_line_profile` / `roof_under_z`).

### 4. תיקון רחב מדי שבר שתי בדיקות ישנות — וזה הציל
rt17 ו-rt86 נפלו: בגג עם כנף הקיר המורם **חייב** לרוץ מעבר לעצמו כדי
לסגור את החור מתחת לכנף. לכן כל הקיצוץ החדש עובד **רק כש-`framed` הוא
nil**. אל תסיר את התנאי הזה.

---

## מה נבנה היום

### Shed — עובד
- **סגנון `'shed'`.** הרעיון: זה הגמלון עם הסימון **הפוך** — כל האיבים
  במהירות 0 חוץ מאחד. אין גאומטריה חדשה: רייקים, קירות מורמים ופשיה
  על האיב הנמוך יוצאים מהקוד שכבר היה.
- `shed_low_edge(poly, wall_ids, marked, avoid)` — האיב הנמוך: הקיר
  שהוא סימן, ואם לא סימן — **האיב הארוך ביותר**. אף פעם לא איב abut.
- `shed_wall_ids` / `set_shed_wall!` — סימון קיר אחד לכל היותר. לחיצה
  שנייה מבטלת. הסימון **גם** מעביר את הסגנון ל-shed (קליק אחד).
- **כפתור `Shed Roof` בסרגל** + פריט תפריט + אייקון `icons/roof_shed.svg`.
  קובץ חדש `roof_shed_tool.rb`, נטען מ-`main.rb`.
- **רדיו `Shed`** בפאנל ליד Hip/Gable/Flat, עם רמז מתחת.
- `tests/rt91.rb` — נופלת 5 פעמים נגד הקוד הישן.

### שלושה תיקונים על מה שהוא צילם
1. **הקיר המורם רץ 12" מעבר לקיר** (אוברהאנג) ונשאר תלוי באוויר.
   `wall_span_on_edge` → **התיבה של הקיר עצמו**. לא קו האמצע:
   הקירות שלו מצוירים על הפנים החיצוניות, וריפוד חצי-עובי דחף 2.5"
   מעבר. `wall_centre_span` נשאר רק כגיבוי לסטאב.
2. **הקיר עמד 4" מעל הרעפים.** ההעתקה מקצה הגג → מדידה על קו הקיר.
3. **סרגל אפור על הגג = רכס-קאפ.** Shed הוא מישור אחד, אין בו רכס.
   עכשיו `%w[flat shed]` לא בונים רכס-קאפ.
4. **שני קווים בפינה** — שני קירות מורמים בנו את אותו ריבוע פינה.
   כל קיר מוותר על קצה ה-START שלו לשכן.

`tests/rt92.rb` מכסה את כל אלה. כל טענה בה נבדקה שהיא **נופלת** נגד
הקוד שלפני התיקון המתאים.

---

## ❗ מה עדיין שבור — הצעד הבא

### 1. עוד קו אחד בפינה (הדבר האחרון שהוא אמר)
*"רק קו אחד מהפינות נעלם אבל עדיין נישאר אחד."*
הוויתור על קצה ה-START מטפל בפינות **בין שני קירות מורמים**. בריבוע
עם איב אחד יש 3 קירות מורמים ושתי פינות כאלה — שתיהן טופלו. הקו
שנשאר הוא כנראה **בפינה שבין קיר מורם לקיר האיב הנמוך** (שלא מורם),
או התפר האופקי בין הקיר המורם לקיר שמתחתיו.
**אל תנחש. תריץ `debug_shed_edge.rb` ותבקש ממנו תמונת זום על הפינה
שנשארה.**

### 2. אין פשיה ואין איב על הקצה העליון של ה-Shed
הוא סימן את זה בצהוב. הקצה העליון נמצא ב-`gables`, ולכן `gable_flags`
מדלג עליו לפשיה/דריפ/סופיט ובונה שם רייק במקום.
**ניסיתי והחזרתי אחורה** (יש הערה בקוד ליד `rakes = gables`):
`build_band!` יודע גובה אחד לכל הגג — `band_top`, גובה האיב — וב-Shed
הקצה העליון קומה שלמה מעל, אז הפשיה נחתה 78" נמוך מדי.
**זה דורש `build_band!` שמקבל גובה לכל איב בנפרד. נוגע בכל הגגות.**
צעד נפרד, עם שרטוט ואישור שלו לפני.
(נכתב `level_gable_edges` שמזהה איב "מפולס" לפי צורה — הוא **לא בשימוש**
עכשיו. אם לא הולכים על זה, למחוק אותו.)

---

## מוקשים שעדיין בתוקף

- `roof_manager.rb` הוא **CRLF**. `roof_dialog.rb`, `toolbar.rb`,
  `main.rb`, `roof_shed_tool.rb`, `downspout_tool.rb` וקבצי `tests/rt*.rb`
  הם **LF**. עריכה שמשנה סופי שורות = דיף ענק.
- `X = {...} unless const_defined?(:X)` **לא** נטען מחדש ב-`reload!`.
  לכן `roof_setting_keys`, `abut_headroom`, `min_abut_slope` הן מתודות.
- שורת קונסולה עם `build_roof!` בלי `replace:` או `level:` **מוחקת את
  כל הגגות שלו**. תמיד:
  `RoofManager.roofs.each { |r| RoofManager.build_roof!(replace: r) }`
- כפתור חדש בסרגל → **הפעלה מחדש של סקצ'אפ**. HTML בדיאלוג → לסגור
  ולפתוח. קוד רגיל → `InteriorPro.reload!`.
- `_snap/` היא תיקיית טיוטות ולא ב-git. שמתי שם היום את חבילות
  ההעברה `shed_patch.tgz` ו-`shed_fix*.tgz`.
- `debug_shed_edge.rb` בשורש (מוחרג ע"י `debug_*.rb` ב-gitignore).
  הפלט שלו `shed_edge_report.txt` **לא** מוחרג — יש שם גם
  `edge_report.txt`, `gable_pair_report.txt`, `soffit_report.txt`.
  הצעתי להוסיף `*_report.txt` ל-`.gitignore` והוא לא ענה.

## איך לעבוד עם דרור

- עברית פשוטה, **משפט-שניים, כמו לילד בן 5**. כל פקודה בקופסה נפרדת
  עם לאן (Ruby Console / CMD). git רק דרך CMD שלו.
- **שרטוט לפני בנייה, ושאלה אחת קצרה.** היום השרטוט הראשון (חתך של
  Shed עם המידות שלו) עבד מצוין — הוא הבין את מגבלת הגובה תוך משפט.
- **אל תתווכח על UI.** הצעתי שלוש פעמים לחסוך כפתור ולשלב עם Gable
  Ends. הוא אמר "פשוט תעשה כפתור כמו לגייבל רק ל-SHED". הוא צודק —
  זה הגג שלו. כשהוא חוזר על עצמו, תפסיק להסביר ותבנה.
- כשהוא שולח תמונה — **תשאל מה להסתכל אם אתה לא בטוח**, אבל אל תשאל
  יותר מפעם אחת על אותו דבר. עדיף קובץ בדיקה שמדפיס מספרים.
- **תמדוד לפני שאתה משנה. ותוודא שמה שאתה מודד הוא באמת מה שהוא רואה.**
- commit אחרי כל סבב שעובד. היום לא היה אף קומיט — זו טעות.
