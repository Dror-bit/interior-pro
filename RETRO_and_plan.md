# רטרוספקטיבה ותוכנית — Interior Pro (דלתות/קירות)

נכתב אחרי סשן שבו נשברו פעולות עבודה. מטרה: להבין את **השורש** ולעבוד אחרת.

## 1. הבעיה השורשית (לא פיצ'ר בודד)
קיימות **שתי מערכות פתחים מתחרות**:
- **חדשה (אמינה):** "לבנות את החור" — `rebuild_wall_native!` בונה את הקיר מחדש מרשימת `door_openings`.
- **ישנה (שבירה):** "למלא + לחתוך boolean" — `DoorBoolean`, `fill_opening!`, `fill_by_snapped_corners`.

עריכה/מחיקה/שינוי-גודל של דלת עדיין נשענות על הישנה.

### למה זה גורם ל"הכל נשבר"
1. פעולת boolean/fill דורשת שהקיר יהיה **solid נקי**.
2. כשהיא נכשלת, הקיר נשאר **not a solid** ("odd edges") ורשימת הפתחים מקבלת פתח **יתום** (בלי דלת).
3. על קיר לא-solid כל פעולה הבאה נכשלת — **קריסה מצטברת** בשרשרת.
4. כל בנייה-מחדש חופרת את כל הפתחים שברשימה, כולל היתומים → חורים ריקים.

לכן זה לא "הרבה באגים", אלא **באג-שורש אחד** שמתפשט.

## 2. מה לא נעשה טוב (התהליך שלי)
1. **נגעתי בקוד משותף** (`rebuild_wall_native_geometry!`) שזרימות הדלת תלויות בו — ושברתי פעולות שעבדו. לוגיקת-פינה צריכה להיות **מבודדת**, לא בתוך bנייה-מחדש משותפת.
2. **ערכתי ישירות** למרות סגנון העבודה, והורדתי שליטה. תיקון: שינוי רק אחרי אישור, וכל שינוי קטן ומבודד.
3. **בדקתי על מודל מזוהם** (פתחים יתומים, קיר לא-solid) — אז התוצאות היו מבלבלות ורדפתי רוחות. תיקון: כל בדיקה על **סצנה נקייה**.
4. **whack-a-mole** — תיקנתי סימפטומים במקום השורש (הערבוב בין שתי המערכות) יותר מדי זמן.
5. לא דרשתי **commit אחרי כל ירוק** ולא tag לנקודה טובה, אז כל נפילה גררה אחורה רחוק.

## 3. השיטה קדימה (כדי לעצור את 4 הימים)
1. **לייצב על commit טוב + tag** (למשל `stable-doors`). אף פעם לא לבדוק על מודל מזוהם — תמיד קיר חדש + דלת אחת.
2. **לבחור ארכיטקטורה אחת ולמחוק את השנייה:** כל פעולות הדלת (הצבה/עריכה/מחיקה/הזזה) דרך ה-native rebuild בלבד. **למחוק** את מסלול ה-boolean/fill (`DoorBoolean`, `fill_opening!`). הערבוב הוא השורש.
   - עריכה צריכה: למצוא את הפתח של הדלת ברשימה → **לעדכן אותו במקום** → `rebuild_wall_native!`. בלי fill, בלי boolean, בלי append חדש.
3. **ריפוי-עצמי:** בכל rebuild לזרוק פתחים בלי דלת תואמת — כך יתומים לא נצברים.
4. **אטומיות:** כל פעולת דלת = `start_operation` אחד; כישלון → `abort_operation` מגלגל הכל (אין יתום, אין קיר שבור).
5. **צעד אחד בכל פעם**, על סצנה נקייה, **commit אחרי כל בדיקה ירוקה**.

## 4. סדר עדיפויות מוצע
1. **קודם:** לתקן את זרימת העריכה/מחיקה לארכיטקטורה אחת + אטומיות + ריפוי-עצמי. זה מה שעוצר את הקריסה המצטברת. בלי זה — כל השאר ימשיך להישבר.
2. **רק אחר כך:** מיטר-פינות (כצעד מבודד, מחוץ לזרימות הדלת).
3. **ואז:** הזזת קיר + דלת ביחד.

## 5. כללי בדיקה (clean-room)
- קיר חדש, דלת אחת, פעולה אחת, בדיקה אחת.
- אחרי כל פעולה להריץ ספירה: מספר פתחים = מספר דלתות, והקיר `manifold?` (solid).
- אם לא 1:1 או לא solid — לעצור ולתקן לפני שממשיכים.

## מקורות (best-practice שמאשרים את הכיוון)
- Medeek / MUZWalls: רשימת הפתחים = מקור-אמת יחיד, בונים מחדש מהמאפיינים; openings "self-healing".
- SketchUp API: חריגה שנתפסת מגלגלת עד `start_operation` האחרון (אטומיות).
- "לבנות את החור, לא לחתוך" אמין יותר מ-boolean cut על solid (תיעוד פורומים).
