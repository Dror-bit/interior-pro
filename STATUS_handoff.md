# STATUS_handoff — Interior Pro (עודכן 2026-07-16)

## סבב 2026-07-15/16 — Tags, חדרים, רצפות (קרא ראשון!)
- **שיטת עבודה חדשה:** העוזר עובד **ישירות בתיקיית ה-Plugins** (יש לו גישה) ומריץ git בעצמו. תיקיית source_readonly כבר לא בשימוש להעתקות. מהמשתמש נדרש רק: reload!/restart ובדיקות. הערה: פקודות git של העוזר משאירות קבצי lock ב-.git שהוא לא יכול למחוק — אם git נתקע, למחוק ידנית HEAD.lock/index.lock.
- **Tags (הושלם):** helper `InteriorPro.assign_tag` ב-main.rb (שינוי ב-main.rb מחייב load ידני/restart — reload! לא טוען אותו!). קיר=IP/Walls, דלת=IP/Doors, חלון=IP/Windows, מולדינג=IP/Moldings, חדר=IP/Rooms, רצפה=IP/Floors.
- **פער נסגר בלי עבודה:** door_openings כבר מכיל גם חלונות (window_tool.rb:266) — השם מטעה בלבד.
- **ישות חדר (הושלם) — room_manager.rb:** זיהוי אוטומטי של לולאות קירות סגורות (גרף צירים + face tracing), boundary מחושב **מפני הקירות הפנימיים** (חיתוך צירים אמיתיים מוסטים t/2 — לא נקודות clustering!). תווית 3D-text שם+שטח על הרצפה (z=0.25). id/name שורדים sync (התאמה לפי centroid, טול' 36"). rename_room! בקונסול. sync אוטומטי אחרי ציור/עריכה/הזזה/מתיחה של קיר (hooks לצד hooks המולדינג) + כפתור Sync Rooms.
- **רצפות (הושלם) — floor_manager.rb + floor_dialog.rb:** סרגל חדש "Interior Pro Floors" (נפרד, לבקשת המשתמש). FLOOR_TYPES: Hardwood/Tile/Carpet/Concrete (צבעים; טקסטורות בהמשך). רצפה פר-חדר לפי boundary_xy, פני הרצפה ב-z=0 ועובי כלפי מטה. עוקבת אחרי sync חדרים (refresh! — רק לחדרים שכבר יש להם רצפה). דיאלוג: שורה פר-חדר (סוג+עובי), Apply/Sync/Remove All.
- **חוזה:** CONTRACT_2D.md מעודכן. הצעד הבא במפה: תקרות → גגות; ובהמשך floor_library JSON (המערך הכללי שהמשתמש רוצה: טייל, גדלי אריח, חומרים).

### שיפוץ חיבורי קירות פנים-חוץ (2026-07-16, הושלם — commit ed685cd):
- **מודל butt joint:** קיר פנים מול קיר חוץ = חיבור ישר בפני הקיר, לא מיטר. apply_butt_joint (היה קיים, לא מחובר) חובר ב-apply_miter כשהקטגוריות שונות; הבחירה מי נחתך לפי **קטגוריה** (פנים נחתך) ולא לפי עובי; רק קיר הפנים נבנה מחדש — קיר החוץ לא נגוע. תיקון side_sign: הפאה שפונה לגוף קיר הפנים (לפי הקצה הרחוק).
- **הזזה מופרדת:** ui_dialogs wall_move — שותפי פינה עוקבים רק באותה קטגוריה; הדבקת T נשארת חוצת-קטגוריות (הזזת קיר חוץ מותחת קיר פנים דבוק).
- **סנאפ לפאה:** snap_start_to_wall_centerline — התחלה על קיר מקטגוריה אחרת נצמדת לפאה הקרובה בנקודת הקליק (clamped לקטע), לא לקצוות; ביטל את ה"בריחה" לפינות/המשכי קיר. אותה קטגוריה = התנהגות ישנה (מיטר).
- **2 עוגנים בלבד:** wall_library_dialog — רק Bottom Left / Bottom Right (ברירת מחדל left); סוגים ישנים בספרייה נכפים ל-bottom-left ב-draw_wall.

### נשאר פתוח מהסבב (לצ'אט הבא — לפי סדר):
1. **מולדינג נשבר בחיבורי butt (הבא בתור!):** אחרי מעבר קירות פנים-חוץ ל-butt, הבייסבורד סביב קירות פנים לא נסגר — רווחים בקצה ה-butt (קצה קיר הפנים על פאת קיר החוץ) ורצועות לא מתחברות. כנראה resolve_tees!/resolve_miters! ב-molding_tool.rb מזהים חיבורים לפי קצוות ישנים (drawn/centerline) ולא מכירים קצה-על-פאה. יש צילום מסך אצל המשתמש. לתקן: זיהוי קצה-butt כ"T" (הרצועה העוברת מקבלת gap ברוחב קיר הפנים; רצועת קיר הפנים נסגרת ישר בפאה).
2. **מחיקת קיר משאירה פינה שבורה:** אין hook על מחיקה — המיטר הישן נשאר על השכנים. לשקול observer או כלי Delete Wall ייעודי (+sync חדרים/רצפות/מולדינג).
3. לוודא שהחדירה של קיר פנים לקיר חוץ לא חוזרת בתרחישים נוספים (T באמצע קיר, פינה, זוויות לא ישרות).

---

## סבב 2026-07-14 ב' — חוזה הדו-ממד (קרא ראשון!)
- **נכתב ואושר CONTRACT_2D.md** (בשורש) — סעיף 1 במפת הדרכים נסגר. מסמך דרישות בלבד, אפס שינויי קוד.
- **החלטות מפתח שהתקבלו:** קהל יעד = קבלן + קבלני משנה; דפים = תוכנית בנייה + אליוויישנים פנימיים (לפי חדר, 4 מבטים) + חזיתות חוץ; מידות קירות = face-to-face; מיקום פתחים = לצד הפתח (rough opening); חדר = ישות מלאה עם זיהוי אוטומטי מלולאת קירות + שם ניתן לעריכה; עורך מלמעלה = דיאלוג HTML בתוך SketchUp עם "פתור" שבונה 3D מ-attributes (סגנון Revit); דלתות ארון = סטנדרט אמריקאי.
- **עיקרון החוזה:** attributes = מקור אמת יחיד; כל גוף חייב regen מלא מה-attributes. corners_xy מוגדר cache.
- **פערים שתועדו בחוזה (סעיף 7):** אין window_openings על קיר; אין Tags; אין ישות חדר; אין level; לתעד מה position_along_wall_in מודד.
- **פתוח במכוון:** גבהי חזיתות חוץ (grade/plate/ridge) — ייסגר בשלב הגגות.
- **הצעד הבא (סעיף 2 במפת הדרכים):** כלים חדשים לפי החוזה — רצפות → תקרות → גגות. לפני זה כדאי לשקול סגירת פערי חוזה זולים (Tags, window_openings).

---


## מה הושלם בסבב 2026-07-13 (קרא ראשון!)
- **קייסינג — כיוון הפרופיל תוקן:** skp_spec ב-door_casing_profiles.rb ממפה עכשיו `[1.0 - z/w, x/d]` — הצד הרחב פונה החוצה מהדלת (אל הבייסבורד). commit 75ed4e7.
- **קייסינג חוץ/פנים הוחלפו צדדים:** בדלתות חוץ v=0 הוא הפאה הפנימית; ב-3 נקודות ב-door_tool.rb הקייסינג החיצוני נבנה עכשיו ב-v=thickness (בליטה החוצה) והפנימי ב-v=0. commit 75ed4e7.
- **רענון מולדינג אחרי עריכת דלת:** hook ב-apply_edit (door_library_dialog.rb) — סוגר את הרווח שנשאר כשמחליפים קייסינג רחב בצר. commit 2c22243.
- **בייסבורד Flat:** פרופיל built-in מרובע 3.5"×0.5" בגלריה (molding_library.rb, FLAT_BASE_NAME='Flat'). לקח: שם עם `"` שובר את ה-HTML של הגלריה; קבועים עם const_defined? דורשים restart מלא. commit 2a319bd.
- **מולדינג נבנה תמיד ב-top level:** make_molding_group משתמש ב-model.entities (לא active_entities) — Apply מתוך עריכת קבוצה יצר קבוצות מקוננות/מוזחות שהניקוי פספס. commit e90e7d5.
- **Apply to House מאפס החרגות:** include_all_walls! לפני apply_all! (היה נתקע על 0 ריצות אחרי Apply to Selected). commit e90e7d5.
- **צבע מולדינג:** backend קיים (molding_color_mode על המודל, InteriorPro_Molding_<hex> פר קיר) אבל ה-UI הוסר לבקשת המשתמש — המולדינג תמיד לבן עם חומר נפרד (לצביעה בלומיון). commit e90e7d5.
- **חיבור T במולדינג:** resolve_tees! + segments_ex + tee_gaps — קיר שנפגש באמצע קיר אחר: הרצועה העוברת מקבלת חור ברוחב הקיר עם מיטרים 45°, ורצועות הקיר הפוגש מקבלות מיטר תואם (בייסבורד+קראון). commit (אחרי e90e7d5).
- **הזזת קיר מותחת קירות T:** ui_dialogs.rb wall_move — זיהוי קצה-על-אמצע (מרחק ≤ עובי+0.5", לא מקביל), הקצה זז בווקטור ההזזה; ולידציה/rebuild/מיטר/מולדינג חלים. commit 003dde3.
- **קייסינג לחלונות:** מסגרת 4 צדדים (Follow Me סגור, מיטרים אוטומטיים) משני צדי הקיר, מאותו קטלוג של הדלתות (none/flat/CASING-*.skp). בוררים בדיאלוג החלון, נשמר ב-attributes, עובר גם בעריכת חלון. build_window_casing! ב-window_tool.rb; יושב על ה-jamb (רוחב 1") עם reveal רבע אינץ'. הפרופיל בכיוון הנכון (הצד הרחב החוצה) בזכות התיקון ב-skp_spec.

## מה הושלם בהמשך הסבב (2026-07-14)
- **קייסינג לחלונות** — ראה למעלה. commit 22a7bc9.
- **צבעי קירות (סעיפים 1+2 נסגרו):** הבאג היה שעריכת קיר עדכנה רק interior_material בעוד קירות פנים נצבעים לפי side_a_color/side_b_color. עכשיו דיאלוג עריכת קיר בודד מציג Side A/B + צ'קבוקס "Same color on both sides" לקירות פנים, שומר wall_category/side_a/side_b; אותו צ'קבוקס נוסף לספריית הקירות ולעריכה המרובה. לקח: בעריכה מרובה חייבים לסמן את הצ'קבוקס שליד השדה כדי שהשינוי יחול.
- **כלי מתיחת קיר חדש (wall_stretch_tool.rb + כפתור):** קליק קרוב לקצה בוחר אותו, רפאים מקווקו של הקטע שמשתנה בלבד (לפי perpendicular_corners_xy — ניצב שמאלי!), קליק מאשר או הקלדה = **תוספת** לאורך (שלילי מקצר, VCB). מותח מהקצה שנבחר; ב-start מוזזים גם door_openings + position_along_wall_in של דלתות/חלונות; חסימה אם פתח נחתך; join_corners + רענון מולדינג. קובץ חדש רשום ב-main.rb + toolbar.rb (restart נדרש בהוספת קובץ).
- **עריכת קיר:** שדה Length (מציג רגל+אינץ', קובע אורך כולל מקצה הסיום) + רענון מולדינג אחרי עריכה; כפתור Edit Wall פותח בקליק אחד (קיר בודד = דיאלוג בודד, בחירה מרובה = מרובה).
- **מתיחה של קיר פינה:** שותפי הפינה הישנים בקצה הנמתח נבנים מחדש ומחוברים מחדש (בלי מיטר תקוע) — partners ב-commit! של wall_stretch_tool.

## מפת דרכים שסוכמה עם המשתמש (2026-07-14)
1. **מסמך "חוזה הדו-ממד"** (הצעד הבא! בצ'אט חדש): מה כל אלמנט חייב לשמור (attributes, Tags, ייצוג תוכניתי) כדי להפיק בעתיד תוכניות עבודה ל-LayOut — תוכניות מלמעלה, אליוויישנים, ועורך מלמעלה בסגנון Rayon.
2. כלים חדשים לפי החוזה: רצפות → תקרות → גגות (כלים נפרדים), וקשתות כשהמשתמש ירצה.
3. בסוף: שכבת הדו-ממד עצמה (סצנות/חתכים/LayOut, עורך מלמעלה).

## רשימת המשימות הפתוחות (עודכן 2026-07-14, אחרי ניקוי)
1. ידיות לדלתות ארון (assets/CLOSET DOOR HANDLES) — המשתמש דחה לבינתיים.
2. פתחים מעוגלים בקיר (קשת + Radius window) — הפיצ'ר הכבד.
3. חוב ישן (לוודא רלוונטיות לפני עבודה): באג מיטר Merge Wall; אייקונים 24px מטושטשים; עריכת עובי קיר לא משנה רוחב.
(הוסרו: כיול פרופילים — בוצע ותקין; גריד כנף בלחיצה — בוצע ב-07-10; חלונות placeholder — נסגר עם הוספת Hung/Slider/XOX/Garden; הרחבת קטלוג Casing — לא רלוונטי כרגע.)

## לקחים חדשים (2026-07-13)
- ה-shell של קלוד מציג קבצים קטומים באופן עקבי — ruby -c דרך ה-shell לא אמין; לוודא מול Read של הקובץ האמיתי.
- קבצי דיבוג בשורש: debug_casing.rb, debug_gap.rb, debug_molding_color.rb, debug_molding_scan.rb, debug_molding_cleanup.rb, debug_wall_colors.rb (אפשר למחוק כשלא צריך).

---

# היסטוריה: סבב 2026-07-10 ב'

## מה הושלם בסבב 2026-07-10 ב'
- **הבעיה הפתוחה נסגרה:** skp_profile_points קורא עכשיו חתך מגוף תלת-ממדי — איסוף פאות רקורסיבי (כולל קבוצות מקוננות, BASE-1) + זיהוי זוג פאות תאומות (נורמלים הפוכים, שטח שווה, מקס' נקודות = החתך). כל 7 הפרופילים עובדים.
- **orient_back_to_wall:** היפוך אוטומטי של פרופיל שהגב השטוח שלו לא ב-d=0 (לפי המקטע האנכי הארוך).
- **soften_facets!:** קצוות בין פאות בזווית < FACET_ANGLE (25°) מרוככים — אין קווי אורך על המולדינג.
- **גובה מולדינג:** שדות Height (in) בדיאלוג לבייס ולקראון, סקיילינג פרופורציונלי (scale_profile). נשמר ב-profile_h attr, שורד refresh!/toggle. ריק = גודל מקורי.
- **Apply to Selected Walls:** כפתור חדש — איחוד עם קירות שכבר יש עליהם מולדינג (לא מחליף). קירות בלי מולדינג ולא נבחרים מסומנים no_molding.
- **החרגת קיר (Toggle):** אחרי החרגה נבנה מחדש כל השאר — השכנים נסגרים בקצה ישר (בלי מיטר פתוח). rebuild_molding! מכבד את הבחירה האחרונה כולל None.
- **מולדינג עוקב אחרי הזזת קיר:** hook ב-ui_dialogs.rb (wall_move, אחרי commit_operation) — קריאה ל-MoldingManager.refresh! עטופה rescue. עריכה ראשונה מאושרת בקובץ המוגן.
- **שני מבנים במודל:** wall_components מקבץ קירות מחוברים (טול' 1"); צד המולדינג בקירות חוץ נבחר לפי מרכז המבנה של הקיר, לא מרכז כל המודל.
- **apply_all! מנקה עכשיו את כל קבוצות המולדינג לפני בנייה** (גם של קירות שהוחרגו מאז).
- commits: 505f59c + המשכים ב-door-stabilize.

## הרשימה לצ'אט הבא (סדר עדיפויות מוסכם — לא להתחיל בלי המשתמש!)

### קייסינג + מולדינג
1. קייסינג בדלתות **חוץ** לא עובד בכלל.
2. קייסינג בדלתות **פנים** הפוך — החלק הרחב צריך לפנות החוצה מהדלת (אל הבייסבורד), לא פנימה.
3. **חיבור T:** קיר פנים שנפגש באמצע קיר חוץ — הרצועות לא מתחברות. resolve_miters! מזהה רק קצה-לקצה; צריך זיהוי קצה-לאמצע (T-junction).
4. כיול סופי: קראון על קיר בכל 7 הפרופילים, מפגשי קייסינג–בייסבורד.
5. צבע למולדינג (כמו דלתות).

### פיצ'רים
6. **קירות: הארכה/קיצור קיר (שינוי אורך).** אופן המימוש (דיאלוג/גרירה) טרם הוחלט — לשאול את המשתמש. ייתכנו עוד דרישות קירות.
7. ידיות לדלתות ארון (assets/CLOSET DOOR HANDLES).
8. הרחבת קטלוג Casing.
9. פתחים מעוגלים בקיר (קשת + Radius window) — הפיצ'ר הכבד.
10. UI: גריד עיצובי כנף פנים נפתח בלחיצה.

### חוב טכני
11. באג מיטר Merge Wall.
12. 4 סוגי חלונות placeholder.
13. עריכת עובי קיר לא משנה רוחב (בהחלטת המשתמש).
14. אייקונים 24px.

---

# היסטוריה: סבב 2026-07-10 א'

## מה הושלם בסבב 2026-07-10
- **יש אישור מפורש מהמשתמש לעבוד גם על קירות וחלונות** (מולדינג/קשתות). CLAUDE.md עדיין אומר דלתות בלבד — האישור גובר.
- **ידיות Front Door** — הושלם ונבדק (ראה סעיף 07-07 למטה).
- **צבעים לדלת ולפריים:** door_color/frame_color ב-DOOR_SETTING_KEYS (restart!), בוררי צבע קטנים בדיאלוג + צ'קבוקס "Frame same color as door". door_frame_material/door_leaf_material ב-door_tool; DoorLeafStyles.build_leaf_body! מקבל leaf_mat:.
- **גלריות מתקפלות:** עיצובי כנף פנים + Front Door נפתחים בלחיצה (כמו ידיות).
- **מולדינג — פיצ'ר חדש שלם:** קבצים חדשים molding_library.rb / molding_tool.rb / molding_dialog.rb (רשומים ב-main.rb):
  - MoldingManager.apply_all!/remove_all!/refresh! — לכל הבית; קירות פנים = שני צדדים, חוץ = צד פנימי (היוריסטיקת מרכז).
  - מיטרים 45° בפינות (resolve_miters! + extrude_profile! עם caps מוזחים).
  - חורים לדלתות לפי bbox של גוף הדלת (כולל קייסינג!) — door_bbox_gaps (דלתות = Group/ComponentInstance עם type='door').
  - רענון אוטומטי אחרי הצבה/הזזה/מחיקה של דלת (hooks ב-door_tool/door_move_tool/door_delete_tool).
  - MoldingToggleTool — קליק על קיר מחריג/מחזיר (no_molding attr).
  - כפתורי toolbar + תפריט + אייקונים (molding_tool.svg, molding_toggle.svg).
  - MoldingDialog — גלריה עם SVG שנוצר מהפרופילים עצמם.
- **ספריית פרופילים של המשתמש:** `assets/MOLDING PROFILES` — BASE-1..3, CROWN-1..3, CASING-1 (.skp). skp_profile_points קורא פאה מהקובץ (זיהוי מישור אוטומטי). קטלוגי הקייסינג צומצמו ל-none/flat + CASING-*.skp (דינמי, DoorLibrary.casing_styles + DoorCasingProfiles.skp_spec).
- **קייסינג:** יושב על הפריים (CASING_REVEAL=0.25, היסט פנימה ב-build_u_casing_followme); הבייסבורד נעצר בקצה הקייסינג.

## הבעיה הפתוחה שבה עצרנו (נפתרה בסבב ב' — ראה למעלה)
קריאת הפרופילים מה-skp מחזירה **מלבנים** — כנראה המשתמש מידל גופים תלת-ממדיים ולא פאה שטוחה אחת, ו-max_by(&:area) תופס פאה לא נכונה. שתי דרכים: (א) המשתמש ישמור כל קובץ כפאת חתך אחת בלבד; (ב) להרחיב את הקורא לחלץ חתך מגוף (למשל הפאה עם השטח הקטן ביותר בין שתי המקבילות, או section). לבדוק עם המשתמש איך הקבצים בנויים.

## לקחים חדשים
- reload! משתמש ב-plugin_files שבזיכרון — קובץ חדש שנוסף ל-main.rb לא ייטען עד restart (או load ידני מפורש).
- שינויי דיאלוג = cp + InteriorPro.reload! + סגירה/פתיחה של הדיאלוג בלבד. restart מלא רק ל-DOOR_SETTING_KEYS.
- ב-Plugins היה קובץ-זבל בשם "MOLDING PROFILES" שחסם יצירת התיקייה (EEXIST) — נמחק. אם mkdir נכשל על EEXIST כשהתיקייה "לא קיימת" — לבדוק File.file?.
- ה-shell של קלוד מציג לפעמים קבצים קטומים — לוודא מול Read לפני בהלה.

## הצעדים הבאים (סדר מוצע)
1. לתקן קריאת פרופילים מ-skp (הבעיה הפתוחה למעלה) + לוודא שכל 7 הפרופילים נראים נכון בגלריה ובבית.
2. כיול מיקום/צורה: קראון בפועל על קיר, מפגשי קייסינג-בייסבורד בכל הפרופילים.
3. צבע למולדינג (כמו דלתות).
4. חלונות/דלתות עגולים וקשתות (יש אישור קירות/חלונות).

---

# היסטוריה: סבב 2026-07-07

קרא את זה ראשון בכל צ'אט חדש. משלים את CLAUDE.md (מוגבל לדלתות) ואת ה-RETRO.

## שיטת העבודה הנוכחית (חשוב!)
- **אין Cursor.** העוזר עורך ישירות בתיקיית המקור `C:\InteriorPro_Agent\source_readonly` (באישור המשתמש לכל צעד).
- הפלאגין החי רץ מ: `C:\Users\rordt\AppData\Roaming\SketchUp\SketchUp 2024\SketchUp\Plugins\interior_pro` (`InteriorPro::PLUGIN_DIR`).
- סנכרון: `FileUtils.cp` ב-Ruby Console (מקור ← Plugins) ואז `InteriorPro.reload!`. שינויי דיאלוג: לסגור ולפתוח את הדיאלוג. שינוי ב-DOOR_SETTING_KEYS (door_manager.rb, מוגן ב-const_defined?) — **חובה הפעלה מחדש מלאה של SketchUp**.
- **git: רק בתיקיית ה-Plugins** (branch: door-stabilize). **בתיקיית המקור אין git!** commit דרך טרמינל cmd עם `git -C "<נתיב Plugins>" ...`.
- **אזהרה קריטית (תקרית 2026-07-07): לפני עריכת קובץ משותף — לוודא שהמקור מסונכרן עם ה-Plugins!** תיקיית המקור הייתה מיושנת (door_manager/door_tool), העתקה דרסה קוד חי ושברה הכל. שוחזר מ-git. הדרך: `copy /Y` מ-Plugins למקור לפני עבודה על קובץ שלא נגענו בו בסשן.
- הדבקות ארוכות בקונסול נקטעות — סקריפטים לקובץ ואז `load`. בשורש: `debug_door.rb`, `debug_handles.rb`, `debug_front_doors.rb`, `debug_front_door.rb`.
- בדיקות תמיד על סצנה נקייה; **רכיבי ידיות/דלתות נשמרים במודל (cache) — בדיקות בקובץ חדש לגמרי (File → New)!**
- המשתמש מדביק פקודות בלבד — לתת פקודות מוכנות עם puts ופלט ברור, ולציין איפה מדביקים (Ruby Console / cmd).

## מה הושלם בסבב 2026-07-07
### ידיות — סגור
- ידית 5: `yflip: true` (שיקוף קדימה-אחורה סביב מרכז הרכיב, לא מזיז כיול) + dy מכויל -3.6875.
- ידית M-8: `yflip: true` + dy מכויל -3.25.
- נושא ה-leader lines ירד — המשתמש אישר שאין קו נראה. לא לגעת.
- דיאלוג: בחירת ידית = גלריה נפתחת בלחיצה (סגורה כברירת מחדל), ציורי SVG 2D פר-ידית, 5 עמודות. ה-select המקורי מוסתר ונשאר לתאימות.
- גריד עיצובי כנף (פנים): 2 שורות של 5, חצי גודל, בלי שמות (tooltip בריחוף).

### תיקונים קריטיים
- **שוחזר `move_hosted_doors!`** ב-door_manager.rb (נמחק בטעות ב-2026-07-02 בעוד הקריאה ב-ui_dialogs.rb נשארה — הזזת קיר עם דלת הייתה שבורה). מזיז דלתות+חלונות של הקיר.
- **`exterior_effective_n`** ב-door_tool.rb: דלתות חוץ נבנו החוצה מהקיר כשגוף הקיר בצד ה-שלילי של פאת הקליק. עכשיו בודקים מהפאות (wall_v_span_from_faces) והופכים את n בצורך. קירות תקינים לא מושפעים.
- אזהרת Yes/No בהצבת דלת חוץ על קיר פנים (wall_category).
- הסף (threshold): מדרגות ה-nose החיצוני הוסרו לגמרי — נשאר בסיס בלבד (append_exterior_threshold_nose! ריק).

### Front Door — פיצ'ר חדש (עובד)
- סוג 'Front Door' ראשון ב-EXTERIOR_TYPES. ברירת מחדל 36×80.
- **תצורות (front_config):** single / single_1sl / single_2sl / double. sidelite = פאנל מזוגג + mullion ‏1.5″. רוחב מתעדכן אוטומטית בדיאלוג לפי תצורה.
- **Transom:** צ'קבוקס + גובה; סימון מגדיל את גובה הפתח אוטומטית (הכנף שומרת גובה). בר אופקי + זכוכית מעל.
- **6 עיצובי כנף (front_leaf_style, גלריה בדיאלוג):**
  1. Craftsman 3-Lite — 3 חלונות למעלה + 2 פאנלים שקועים בשני הצדדים (בלי מדף).
  2. 5-Lite Ladder — 5 זכוכיות אופקיות.
  3. Steel Glass — מסגרת פלדה שחורה + זכוכית מלאה; גריד מ-Glass Grid (none=חלק).
  4. Farmhouse 4-Lite — זכוכית למעלה, גובה ב-% (front_glass_ratio, ברירת מחדל 50) + גריד מ-Glass Grid.
  5. Modern Lines — כנף חלקה + 4 פסים אופקיים כהים.
  6. Steel Arch — קשת פלדה + זכוכית + גריד קבוע (קו אמצע עד הרצפה), מילוי אטום בפינות מעל הקשת (הפתח בקיר נשאר מלבני!).
- **מפתחות חדשים ב-DOOR_SETTING_KEYS:** front_config, front_leaf_style, front_glass_ratio, sidelite_width, transom, transom_height (+ attributes תואמים). שינוי בהם = restart.
- **בנאים ב-door_tool.rb:** build_front_door_geometry!, front_door_spans, build_front_leaf_styled!, build_front_arch_leaf!, build_front_panel_with_holes!, add_front_grid_bars!, add_front_bar!, front_door_mats (חומרים: InteriorPro_Front_Steel, InteriorPro_Front_Groove).
- **תצוגת כל העיצובים:** File → New ואז `load 'C:/InteriorPro_Agent/source_readonly/debug_front_doors.rb'` — בונה את כולן ממוספרות בשורה.

## לקחים מהסבב (קריטי!)
- **לא להעתיק קובץ מהמקור ל-Plugins בלי לוודא שהמקור עדכני** — זו הייתה התקלה הגדולה של הסשן.
- אחרי כל צעד ירוק — commit מיד. ה-git הציל את הסשן.
- כשגוף/פאה נמחקים אחרי erase של פאה פנימית — לאתר מחדש את הפאה (grep(Sketchup::Face).first) לפני pushpull.
- פוליגון קשת: בלי נקודות כפולות בקצוות, ולשמור על סדר נקודות רציף (self-intersection שובר add_face).
- דלתות חוץ ופנים משתמשות בקונבנציות עומק שונות — פנים לפי פאות הקיר, חוץ 0..thickness + היפוך n בצורך.

## הושלם 2026-07-07 (סבב ב')
- **עריכת Front Door נבדקה** — כל השדות חוזרים, rebuild תקין.
- **ידיות Front Door עובדות end-to-end:** קבצים 1-4.skp ב-`assets/FRONT DOOR HANDLES`; טבלת כיול נפרדת `FRONT_HANDLE_FIT` ב-door_handles.rb (שמות מספריים לא מתנגשים עם interior); תמיכה ב-rz (סיבוב אנכי) ב-fit_transform; `fit_offset/fit_transform/both_sides?` מקבלים kind; door_tool: `handle_kind`, `handle_enabled?` כולל front, הצבה ב-build_front_door_geometry! (single לפי swing, double במרכז, עוגן 3" מהקצה); דיאלוג: גלריה + thumbs נפרדים ל-front (`FRONT_HANDLE_NAMES`, `frontHandleThumbSvg`), רשימת ידיות מוחלפת לפי סוג דלת. נבדק: single, double, עריכה.
- סצנת כיול: `debug_front_handles.rb` (שני צדדים). **לקח: מוסך ה-shell של קלוד הציג קבצים קטומים — אם ruby -c נכשל על EOF, לבדוק את הקובץ האמיתי לפני בהלה.**

## נושאים פתוחים / הצעדים הבאים (לפי סדר שסוכם)
3. **ידיות לדלתות ארון** — תיקייה: `assets/CLOSET DOOR HANDLES`.
4. **סוגי Casing** — הרחבת הקטלוג (המשתמש רצה, טרם פורט).
5. **מולדינג בייסבורד + תקרה (crown)** — חורג מ"דלתות בלבד"; דורש אישור מפורש + קבצים חדשים נפרדים.
6. **פתחים מעוגלים בקיר** (קשת אמיתית + Radius window) — הפיצ'ר הכבד; דורש שינוי במודל הפתחים ובבנאי הקיר (wall_tool.rb אסור כרגע).
7. UI פתוח: גריד עיצובי כנף פנים שייפתח רק בלחיצה (כמו הידיות).
8. חוב ישן: באג מיטר Merge Wall; 4 סוגי חלונות placeholder.

## קבצים זמניים בשורש
- `debug_front_doors.rb` — תצוגת עיצובי Front Door (להשאיר).
- `debug_front_door.rb` — דיבוג מיקום דלת מול קיר (להשאיר).
- `debug_handles.rb` — סצנת כיול ידיות (להשאיר).
- `report_handle_lines.rb`, `door_manager_before_delete.rb`, `dh_c319962.rb` — אפשר למחוק.
