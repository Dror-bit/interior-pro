# סטטוס והעברה — Interior Pro

## עיקרון-הליבה (חוק, לא המלצה)
**הגיאומטריה תמיד נבנית מחדש מהנתונים — אף פעם לא מתוקנת/מטולאת.**
- ה-attributes (מיקום, גודל, גובה, רשימת פתחים) = מקור-האמת.
- הגיאומטריה = תוצאה בלבד. כל עריכה = שינוי נתונים → `rebuild` מלא.
- אסור: delete→heal→recut, boolean על solid קיים, fill של פתח. אלה גורמים לקריסה מצטברת.

## מה עובד עכשיו (נבדק ב-SketchUp)
- **קירות:** ציור, ספרייה, חומרים, מיטר לקירות **בלי** פתחים (דרך `corners_xy` ב-`build_geometry_in_group`). ✅
- **דלתות — הצבה:** native ("בנה את החור", `rebuild_wall_native!`). ✅
- **דלתות — מחיקה / הזזה / עריכה:** הומרו ל-native (regenerate-from-data). ✅ נבדק ועובד.

## מה שונה בסשן הזה (הכל ב-`door_manager.rb` בלבד)
מתודות-עזר חדשות (קוראות ל-`WallTool.read_door_openings` / `persist_door_openings!` / `rebuild_wall_native!`):
- `remove_native_opening!(wall, t)` — מחיקה: מסיר פתח מהרשימה.
- `update_native_opening_t!(wall, old_t, new_t)` — הזזה: מזיז פתח לאורך הקיר.
- `update_native_opening_dims!(wall, t, w, h, fo)` — עריכה: משנה גודל פתח במקום.
- `delete_door` / `move_door` / `update_door` — קיבלו מסלול native (אטומי, operation אחד, abort על כישלון). מסלול ה-boolean/fill הישן נשאר כ**fallback לקירות לא-native בלבד** — לא נקרא בזרימות הללו.

## כללי עבודה (חובה)
- כתוב בעברית, קצר.
- שינוי אחד בכל פעם → בדיקה על **סצנה נקייה** (קיר חדש, דלת אחת) → commit אחרי כל ירוק.
- regenerate-from-data בלבד. לא להחזיר fill/boolean לזרימות החיות.
- bash mount מפגר אחרי עריכות — מקור-האמת לתחביר הוא `InteriorPro.reload!` ב-SketchUp, לא `ruby -c` ב-bash.
- Claude עורך קבצים ישירות; **לא עושה commit** — המשתמש שומר.

## הצעד הבא (מעוצב, מחכה לאישור) — מיטר-פינות לקירות עם דלת
**הבעיה:** קיר עם פתח נבנה native מ-`drawn_start/end` ישר — מתעלם מ-`corners_xy` הממוטר. לכן הפינה לא נסגרת (החור כן שורד).
**הכיוון הנכון (לפי החזון — בנאי-קיר אחד):** ה-native rebuild צריך לבנות את הקיר מ**נתוני הפינה הממוטרים** + הפתחים, כך שהמיטר אינטגרלי לבנייה (לא post-fix). זה מאחד מיטר + פתחים תחת אותו `rebuild`.
**חשוב — מה שנכשל קודם ואסור לחזור עליו:** הזזת קודקודים (`apply_native_miter_corners!`) בתוך ה-rebuild המשותף — היא רצה גם בזרימות דלת ויצרה אי-יציבות. אם נעשה מיטר, הוא חייב להיות חלק מבניית-הקיר עצמה, מבודד מזרימות הדלת.

## מפת דרכים (מהחזון של דרור)
1. **בסיס יציב** — דלתות native ✅ ; נשאר: מיטר-פינות לקירות-עם-פתח. ← כאן
2. דלתות + **חלונות** באותה ארכיטקטורה (איחוד תחת רשימת-פתחים אחת עם שדה `type`).
3. מצב שרטוט 2D נעול (top view + parallel projection).
4. אוטומציה ל-LayOut ("Generate Construction Set").
5. מטבחים/ארונות פרמטריים.
6. עתיד: ייבוא אדריכל / Rayon.

## נקודת עיגון
commit אחרון יציב: דלתות native (delete/move/edit). מומלץ tag `stable-doors`.
