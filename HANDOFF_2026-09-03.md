# Interior Pro — מסירה 2026-09-03 (נסיגה 0, החור שנסגר, והפאנלים)

קרא את זה קודם, ואז `CLAUDE.md`. הקודמת: `HANDOFF_2026-09-02B.md`.
**אל תסמוך על הסיכום — תאמת מול הקוד החי.**

ענף `door-stabilize`. בדיקות: `cd tests` ואז `LANG=C.UTF-8 bash run_all.sh ..`
→ **153 ירוקות**. חדשות היום: **rt104** (סגירת החור) ו-**rt105** (הפאנלים
נחתכים בדורמר). שתיהן נופלות על הקוד הישן — נבדק.
**rt51 ו-rt52 אדומות ולא קשורות אליי:** הקובץ `tests/rt51_tmp/a (2).jpg`
נכנס בטעות ל-git, אז הבדיקה מקבלת `a (3).jpg` במקום `a (2).jpg`. למחוק
אותו מ-git דרך ה-CMD שלו וזה ייפתר.

---

## מה נסגר היום

### 1. שלוש דרכים להציב דורמר (הוא ביקש)
שורה חדשה בפאנל, **Where it sits**:
- `Click anywhere` — הקליק קובע את הנסיגה, כמו עד היום.
- `Depth from the fascia` — מספר מוקלד; העכבר מזיז אותו **רק הצידה**.
- `Flush with the wall` — הקיר הקדמי בדיוק על קיר הבית.

`DormerManager.place_mode(spec)` מחליט; `wall_setback(rf)` הוא
**ה-overhang של הגג** — נמדד במודל שלו: מקו הזחילה עד פני הקיר בדיוק 12.00"
כשה-`overhang_in` הוא 12. לא ניחוש.
תאימות לאחור: קורא בלי `place_mode` ובלי `follow_click` מקבל בדיוק את
ההתנהגות הישנה, ולכן rt99/rt103 לא זזו.

### 2. הזחילה כבר לא מגבלה
`fits_on_face?` דרש 12" מכל קצה של הפאה, **כולל מהזחילה**, ולכן פלוש נדחה.
עכשיו `eave_edges(pts)` מזהה את הקצה הנמוך, `ring_distance` מקבל רשימת
קצוות לדלג עליהם, ופינה שיצאה מהפאה מותרת אם היא יצאה דרך הזחילה.
הרכס וההיפים עדיין דורשים 12".

### 3. הקירות — לפי מיקום, לא לפי נורמל
`paint_wall!` קרא את `f.normal`, והנורמל נקבע לפי כיוון הליפוף של
`add_face`. שתי הלחיים נבנות מאותו סדר נקודות ב-w משוקף, ולכן בלחי אחת
הנורמלים הפוכים — והטקסטורה של הבית ישבה בפנים והלבן בחוץ.
**ההוכחה שנמדדה:** בלחי אחת הפאה של 570.4 אינץ' רבועים היתה Stucco,
בשנייה אותה פאה עצמה היתה `#ffffff`.
עכשיו ההחלטה לפי **המיקום** של הפאה ביחס לאמצע הקיר. מיקום לא מתהפך.
**אי אפשר לבדוק את זה בענן** — אין שם חומרים אמיתיים. נבדק רק אצלו.

### 4. מחיקה והזזה באמת סוגרות את החור — הבאג הגדול של היום
`heal_roof!` הדפיס "2 skin(s) back" מעל חור פתוח לרווחה. שלושה דברים
נמדדו, כל אחד מהם לבד היה מספיק כדי להשאיר חור:

1. **`ents.add_face(ring)` על טבעת שכבר קיימת כלולאה פנימית לא יוצר כלום —
   הוא מחזיר את הפאה המקיפה.** נמדד: `area 204772.9`, כל המדרון.
   הקריאה שבאמת מותחת עור על לולאה קיימת היא `Edge#find_faces`.
2. **הרים (הדפנות האנכיות של עובי הגג) חייב ללכת *אחרי* העור, לא לפני.**
   `find_faces` בונה כל פאה שהקצוות האלה יכולים לתחום — כולל הרים.
   נמדד: `rim 5, skinned +5, seam 0` — מחקנו 5 ובנינו 5.
3. **התפר.** אחרי העור יש שתי פאות באותו מישור עם קצה ביניהם, והקצה
   מצויר כקו על הגג ("אבל עדיין רואים אותו בגג"). מוחקים אותו והן
   מתמזגות לפאה אחת.

בנוסף, `heal_roof!` אסף מישור **מכל** פאה משופעת בקבוצת הגג בלי לבדוק
`covers_point?`. בגג היפ זה ארבעה מדרונות, ולכן כל מחיקה השאירה טלאים
תלויים באוויר מעל הבית. `cut_roof!` תמיד בדק את זה; ההיפוך שלו לא.

הסדר הנכון עכשיו: **מוצאים את הלולאה שקיימת → עור → רים → תפר.**
`tests/rt104.rb` מצמיד את הטענה שאפשר לבדוק בסטאב (מישור אחד, לא ארבעה).
הסטאב לא יודע לולאות/קצוות/`find_faces`, ולכן המסלול האמיתי נבדק רק אצלו.

### 5. הפאנלים יודעים שיש חור בגג
הוא הסתכל מבפנים והפאנלים רצו דרך הדורמר.
- `planes_from_faces` קורא את המתאר מ-**`outer_loop`** (`f.vertices` מחזיר
  גם את קודקודי החורים — המוקש המוכר) ומעביר כל לולאה פנימית כ-`holes`.
- `RoofTileMath.spans_minus(spans, cuts, min_len)` — חיסור מקטעים.
- `run_slots` מחסר את החורים מהמקטעים, וכך ריב שחוצה דורמר נשבר לשניים —
  בדיוק כמו שוואדי כבר עשה לו.
- **דורמר הוא לא רכס:** קצה שנוצר מדורמר לא מקבל `setback`. הנסיגה קיימת
  כדי להתחבא מתחת לרכס-קאפ או לתעלת הוואדי; לדורמר אין אף אחד מהם, והקרש
  צריך **להיפגש**. בלי זה נשאר שטח דק חשוף מסביב ("הוא נחתך יותר מדי").
- `RoofTilePlace.relay_runs!(roof)` פורס מחדש את השדה על גג אחד, ו-
  `DormerManager` קורא לו אחרי כל הצבה, מחיקה ו-replant (פעם אחת בסוף
  ה-replant, לא פעם לכל דורמר).

---

## מה נשאר פתוח

1. **הריב נמדד בעמודה אחת ומצויר בעמודה אחרת — 3⅝".**
   הוא מדד את הפער ואמר "3 1/2". המספר המדויק מהמדידה:
   בגג 0 דורמר 1 הריב מצויר ב-`w 2.73..4.73` (מרכז 3.73) אבל המקטע שלו
   חושב ב-`w 7.34` — **הפרש 3.61"**. בגג 1 דורמר 0 ההפרש 2.15".
   כלומר זה **לא קבוע**, ולכן זה לא סתם `u_off` לא נכון.
   זה אותו סוג באג כמו ההערה מ-2026-08-21 ב-`run_slots` ("on a hip every
   rib was measured 14" away from where it was drawn"), שתוקנה עם
   `u_off = run_pitch/2`. הוואדי של הדורמר אלכסוני, ולכן כל אינץ' של
   טעות ב-u הופך לאינץ' ומשהו של טעות ב-s. **הוא אמר "זה בסדר גמור"
   ובחר להשאיר את זה — לא לפתוח בלי שיבקש.**
   הקובץ שמודד את זה: `debug_dormer_panels.rb`.
2. **חלון בקיר הקדמי**, מספריית החלונות הקיימת. זה מה שכל עניין
   "הגובה מוקלד" קיים בשבילו.
3. **הגגון יקבל את אותם רעפים ורידג'-קאפ כמו הגג שעליו הוא עומד** — לא רק
   את הצבע. אישר במפורש. אותה מכונה (`RoofTilePlace.place_runs!` /
   `place_eaves!`) צריכה לרוץ גם על מישורי הגגון, ורידג'-קאפ על הרכס שלו.
   עכשיו זה קל יותר: `planes_from_faces` כבר מחזיר מישורים מוכנים.
4. **גאטרס.**
5. **rt51/rt52** — למחוק את `tests/rt51_tmp/a (2).jpg` מ-git.

---

## מוקשים שנמדדו היום (לא לחזור עליהם)

- **`add_face` על לולאה קיימת מחזיר את הפאה המקיפה.** `find_faces` הוא
  הכלי. (זה אותו דבר ש-`window_tool.rb` צריך אחרי `add_line`.)
- **`find_faces` בונה גם את הרים.** קודם עור, אחר כך מוחקים רים.
- **שתי פאות באותו מישור עם קצה ביניהן = קו על הגג.** מוחקים את הקצה.
- **`f.normal` לא אמין לקביעת פנים/חוץ** — הוא נקבע לפי כיוון הליפוף.
  להשתמש במיקום.
- **`f.vertices` מחזיר גם קודקודי חורים** — `outer_loop` תמיד.
- **`heal_roof!` חייב `covers_point?` בדיוק כמו `cut_roof!`.**
- **סנכרון הקבצים לתיקייה שלו לוקח כמה דקות.** פעמיים קראתי דוח ישן
  וחשבתי שהקוד לא עובד. לבדוק `ls -l` על הקובץ לפני שמסיקים משהו.
- **git מהצד שלי מראה 54 קבצים "modified" בגלל CRLF.** אצלו ב-CMD הם
  נקיים. לא להסיק כלום מ-git מהצד שלי.
- `roof_manager.rb` הוא **CRLF**. `dormer_*.rb`, `roof_tile_*.rb`,
  `main.rb`, `toolbar.rb`, `tests/rt*.rb` — LF.

## קבצי דיבוג חדשים (gitignored)
`debug_dormer_setback0.rb` — איפה קיר הבית לעומת הזחילה, ומה frame()
אומר בכל נסיגה.
`debug_after_delete.rb` — לכל פתח בגג: **OPEN או PATCHED**. לספור לולאות
זה לא מספיק — טלאי סגור משאיר לולאה.
`debug_dormer_walls.rb` — לכל פאה של קיר דורמר: לאן היא פונה ומה החומר
שעליה.
`debug_dormer_panels.rb` — לכל פאנל ליד דורמר: המרחק לכל קו של הדורמר.
`debug_fill_holes.rb` — **משנה את המודל**: סוגר כל פתח יתום בגג (עור,
רים, תפר). זה מה שניקה לו את הגג.

## איך לעבוד איתו
- עברית, משפט-שניים, כל פקודה בקופסה נפרדת עם "Ruby Console" או "CMD".
- **git רק דרך ה-CMD שלו.** commit אחרי כל צעד עובד.
- **שרטוט לפני בנייה** — תמונה מרונדרת עם המידות האמיתיות של המודל שלו,
  ואז **שאלה אחת קצרה**. ASCII הוא לא מבין.
- **אף פעם לא להגיד "זה עובד" מתמונה.** היום אמרתי "הדק נסגר" מתמונה
  וזה היה חור, והוא צדק שצעק. אם לא מדדת — תגיד שלא מדדת.
- כשהוא אומר "לא עובד" — **קובץ שמודד**, לא ניחוש.
- כשהוא חוזר על עצמו — להפסיק להסביר ולבנות.
