# Interior Pro — מסירה 2026-09-16 (וואלי: חיבור גג הגראז' לגג הבית)

קרא את זה קודם, ואז `CLAUDE.md`. הקודמת: `HANDOFF_2026-09-15.md`.
**אל תסמוך על הסיכום — תאמת מול הקוד החי.**

בדיקות: `cd tests` ואז `LANG=C.UTF-8 bash run_all.sh ..` — **197 ירוקות**
(rt51/rt52/rt60 אדומות ישנות). חדשה: **rt151** — כל מתמטיקת הוואלי, על
המספרים האמיתיים מהמודל שלו (valley_report.txt).

## דגל

`InteriorPro::RoofManager::USE_ROOF_VALLEY` — **false בקובץ בכוונה**.
להדלקה ובנייה מחדש (קופסה אחת):
```
InteriorPro.reload!; InteriorPro::RoofManager.send(:remove_const, :USE_ROOF_VALLEY); InteriorPro::RoofManager.const_set(:USE_ROOF_VALLEY, true); InteriorPro::RoofManager.roofs.each { |r| InteriorPro::RoofManager.build_roof!(replace: r) }; puts "valley ON"
```
rt151 מדליק אותו לבד לבדיקה ובודק שה-ברירת מחדל כבויה.

## מה נבנה (הכל ב-roof_manager.rb, חסום ב-USE_ROOF_VALLEY)

הגיאומטריה של הבית שלו: בית 102", גראז' מונמך (top 84) דבוק בדרום,
קירות מערב בקו אחד. רכס הגראז' (x=-459.975) מת במישור גג הבית ב-
(y=74.54, z=128.63) — בדיוק גובה הרכס שלו. רגלי הוואלי נוחתות על איב
הבית ב-x=-546.5 ו--373.4 (קטע של 173").

- **מתמטיקה טהורה**: `plane_abc`, `planes_meet`, `planes_apex`,
  `roof_valley`, `valley_cuts`, `valley_low_spans`,
  `valley_low_deck_cuts`, `two_side_planes`, `roof_geom` (deck_z = על
  קו האיב, לא על הקיר!). הכל ב-rt151.
- **הגראז' מתארך** עד הוואלי: `valley_extend!` — שני משולשים על
  המישורים שלו, מהקו y=17 עד הקודקוד.
- **הבית נחתך**: פאשיה/דריפ/סופיט מדלגים על קטע הוואלי (דרך
  `gable_spans` הקיים); הדק וכל הקליפות המקבילות + שדה הרעפים נחתכים
  במשולש (`valley_cut_deck!` / `valley_cut_one_plane!`); הגטר
  והדאון-ספאוטים מתים על **כל** הקטע שמעל הגראז' (`over`,
  `valley_over`, פרמטר `avoid` ב-`build_downspouts!`).
- **הגראז' נחתך בקיר הבית**: הטרים שלו נעצר בקו הקיר
  (`valley_low_spans`), ורצועת האוברהנג של הדק/רעפים נחתכת לאותו קו
  (`valley_low_deck_cuts`).
- **רעפים**: לפני ההנחה חותכים חור פנימי (inset 0.35) — המניח יודע
  חורים; אחרי ההנחה חיתוך מלא + `valley_cull_pieces!` שמעיף חתיכות
  רעף שמרכזן בתוך אזור חתוך. **טרם נבדק אצלו** (הצעד האחרון שנכתב).

## מלכודות שנמדדו (לא לחזור עליהן)

1. **חיתוך שקע בשפת פאה לפני הנחת רעפים מעלים את הרצים מכל האזור** —
   רק חור פנימי עובד. (התמונה שלו: חצי גג בלי רעפים.)
2. **שדה הרעפים יושב ב-110.52, לא 110.5** — חיתוך חייב את הגובה
   המדויק מהפאה עצמה; add_line עם סטייה 0.02 לא נדבק ולא מפצל.
3. **הדק הוא סנדוויץ'** — תחתית 107, עליון 108, שדה 110.5. חיתוך של
   קליפה אחת לא מספיק.
4. **ענף ה-spans ב-build_band!/build_profile_band! התעלם מ-
   square_flags** — ברגע ששמים דילוג על צלע, הפינות הרחוקות שלה איבדו
   את החיתוך הישר. תוקן: קצה-על-פינה מקבל את טיפול הפינה המלא.
5. רעפי האיב התלת-ממדיים מונחים לפי הקצה, לא לפי הדק — חור בדק לא
   מזיז אותם; לכן `valley_cull_pieces!`.

## מה נשאר (הרשימה שלו, לפי הסדר)

1. **לוודא את חיתוך הרצועה + הרעפים אצלו** (הקוד נכתב, לא נבדק).
2. **קצה הפאשיה בוואלי באלכסון** — הפאשיה/דריפ של הבית נחתכות היום
   ישר ברגל הוואלי; הוא רוצה חיתוך על מישור גג הגראז', "כמו מדיק".
   כל רכיב נפגש עם המישור בגובה אחר ⇒ t שונה לכל רכיב. הוא הציע לבנות
   ידנית דוגמה — לקחת אותו על זה ולמדוד.
3. **החור המשולש ליד קיר הבית** (בתמונה שלו, מעל הקיר מתחת להארכת
   הגראז') — לסגור בקיר, כמו gable_wall_top.
4. **רידג' קאפ לגראז' נעלם** אחרי ההארכה — להחזיר, כולל עד הקודקוד.
5. הדאון-ספאוט של הגראז' ליד הקיר — אינץ'-שניים דרומה.
6. הוא הזכיר שמדייק רוף עושה את החיבור יפה — יש אצלו medeek_truss/wall
   בפלאגינס, אפשר להציץ לרעיונות.

## קבצי מדידה מהסבב (בשורש)
`debug_valley.rb` (שני הגגות, מישורים, מי מול מי) · `debug_valley2.rb`
(קליפות דרום + כיסוי בפועל) · `debug_valley3.rb` (ניסוי חי של החותך,
undo אחד) · `debug_valley4.rb` (מתי נבנה, מה נשאר ברצועה).
הדוחות: `valley_report.txt` · `valley2..4_report.txt`.

## לשימוש
תג חזרה: `before-valley` (git). גיבוי מלא:
`C:\Users\rordt\interior_pro_backup_2026-09-03.zip`.
