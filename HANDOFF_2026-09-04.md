# Interior Pro — מסירה 2026-09-04 (יום של באגים בגג, כולם נסגרו)

קרא את זה קודם, ואז `CLAUDE.md`. הקודמת: `HANDOFF_2026-09-03.md`.
**אל תסמוך על הסיכום — תאמת מול הקוד החי.**

ענף `door-stabilize`, עץ נקי. בדיקות: `cd tests` ואז
`LANG=C.UTF-8 bash run_all.sh ..` → **156 ירוקות**.
**rt51/rt52 אדומות ולא קשורות:** `tests/rt51_tmp/a (2).jpg` נכנס בטעות
ל-git, אז הבדיקה מקבלת `a (3).jpg`. למחוק אותו מ-git ב-CMD שלו וזה ייפתר.

היום לא נבנתה אף תכונה חדשה. כל היום היו באגים בגג, וכולם נמדדו לפני
שתוקנו. הצעד הבא הוא בנייה: רעפים על הגגון.

---

## מה נסגר היום

### 1. הריבים בלטו מתחת לקצה הגג — קומפוננט ישן במודל שלו
"שברת את הקוד בגג... הם יוצאים מחוץ למסגרת."

`debug_ribs_edge.rb` מדד: הריבים מתחילים ב-v ‎-7.03 כשקצה הגג הוא 0.
`debug_rib_defn.rb` מצא למה: הקומפוננט `IP_TileRun_seamRib_...` ששמור
במודל שלו הוא **ישן** — 11 פאות, ובהן קופסה דקה נוספת מ-0 עד ‎-0.0237.
הקוד היום בונה שש פאות בלבד, בדיוק מ-0 עד 1. `fetch_or_build` מושך לפי
**שם**, ולכן החזיר את הישן. הריב נמתח לאורך הריצה, אז 0.0237 הפכו ל-7"
בגג של 348" ו-2.2" בגג של 145" — לא קבוע, וזה מה שבלבל בהתחלה.

**התיקון היה במודל שלו, לא בקוד:** מחקנו את הקומפוננט ובנינו מחדש.
**המוקש עצמו עדיין פתוח — ראה "מה נשאר פתוח" §2.**

### 2. `relay_runs!` מחק רק סוג אחד של רעף (rt106)
"שלא נוצר חור ברעפים בספניש ובטיל המרובעים... הספאניש מכפיל את כמות
הטיל מסביב לגמלון."

שורה אחת עשתה את שניהם: היא מחקה רק אינסטנסים ששמם מתחיל ב-`IP_TileRun`
— הצינור המיוצר. הטיל השטוח הוא `IP_TileFlatWedge`, מתכת לחוצה היא
`IP_TileSheet`, והספאניש שלו הוא קומפוננט **מהקובץ שלו** עם שם משלו. על
שלושת אלה לא נמחק כלום: השדה החדש הונח על הישן. מכאן גם הכפילות וגם
"אין חור" — הרעפים שכיסו את הדורמר היו הישנים, שהונחו לפני שהדורמר היה.

עכשיו `field_part?` / `field_piece?` שואלים את ה-**stamp** על הדפיניציה
(`part`: tile_run / tile_sheet / tile_flat) ולא את השם; הרעף של המשתמש
מזוהה לפי זהות מול `asset_tile`. **המסגרת (`tile_edge`) והרידג' קאפ לא
נמחקים** — relay לא בונה אותם מחדש.

### 3. הרעף השטוח לא ידע בכלל שיש חור (rt107)
`flat_slots` הוא מסלול פריסה **נפרד** מ-`run_slots`, ושום דבר בו לא הסתכל
על `pl[:holes]`. נמדד במודל שלו: 59 ו-52 רעפים שלמים בתוך שני הפתחים.

נוסף `RoofTileMath.clip_outside_poly(rect, hole)` — התאום של
`clip_to_poly`: רעף שכולו בפנים נעלם, רעף על הגבול נשאר רק בחלק שבחוץ.
הוא חותך **לפי הצלע הקרובה בלבד** ובוחר את החתיכה הגדולה ביותר, כי רעף
חייב לחזור כפוליגון אחד. על פינה של החור זה מוותר על בלוק הפינה במקום
להשאיר רעף תלוי מעל הפתח.

### 4. הכל נעצר בקיר החיצוני, לא בפנימי (rt108)
"כל הדברים האלו הקשורים לגג צריכים להיחתך בקצה החיצוני של הקיר ולא הפנימי."

נוסף `RoofTileMath.grow_poly(poly, d, mode)` — הזזת כל צלע החוצה לאורך
הנורמל שלה וחיתוך מחדש בפינות. שלושה מצבים:
- `:all` — כל הצלעות.
- `:walls` — הכל **חוץ מהצלעות שפונות במעלה המדרון**. אלה צלעות
  ה-**וואלי**, שם הגגון מת לתוך הגג הראשי ואין קיר להסתתר מאחוריו.
  כשהזזנו גם אותן נאכלו רעפים **מעל** הדורמר והוא הקיף את זה בירוק
  בתמונה. עכשיו הקצה העליון לא זז בכלל.
- `:sides` — רק שתי הלחיים.

ובנוסף, ב-`run_slots`: ריצה נחתכת לפי **קו האמצע** שלה בזמן שהגוף שלה
רחב `half_piece` לכל צד, ולכן צינור רומא של 14" עדיין נכנס פנימה. הלחיים
מקבלות `half_piece - wall_th`. **ניסיון ראשון היה `half_piece` מלא וזה
פתח לֵיין ריק של פוט לכל צד — "יותר מידי פתחים".** להסתתר מתחת לקיר זה
בסדר גמור (שם יושב הפלאשינג); מה שאסור זה לצאת מהצד השני, לתוך החדר.

### 5. גם הסיפון עצמו — `opening_plan` (rt95/rt100/rt101)
"השכבה שמתחת לרעפים/שינגלס צריכה להיחתך בקצה החיצוני של הקיר."

הפתח בסיפון היה הפתח ה"גס" — הפאות **הפנימיות** של שלושת הקירות — אז
הסיפון המשיך מתחת לכל קיר וקצהו נראה כשפה מסביב לפתח. עכשיו `hw = half`
ו-`s0 = s_front`: הסיפון נעצר על הפאה החיצונית, בדיוק כמו הרעפים מעליו.
**מאחור, בוואלי, שום דבר לא זז.**
בעקבות זה: `relay_runs!` מעביר `hole_grow: 0` (החור כבר בחוץ) ו-`wall_th`
בנפרד, שממנו מחושבת ההנחה בלחיים.

### 6. הרידג' קאפ לא נעלם — הוא היה כבוי
נמדד: `roof 1: set_ridge_cap=false`. הגג הגדול שמור עם רידג' קאפ כבוי,
עוד לפני שנגענו במשהו. הודלק עם
`build_roof!(replace: r, ridge_cap: true)`. לא באג.

---

## מה נשאר פתוח

1. **`relay_runs!` מזין למניח הרעפים גם את הפאה התחתונה של הגג.**
   הפילטר הוא `f.normal.z > 0.2`, ו-**נורמל לא אמין** — הפאה התחתונה של
   הלוח כתובה בכיוון ליפוף הפוך והנורמל שלה מצביע למעלה. נמדד במודל שלו:
   זוגות פאות באותו שטח בדיוק, 0.54" זו מעל זו, שתיהן עוברות את הפילטר.
   התוצאה: שדה שני מונח **מתחת** לסיפון, שלא רואים מבחוץ. הפתרון הוא
   אותו חוק שכבר למדנו פעמיים — **לפי מיקום, לא לפי נורמל**: משתי פאות
   שחולקות טביעת רגל בתוכנית, העליונה היא הגג.
   `debug_ribs_edge.rb` כבר מדפיס לכל פאה `top skin` / `UNDERSIDE`.
2. **`fetch_or_build` סומך על השם.** זה מה שהחזיק את הריב הישן במודל שלו
   ועלה לנו את חצי הצ'אט. אם הגיאומטריה של חתיכה משתנה בלי ששום מספר
   בשם שלה משתנה, מודל קיים ימשיך לקבל את הישנה לנצח. הפתרון: חותמת
   `build_id` על הדפיניציה, ובנייה מחדש כשהיא לא תואמת.
3. **הריב נמדד בעמודה אחת ומצויר באחרת — 3⅝"** (מהמסירה הקודמת).
   **הוא ביקש במפורש לא לגעת בזה כרגע.**
4. **rt51/rt52** — למחוק את `tests/rt51_tmp/a (2).jpg` מ-git.

---

## הצעד הבא, בסדר שהוא קבע

1. **הגגון יקבל את אותם רעפים ורידג' קאפ כמו הגג שהוא עומד עליו** — לא רק
   את הצבע. אישר במפורש, פעמיים. אותה מכונה
   (`RoofTilePlace.place_runs!` / `place_eaves!` / `place_eave_bars!`)
   צריכה לרוץ גם על מישורי הגגון, ורידג' קאפ על הרכס שלו.
   `planes_from_faces` כבר מחזיר מישורים מוכנים, ו-`build_roof_plane!`
   בונה כל מדרון כתת-קבוצה `dormer_roof`.
2. **חלון בקיר הקדמי**, מספריית החלונות הקיימת.
3. **דורמר פלש**: כשהוא פלש עם קיר הבית — אופציה שהגג ייפתח והקיר הקדמי
   של הדורמר יהיה **המשך של קיר הבית**.
4. **גאטרס.**

### מה כבר נבדק בקוד לקראת החלון (צעד 2)
- `WindowTool#cut_window_opening(wall_group, ...)` **לא יעבוד כמו שהוא**
  על קיר דורמר: הוא קורא `start_x/start_y/end_x/end_y/thickness/height/
  anchor` מהקבוצה, ולקיר דורמר אין אותם.
- מה שכן חוזר בשלמותו: בוני הגוף —
  `build_casement_body(window_group, unit, n, thickness, clicked_side)`,
  `build_arched_body(...)`, `build_garden_body(...)`. הם בונים סביב
  ראשית הקבוצה, והמידות מגיעות מ-`@width/@height/@frame_width/...`.
  התבנית: לחתוך פתח מלבני בקיר הקדמי במסגרת של הדורמר, ליצור
  `window_group` שה-transformation שלו הוא מרכז הפתח, ולקרוא לבונה.
- הקיר הקדמי הוא תת-קבוצה בשם `dormer_front` (`InteriorPro_DormerWall`):
  מחומש חיצוני, מחומש פנימי הפוך, וארבע/חמש דפנות ביניהם —
  `build_front_wall!` ב-`dormer_manager.rb`.
- `WindowLibrary.BUILT_IN_TYPES` + `PRESETS` הם רשימת הסוגים והמידות.

---

## מוקשים שנמדדו היום (לא לחזור עליהם)

- **קומפוננט ששמור במודל מנצח את הקוד.** `fetch_or_build` לפי שם. אם
  משהו נראה כאילו הקוד לא עובד — קודם למדוד את הדפיניציה עצמה
  (`debug_rib_defn.rb` מדפיס כל פאה וכל קודקוד).
- **חתיכה נמתחת, אז שגיאה זעירה בדפיניציה נכפלת באורך.** 0.0237" הפכו
  ל-7". אם הסטייה גדלה עם אורך הגג — תחפש אותה בדפיניציה, לא בהנחה.
- **`f.normal` לא אמין** לקביעת עליון/תחתון. לפי מיקום.
- **רשימת פורמטים לפי שם היא מלכודת.** ה-`part` על הדפיניציה הוא התשובה.
- **חור בגג לא מתפשט לבד לכל מסלולי ההנחה.** יש שניים: `run_slots`
  ו-`flat_slots`. תיקון באחד לא נוגע בשני.
- **צלע שפונה במעלה המדרון היא וואלי, לא קיר.** אסור להזיז אותה.
- **הקצה של המסגרת (`tile_edge`) והרידג' קאפ אינם "שדה"** — relay לא
  בונה אותם מחדש, אז אסור למחוק אותם.

## הרצת הבדיקות (חשוב — השתנה)
ל-Windows שלו אין רובי, אבל ל-VM שדרכו רצות הפקודות **יש** רובי 3.0
ו-node 22. אפשר להריץ את כל החבילה שם ישירות בתיקיית התוסף:
```
cd tests && LANG=C.UTF-8 bash run_all.sh ..
```
כל קריאת shell מוגבלת ל-45 שניות והריצה המלאה ארוכה יותר — להריץ ברקע
עם `nohup ... > /tmp/run.log 2>&1 &` ואז לקרוא את הלוג.
בדיקה חדשה — להריץ אותה גם נגד הקוד הישן (עותק בתיקייה זמנית) ולוודא
שהיא נופלת.

## קבצי דיבוג חדשים (gitignored)
`debug_ribs_edge.rb` — מלאי מלא של כל אינסטנס בגג לפי דפיניציה (כפילות
נראית פה מיד), טופ-סקין מול תחתית, ואיפה כל חתיכה מתחילה ונגמרת לאורך
המדרון מול קו המתאר.
`debug_rib_defn.rb` — מה **בתוך** הדפיניציה: כל פאה, כל קודקוד, וטווח ה-y
האמיתי מול ה-bounds.
`debug_dormer_cut.rb` — לכל חור של דורמר: אילו חתיכות עדיין נכנסות לתוכו
ובכמה.

## איך לעבוד איתו
- עברית, משפט-שניים, כמו לילד בן 5. כל פקודה בקופסה נפרדת עם
  "Ruby Console" או "CMD".
- **git רק דרך ה-CMD שלו.** commit אחרי כל צעד עובד, לא בסוף היום.
- **אף פעם לא "זה עובד" מתמונה.** אם לא מדדת — תגיד שלא מדדת.
- כשהוא אומר "לא עובד" — **קובץ שמודד שכותב דוח לתיקייה**, ואתה קורא
  את הדוח בעצמך. הוא לא צריך להדביק כלום.
- **הוא מבקש דבר קטן — תעשה דבר קטן.** היום קיבלתי "יותר מידי פתחים"
  אחרי שתיקנתי בעודף, והוא אמר: "מבקשים ממך לעשות משו קטן אתה משנה
  כיוון ב-180 מעלות." לתקן את הסיבה, בכמות שנתבקשה.
- שרטוט לפני בנייה, תמונה מרונדרת עם מידות אמיתיות, ושאלה אחת קצרה.
  ASCII הוא לא מבין.
