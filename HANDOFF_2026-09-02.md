# Interior Pro — מסירה 2026-09-02 (הגג התחתון, ודורמר צעדים 1–2)

קרא את זה קודם, ואז `CLAUDE.md`. הקודמת: `HANDOFF_2026-09-01.md`.
**אל תסמוך על הסיכום — תאמת מול הקוד החי.**

ענף `door-stabilize`. בדיקות: `cd tests` ואז `LANG=C.UTF-8 bash run_all.sh ..`
→ **146 ירוקות** (היו 144; rt95 ו-rt96 חדשות, rt85 עודכנה בכוונה).
**תמיד run_all.sh.**

---

## ⚠️ קרא לפני שאתה מריץ בדיקות (חדש)

- `run_all.sh` חייב לרוץ **בתוך תיקיית הפלאגין** — rt26 קורא
  `../window_library_dialog.rb`. עותק של tests/ לבד ייפול.
- הריצה מוחקת קבצים זמניים (rt51/rt52). בסביבה שאין בה הרשאת מחיקה
  על התיקייה, rt51 ו-rt52 ייפלו **בלי קשר לקוד** — rt51 מוצא שאריות
  `rt51_tmp/a.jpg`, ו-rt52 נופל על `File.delete`.
- `tests/rt50_tmp/` ו-`tests/rt51_tmp/` הם **קבצים במעקב git** — לא
  למחוק ולא להזיז אותם. (קרה בסבב הזה, שוחזר.)

---

## 1. הגג התחתון — קיר עליון שחציו על הקיר וחציו באוויר

**מה היה:** הבית שלו = קומה תחתונה בצורת L (גוף גדול + אגף קדמי) וקומה
עליונה שיושבת על הגוף הגדול. הגג התחתון נבנה על **כל** הקומה, כולל מתחת
לבית העליון. בקונסולה לא הופיעה השורה `[Roof] level 1: cut at N upper wall(s)`
— סימן ש-`exposed_polygon` החזירה nil.

**למה:** `exposed_polygon` החליטה מי "קיר חוצה" לפי **נקודת האמצע** של הקיר
העליון בלבד. הקיר הדרומי של הקומה העליונה הוא חצי-חצי: 377" עומד על הקיר
שמתחתיו ועוד 249" עובר מעל האגף הפתוח. האמצע נפל בחצי שעומד → אפס
"דיוויידרים" → נפילה חזרה ללולאה המלאה.

**התיקון:** `RoofManager.crosses_storey?(base_pts, w)` — דוגם 32 נקודות לאורך
הקיר וסוכם כמה ממנו באמת עובר בתוך הקומה, נקי מקו הגבול. ריצה של
`MIN_CROSS_RUN = 12"` ומעלה = קיר חוצה. קיר שרק עומד על הקיר שמתחתיו נשאר
מסונן בדיוק כמו קודם. `tests/rt96.rb` בנויה על המודל שלו קיר-קיר.

## 2. הקצה שנכנס לקיר של הקומה העליונה

- **`ABUT_TUCK = 0.0`** (חדש, ב-roof_manager). הקצה היה נגמר **אינץ' בתוך**
  הקיר העליון; הוא ראה את הרעפים נעלמים בתוך הסטוקו וביקש את האינץ' הזה
  בחזרה. עכשיו הקצה נוחת **על פני הקיר**.
- **`wrap_flags`** — המטל-אדג' לא עוטף יותר פינת abut. הוא נבנה עם
  `square_k = פשיה + דריפ`, וזה מה שבלט **7/8"** מהסטוקו (הוא מדד). בפינת
  abut אין קרש בחוץ לנחות עליו — יש קיר. שאר הפינות לא זזו.
- **rt85 עודכנה בכוונה** (138.0 → 137.0, 252.0 → 253.0): היא הצמידה את כלל
  האינץ' הישן. אל תחזיר אותו. אם צריך לכוונן — `ABUT_TUCK` הוא המספר
  היחיד שקובע.

## 3. דורמר — צעדים 1 ו-2 מתוך 5

הוסכם איתו: **גמלון = ברירת מחדל**, ובהמשך יהיה אפשר להחליף את הגגון
ל-**היפ / שד / פלאט**. הוא רוצה לקבוע **רוחב ואורך**, נסיגה ברירת מחדל
3 פיט (ניתנת לשינוי; 0 = פלוש עם הקיר, ואז הגג נחתך משני הצדדים — עדיין
לא מומש), ולמקם אותו עם **ריבוע רפאים** על הגג כמו דלת/חלון.

**`dormer_manager.rb` (קובץ חדש, רשום ב-main.rb — קובץ חדש דורש ריסטרט).**

- **כלל היחיד שקובע גבהים:** הדורמר מת **בתוך** הגג בסוף האורך שלו —
  `ridge z = roof z at (setback + length)`. לכן המשתמש מקליד רוחב ואורך,
  וגובה הקיר הקדמי **יוצא מהחשבון**. פיץ' הגגון = פיץ' הגג, אלא אם נאמר אחרת.
- **חוק הקרשים מכובד:** כל קיר (קדמי + לחיים) נעצר על **תחתית** לוח הגגון.
- `frame(spec)` — כל המתמטיקה, בלי SketchUp. מסרב (עם הסבר) לגודל שלא
  מייצר דורמר.
- `build_dormer!` / `add_dormer!` / `place_on_roof!` / `demo_on_roof!`.
  `roof_frame(roof, x, y)` **מודד את פאת הגג עצמה** — שיפוע, כיוון עלייה,
  קו האיב וגובהו — כך שהדורמר יורש את הגג האמיתי בלי מספרים מוקלדים.
- **החור אמיתי** (הוא ביקש, בשביל עיצוב פנים בהמשך): טבעת הפתח מצוירת על
  שתי הפאות של לוח הגג, השוליים ביניהן נסגרים, **ואז** נמחקות כל הפאות
  השטוחות שמעל הפתח.
  **הלקח:** סקצ'אפ **סוגר בחזרה** לולאה שטוחה סגורה ברגע שהשוליים נבנים —
  מחיקה מיד אחרי `add_face` לא מוחקת כלום ("אני לא רואה פתח"). המחיקה
  חייבת להיות **בסוף**, כסריקה על כל מה שמכסה את הפתח.
- `tests/rt95.rb` מצמידה את הכל. `debug_dormer.rb` = סצנת דוגמה (גג 8:12
  + דורמר). על המודל האמיתי: `InteriorPro::DormerManager.demo_on_roof!`.

**נבדק אצלו ואושר:** הדורמר יושב על הגג האמיתי, והחור נראה.

## הצעד הבא — סוכם איתו

1. **חומרים וטרים לדורמר** (הצעד הבא!): קירות בטקסטורת קירות הבית, הגגון
   ברעפים של הגג, ופשיה + מטל-אדג' על הקצוות שלו. שים לב לחוק הקרשים
   בפינות מול הפשיה של הגג הראשי.
2. **חלון** בקיר הקדמי, מהספרייה הקיימת.
3. אחר כך (מהרשימה המקורית): **כלי הצבה עם רפאים + כפתור בסרגל**
   (הוא שאל במפורש: "לדורמר יהיה כפתור משלו נכון?" — כן), **דיאלוג**
   (מידות, נסיגה, סוג גגון גמלון/היפ/שד/פלאט, חלון), ו**נסיגה 0**.

עוד פתוח מהרשימה שלו: **גאטרס** (הוא אמר שזה עדיין פתוח). גג מעל קיר
מעוגל — **סגור**, לא ברשימה.

## מוקשים שעדיין בתוקף

- `roof_manager.rb` הוא **CRLF**. main.rb, dormer_manager.rb, tests/rt*.rb,
  run_all.sh — **LF**. אל תהפוך.
- שורת קונסולה עם `build_roof!` בלי `replace:`/`level:` **מוחקת את כל הגגות**.
  תמיד:
  `InteriorPro::RoofManager.roofs.each { |r| InteriorPro::RoofManager.build_roof!(replace: r) }`
- `X = {...} unless const_defined?` לא נטען מחדש ב-reload!.
- קובץ חדש ב-main.rb → ריסטרט, או
  `load File.join(InteriorPro::PLUGIN_DIR, 'main.rb')`.
- הסטאב בענן לא מפצל פאות ולא בונה חומרים אמיתיים — **חיתוך אמיתי אפשר
  לבדוק רק אצלו**. הבדיקות מצמידות את המתמטיקה ואת מה שהחיתוך מוצא, לא
  את הפיצול עצמו.

## איך לעבוד איתו — מה שהסבב הזה לימד

- **git רק דרך ה-CMD שלו.** אני לא מריץ git, גם לא קריאה בלבד.
- **לא להזיז ולא למחוק קבצים בתיקייה שלו** בלי לבדוק אם הם ב-git.
- כשהוא אומר "לא עובד" — **מדידה, לא ניחוש**. קובץ דיבוג שרק מודד סגר פה
  שתי חקירות בסבב אחד (`debug_roof_levels.rb`, `debug_stickout.rb`).
- הוא לא יכול לבחור קרש בודד בתוך קבוצה בקלות — עדיף קובץ שמוצא את
  החריגה בעצמו על פני "תבחר ותריץ".
- commit אחרי כל צעד עובד.
