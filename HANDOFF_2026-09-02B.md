# Interior Pro — מסירה 2026-09-02B (הדורמר, מהגוף עד הכפתורים)

קרא את זה קודם, ואז `CLAUDE.md`. הקודמת: `HANDOFF_2026-09-02.md`.
**אל תסמוך על הסיכום — תאמת מול הקוד החי.**

ענף `door-stabilize`. בדיקות: `cd tests` ואז `LANG=C.UTF-8 bash run_all.sh ..`
→ **153 ירוקות** (היו 146). חדשות: rt97, rt98, rt99, rt100, rt101, rt102,
rt103. rt95 עודכנה בכוונה. **תמיד run_all.sh.**

---

## מה נסגר היום

הדורמר עבר מ"גוף + חור" ל**כלי מלא**: אוברהנג, חומרים, פינות, כל הקרשים,
ארבעה סוגי גגון, כפתור משלו עם פאנל וריבוע רפאים, ועריכה/הזזה/מחיקה.

### הגוף
- **אוברהנג** (`DEFAULT_OVERHANG = 6"`). האוברהנג נמדד עד **קו הפשיה** —
  `w_edge` הוא הפנים החיצוניות של הפשיה, בדיוק כמו פוליגון הזחילה של גג.
- **הקירות לוקחים את הבית לבד**: `house_wall_material` סופר את
  `exterior_material` של קירות הבית ולוקח את הנפוץ; הגג לוקח את החומר של
  הפאה שעליה הוא עומד (`roof_frame` מחזיר `roof_mat`).
- **טקסטורה בחוץ, לבן בפנים** (הוא ביקש): `paint_wall!` מקבל את הכיוון
  שהקיר פונה אליו, וצובע רק את הפאה שפונה הפוך ב-`DEFAULT_INTERIOR_COLOR`.
  כל השאר נשאר בחומר החוץ - הקצוות המְמוּטָרים והקצה העליון מוסתרים ממילא,
  ולהשאיר אותם ריקים היה מראה פסים לבנים בפינה.
- **פינות קדמיות במיטר 45°**: הפנים הפנימיות של כל קיר נמשכות אחורה בעובי
  אחד, כך שמישור `s + |w| = setback + half` מפריד בין הקיר הקדמי ללחיים
  ואף אחד לא נכנס לשני.
- **הדק מכסה את הקרשים שלו**: מעל זחילה הוא רץ עוד `DRIP_THICK`, מעל רייק
  עוד `FASCIA_THICK + DRIP_THICK` — זה `rake_out` של הגגות.

### הקרשים — עוברים דרך RoofManager
הדורמר **לא בונה קרשים משלו** חוץ ממה שאי אפשר: הוא מוסר מתאר ומפת גבהים
ו-`build_band!` / `build_rake_board!` / `build_rake_soffit!` של הגגות בונים.
היחיד שנבנה מקומית הוא קרש שנחתך **באלכסון על מישור הגג** — `build_band!`
בונה תיבה בין שני גבהים ולא יודע לחתוך כך.

**החוקים שהוא קבע היום, ואסור להפוך אותם:**
1. **הגיבל בא לפני האנכי.** בגמלון הרייק רץ את כל הקצה שלו ומכסה את הקצה
   של הפשיה האנכית; הפשיה נעצרת על קו הגיבל. (הפוך מ-`rake_meet_span`
   של הבית — הוא הראה תמונה של הגג שלו וביקש ככה.)
2. **הפשיה מגיעה עד סוף הגג ונחתכת באלכסון.** הקצה העליון רץ עד שהגג
   מגיע אליו, התחתון נעצר מוקדם יותר, והחתך הוא מישור הגג עצמו.
3. **מרובע מול מרובע.** אם האיבס מתחת רץ מרובע עד הקו — גם הפשיה נגמרת
   מרובעת שם. פשיה ב-45° מול איבס מרובע השאירה משולש פתוח.
4. **המטל-אדג' עוטף כל פינה** ונוחת על הפנים החיצוניות של המטל השכן
   (`wrap_flags`, `square_k = FASCIA_THICK + DRIP_THICK`).
5. **האיבס**: הקדמי מחזיק את הפינה, הצדדי נמשך אחורה אוברהנג שלם ונפגש
   איתו. מתחת ל**רייק** האיבס מגיע עד הקו עצמו (הקרש בחוץ); מתחת
   ל**זחילה** הוא נעצר בעובי קרש (הפשיה בפנים).

### ארבעת הסוגים
| סוג | גובה הקיר הקדמי | הקצוות |
|---|---|---|
| גמלון | `length*slope - half*pitch` | חזית = גיבל (רייק), צדדים = זחילות |
| היפ | אותו דבר | **שלושתם זחילות** — אין רייק בכלל |
| שד | `length*(slope - pitch)` | חזית = זחילה, צדדים = רייקים |
| פלאט | `length*slope` | שלושתם זחילות |

- **פלאט הוא שד עם פיץ' 0** — לא בונה רביעי. `shed_like?` מכסה את שניהם.
- **שד חייב פיץ' שטוח יותר מהגג**, אחרת הוא צולל מתחתיו. "Same as roof"
  נותן לו **חצי** מהפיץ' של הגג.
- **היפ**: הרכס מתחיל חצי-רוחב אחורה (`s_hip`), שלושה מישורים בפיץ' אחד
  נפגשים בשני היפים ב-45°. רחב מדי לאורך שלו → נדחה עם כמה אורך חסר.

### הכלל שלא זז באף סוג
הגגון **מת בתוך הגג בסוף האורך שלו**: `deck z at s_ridge = roof z there`.
לכן גובה הקיר הקדמי הוא תמיד תוצאה ולא מספר רביעי שיכול לריב.

### מה מוקלד ומה נגזר (הוא ביקש)
- הוא מקליד **גובה קיר קדמי** (חלון ייכנס בו). **האורך נגזר ממנו.**
- **הקליק קובע את הנסיגה** — הגגון הולך עם העכבר במעלה המדרון, ובשורת
  המצב רשום כמה אינץ' מהזחילה. שדה הנסיגה ירד מהפאנל (לא עשה כלום).
- **מגבלה לכל הכיוונים**: כל פינה של הדק חייבת לשבת בתוך פאת הגג ולהשאיר
  `RIDGE_CLEARANCE = 12"` מכל קצה שלה — רכס, היפים, זחילה. אין רפאים,
  ובשורת המצב כתוב מה חסם ומה הגובה הכי גבוה שנכנס.

### הכפתורים (סרגל "Interior Pro Roofs", בסוף, אחרי קו מפריד)
`Dormer` (פאנל → כלי הצבה) · `Edit Dormer` · `Move Dormer` · `Delete Dormer`.
- `DormerManager.dormer_spec(g)` קורא דורמר בנוי חזרה ל-spec שבנה אותו
  (נשמרים גם `z0`, `slope`, `base_xy`, `along_xy`, `into_xy`, `z_top`).
  **בלי זה אף אחד משלושת הכלים לא עובד.**
- `remove_dormer!` מוחק **וסוגר את החור** (`heal_roof!` = ההפך של
  `cut_roof!`). `replace_dormer!` = מחיקה + בנייה מחדש מה-spec הממוזג.

### דורמרים שורדים בנייה מחדש של גג
`build_roof!` מוחק את כל קבוצת הגג — והדורמרים יושבים בתוכה. עכשיו
`DormerManager.harvest(doomed)` קורא אותם לפני המחיקה ו-`replant!` מציב
אותם מחדש על הגג החדש בסוף. **הם שומרים גובה ורוחב, לא אורך** — כמה עמוק
הם נכנסים נגזר מהפיץ' החדש. אם אין מספיק מקום הם **מונמכים** לגובה הכי
גבוה שנכנס (חיפוש בינארי) ולא נמחקים. רק גג שלא יכול להחזיק אפילו את
הנמוך ביותר מאבד אותם. `tests/rt103.rb`.

---

## מוקשים (חדשים ומאומתים היום)

- **`X = ... unless const_defined?` לא נטען מחדש ב-reload!** — עלה שוב:
  `BUILT_STYLES` השאיר את Shed אפור אחרי שהוא כבר עבד. כל רשימה שגדלה
  צעד-צעד חייבת להיות **מתודה** (`DormerDialog.built_styles`).
- **`face.vertices` מחזיר גם את קודקודי החורים.** פאת גג שכבר יש בה
  דורמרים מחזירה מתאר שטות. `face_points` משתמש ב-`outer_loop`.
- **PickHelper לא נותן נקודה תלת-ממדית נתמכת.** הנקודה מגיעה מ-
  `view.inputpoint(x, y)`, עם `pickray` על מישור הגג כגיבוי.
- **`roof_manager.rb` הוא CRLF.** עריכה דרך פייתון הפכה אותו ל-LF והוחזר.
  `dormer_*.rb`, `main.rb`, `toolbar.rb`, `tests/rt*.rb` — LF.
- קובץ חדש ב-main.rb → **ריסטרט**. כפתור חדש → **ריסטרט**. HTML בדיאלוג →
  לסגור ולפתוח את החלון. שאר השינויים → `InteriorPro.reload!`.
- שורת קונסולה עם `build_roof!` בלי `replace:`/`level:` **מוחקת את כל
  הגגות**. תמיד:
  `InteriorPro::RoofManager.roofs.each { |r| InteriorPro::RoofManager.build_roof!(replace: r) }`

## קבצי דיבוג בשורש (gitignored, `debug_*.rb`)
`debug_dormer_fit.rb` — הולך במעלה המדרון ואומר לכל נסיגה אם נכנס ולמה לא.
`debug_dormer_parts.rb` · `debug_dormer_place.rb` · `debug_reset_dormer.rb`
(מוחק את כל הדורמרים ובונה אחד נקי) · `debug_clean_dormers.rb`.

## מה נשאר בדורמר — סוכם איתו שזה עובר לצ'אט הבא

1. **נסיגה 0** — פלוש עם הקיר של הבית, ואז הגג נחתך **משני הצדדים**.
   היום `frame` דוחה `setback < 0` ומקבל 0, אבל אף אחד לא בדק מה קורה
   כשהקיר הקדמי יושב על קו הזחילה: `s_rake = -overhang` יוצא שלילי,
   ו-`fits_on_face?` דורש 12" מכל קצה — כולל מהזחילה. שניהם צריכים
   תשובה מכוונת, לא הרחבה בשקט של המגבלה.
2. **חלון בקיר הקדמי**, מספריית החלונות הקיימת. זה מה שכל עניין
   "הגובה מוקלד" קיים בשבילו.
3. **הגגון חייב לקבל את סוג הגג של הבית, לא רק את הצבע.** הוא ראה
   את זה בסוף היום: *"אין סוגי גגות בכלל לגמלון הזה, הוא חייב להשתנות
   בהתאם לגג."* היום `roof_frame` מחזיר את ה-material של הפאה והדק
   נצבע בו — וזה מספיק כשהגג בשינגל. אבל בסגנון רעפים הגג בונה
   **רעפים תלת-מימדיים אמיתיים** (`RoofTilePlace.place_runs!` /
   `place_eaves!`) ורידג'-קאפ, והגגון נשאר לוח שטוח צבוע. אותה
   מכונה צריכה לרוץ גם על מישורי הגגון — `planes_from_faces` על
   הפאות שלו — ורידג'-קאפ על הרכס שלו.
   **הוא אישר במפורש:** *"כן, הוא יקבל את אותם רעפים ורידג' קאפ כמו
   הגג שעליו הוא ממוקם."* לא רעפים כלליים — של הגג שמתחתיו, כך
   שדורמר על גג אחר יקבל את שלו.

עוד פתוח מהרשימה שלו: **גאטרס**.

## איך לעבוד איתו
- עברית, משפט-שניים, כל פקודה בקופסה נפרדת עם "Ruby Console" או "CMD".
- **git רק דרך ה-CMD שלו.** commit אחרי כל צעד עובד.
- **שרטוט לפני בנייה** — תמונה מרונדרת עם המידות האמיתיות של המודל שלו,
  ואז **שאלה אחת קצרה**.
- כשהוא אומר "לא עובד" — **קובץ שמודד**, לא ניחוש. זה סגר היום שלוש
  חקירות: החורים בפאה, הנסיגה שלא זזה, והמגבלה שנראתה שרירותית.
- כשהוא חוזר על עצמו — להפסיק להסביר ולבנות.
