# מסירה לצ'אט הבא — Interior Pro, סבב 2026-08-12C

## 0. לפני הכל
התיקייה החיה (PC):
`C:\Users\rordt\AppData\Roaming\SketchUp\SketchUp 2024\SketchUp\Plugins\interior_pro`

סדר קריאה: **הקובץ הזה** ← `CLAUDE.md` ← `HANDOFF_2026-08-12B.md`.
לא לסמוך על סיכומים — לאמת מול הקוד החי.

### בדיקות
```
cd tests
"C:\Program Files\Git\bin\bash.exe" run_all.sh ..
```
אין Ruby אצל המשתמש — מריצים בענן. **rt36 חדש** (34 בדיקות) ונוסף ל-run_all.sh.
ירוקים שאומתו הסבב הזה: rt15, rt17, rt18, rt19, rt20, rt30, rt31, rt32, rt35, rt36.

### כללי עבודה (המשתמש אכף אותם שוב היום)
1. עברית, משפט-שניים, כמו לילד בן חמש.
2. כל פקודה בקופסה נפרדת + לאן (Ruby Console / CMD).
3. העוזר עורך קבצים בעצמו ומעביר ב-device_commit. לא מריץ git — נותן פקודות CMD.
4. **דבר ויזואלי = לא מנחשים.** מודדים.
5. **"כלום לא השתנה" = הקוד לא נטען.** יש תבנית הוכחה: בונים שתי פאות
   מדומות (Struct עם pts+normal) וקוראים ל-`ridge_lines` — הקוד הישן
   מחזיר מספר אחר מהחדש. עבד מצוין היום.
6. **אל תבקש מהמשתמש לקרוא פלט ארוך.** הוא אמר במפורש: "אני לא בורר
   כלום מהקודים, אתה תמצא את זה". הדרך שעבדה: לוקחים את המספרים מהמודל
   שלו בשורה אחת קצרה, **משחזרים את הבית בענן**, ומוצאים שם.
7. **סקריפט debug לא מעתיק את הקוד שהוא בודק** — הוא קורא לו. פעמיים
   היום סקריפט עם העתק של הכלל הישן שלח אותנו לכיוון שגוי.

## 1. מה נסגר בסבב הזה

### גג Hip מעל קיר מעוגל — שלב 3 אומת (אין שינוי קוד)
סימולציה בענן: 400 תוכניות אקראיות עם 1–2 קירות מעוגלים, כולל sag עד 0.6
מהמיתר — **0 חורים**, כיסוי שטח 100%. השלד הכי איטי 0.056 שניות, גג שלם
0.01–0.02 שניות. פאסטה קטנה ביותר 30", תא קטן ביותר 83 אינץ' רבוע. שני
מפלסים עובד. אומת גם על הבית האמיתי (12 נקודות, 9 פאסטות, 15 פאות).

### רידג'-קאפ — שני באגים אמיתיים
**א. הצד של הפאה נקבע לפי מרכז כובד — וזה שקר בפאה בצורת L.**
`ridge_lines` דרשה "מישור אחד בכל צד של הקו" והשוותה מרכז-כובד מול אמצע
הקו. בתא ארוך של כנף, מרכז הכובד נופל מעבר לקו, שתי הפאות נספרו באותו צד,
והרכס נזרק. אצל המשתמש זה השאיר את הרכס של הכנף ואת האלכסון שלו בלי קאפ
(הוא סימן אותם באדום פעמיים).
`face_side_of_edge` חדש: צועד רבע אינץ' לכל צד מאמצע הקו ושואל את מתאר
הפאה עצמו (`point_in_poly?`) איזה צעד נחת בפנים. נפילה חזרה למרכז-כובד רק
אם שניהם/אף אחד. rt36 נועל את זה על הפוליגון האמיתי של המשתמש (8 קווים).

**ב. הכלל לקאפ סביב קשת — הגרסה השלישית היא הנכונה.**
המשתמש במילים שלו: *"על כל שיאי הגובה, קו פרשת המים, וגם על האלכסונים
היורדים — חוץ מעיגול"*.
- ניסיון 1: סף שטוח 30° — נכשל.
- ניסיון 2: חצי מזווית הפאסטה של אותה קשת — נכשל.
- **מה שנכון: כל פינה שנוגעת בקיר מעוגל הולכת בלי קאפ, בלי למדוד זווית.**
המספרים שהכריעו (מהמודל שלו): קיר A — 9 פאסטות, פאסטה 12.6°, חיבורים
39.6°. קיר B — 10 פאסטות, פאסטה 14.9°, חיבורים 22.9°. כלומר הקשתות שלו
נפגשות עם השכנים בזווית אמיתית, לא משיקות — ובכל זאת הוא רוצה אותן חשופות.
`soft_hip_points` עכשיו מחזירה כל פינה קיר→קיר שאחד מצדדיה מעוגל. פינה בין
שני קירות ישרים לא מוחזרת לעולם.

### Gable מעל קיר מעוגל = סירוב מנומס (שלב 4 — נסגר)
שלוש רשתות ביטחון:
- `toggle_gable_wall!` מסרב לסמן קיר מעוגל, עם הודעה, **לפני** ששומר משהו.
  ביטול סימון של קיר שסומן ואז עוקם — עדיין עובד, כדי שאף אחד לא ייתקע.
- `build_roof!` מתעלם מסימוני גייבל על קיר שהתעקם אחרי הסימון.
- סגנון Gable שבוחר קצוות לבד לא ייגע בפאסטות של קשת.
עזר חדש: `gable_refused?`.

## 2. גיט
branch: `door-stabilize`.
קומיט שעבר היום: `ridge caps: face side by outline, not centroid; no cap on
tangential curve join (rt36)`.
**עדיין לא בקומיט** (הקבצים אצלו על הדיסק): הגרסה הסופית של
`soft_hip_points` (כל פינה שנוגעת בקשת) + הסירוב לגייבל + rt36 המעודכן.
לתת לו פקודות CMD.
`debug_*.rb` ב-.gitignore — לא נכנסים לקומיט, וזה בסדר.

## 3. הצעד הבא — מה שהמשתמש בחר
**מסגרות פינה בסיידינג: פינה כ-L אחד** (הוא בחר את זה מפורשות).
היום: כל קיר בונה קרש משלו, ומאריך אותו ב-1" מעבר לקצה כדי שהשניים
יחפפו ל-L סגור (`corner_trim_runs` עם `ext_start`/`ext_end`,
`build_siding_board!`, `SIDING_TRIM_WIDTH=3`, `SIDING_TRIM_DEPTH=1`).
הרעיון: **גוף אחד** במקום שני קרשים חופפים — חתך L במבט-על, נדחף אנכית
מ-z_min ל-z_max.
- בעל הפינה נקבע דטרמיניסטית (למשל ה-id הקטן) כדי שכל פינה תיבנה פעם אחת.
- הזרוע השנייה צריכה את כיוון הפאה החיצונית של השכן
  (`exterior_face_polyline`) ואת רוחב הטרים שלו.
- להשאיר את המסלול הישן כשאחד השניים מעוגל או שהטרים כבוי, ולשים דגל כיבוי.
נקודות כניסה: `add_horizontal_siding` (~שורה 810) ו-`add_board_and_batten`
(~שורה 1197) ב-wall_tool.rb.

## 4. פתוח / לא לאבד
- **סעיף 4 הישן (דלת בקיר מעוגל יושבת בתוך הבית) — סגור. המשתמש אמר
  במפורש: לא לשאול על זה שוב.**
- לעקם קיר שכבר יש בו דלת/חלון — המשתמש בדק בעצמו, נראה עובד (צילום עם
  דלת וחלון על קיר מעוגל).
- **הגג לא מתעדכן לבד.** מעקמים קיר → הגג נשאר על הקו הישן עד בנייה מחדש.
  זה הסביר את כל "העיגול נעלם מהגג". לשקול רענון אוטומטי, או לפחות הודעה.
- תוכניות 2D ל-PDF — עדיפות 2 של המשתמש.
- קיר מעוגל ב-plan_generator — עדיין כורדה.
- butt joint פנים-אל-קשת — עדיין כורדה.
- זווית בין שני קירות בעורך 2D — שאלה פתוחה: מי משניהם זז.
- מסגרות פינה: נשארו גם "טרים סביב פתחים" ו"קרש סיום עליון" (הוא בחר
  קודם את ה-L).

## 5. דגלי חירום
```ruby
InteriorPro::WallTool::USE_CURVED_WALLS = false
InteriorPro::WallTool::USE_3D_SIDING = false
InteriorPro::WallTool::USE_NATIVE_OPENINGS = false
```

## 6. כלי debug בשורש
- `debug_ridge_caps.rb` — בונה יריעה אחת, קורא ל-`ridge_lines` האמיתי,
  מדפיס מי קיבל קאפ ומי נפסל ולמה, ומחזיר את הגג כמו שהיה.
- `debug_curved_door.rb` — face שמור מול face מחושב לכל דלת על קיר מעוגל.
- `debug_arc_corner.rb`, `fix_corners_once.rb` — מהסבבים הקודמים.
