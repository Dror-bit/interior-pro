# Interior Pro — מסירה 2026-08-24C (סופיט: כל 5 הסוגים + כפתור בדיאלוג)

קרא את זה קודם, ואז `CLAUDE.md`. הקודמת: `HANDOFF_2026-08-24B.md`.
**אל תסמוך על הסיכום — תאמת מול הקוד החי.**

בדיקות: בענן בלבד (למשתמש אין רובי): `cd tests` ואז
`LANG=C.UTF-8 bash run_all.sh ..` → **134 ירוקות**. להעתיק לענן גם
`icons/` ו-`textures/` (בלי `textures/floors/`), אחרת rt43/rt73 נופלים
בכזב. `run_all.sh` מעתיק את קבצי המקור לתוך `tests/` — הרצה של
`ruby rt84.rb` לבד עובדת על עותק ישן ומשקרת. **תמיד run_all.sh.**
(זה בדיוק מה שהטעה אותי היום פעם אחת — נראה כאילו הקוד לא רץ.)

git: ענף `door-stabilize`. **צעד ראשון חובה: `git status` דרך CMD שלו** —
אל תריץ git בעצמך, אתה תוקע `index.lock`.

## מה נסגר היום ואושר על ידו

1. **`debug_soffit` הוסר** מ-`roof_manager.rb` (המתג וההדפסה). commit.
2. **דריפ בגייבל.** `build_rake_board!` קיבל `k_in:` / `k_out:`
   (ברירת מחדל `0..FASCIA_THICK` — כל קריאה קיימת זהה), ו-`if s[:drip]`
   קורא לו שוב עם `FASCIA_THICK..FASCIA_THICK+DRIP_THICK`. commit.
3. **סטאקו + עץ.** אותו לוח בדיוק כמו `boxed`, טקסטורה אחרת.
   `soffit_wood.jpg` יוצא מהמודל שלו (`WOOD`) עם `texture.write`.
   הקרשים מסתובבים לפי `ring_longest_dir` — הצלע הארוכה של כל פאת סופיט
   היא כיוון הפאשיה, גם באגף באלכסון וגם ברייק. commit.
4. **כפתור Soffit בדיאלוג הגג.** רשימה + בורר צבע. הבורר "נקי" עד
   שנוגעים בו (`soffitPickedFlag`) — מחרוזת ריקה = תן לטקסטורה לעבוד.
   שני הארגומנטים החדשים בסוף `apply_roof` בכוונה, כדי שקריאה ישנה
   עם 12 ארגומנטים עדיין תעבוד. commit.
5. **קורות גלויות (`beams`).** 2x4 (1.5×3.5) כל 18", מהקיר עד הפאשיה,
   תלויות מתחתית הגג. בצבע הפאשיה (לא טקסטורת עץ — הוא ביקש).
   נעצרות בפינת הקיר: `beam_centers` מקבל `margin = overhang + w/2`,
   כי `poly` הוא קו הקיר דחוף החוצה באוברהנג ובפינה חיצונית אין שם
   קיר — קורה שם נשארת באוויר (הוא ראה את זה וזה תוקן). commit.
6. **Lookouts בגייבל.** אותן קורות עולות על הרייק, כל אחת בגובה שלה.
   הן **מאוזנות** בכוונה: גובה גג רעפים תלוי רק במרחק מהרכס, אז זזים
   פנימה מהרייק ו-z לא משתנה (אותו נימוק כמו `rake_soffit_quad`).
   קורא את הגובה עם `chain_z_at` **שכבר היה בקובץ** — אל תכתוב שני
   קוראים לאותו פרופיל. commit.

## הסופיט הספרדי — אושר גם הוא (הסבב האחרון של היום)

הוא בנה והסתכל: "הגג נראה מעולה". הכל בקומיט.

מה נבנה שם:

- לוח **סטאקו** (בדיוק הלוח של `stucco`) + **קורות עץ כהה מתחתיו**.
  זה הסגנון היחיד שעונה כן לשתי השאלות, ולכן הבלוק ב-`build_roof!`
  מפריד `board` (כל סגנון חוץ מ-`beams`) מ-`tails`
  (`beams` או `spanish`). אל תחזיר את זה ל-`style == ...`.
- קורה 4×6, כל 24".
- **הקורה נעצרת 1" לפני הפנים החיצוניות של הפאשיה** (`out: -1.0`).
  זה לא קישוט: הוא ביקש מקום לגאטרס. `k` נמדד החוצה מקו המתאר,
  שהוא הפאשיה עצמה, אז רק ערך שלילי משאיר את הפנים פנויות.
- הקצה מסוגנן: גובה מלא 3", ואז **קו S** (smoothstep) עד 2" בקצה.
  לא מדרגות — הוא צייר את זה מעל הגרסה עם המדרגות ואמר
  "יותר מעוגל באמצע". smoothstep נבחר כי השיפוע אפס בשני הקצוות,
  אז הוא לא יכול לעשות שבר איפה שהוא נפגש עם החלק המרובע.
- הצבע: `beam_colors['spanish'] = '#4e342e'`, נצבע מרשימת פאות
  **נפרדת** מהלוח. אם תאחד את הרשימות — הקורות יקבלו סטאקו.

מספרים לשינוי מהיר, אם הוא יבקש: `beam_spec('spanish')` —
`w/h/spacing/out/tail{run,end_h,steps}`. הכל במקום אחד.

עלות: `spanish` על גג גייבל קטן = 662 פאות (מתוכן 544 קורות, 34 קורות
× 16 פאות). אם זה יכביד — `steps` מ-10 ל-6 מוריד ל-12 פאות לקורה.

## מוקשים שעדיין בתוקף

- הקבצים **CRLF**. עריכה עם סקריפט שמחפש מחרוזות חייבת להמיר
  `\n` → `\r\n`, אחרת העוגן לא יימצא. בזבז לי שני סבבים היום.
- `X = {...} unless const_defined?(:X)` **לא** נטען מחדש ב-`reload!`.
  לכן `soffit_textures`, `soffit_colors`, `beam_spec`, `beam_colors`
  הן **מתודות**. אל תהפוך אותן לקבועים.
- כל פאה עוברת `add_ring!` / rescue — raise אחד מפיל קצה שלם.
- הסטאב בענן זורק על נקודות כפולות כמו סקצ'אפ אמיתי. לא לרכך.
- `position_material` לא קיים בסטאב, אז סיבוב הטקסטורה נבדק רק אצלו.
  `paint_soffit_face!` תמיד נופל חזרה ל-`f.material=`.
- אם ריכוך לא עוזר פעמיים — הבעיה גיאומטרית. למדוד.

## איך לעבוד עם דרור

- עברית פשוטה, **משפט-שניים, כמו לילד בן 5**. הוא ביקש את זה במפורש.
- כל פקודה בקופסה נפרדת עם לאן (Ruby Console / CMD).
- git רק דרך CMD שלו. הוא לא כותב קוד — אתה עורך, הוא מדביק ובודק.
- צעד אחד, מדידה, תשובה שלו, ורק אז הצעד הבא. commit אחרי סבב שעובד.
- **אל תיקח יותר מדי זמן על צעד אחד.** הוא אמר היום: "בדרך כלל
  שלוקח לך ככה הרבה זמן זה לא יוצא טוב". תחתוך את הצעדים קטן יותר.
- שינוי HTML בדיאלוג → לסגור ולפתוח את החלון, לא רק `reload!`.
- אם לא ברור מה הוא רואה — לבקש תמונה מסומנת. הוא מצייר באדום ומחזיר.
- הוא אוהב שרטוט לפני בנייה. שני סבבי שרטוט חסכו היום בנייה מיותרת
  (חשבתי קורות עגולות, בפועל זה קורות מרובעות עם סיומת מסוגננת).

## TODO

- הוא אמר: "יש לי עוד כמה דברים לשיפור ותיקון אחרי שנסיים עם החומרים".
  לשאול מה.
- גאטרס — הוא הזכיר שירצה. הפאשיה כבר נשמרת פנויה בשבילם.
- סופיט משופע במקביל לגג, כאופציה נוספת (רעיון שלו שנשמר).
- מ-`CLAUDE.md`: קירות מעוגלים, תוכניות 2D ל-PDF, NOTE מצויר, צבע
  בגיליון, לוגו per-installation, corner boards.
