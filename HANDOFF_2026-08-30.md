# Interior Pro — מסירה 2026-08-30 (מכסה גובה לגג שנשען על הקומה שמעליו)

קרא את זה קודם, ואז `CLAUDE.md`. הקודמת: `HANDOFF_2026-08-29.md`.
**אל תסמוך על הסיכום — תאמת מול הקוד החי.**

בדיקות: בענן בלבד (למשתמש אין רובי): `cd tests` ואז
`LANG=C.UTF-8 bash run_all.sh ..` → **140 ירוקות** (היו 139; rt90 חדשה).
להעתיק לענן גם `icons/` ו-`textures/` (בלי `textures/floors/`).
**תמיד run_all.sh**, אף פעם לא `ruby rt90.rb` לבד.

git: ענף `door-stabilize`. שלושה קומיטים היום:
`eaaf2a5` (החשבון הטהור + rt90), השני (החיבור ל-build_roof!),
`aaa66a6` (השדה בפאנל).
**צעד ראשון חובה: `git status` דרך CMD שלו** — אל תריץ git בעצמך,
אתה תוקע `index.lock`.

---

## מה נסגר היום ואושר על ידו

### הבעיה שהוא צילם

בית שתי קומות. הגג של הקומה **התחתונה** נחתך נכון בקירות של הקומה
שמעליו (`exposed_polygon`, מ-26.08 — זה עבד), אבל הוא **טיפס גבוה
מדי**: קצוות ה-abut רצים במהירות 0, אז הגג עולה בשיפוע עד שהוא פוגע
בקיר, בלי שום רכס שיעצור אותו. אצלו זה 507" של ריצה בשיפוע 4:12 =
169" של עלייה. הגג נגמר ב-275" בזמן שהקיר של הקומה השנייה נגמר ב-212"
— כלומר הוא בלע את כל הקומה השנייה.

**הכלל שהוא בחר, אחרי שרטוט ושתי סבבי מספרים:** הגג התחתון ייגמר
לכל היותר **4 פוט מעל הרצפה של הקומה שמעליו** — כדי שאפשר יהיה לשים
שם חלון. הוא שאל את זה בעצמו: "מקסימום 3 או 4 פוט מעל הריצפה של הקומה
העליונה מהאם אני רוצה לשים שם חלון?"

**הוא בחר "להקטין שיפוע"** ולא "לחתוך שטוח" ולא "רכס באמצע" — אחרי
שראה חתך לפי המידות האמיתיות שלו. הוא הבין מיד ואמר: "כניראה שרוב
הגגות לא יהיו 43 פוט אז השיפוע יהיה יותר תלול."

### איך זה בנוי — שלושה צעדים, כל אחד בקומיט משלו

**1. חשבון טהור.** `cell_reach(poly, cells)` — כמה רחוק, בתוכנית,
הנקודה הכי גבוהה של הגג מהאיב שלה. זה **לא תלוי בשיפוע**:
`build_hip_geometry!` מרים כל נקודה ב-`slope * המרחק מהאיב של התא
שלה`, אז ראש הגג הוא תמיד `z0 - slope*overhang + slope*reach`
והשיפוע הוא רק המכפיל. בגלל זה אפשר לחשב את המכסה **לפני** הבנייה
ולא לבנות פעמיים. המדידה נעשית בדיוק כמו ב-`build_hip_geometry!`
(היטל על הנורמל של האיב של אותו תא), כדי שהמספר פה וה-z שם לא יוכלו
להתפצל.
`slope_for_limit(reach, overhang, z0, limit)` הופך את אותה נוסחה.
`nil` = אין מה למכסה.

**2. החיבור.** ב-`build_roof!`, מיד אחרי ש-`cells` נוצרים ורק כש-
`abut_ids` לא ריק. **רק ממתן, אף פעם לא מתליל**: לוקחים את המינימום
בין השיפוע שהוא בחר לבין המכסה. כנף קטנה שומרת על 4:12 בלי שינוי.
`s[:pitch]` **לא** נדרס — המספר שהוא בחר נשאר שלו, והמכסה מחושב מחדש
בכל בנייה. רצפת הקומה שמעל = ה-`base_z` הכי נמוך של קירות ה-abut.
`min_abut_slope` = חצי אינץ' לרגל, הכי שטוח שהוא ילך, כדי שתמיד יהיה
ניקוז.

**3. השדה בפאנל.** `Max over storey above`, ב**פוט** (ככה הוא ביקש),
מתחת ל-Pitch. הגדרה `abut_headroom` באינצ'ים, ברירת מחדל 48,
**0 = בלי הגבלה** (בדיוק ההתנהגות שהייתה עד היום). נוספה ל-
`roof_setting_keys`, אז כל גג נושא את המכסה שלו. הפאנל גדל ל-790.

בסימולציה של הבית שלו: 272.3" → **154.0"** בדיוק, שיפוע 1.15:12.
הוא בדק בסקצ'אפ ואמר "בדקתי וזה ניראה מעולה".

---

## מה עלה ביוקר היום — אל תחזור על זה

1. **הוא שלח שלוש תמונות בלי מילה, ואני זיהיתי את הבעיה הלא נכונה.**
   ראיתי דאונספאוט תלוי באוויר וציירתי שתי אפשרויות בשבילו — והוא
   ענה "נכון אבל זה לא מה שהתכוונתי, זה הגג הבעייתי". **כשהוא שולח
   תמונה בלי טקסט, תשאל מה לראות לפני שאתה מצייר.**

2. **`exposed_polygon` עבד כל הזמן.** הפיתוי היה לתקן את החיתוך.
   מדידה קצרה בקונסולה (`exposed=3 abut`) חסכה תיקון של משהו תקין.
   **תמדוד לפני שאתה משנה** — זה מה שהציל את הסבב הזה.

3. **rt85 נפלה, וזה היה נכון.** המודל שלה בונה 6:12 מעל reach של 153",
   כלומר 70" מעל הרצפה העליונה — המכסה החדש ממתן אותה ל-4.09:12 וכל
   המספרים בה זזים. **לא לשכתב את המספרים שהוא כבר אישר**: rt85 מכבה
   את המכסה אצלה (`def self.abut_headroom; 0.0; end` אחרי ה-require),
   ו-rt90 היא הבית של המכסה.

4. **התנגשות שם.** ברגע ש-`abut_headroom:` נהיה ארגומנט מילה של
   `build_roof!`, הוא **מסתיר את המתודה** באותו scope. הקוד בפנים חייב
   לקרוא `s[:abut_headroom]`, אחרת `nil > 0.0` מתפוצץ. המתודה נשארת
   רק כברירת המחדל של `settings`.

5. **רנדר אקסונומטרי עם מיון פאות לפי עומק יצא בלתי קריא.** הקירות
   כיסו את הגגות. **חתך חזית** (הטלה על x/z, מיון לפי y יורד) לקח
   חמש דקות והראה בדיוק את מה שהוא צילם. לגובה — תמיד אלוויישן.

6. **הסימולציה של הבית שלו נבנתה מהמספרים שהקונסולה הדפיסה** —
   מלבן קומה 1 `(-225,-13)..(689,1056)`, קומה 2 שישה קירות
   `[-225,1056],[-225,261],[177,260],[177,485],[689,485],[688,1056]`,
   גובה 106 כל אחת. יצא 308.2 מול 309.7 האמיתיים ו-272.3 מול 274.9 —
   מספיק מדויק כדי לסמוך עליו. **שווה לשחזר ככה לפני כל מסירה.**

---

## מוקשים שעדיין בתוקף

- `roof_manager.rb` הוא **CRLF**, `roof_dialog.rb`, `toolbar.rb`,
  `main.rb`, `downspout_tool.rb` הם LF, וקבצי `tests/rt*.rb` הם **LF**.
- `X = {...} unless const_defined?(:X)` **לא** נטען מחדש ב-`reload!`.
  לכן `roof_setting_keys`, `abut_headroom`, `min_abut_slope` הן **מתודות**.
- שורת קונסולה עם `build_roof!` בלי `replace:` או `level:` **מוחקת את
  כל הגגות שלו**. תמיד:
  `RoofManager.roofs.each { |r| RoofManager.build_roof!(replace: r) }`
- **הסטאב לא יוצר Edges מ-`add_face`** — `soften_shallow_edges!` והחור
  במכסה של הגאטר אי אפשר לבדוק בענן.
- שינוי HTML בדיאלוג → **לסגור ולפתוח את החלון**, לא רק `reload!`.
- כפתור חדש בסרגל → **הפעלה מחדש של סקצ'אפ**.
- **לסגור את פינות הגייבל עם `build_end_caps!` — נוסה ונכשל (26.08).**
- `full: true` ב-`edge_profile_chain` — נוסה ונכשל (27.08), שובר rt17.
- `_snap/` היא תיקיית טיוטות ולא ב-git. שמתי שם היום
  `cap_cmp2.png` ו-`roof_panel.html`.

## איך לעבוד עם דרור

- עברית פשוטה, **משפט-שניים, כמו לילד בן 5**.
- כל פקודה בקופסה נפרדת עם לאן (Ruby Console / CMD).
- git רק דרך CMD שלו. הוא לא כותב קוד — אתה עורך, הוא מדביק ובודק.
- **שרטוט לפני בנייה, ושאלה אחת קצרה.** היום כל שרטוט הזיז את השיחה
  קדימה, וכשהצגתי לו חתך עם המידות האמיתיות שלו הוא הבין את המגבלה
  לבד ובחר נכון תוך משפט.
  לציור בענן: SVG + `cairosvg`, ועברית חייבת לעבור `python-bidi`
  (`get_display`) לפני שהיא נכנסת ל-SVG.
  לאלוויישן מהסטאב: `PIL` (יש גם על המכונה של המשתמש), הטלה x/z,
  מיון פאות לפי y יורד.
  לצילום דיאלוג: `build_html` לקובץ + playwright (chromium מותקן בענן).
- **תמדוד לפני שאתה משנה, ותסתכל על התוצאה בענן לפני שאתה מוסר לו.**
- הוא כותב לפעמים בעברית עם מקלדת אנגלית. תפענח, אל תשאל.
- **הוא מבקש חיפוש באינטרנט כשהוא רוצה לדעת איך עושים את זה בבתים
  אמיתיים.** היום זה הביא את הכלל "4-6 אינץ' מתחת לאדן החלון" ואת
  האזהרה על dead valley, והוא קיבל את שניהם בלי ויכוח.
- צעד אחד, קטן, ולתת לו לאשר כל אחד. היום זה התפרק ל-3 והוא אישר
  כל אחד בנפרד.
- **commit אחרי כל סבב שעובד**, דרך CMD שלו, ואז לעדכן את קובץ המסירה.

## TODO

- **הדאונספאוט של הקומה העליונה נגמר באוויר.** מצאתי את זה היום, הוא
  ראה, ואמר שזה לא מה שהוא רצה לטפל בו עכשיו. הסיבה בקוד:
  `ground = walls.map { base_z }.min` לוקח את הקירות של **אותה קומה**
  בלבד, אז לגג העליון "הקרקע" היא רצפת הקומה השנייה. שתי האפשרויות
  שציירתי לו: עד הקרקע, או לשפוך על הגג התחתון. **הוא עוד לא בחר.**
- **צבע נפרד לדאונספאוט** — כרגע לוקח את צבע הגאטר.
- **גאטר על גג מעוגל** — `eave_polygon` מתייחס לקיר מעוגל כקטע ישר.
- מ-`CLAUDE.md`: קירות מעוגלים, תוכניות 2D ל-PDF, NOTE מצויר, צבע
  בגיליון, לוגו per-installation, corner boards.
