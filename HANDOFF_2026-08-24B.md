# Interior Pro — מסירה 2026-08-24B (הקו האלכסוני נסגר — סיום מרובע)

קרא את זה קודם, ואז `CLAUDE.md`. הקודמת: `HANDOFF_2026-08-24.md`
(שם הבאג עוד היה פתוח — הפתרון שמתואר שם, ריכוך, הוא **לא** מה שעבד).
**אל תסמוך על הסיכום — תאמת מול הקוד החי.**

בדיקות: בענן בלבד (למשתמש אין רובי): `cd tests` ואז
`LANG=C.UTF-8 bash run_all.sh ..` → **134 ירוקות**. להעתיק לענן גם
`icons/` ו-`textures/` (בלי `textures/floors/`), אחרת rt43/rt73 נופלים
בכזב. שים לב: `run_all.sh` מעתיק את קבצי המקור לתוך `tests/` — הרצה של
`ruby rt84.rb ..` לבד עובדת על עותק ישן ומשקרת. תמיד להריץ `run_all.sh`.

git: ענף `door-stabilize`. העבודה של אתמול+היום נסגרה בקומיט
"soffit: boxed eave soffit; square end at the gable corner...".

## מה קרה היום — הקו האלכסוני על הסופיט

המשתמש אישר: **הפינה נראית טוב עכשיו.** הסבב נגמר בקומיט.

### למה שלושת ניסיונות הריכוך נכשלו

מדדנו במקום לנחש (`debug_edge.rb` — בוחרים את הקצה בסקצ'אפ והסקריפט
מדפיס נקודות, פאות, נורמלים, `soft?`; הקובץ עבר ל-`_to_delete/`).
הפלט הראה ששני האלכסונים באזור **כבר היו** `soft=true, smooth=true`.
כלומר זו מעולם לא הייתה בעיית ריכוך. מה שכן היה שם:

- שני אלכסונים על אותו קו באורכים שונים: 15.910" ו-16.971". ההפרש
  0.75" = `FASCIA_THICK`.
- הלוח השטוח נעצר על `soffit_band[:k_out] = -FASCIA_THICK` (מאחורי
  הפאשיה), ומשולש ה-return של הרייק נמשך עד קו המתאר עצמו — כלומר
  **לתוך** הפאשיה של האיב. התחתית שלו (z=84.0) יושבת בדיוק על התחתית
  של הפאשיה: שתי פאות קו-מישוריות אחת על השנייה.
- ובנוסף שתי פאות קצה אנכיות על אותו מישור 45° (11.93 ו-12.73 אינץ'
  מרובע) — פאות קבורות, שרידי המיטר של שני הלוחות.

**הלקח: אם ריכוך לא עוזר פעמיים — הבעיה גיאומטרית, לא תצוגתית. למדוד.**

### מה שעשינו במקום (הרעיון של המשתמש)

אין אלכסון בכלל. בפינת גייבל הלוח השטוח **נחתך מרובע** וממשיך על כל
ריבוע הפינה, ולוח הרייק מתחיל אחריו.

- `build_band!` קיבל שני פרמטרים חדשים: `square_flags` (איזה שכן
  גורם לחיתוך מרובע) ו-`square_k` (על איזה קו לחתוך). רק הסופיט מעביר
  אותם — הפאשיה והדריפ נשארו ממוטרים בדיוק כמו קודם.
- עוזרים טהורים חדשים: `offset_line`, `line_cross`, `band_square_corner`.
  כולם מכוסים ב-`tests/rt84.rb`.
- `build_rake_soffit!` כבר **לא בונה** את לוח ה-return המפולס (הקופסה
  היא עכשיו הקצה של הלוח השטוח). הוא עדיין בונה את המדרגה של 90°
  (`rake_soffit_step`) ואת המשולש החיצוני (`rake_return_skirt`).
- `rake_soffit_miter` נשאר בקובץ אבל **כבר לא בשימוש** בבנייה — רק
  rt84 קורא לו.

### המוקש שדרכנו עליו בדרך (חשוב!)

הגרסה הראשונה חתכה את המרובע על `k_out` של הסופיט (0.75" פנימה מקו
המתאר) — והמשתמש ראה **רווח** בין הסופיט לפאשיה של הגייבל. הסיבה:

- הפאשיה של האיב נבנית ב-`build_band!` עם `k_in=-FASCIA_THICK, k_out=0`
  → **פנימה** מקו המתאר.
- לוח הרייק נבנה ב-`build_rake_board!` עם `out = +FASCIA_THICK`
  → **החוצה** מקו המתאר. הפנים שלו הוא קו המתאר עצמו.

לכן קו החיתוך הנכון הוא `square_k = 0` (קו המתאר), ו-`build_rake_soffit!`
רץ עד קו המתאר בלי שום inset. **אל תוסיף inset שם שוב.**
וגם: בחיתוך מרובע חייבים להזיז את **שתי** הפינות (הפנימית והחיצונית),
אחרת הקצה יוצא טריז ולא מרובע.

## מוקשים ישנים שעדיין בתוקף (לא לחזור עליהם)

- כל פאה עוברת `add_ring!` (מנקה כפולים ב-1/1000", בולע כישלון של פאה
  בודדת). raise אחד בתוך `build_rake_soffit!` מפיל את כל הקצה.
- הסטאב בענן (`tests/sketchup_stub.rb`) זורק על נקודות כפולות כמו
  סקצ'אפ אמיתי. לא לרכך את זה.
- `rake_soffit_quad`: פינה שנמשכת לאורך רייק מטפס חייבת לעלות איתו.
- ריכוך תפרים רץ **אחרי** שכל הפאות נבנו.
- הסתרת פאה לא מסתירה את הקצוות שלה.

## איך לעבוד עם דרור

- עברית פשוטה, משפט-שניים. כל פקודה בקופסה עם לאן (Ruby Console / CMD).
- git רק דרך CMD שלו. הוא לא כותב קוד — אתה עורך, הוא מדביק ובודק.
- צעד אחד, מדידה, תשובה שלו, ורק אז הצעד הבא. commit אחרי סבב שעובד.
- לקרוא את ההודעות שלו לאט. לא להכריז "סיימנו" לפני שהוא אישר.
- אם לא ברור מה הוא רואה — לבקש תמונה עם סימון, לא להניח.

## TODO — מחר

- להסיר את המתג `debug_soffit` ואת ההדפסה שלו (זמניים, עוד בקוד).
- המשתמש ביקש: מטל פלשינג (drip edge) גם בגייבל — לא התחלנו.
- 4 סוגי הסופיט הנוספים: קורות גלויות, עץ, סטאקו שטוח, ספרדי
  (סטאקו + קורות עגולות). היום נבנה רק `'boxed'`.
- רעיון שלו שנשמר: סופיט משופע במקביל לגג כאופציה נוספת.
- חיבור ל-RoofDialog (עכשיו רק דרך `build_roof!(soffit: 'boxed')`).
