# Interior Pro — מסירה 2026-08-17C (חלון התוכניות: מחיקה, גודל טקסט, עיצוב כיתוב)

קרא את זה קודם, ואז `CLAUDE.md`. **אל תסמוך על הסיכום — תאמת מול הקוד החי.**
הקובץ הקודם: `HANDOFF_2026-08-17B.md` (גדר ויניל + חומה).
כללי העבודה: `HANDOFF_2026-08-14.md` §0. **תקרא אותם ברצינות.**

התיקייה החיה:
`C:\Users\rordt\AppData\Roaming\SketchUp\SketchUp 2024\SketchUp\Plugins\interior_pro`

**הבדיקות: 3971 ירוקות** (2973 רובי + 998 ג'אווהסקריפט). אפס אדומות.

---

## 0. השורה התחתונה של הסבב הזה

הסבב עבר מ**גדרות וחומות** ל**הוצאת התוכניות**, לבקשת המשתמש. נכנסו שלוש
תכונות, נמצאו שני באגים אמיתיים — ומעל הכל:

> **ל-62KB של הקוד בחלון התוכניות לא הייתה אף בדיקה. אף פעם.**

לעורך ה-2D יש בדיקות מהיום הראשון (`extract.rb` + `t1`..`t45`). לחלון
התוכניות לא היה כלום, אז שגיאת תחביר אחת שם הייתה הורגת את **כל** החלון
בשקט. זה תוקן (`extract_sheet.rb` + `t46.js`), **וזה מה שתפס את הבאג
שהמשתמש דיווח עליו.** אל תוריד את זה.

**מה שלמדתי הכי חזק:** בדקתי את `hitText` (מוצא הכיתוב) והוא עבר, אז אמרתי
למשתמש "זה עובד, כנראה לא טענת". הוא הריץ את סקריפט הבדיקה שנתתי לו
והתשובה הייתה "הכל טעון". **הבדיקה שלי בדקה את מה שמתחת לכפתור ולא את
הכפתור.** רק כשהרצתי את `svg.ondblclick` עצמו נפל ה-`ReferenceError`.
**תמיד תריץ את נקודת הכניסה האמיתית, לא את מה שהיא קוראת לו.**

---

## 1. מה נכנס

### א. מחיקת קו בודד מהתוכנית (`rt65`, 57 בדיקות)

המשתמש שלח תמונות של התוכנית שלו עם שלושה עיגולים אדומים: קווים שחוצים את
השביל, והתפר באמצע הקופינג של הבריכה. *"אני רוצה למחוק אותם שהציור יהיה
יותר נקי."*

קודם היו רק שתי אפשרויות: כל הגיאומטריה החופשית או כלום
("נקה גיאומטריה חופשית"). עכשיו: **כפתור מחק** (רביעי בשורת הכלים של
"מידות והערות") → ריחוף צובע את הקו אדום → קליק → נעלם.
**"החזר קווים שנמחקו"** בקופסה "מהמודל".

**איך זה עובד, וזה כל הטריק:** `PlanGeometry.build!` כותב את הקווים לשכבת
`SITE` **בסדר שהם יושבים ברשימה** של `PlanSheetDialog`, פוליליין לכל אחד.
אז **צורה מספר i על הדף היא `site_lines[i]`**. החלון שולח מספר. אין שום
התאמה לפי קואורדינטה, אז שני קווים זה על זה לא יכולים להתבלבל.

**המודל לא נגע.** התפר בקופינג הוא קצה אמיתי בין שתי פאות והוא נשאר.
יש בדיקה שנופלת אם מישהו יכניס `erase!` לקולבק הזה.

**רק גיאומטריה חופשית.** קירות/דלתות/חלונות לא נמחקים שם בכוונה — הם היו
נמחקים מהמודל. `siteLayer()` שואל את `'SITE'` בשם, ויש בדיקה לכל שכבה
אחרת שהיא לא נגעת.

### ב. גודל טקסט — ארבע קבוצות (`rt66`, 58 בדיקות)

קופסה חדשה **"גודל טקסט"** בפאנל הצד (שלישית מלמעלה), ארבעה שדות באחוזים:

| שדה | על מה | היום |
|---|---|---|
| מידות | `DIMENSIONS` + `NOTES` (אוטומטיות **וגם** ידניות) | 5.0" |
| שמות חדרים | `ROOMS` — השם והשטח | 7.0" / 4.5" |
| תגי דלת/חלון | `DOORS` + `WINDOWS` | 4.2" |
| טבלאות | לוחות דלתות/חלונות | 3.6" |

**100% = בדיוק מה שהיה תמיד**, אז מודל ישן נפתח בלי שינוי. יש בדיקה על זה.

**למה זה עלה כמעט כלום:** כל טקסט **כבר** נושא את הגובה שלו (`Recorder`
לוקח אותו מ-`add_3d_text`) **וכבר** יושב על שכבה עם שם (`build` קובע
`rec.layer_name` לפני כל קבוצה). אז הגדלים משתנים במעבר אחד על המסמך הגמור
ו-**`plan_generator` לא נגע בכלל**. אם מישהו יתחיל לכתוב טקסט בלי גובה או
על שכבה לא נכונה — `rt66` תיפול.

**המלכודת, ותקרא את זה לפני שאתה נוגע:** `layout_pages!` רץ **בכל קליק**
בחלון. אם היו מכפילים את `:h` באחוז בכל ריצה, המשתמש היה מקליד 110 פעם אחת
ואז רואה את הכתב תופח בכל נגיעה, בלי דרך חזרה. לכן **הגובה המקורי נשמר
ב-`:h0`** וכל גודל נגזר ממנו. הרצה פעמיים = הרצה פעם אחת. יש בדיקה.

**הטבלאות היו הקשות.** טבלה זה לא פסקה: אותיות גדולות בשורות שלא גדלו
נכתבות על קווי הרשת. רוכב על מנגנון הזום שכבר היה שם לטבלה בדף מלא, שמגדיל
אותיות + שורות + עמודות ביחד, ו-`reserved_width` גדל איתן כדי שהתוכנית
תפנה מקום.

### ג. שם קבוע לכל כיתוב + עיצוב פר-כיתוב (`rt67`, 57 בדיקות)

**דאבל-קליק על כל כיתוב על הדף** — שם חדר, תג, מספר מידה, הערה — פותח
חלונית עם: גודל באחוזים, **B**, *I*, "אפס", "סיים". הכיתוב מסומן בכחול.

**הבעיה שזה פותר:** הכיתוב האוטומטי **נזרק ונבנה מחדש מהמודל בכל פריסה**.
תצביע עליו לפי מיקום או אינדקס והבחירה נוחתת על כיתוב אחר ברגע שמשהו זז.

**השם:** `שכבה|המילים|איזה מהם` — למשל `ROOMS|ROOM 2|1`.
`PlanCanvas.text_key` / `name_texts!`.

**למה דווקא ככה:** זה עולה ל-`plan_generator` **אפס**. החלופה הייתה
להשחיל מזהה דרך כל מתודת `draw_`, וכל התכנון של הקובץ הוא שהגנרטור לא יודע
שהדף קיים.

**מה זה לא יכול, כתוב בגלוי כדי שלא יתגלה מאוחר:**
- **תשנה שם של חדר → העיצוב מתייתם.** "ROOM 2" → "KITCHEN" זה כיתוב אחר.
- **שתי מידות שכתוב בהן אותו דבר** מובחנות רק לפי סדר. תוסיף קיר ביניהן
  והעיצוב יתחלף.

שתיהן מתקנות בקליק אחד ואף אחת לא יכולה לאבד יותר מהעיצוב עצמו.
**שתיהן נעוצות כבדיקות ב-`rt67`**, לא נשארו כהפתעה. שם חסין יותר = ללמד
את הגנרטור על מזהים, וזה שינוי גדול בהרבה ממה שזה מצדיק.

**"אפס" מחזיר לגודל של הקבוצה, לא ל-100%** — הקבוצה היא הסטנדרט של הציור.
העיצוב הפר-כיתובי **מחליף** את אחוז הקבוצה, לא מוכפל בו: מי שהקליד 150
התכוון ל-150.

### ד. מקש רווח = מצב בחירה

כמו בסקצ'אפ. מכל כלי. לא עובד בזמן הקלדה בתיבה — שם רווח הוא רווח.

---

## 2. שני באגים אמיתיים שנמצאו ותוקנו

### א. הדאבל-קליק נפל על `ReferenceError` — **הבאג המרכזי**

**התסמין:** *"אין לי בכלל את האופציה לבחור את הכיתוב."*

**הסיבה:** ה-`ondblclick` שלי קרא ל-`closeNote()`, שמוגדרת **בתוך**
`window.addEventListener('load', ...)` ולא קיימת ברמה העליונה. זריקה
**בשורה הראשונה** של המטפל, לפני שהספיק לעשות כלום. **הדפדפן בולע שגיאות
בתוך מטפלי אירועים — שום דבר לא הופיע בשום קונסולה.**

**התיקון:** `hideNoteBar()` ברמה העליונה; `closeNote` מאצילה אליה, כדי
שרק מקום אחד יודע לסגור את הסרגל הזה.

**השומר:** `t46` בודקת שכלום שמוגדר בתוך מטפל ה-`load` לא נקרא מבחוץ.
**החזרתי את הבאג בכוונה כדי לוודא שהשומר תופס — הוא תפס.**

### ב. החלוניות נגררו החוצה בזום

`#textbar` ו-`#notebar` היו `position:absolute` בתוך `#stage`, שהוא
**האלמנט שנגלל**. זום פנימה → גלילה → החלונית מחליקה מחוץ למסך.
*"אם אני עושה זום אין הוא נעלם לי כי הוא נשאר למעלה."*

**חלונית שמקלידים בתוכה לא יכולה לגור בתוך הדבר שמגלגלים.**

**התיקון:** `position:fixed` לשתיהן, **וגם אפשר לגרור** את חלונית הכיתוב
בשם שבצד. לא ניתנת לגרירה מחוץ למסך — חלונית שנעלמת בקצה אי אפשר להחזיר.
המיקום נזכר כל עוד החלון פתוח, לא נכתב על המודל.

### ג. `rt29` היה אדום **לפני** שנגעתי בכלום

בדיוק כמו `rt63` בסבב הקודם. הבדיקה חיפשה `aDeg + '°'` אבל הקוד עבר מזמן
ל-`fmtAngle(aDeg)`. הפיצ'ר תקין. **תיקנתי את הבדיקה, לא את הקוד.**

> **דפוס:** בשני הסבבים האחרונים נמצאה חבילה אדומה שהייתה שם מראש.
> **תריץ את כל החבילה לפני שאתה נוגע במשהו**, אחרת אתה מתחיל על יסוד שבור.

---

## 3. קבצים

| קובץ | מה |
|---|---|
| `plan_sheet_dialog.rb` | `drop_site_line`, `restore_site_lines`, `set_text_scale`, `set_text_mark`, `site_dropped`, מצב `erase`, `hitSite`/`paintErase`, `hitText`/`textBox`/`openTextBar`/`paintTextPick`, `hideNoteBar`, `dragTextBar`, מקש רווח, קופסת "גודל טקסט", `#textbar` |
| `plan_canvas.rb` | `TEXT_GROUPS`, `text_pct`, `apply_text_scale!`, `text_key`, `name_texts!`, `apply_text_overrides!` |
| `plan_tables.rb` | `place!(zoom:)`, `reserved_width(doc, hidden, zoom)` |
| `tests/extract_sheet.rb` | **חדש.** מוציא את ה-JS של חלון התוכניות מה-heredoc |
| `tests/t46.js` | **חדש, 58 בדיקות.** מריץ את הקוד האמיתי של החלון |
| `tests/rt65.rb` `rt66.rb` `rt67.rb` | **חדשות**, 57/58/57 |
| `tests/rt29.rb` | תוקנה טענה ישנה |
| `tests/run_all.sh` | `extract_sheet.rb`, `sheet.js`, `t46`, `rt65`–`rt67` |
| `debug_sheet_loaded.rb` | **חדש.** דיסק מול זיכרון, לחמש התכונות האחרונות |
| `LANDSCAPE_ROADMAP.md` | **חדש.** מפת הדרכים לגדרות/חומות/ריילינג |

---

## 4. מה פתוח — הצעד הבא

1. **צבע.** הדבר הגדול שנשאר מהרשימה שלו. **אין מושג של צבע קו בשום מקום
   במסמך הציור:** `plan_doc.rb` יודע רק עובי ומילוי למצולעים, התצוגה כותבת
   `stroke="#000"` קשיח, וה-PDF כותב `0 0 0 RG` קשיח. צריך לעבור בארבעה
   קבצים כדי שהתצוגה וה-PDF לא יסתרו. נכנס לאותה חלונית של הדאבל-קליק.
2. **לוגו גלובלי.** היום ב-`sheet_state`, שזה **אטריביוט על המודל** — פר
   פרויקט. הוא רוצה פר התקנה. `Sketchup.write_default`/`read_default` —
   **התוסף לא משתמש בהם בכלל היום.** עבודה קצרה.
3. **ההערה מכוערת.** הוא ניסה לשים "קירות פנים" וזה יצא רע (ריבוע + חץ).
   *"עזוב את זה שזה לא נראה טוב ואנחנו צריכים לעבוד על זה."* לא נשכח.
4. **פונט — אזהרה.** ה-PDF נכתב ידנית עם Helvetica בלבד (רגיל/מודגש/נטוי)
   כולל טבלת רוחב-אותיות קשיחה למרכוז. **גודל, צבע, מודגש ונטוי — כן.
   פונט אחר — טבלת רוחב נוספת או הטמעת פונט.** הוא ביקש "כל האופציות של
   Word"; נאמר לו במפורש שזה לא יקרה במלואו.
5. **`text_scale` נשמר על המודל**, פר פרויקט. אמרתי לו שאם ירצה גלובלי זה
   מתחבר לאותו שינוי כמו הלוגו. הוא לא ענה.
6. **גדרות/חומות/ריילינג** — כל הרשימה ב-`LANDSCAPE_ROADMAP.md`. הסדר:
   ספרייה מאוחדת → עריכה והחלפת סוג → גבהים → קשתות → שערים → ריוורס-פייס.
7. הפתוחים של החומה מ-`HANDOFF_2026-08-17B.md` §4 — רשימת חומרי גמר,
   טקסטורות, אייקון.

---

## 5. מה למדתי הסבב הזה

1. **תריץ את נקודת הכניסה, לא את מה שמתחתיה.** בדקתי את מוצא הכיתוב, הוא
   עבר, ואמרתי למשתמש "כנראה לא טענת". הוא הוכיח שכן. הכפתור היה שבור, לא
   מה שמתחתיו. זו הטעות הכי גדולה שעשיתי כאן.
2. **תן לו כלי מדידה במקום ויכוח.** `debug_sheet_loaded.rb` סגר תוך 10
   שניות שאלה ששני צדדים היו יכולים לנחש עליה עוד שלושה סבבים.
3. **תוודא שהשומר נופל.** בשני התיקונים החזרתי את הבאג ובדקתי שהבדיקה
   אדומה. בדיקה שלא ראית נופלת היא לא בדיקה.
4. **קוד בלי בדיקות מצטבר בשקט.** 62KB חיו שנה בלי כלום. אף אחד לא החליט
   את זה — זה פשוט לא נכתב אף פעם.
5. **"איפה אני אמור לראות את זה?" נשאל שלוש פעמים.** אחרי כל תכונה בפאנל
   הצד. **תגיד לו איפה הכפתור לפני שהוא שואל** — הקופסאות סגורות כברירת
   מחדל והוא לא יודע לפתוח אותן.
6. **הוא כתב פעם אחת במקלדת האנגלית** (`,xchr kh cnapy tjs nv gah,`).
   תפענח, אל תבקש שיכתוב שוב.
7. **גם `rt29` וגם `rt63` היו אדומים מראש.** תריץ הכל לפני שאתה מתחיל.
