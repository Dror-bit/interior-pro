# מסירה לצ'אט הבא — Interior Pro, סבב 2026-08-05B (מנוע גמלונים חדש)

## 0. לפני הכל
התיקייה החיה היחידה:
`C:\Users\rordt\AppData\Roaming\SketchUp\SketchUp 2024\SketchUp\Plugins\interior_pro`
סדר קריאה: **הקובץ הזה** ← `HANDOFF_2026-08-05.md` ← `STATUS_handoff.md`.
כללי העבודה לא השתנו: עברית, קצר, פקודות בקופסאות עם ציון לאן,
צעד ← בדיקת משתמש ← commit, **לא לסמוך על סיכומים — לאמת מול הקוד והקונסול.**

## 1. ההחלטה העיצובית של המשתמש (מחייבת)
- **היפ הוא הבסיס.** Apply במצב Hip נותן היפ מלא — זה עובד ויציב.
- **עריכה ידנית בקליקים**: Gable Ends על קיר הופך את *כל* הקיר למשולש
  מלא של הכנף שלו (כמו Defines Slope=off ברוויט). קליק שני מחזיר להיפ.
- **מצב Gable בחלון** = קיצור: כל קצה של כל כנף גמלון אוטומטית, בלי קליקים.
- אין יותר "רצועות לפי קליק" במסלול הראשי (הקוד הישן קיים רק כ-fallback).

## 2. מה נבנה בסבב הזה (הכל ב-roof_manager.rb)
### מנוע over-framing (מהדגם הידני של המשתמש — "ככה בונים גג")
- `framed_plan` → `grid_rects`: חלוקת המתאר למלבנים על רשת קווי הקירות —
  שוב ושוב המלבן הכי גדול שנשאר. עובד גם כשכנפיים יוצאות בשני צירים
  (הבית השלישי). עד 8 מלבנים; נכשל → fallback לשלד הישן.
- `assemble_framed_plan`: ראשי = המלבן הגדול; שאר המלבנים נתלים כעץ
  (גם כנף-על-כנף). קצה מסומן = side_gabled (חפיפת קצה עם קיר מסומן).
- `build_main_rect!` / `build_wing_rect!`: פריזמות גמלון; רכס כנף צולל
  לתוך ההורה בחצי רוחב הכנף → ואלי אמיתי ב-45°; קצה לא מסומן = היפ.
- משולשי הגמלון אנכיים, לא צבועים → נצבעים לבן ב-catch-all.
- פשיה/מטל-אדג' מדלגים על קצוות גמלון (gable_flags); rake לפי zmap.
### תיקונים במסלול השלד (נשארים גם ב-fallback)
- zmap שומר את ה-z הגבוה לכל נקודה; פאות סגירה אנכיות לקרעים.
### סימונים
- `toggle_gable_wall!` מנקה אוטומטית מזהים מתים (קיר שפוצל/חובר —
  הסיומות A/B/AJ משנות id!). build מתעלם ממסומנים שלא בלולאה ומדפיס
  `ignoring N stale...`. **באג שנפתר**: סימון מת חסם את מצב Gable.

## 3. בדיקות
```
bash tests/run_all.sh .
```
30 JS + 15 רובי — הכל ירוק במסירה. `rt15.rb` מכיל רגרסיות על שלושת
הבתים האמיתיים של המשתמש (10 פינות; 4 גמלונים; כנפיים בשני צירים)
+ סימונים מתים + Gable אוטומטי. תמיד להריץ לפני מסירה למשתמש.

## 4. מצב מול המשתמש — הצעד הראשון של הצ'אט הבא
1. המשתמש עוד **לא אישר ויזואלית** את תיקון הרשת על הבית השלישי שלו.
   קודם: `InteriorPro.reload!` ← חלון גג ← Gable ← Apply ← צילום.
2. אחר כך סבב היפ: Hip ← Apply ← קליקים הלוך-חזור עם Gable Ends על
   קירות שונים ← לוודא שכל קליק עושה בדיוק מה שמצופה.
3. כל תקלה: לבקש קונסול (`[Roof]` lines) + צילום + להריץ את סקריפט
   האבחון (מדפיס מתאר, rects, mouths) לפני כל תיקון. לשחזר ב-rt15.

## 5. פתוח / ידוע
- חוב קוסמטי: בקיר ארוך משותף (ראשי+כנף) שקצהו גמלון — כל הקצה מסומן
  כ-gable לפשיה, אז חלק האיבס של הכנף מקבל "rake שטוח" במקום פשיה. זניח.
- כנף שנתלית על קצה-גמלון של ההורה: החדירה נבנית אך מוסתרת — תקין ויזואלית.
- slab_rects/merge_rects כבר לא בשימוש במסלול הראשי (נשארו לדיבוג).
- מהסבבים הקודמים: Dutch Gable (הובטח), חומרים אמיתיים לגג, gable walls
  אמיתיים (קיר שעולה למשולש), תקרה עליונה קומה 2, מיטר Merge Wall.

## 6. קבצים שהשתנו בסבב
`roof_manager.rb` (מנוע חדש + תיקונים), `tests/rt15.rb` (רגרסיות).
