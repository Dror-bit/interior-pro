# Interior Pro — מסירה 2026-08-23B (הלשון בגיבל: תוקן ✓; שני קווים על השיפוע: פתוח)

קרא את זה קודם, ואז `CLAUDE.md`. הקודמת: `HANDOFF_2026-08-23.md`.
**אל תסמוך על הסיכום — תאמת מול הקוד החי.**

הבדיקות: מריצים בענן (למשתמש אין רובי): `cd tests` ואז `LANG=C.UTF-8 bash run_all.sh ..`
במצב השמור: **133 ירוקות.**

> **git:** המשתמש התבקש להריץ `git checkout -- roof_manager.rb tests/rt83.rb`
> (חזרה ל-commit של תיקון הלשון) — **לא אומת שרץ.** צעד ראשון: `git status`
> דרך CMD שלו (לעולם לא בעצמך — index.lock נתקע).

---

## 1. מה נסגר היום — הלשון הקבורה בגיבל ✓ (commit קיים)

**האבחנה (נמדדה במודל שלו, לא ניחוש):** "הסכין" (`buried_split_segments`)
עבדה כל הזמן. הפאה העליונה של הלשון כבר נמחקה; מה שנשאר היה
**תחתית הדק** — לדק שתי שכבות (עליון + תחתון ב-0.5" הפרש), ו-
`drop_buried_faces!` קיבלה רק את העליונה. במדידה: f2 הייתה הפאה היחידה
בקבוצה בלי תאומה.

**התיקון (ב-commit "gable: erase the slab underside twin..."):**
`drop_buried_twins!` ב-`roof_manager.rb` — כשפאה עליונה נמחקת כקבורה,
נמחקת גם התאומה התחתונה שלה (התאמה: אותו מרכז ±1", אותו כיוון מישור,
נמוכה ממש). **אסור** להעביר את התחתית דרך מבחן הכיסוי עצמו — נמדד: הוא
מסמן את *כל* תחתית הדק כקבורה (כל תחתית מכוסה בידי העליון של עצמה).
בדיקות ב-`tests/rt83.rb`. המשתמש אישר ויזואלית: הלשון נעלמה.

## 2. הפתוח: שני קווים על השיפוע הראשי ליד כנף הגיבל

המשתמש רואה שני קווים דקים חוצים את שדה הרעפים של השיפוע הראשי, באזור
המפגש עם הכנף. **מה שכבר נמדד — אל תחזרו על זה:**

- ברמת הדק נמצאו בדיוק 3 קצוות ששוכבים בין שתי פאות באותו מישור:
  אחד באורך 250 מ-(-612,-12,92) עד (-440,160,149), ושני סטאבים
  אנכיים של 0.5". **מחקנו אותם ישירות בקונסול — הקווים על המסך לא
  נעלמו.** כלומר הקווים הנראים הם לא הקצוות האלה.
- הוסתרו *כל* ה-instances וה-groups בתוך קבוצת הגג (רעפים, קאפים...) —
  **הקווים עדיין נראו.** כלומר הם גם לא גיאומטריית רעפים... אלא אם
  ההסתרה לא באמת רעננה את התצוגה. שווה לאמת שוב לפני מסקנה.
- נשארו כחשודים: קצוות תפר/שפה על המעטפת שפאותיהם במישורים שונים
  (rim של החור שנמחק), או גיאומטריה מחוץ לקבוצת הגג, או קצה של רעפים
  חתוכים (cover_clips) שנגמרים בשורה ישרה על קו החיתוך.
- ניסיון תיקון `drop_knife_leftovers!` (מחיקת קווי סכין בין פאות
  קו-מישוריות אחרי המחיקה) נכתב, עבר 133 ירוקות, **לא פתר את מה
  שרואים**, והמשתמש בחר לבטל אותו (git checkout). אם רוצים אותו בחזרה —
  הוא ב-reflog, אבל אל תחזירו בלי שהוא פותר משהו נראה לעין.

**דרך מדידה שמומלצת לסבב הבא:** לגרום למשתמש לבחור את הקו עצמו
(דאבל-קליק לתוך קבוצת הגג, קליק על הקו) ואז סקריפט שקורא מה-selection —
פעמיים זה נכשל כי נבחרה פאה/כלום; שווה להתעקש, כי selection אחד מוצלח
עונה על הכל (כמה פאות, אילו מישורים, איפה). לחלופין: להסתיר את
*המעטפת* (הפאות עצמן) במקום את הרעפים.

## 3. תקריות מהסבב — שלא יחזרו

- **"הקיר נמחק":** לא נמחק. סקריפט ההסתרה הסתיר את כל הקבוצות בתוך
  קבוצת הגג — **וגם קיר הגמלון בנוי שם.** `hidden = false` על כל
  החלקים מחזיר. ודא בתחילת הסבב שהמשתמש רואה את הקיר; אם לא — זו הסיבה.
- במודל בוצעו פעולות ידניות (מחיקת 3 קצוות, הסתרות). **בנייה מחדש של
  הגג מנקה הכל** — לעשות את זה לפני כל מדידה חדשה.
- AutoBackup מדפיס "Model must be saved before copying" — רעש, לא באג.

## 4. איך לעבוד איתו (זה קרס הסבב — קרא באמת)

- עברית. משפט-שניים, כמו לילד בן 5. כל פקודה בקופסה נפרדת עם לאן
  להדביק (Ruby Console / CMD). בלי git בעצמך — רק דרך CMD שלו.
- **בדיוק מה שביקש.** מספר שלא ביקש לשנות = באג.
- **צעד אחד, מדידה אחת, ואז לחכות לתשובה שלו.** לא לשלוח שלוש קופסאות
  קדימה. סבב שעובד נגמר ב-commit לפני שנוגעים בדבר הבא.
- למדוד ברובי בענן לפני שמוסרים לו. מרחק לקצוות, לא לקודקודים.
- הוא מתוסכל מסבבים ארוכים של ניחושים. אם מדידה לא מכריעה תוך
  שניים-שלושה צעדים — לעצור ולהציע לו לבחור איך להמשיך.
