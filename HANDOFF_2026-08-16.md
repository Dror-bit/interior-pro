# Interior Pro — מסירה 2026-08-16

קרא את זה קודם, ואז `CLAUDE.md`. אל תסמוך על הסיכום — תאמת מול הקוד החי.
הקובץ הקודם: `HANDOFF_2026-08-14B.md` (שם §2 — מלכודת הטרנספורמציות).
כללי העבודה: `HANDOFF_2026-08-14.md` §0 — לא השתנו, תקרא אותם.

התיקייה החיה:
`C:\Users\rordt\AppData\Roaming\SketchUp\SketchUp 2024\SketchUp\Plugins\interior_pro`

---

## 0. שתי עובדות חדשות על סביבת העבודה

1. **יש לו גם מאק** עם סקצ'אפ **2026** ואותו תוסף מותקן, בנתיב
   `~/Library/Application Support/SketchUp 2026/SketchUp/Plugins/interior_pro`.
   הוא מעדכן אותו **בעצמו דרך git בטרמינל**. אל תציע לו זיפים ואל תציע
   להקים git — הוא כבר עושה את זה. מה שאתה כן חייב: לתת לו `git push`
   בסוף כל commit, אחרת המאק לא רואה כלום.
2. **הפרויקטים שלו יושבים ב-OneDrive**, ולכן גם הגיבויים (שנשמרים ליד
   הקובץ) מסתנכרנים בין שני המחשבים לבד.

---

## 1. האסון של היום — קרא את זה לפני כל דבר אחר

הוא יצא למדידות, סרטט מבנה שלם על תמונה במאק, שמר, חזר הביתה, פתח,
לחץ **"הפוך ל-3D"** — ו**הכל נמחק**. לא סירטוט ולא 3D. לא היה שום גיבוי.
העבודה אבדה סופית.

**הסיבה, מאומתת בקוד:** `sketchesDone(n)` ב-`plan_editor.rb` קיבלה מרובי
את מספר הצורות שנבנו — ו**לא הסתכלה עליו בכלל**. היא ניקתה את
`pendingSketches` תמיד, ואז `saveDraft(true)` כתבה טיוטה ריקה מעל
הטיוטה הטובה. הבנייה נכשלה בשקט והקוד מחק גם את המקור וגם את הגיבוי.
`applyDone(n)` עשתה בדיוק אותו דבר לקירות.

**הכלל שנולד מזה, ואסור לשבור אותו:**
> שום כתיבה לא מוחקת את מה שהיא דורסת. ושום ניקוי מסך לא קורה לפני
> שהמודל באמת אישר שהוא קיבל.

**מה שעדיין לא ידוע:** *למה* הבנייה החזירה 0 מלכתחילה. זה לא נחקר.
עכשיו לפחות זה צועק במקום למחוק. בפעם הבאה שזה קורה — תבקש ממנו צילום
של ההודעה.

---

## 2. מה נסגר היום

### א. גיבוי אוטומטי של הפרויקט — `auto_backup.rb` (חדש)
כל **5 דקות** (בחירה שלו) נשמר עותק שלם של ה-skp לתיקייה
`InteriorPro_backups` **ליד הקובץ**. שם עם תאריך ושעה, שומר **20**
אחרונים ומוחק ישנים לבד. משתמש ב-`model.save_copy`, אז הקובץ שהוא עובד
עליו לא נגוע והכותרת לא משתנה.

- נדלק לבד מ-`main.rb` → `start_auto_backup!`, גם ב-`setup_ui!` וגם
  ב-`reload!`. `start!` עוצר טיימר קודם, אז reload פי מאה = טיימר אחד.
- מודל ריק לא מגובה. מודל שלא נשמר מעולם כן — ל-Documents.
- כיבוי: `InteriorPro::AutoBackup.stop!`

### ב. גיבוי מצב — `state_backup.rb` (חדש)
כל כתיבה של מצב שמורה שומרת את הגרסה שהיא דורסת. 6 גרסאות אחורה לכל
מפתח, עם תקרת בייטים כדי שה-skp לא יתנפח. כתיבה זהה לא נספרת (העורך
שומר טיוטה כל 1.5 שניות — בלי זה ההיסטוריה הייתה מתמלאת בעותקים זהים).

מגובה עכשיו:
| מפתח | מה זה |
|---|---|
| `plan_draft` | הטיוטה של העורך — קירות כחולים, צורות שלא הוחלו |
| `sheet_state` | חלון התוכניות: דף, קנה מידה, תמונות, מידות ידניות |
| `sheet_site_lines` | גאומטריה שנבחרה ידנית לשכבת SITE |
| `underlay_state` | תמונת הרקע **והכיול שלה** |

שחזור (אין כפתורים — ראה §3):
```
InteriorPro::StateBackup.report                  # כותב backup_report.txt
InteriorPro::StateBackup.restore!('plan_draft')
InteriorPro::StateBackup.restore!('sheet_state')
InteriorPro::PlanEditor.restore_underlay!
```

### ג. "הפוך ל-3D" כבר לא מוחק כלום כשהבנייה נכשלת
`sketchesDone(n)` ו-`applyDone(n)` בודקות את `n`. אפס = הסירטוט/הקירות
נשארים על המסך, לא נכתבת טיוטה, ומופיעה הודעה בעברית. חלקי = הודעה
שאומרת כמה מתוך כמה.

### ד. רצפת המפתן עוקבת אחרי הדלת
`FloorManager.refresh_door_patches!` חדשה (עוטפת פעולה אחת, `transparent:`
נתמך, לא פותחת פעולה כשאין רצפות). נקראת מארבעה מקומות:
`door_move_tool.rb`, `door_delete_tool.rb`, `door_tool.rb`
(place_door_via_native_openings!), `door_library_dialog.rb` (apply_edit).

לפני זה `build_door_patches!` נקרא **רק מתוך `floor_manager.rb` עצמו** —
כלומר רק ב-Build Floors. הוא ראה את זה: הזיז דלת חוץ והמפתן נשאר מאחור.
**אושר אצלו בסקצ'אפ: עובד.**

### ה. Landscape Pro — המוצר החדש
תוסף-בתוך-תוסף לכל מה שמחוץ לבית. תיקייה `landscape/`, מודול
`InteriorPro::Landscape`, סרגל כלים נפרד בשם **Landscape Pro**.

- `landscape/fence_math.rb` — חשבון גדר **טהור**, אפס API של סקצ'אפ.
  אותו עיקרון של `arc_math.rb`.
- `landscape/fence_tool.rb` — הכלי. שתי לחיצות. **לא** יורש מ-WallTool
  (קיר = קופסה אחת עם גג שטוח; גדר = עמודים ושדות). כן **קורא** ל-
  `WallTool.apply_axis_magnet` — לווה בקריאה, לא בהעתקה.
- הגדרות ב-`UI.inputbox` פשוט, לא HTML. הדיאלוג HTML יגיע עם הסוגים.
- **אושר אצלו בסקצ'אפ: "ההגדרות עובדות מעולה".**

---

## 3. שתי החלטות שלו שאסור לבטל

### א. בלי כפתורי גיבוי בתפריט
בניתי ארבעה פריטי תפריט לשחזור. הוא אמר:
*"תעיף אותם, אני לא רוצה לסבך את העניינים."*
הגיבוי חייב לרוץ לבד; שחזור נדיר מספיק כדי להיעשות ביד מהקונסולה.
**אל תחזיר כפתורים בלי לשאול אותו.** יש הערה על זה ב-`toolbar.rb`.

### ב. `ground_start` + `ground_end` — שני מספרים, לא אחד
כל אובייקט חוץ שומר גובה קרקע ב**שני** קצוות. היום שניהם 0 ואין הבדל
מקיר. ביום שתהיה טופוגרפיה, הקרקע רק תמלא את שני המספרים ושום קוד
גאומטריה לא ישתנה. **מספר אחד לעולם לא יכול להיות במדרון.**

---

## 4. מלכודות שנפלתי בהן היום

### א. `\n` בתוך ה-heredoc של `plan_editor.rb`
כל ה-JS של העורך יושב ב-`<<~HTML` של רובי, והוא **מפרש** `\n`. כתבתי
`alert('...\n...')` ו-`out.js` הפסיק להיפרס. **בלי `\n` בתוך המחרוזות
שם.** משפט אחד, או שרשור.

### ב. הסטאב לא ידע לעשות כלום שכלי אמיתי עושה
`Sketchup::Face` היה `class Face; end` ריק — בלי `pushpull`, `normal`,
`all_connected`. `Model` בלי `active_entities`, בלי `save_copy`, בלי
`attribute_dictionary`. `UI.start_timer` החזיר nil וזרק את הבלוק.
כל אלה תוקנו ב-`tests/sketchup_stub.rb`. `Face#normal` הוא Newell אמיתי,
כדי שענפי "לאיזה כיוון הפָּן נולד" ירוצו בענן כמו בסקצ'אפ.

### ג. `FM = nil` בתוך מתודה
`SyntaxError` — dynamic constant assignment. טעות טיפשית, תפס אותה
`ruby -c`. תריץ `ruby -c` על כל קובץ לפני שאתה שולח.

---

## 5. בדיקות

הכל ירוק בענן.

| חבילה | מה |
|---|---|
| **rt56** | `fence_math` — 90 בדיקות: עמודים, שדות, rake מול step, קרשים |
| **rt57** | `fence_tool` — בונה באמת, ומאמת שהוא **קורא** ל-fence_math ולמגנט |
| **rt58** | המפתן עוקב אחרי הדלת — מריץ `commit_move` ו-delete באמת |
| **rt59** | `state_backup` — ההבטחה שכתיבה לא מוחקת, כולל תמונת הרקע |
| **rt60** | `auto_backup` — שמות, ניקוי, קובץ אמיתי לדיסק, טיימר אמיתי |
| **t45** | `sketchesDone(0)` / `applyDone(0)` — **מריץ אותן** ובודק שכלום לא נמחק |

`run_all.sh` מעודכן: מעתיק גם מ-`landscape/`, ומכיר את rt56–rt60 ו-t45.

בענן נכשלות תמיד **rt7, rt18, rt26, rt43** — חסרים שם `door_tool.rb`,
`textures/`, `icons/`. **סביבה, לא קוד.**

---

## 6. הצעד הבא — גדרות (זה מה שהוא ביקש להמשיך)

### שתי החלטות פתוחות שהוא עוד לא בחר
1. **ברירת מחדל במדרון: `rake` או `step`?**
   `rake` = הגדר נוטה עם הקרקע (גדר עץ). `step` = כל פאנל ישר ויורד
   במדרגה (גדר בלוקים/פאנלים מוכנים). שניהם בנויים ובדוקים.
   על קרקע שטוחה שניהם נותנים מספרים זהים, ויש בדיקה שנופלת אם זה נשבר.
2. **סוגי גדרות** — זה מה שהוא רוצה עכשיו. "סוג" הוא רק חבילת מספרים
   (גובה, מרווח עמודים, רוחב קרש, מרווח בין קרשים, עובי, חומר, צבע)
   שנכנסת לאותו מנוע — בדיוק כמו ספריית הקירות.

### מה יידרש לסוגים
- `landscape/fence_library.rb` + דיאלוג HTML (במקום ה-`UI.inputbox`)
- סוגים סבירים להתחיל מהם: Wood Privacy, Picket, Ranch Rail, Vinyl,
  Chain Link, Wrought Iron, חומה (בלוק/טיח)
- חומה אמיתית תצטרך גוף רציף במקום קרשים — **תשאל אותו לפני שאתה בונה
  את זה**, זה לא אותו מנוע.

### אחרי הגדרות, הסדר שהוא הציב לחוץ
פטיו → תאורה וצמחייה (סמלים) → BBQ → בריכה → **ואז טופוגרפיה**, שתרים
את כולם. הוא מודע לכך שטופוגרפיה שוברת את ההנחה ש-Z קבוע, ובחר במפורש
לדחות אותה לסוף.

---

## 7. עוד פתוח מקודם (לא נגעתי היום)

- **הטייטל-בלוק האמיתי שלו** — חסום עד שהוא שולח תמונה.
- **עוד יצרני שכבות** ל-2D: אריחים, חשמל, ריהוט, צמחייה.
  בדקתי את המבנה: שכבה = שם ב-`PlanCanvas::LAYERS` + מתודת `draw_*` ב-
  `plan_generator.rb`. הצ'קבוקס בחלון נבנה **לבד** מרשימת השכבות.
  לאריחים כבר יש הכל — `floor_pattern.rb` מחשב את הדפוסים ב-3D.
- **מנוע LayOut** — הצינור הוכח ב-`debug_layout_spike.rb`, לא חובר.
- **אליוויישנים.**
- **באג פתוח:** `[Sheet] add_selection: no implicit conversion to
  Transformation` — עדיין בלי stack trace.
- **שני קירות פנים עקומים במודל הישן** (0.2337° ו-0.0958°,
  `floor_report.txt`). הוא בחר לא לתקן ולבנות מבנה חדש. הלחצן הימני
  "זווית מול הציר האדום…" הוא הכלי לזה אם יחזור.
