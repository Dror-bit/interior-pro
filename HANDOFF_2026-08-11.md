# מסירה לצ'אט הבא — Interior Pro, סבב 2026-08-11

## 0. לפני הכל
התיקייה החיה (PC):
`C:\Users\rordt\AppData\Roaming\SketchUp\SketchUp 2024\SketchUp\Plugins\interior_pro`

סדר קריאה: **הקובץ הזה** ← `CLAUDE.md` (עודכן היום) ← `HANDOFF_2026-08-10B.md`.

### בדיקות
```
cd tests
"C:\Program Files\Git\bin\bash.exe" run_all.sh ..
```
אין Ruby במחשב של המשתמש — ההרצה האמיתית אצל העוזר בענן.
מצב נוכחי: **30 JS + 27 רובי, ALL GREEN** (rt19–rt27 חדשים מהסבב הזה).

### כללי עבודה (המשתמש חזר עליהם שוב היום)
1. עברית, משפט-שניים, כמו לילד בן חמש. **לא להאריך** — זה עצבן אותו היום.
2. כל פקודה בקופסה נפרדת עם ציון לאן (Ruby Console / CMD).
3. העוזר עורך קבצים בעצמו. לא Cursor.
4. העוזר לא מריץ git — נותן פקודות CMD. פקודה אחת לשורה (נדבקו לו פעמיים).
5. לאמת מול הקוד החי. commit אחרי כל צעד עובד.

---

## 1. מה נבנה בסבב (גדול!)

### קירות מעוגלים — עובד מקצה לקצה
- `arc_math.rb` (חדש) — מתמטיקת קשת טהורה, בלי SketchUp API. rt19.
- קיר = attribute יחיד `arc_sag` (בוהן אמצע, חתום, שמאל=חיובי). בלי sag —
  הקוד הישן בדיוק. `USE_CURVED_WALLS` דגל כיבוי.
- `set_wall_sag!` = נקודת הכניסה היחידה. מאפסת פינות ומריצה join_corners.
- `wall_curve_tool.rb` (חדש) — גרירת אמצע + הקלדה. גם בקליק ימני על קיר.
- `wall_arc_tool.rb` (חדש) — קשת ב-3 קליקים, יורש מ-WallTool, בונה דרך
  create_wall (rt23 שומר שלא יצמח לו בילדר משלו).
- פינות: קיר מעוגל מדווח את ה-tangent שלו בקצה (`corner_direction_xy`),
  קצוות נחתכים רדיאלית, מיטר עובד. פינה כמעט-ישרה (קשת שממשיכה קיר באותו
  כיוון) מזוהה ומרובעת במקום טריז מטורף. rt20+rt22.
- תפרים בין הפאות מוסתרים (`smooth_curved_wall_edges!`) — הקיר נראה חלק.
- **דלתות וחלונות על קיר מעוגל עובדים**: כל פתח מקבל "כיס שטוח" במתאר
  (rt24), הדלת נמדדת לאורך הקשת ומיושרת לפאנל (`geo_at` ב-door_manager,
  rt25). חלונות רשומים כ-openings רגילים אז גם הם עובדים.

### סיידינג 3D — עובד ולא מפיל את סקצ'אפ
- `Horizontal Siding` = לוחות לאפ אמיתיים (חשיפה 6", טייפר 0.75→0.125).
- `Board and Batten` = הרצועות הקיימות, עכשיו גם על קיר מעוגל, נעצרות
  בפתחים (rt26 batten_z_bands).
- לוחות נעצרים משני צדי כל פתח (clear_t_intervals) ובמסגרות הפינה.
- מסגרת פינה: לוח אנכי 3" בכל קצה קיר, `SIDING_TRIM_WIDTH/DEPTH`.
- הכל לבן כברירת מחדל.
- **תיקון הקריסה (חשוב!)**: כל הסיידינג נבנה בקבוצת משנה
  `InteriorPro_Siding` בתוך הקיר — אסור לצייר לוח ישירות בקבוצת הקיר,
  הגב שלו חותך את פאת הקיר ומאות חיתוכים מפילים את סקצ'אפ. בנוסף:
  צביעה אחת על הקבוצה, tolerance גס לעיקול (`SIDING_CURVE_TOL=1.5`),
  תקרת חתיכות `MAX_SIDING_PIECES=260`, ודגל `USE_3D_SIDING`.
- rt27 (104 בדיקות) שומר על כל זה.

### דיאלוגים
- כל דיאלוגי הקיר/חלון נפתחים בגודל מלא: בלי preferences_key (הוא זכר
  גודל קטן לנצח), min/max, ו-set_size לפני **ואחרי** show
  (`force_dialog_size`). גלילה אנכית, בלי אופקית.

### באגים שתוקנו בדרך
- rebuild של קיר עם פתחים מחק את הרצועות (קיר שחור) — הוחזר.
- דלתות עפו לאמצע החדר על קיר מעוגל — t נמדד עכשיו לאורך הקשת.
- `CLAUDE.md` שוכתב — אין יותר "דלתות בלבד".

---

## 2. הבא בתור — מה שהמשתמש ביקש בסוף הסבב (מסגרות פינה)

1. **אופציה להוריד את מסגרת הפינה** ו**לקבוע את הרוחב שלה** (2"/3"/4"...).
   כנראה בדיאלוג עריכת הקיר, לפי כלל ה-UI: מעט כפתורים. הרוחב היום קבוע
   ב-`SIDING_TRIM_WIDTH = 3.0`. צריך attribute פר-קיר (או גלובלי?) —
   לשאול שאלה קצרה אחת.
2. **הפינה צריכה להיות L אחד ב-90 מעלות, לא שני קרשים נפרדים.** היום כל
   קיר בונה לוח שטוח משלו בקצה, ובפינה רואים שני לוחות עם תפר/רווח
   (יש צילום מהמשתמש). צריך פרופיל L אחד: שתי כנפיים 3"x‏3" (או הרוחב
   שנבחר) סביב פינת הקיר, בעובי `SIDING_TRIM_DEPTH`. כנראה: לזהות אם יש
   שכן בפינה (join_corners כבר יודע), ולבנות את ה-L פעם אחת.

## 3. פתוח מסבבים קודמים (לא נגענו היום)
- הפקת תוכניות 2D ל-PDF (עדיפות 2 המקורית) — עדיין לא התחיל.
- קיר מעוגל בתוכנית 2D (`plan_generator`) ובעורך 2D — עדיין קו ישר.
- גג מעל קיר מעוגל — `eave_polygon` עדיין רואה קו ישר.
- butt joint (קיר פנים לחוץ) עדיין לפי הקו הישר, לא tangent.
- גרירת פינות תמונת הרקע בעורך 2D; להב הפשיה; המשך גגות לפי ROOFS_ROADMAP.

## 4. git
branch: `door-stabilize`. הסבב הזה נדחף בסדרת קומיטים (arc math → curved
walls → corners → doors → windows → siding → crash fix). לוודא שהקומיט
האחרון ("siding: build into its own sub-group") נדחף.

## 5. דגלי חירום
```ruby
InteriorPro::WallTool::USE_CURVED_WALLS = false   # קירות חוזרים ישרים
InteriorPro::WallTool::USE_3D_SIDING = false      # סיידינג חוזר שטוח
```
(שניהם דורשים remove_const קודם, ראה CLAUDE.md.)
