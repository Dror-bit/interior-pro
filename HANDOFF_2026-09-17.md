# Interior Pro — מסירה 2026-09-17 (וואלי: לחתוך את גג הגראז' במפגש)

קרא את זה קודם, ואז `CLAUDE.md`. הקודמת: `HANDOFF_2026-09-16.md`.
**אל תסמוך על הסיכום — תאמת מול הקוד החי.**

בדיקות: `cd tests` ואז `LANG=C.UTF-8 bash run_all.sh ..` — **198 ירוקות**
(rt51/rt52/rt60 אדומות ישנות). חדשה: **rt152**.

## דגל

`InteriorPro::RoofManager::USE_ROOF_VALLEY` — **false בקובץ בכוונה**.
הדלקה + בנייה מחדש (קופסה אחת ל-Ruby Console):
```
InteriorPro.reload!; InteriorPro::RoofManager.send(:remove_const, :USE_ROOF_VALLEY); InteriorPro::RoofManager.const_set(:USE_ROOF_VALLEY, true); InteriorPro::RoofManager.roofs.each { |r| InteriorPro::RoofManager.build_roof!(replace: r) }; puts "valley ON"
```

## הכלל שהוא קבע היום (זה מה שבונים, לא טלאים)

> "החיתוך של הגגות צריך להיות במפגש. גג הגראז' נכנס לתוך הבית — צריך
> שהחיתוך יהיה בקו המפגש (הוואלי), וגם הפאשיות — שלא יכנסו אחד בשני,
> גם במאונך וגם במאוזן. זה יפתור את רוב הבעיות."

במספרים של המודל שלו (valley_report.txt), **הכלל האחד**:
גג הגראז' קיים רק איפה שהוא **מעל מישור גג הבית**, או **מדרום לקו הקיר
של הבית (y ≤ 0)**. כל השאר — בפנים, נמחק.

מה זה אומר בפועל (תמונה: הוא שלח תוכנית עם צהוב=מפגש, ירוק=חיתוך היום):
1. **שני טרפזים בעליית הגג נמחקים**: מקו הקיר y=0 עד קצה הגראז' y=17,
   מחוץ לרגלי הוואלי. מערב: x מ-‎-606.51 (פני הקיר) עד רגל הוואלי
   (x=-534.51 ב-y=0, x=-517.51 ב-y=17). מזרח: מראה, סביב הרכס
   x=-459.975 (‎-385.44 ב-y=0, ‎-402.44 ב-y=17, עד ‎-313.44).
   קו רגל מערב: `y = x + 534.51`. היום הקוד משאיר שם מלבן מלא עד y=17
   ("נכנס לתוך הבית").
2. **מדרום לקו הקיר (y ‎-12..0) הגג ממשיך מתחת לאיב של הבית** ונגמר
   בקו הקיר — זה מה שתיקנו היום ברצועה שמעבר לקיר (x < ‎-606.51).
3. **הפאשיה של הבית נחתכת על מישור גג הגראז'** — 32" בכל צד
   (מערב x ‎-578.51..‎-546.51, מזרח מראה): שם הגג עובר היום דרך הפאשיה
   (99–107). "נפגשים, לא נכנסים". (= סעיף 2 מהמסירה הקודמת.)
   40 האינץ' שממערב לזה (x ‎-618.51..‎-578.51) הפאשיה נשארת שלמה, הגג
   מחליק מתחת לסופיט (99) עם עד 10" אוויר — זו הגיאומטריה האמיתית.

**איך לבנות**: לא לגעת בבונה (התוכנית לא decomposable — "strip-gable
fallback"). לחתוך אחרי הבנייה עם המכונה הקיימת שכבר עובדת:
`valley_cut_deck!` (קליפות מקבילות) + `valley_low_strip_sweep!`
(פאות אנכיות, לפצל על כל צלע של הפוליגון — היום הוא יודע רק שני קווים:
קיר + פנימי; להכליל) + `valley_cull_pieces!` (רעפים). הטרפזים = פוליגונים
חדשים על מישורי הגראז', בדיוק כמו ה-`poly` של `valley_low_deck_cuts`.

## מה נבנה היום (הכל ב-roof_manager.rb, מאחורי USE_ROOF_VALLEY)

- **`cap_corner_pull_back`** (טהור, rt152): כיסוי הקצה של האיב
  (`build_end_caps!`) עמד בפינת הפוליגון (y=17) בעוד הפאשיה נגמרת בקו
  הקיר (y=0) — לוח 12x11 באוויר. עכשיו `build_end_caps!` מקבל
  `pull_back:` (= `lspans`) ומזיז את הפינה לקצה הפנימי של הספאן.
  head/tail נקראים מהפינה המקורית c0.
- **`valley_low_strip_sweep!`**: הרצועה היא קופסה, לא מישור. חותך פאות
  אנכיות ברצועה (קצה הדק 1/2", קצה הצפוני של הדק) על קו הקיר ועל הקו
  הפנימי, ומוחק את מה שמרכזו ברצועה מעבר לקו הקיר. **נקודת חיתוך = גם
  קודקוד שכבר יושב על הקו** (החיתוך הקודם השאיר קודקודים בדיוק על y=0
  ובלעדיהם שום דבר לא התפצל — valley7_report.txt).
  `valley_low_deck_cuts` מוסיף ל-hash: `wall`, `along`, `inner`, `inw`.
- rt152 — טהור + ה-hash. run_all.sh כולל אותו.

## מלכודות מהיום (אל תחזור עליהן)

1. **ניסיתי למחוק את הפאשיה של הבית מעל כל הגראז'** (`v[:over]` במקום
   `v[:span]` ב-gable_spans) — הוא לא רצה. הוחזר. **הפאשיה של הבית
   נשארת**, רק נחתכת על מישור הגראז' (סעיף 3 למעלה).
2. **זיהוי מתמונה נכשל 3 פעמים**. מה שעובד: `debug_pick.rb` — הוא שם את
   החלק במרכז המסך, הסקריפט דוגם רשת ±160px ומדפיס כל פאה (בלי רעפים)
   עם הגג שלה והמידות → `pick_report.txt`. **לא לנגוע בקוד לפני
   שהדוח הזה אומר איזו פאה בדיוק, ולפני "כן" שלו על החלק המדויק.**
3. `PickHelper#point_at` ו-`picked_point` לא קיימים ב-SU2024. `leaf_at`,
   `path_at` — כן.
4. `def dump(L, ...)` — L באות גדולה = קבוע, SyntaxError.
5. הרגל "כן" שלו על ניסוח שלי ≠ הבנה. לשאול על החלק, לא על הפעולה.

## קבצי מדידה מהסבב (בשורש)
`debug_pick.rb` (מה במרכז המסך) · `debug_valley5.rb` (אזור פינת הבית)
· `debug_valley6.rb` · `debug_valley7.rb` (כל פאה בכל קבוצה ברצועה
המערבית) + הדוחות `pick_report.txt`, `valley5..7_report.txt`.

## מה נשאר (לפי הסדר)

1. **החיתוך במפגש** — הכלל למעלה, סעיפים 1+3. זה הצעד הבא, ורק הוא.
2. החור המשולש ליד קיר הבית (מהמסירה הקודמת).
3. רידג' קאפ לגראז' עד הקודקוד.
4. הדאון-ספאוט של הגראז' ליד הקיר — אינץ'-שניים דרומה.

## לשימוש
תג: `before-valley` (git). גיבוי: `C:\Users\rordt\interior_pro_backup_2026-09-03.zip`.
