# Interior Pro — מסירה 2026-09-01 (Shed: פינות, פשיה עליונה, החוק)

קרא את זה קודם, ואז `CLAUDE.md`. הקודמת: `HANDOFF_2026-08-31.md`.
**אל תסמוך על הסיכום — תאמת מול הקוד החי.**

ענף `door-stabilize`. בתחילת הסבב הכל היה בקומיט (המסירה הקודמת טעתה
כשכתבה "כלום לא בקומיט"). כל העבודה של היום ב-`roof_manager.rb`,
`tests/rt92-rt94`, `tests/run_all.sh`, `CLAUDE.md`.

בדיקות: `cd tests` ואז `LANG=C.UTF-8 bash run_all.sh ..` → **144 ירוקות**
(היו 142; rt93 ו-rt94 חדשות, rt92 עודכנה בכוונה — ראה למטה).
**תמיד run_all.sh.**

---

## ⚖️ החוק החדש של דרור — נכנס ל-CLAUDE.md, אל תפר אותו

*"תרשום לעצמך חוק שהם חייבים להיפגש ולא להיכנס אחד לתוך השני."*

**קרשים נפגשים.** כל שני חלקי טרים (פשיה, דריפ, סופיט, רייק, דק) —
בדיוק אחד מהם הבעלים של בלוק הפינה המשותף, והשני **נחתך** להיגמר
פלוש על הפנים שלו. שני מוצקים באותו נפח = תפרים על הפנים, והוא רואה
את זה מיד. העוזר: `RoofManager.rake_meet_span` (ריווח לרייק מול בנד).
הוא תפס אותי על זה שלוש פעמים ביום אחד. לפני כל חתיכת טרים חדשה —
תשאל: לתוך מה כל קצה שלה נכנס, והאם הוא נחתך להיגמר בדיוק שם.

---

## מה נבנה היום (הכל אושר על ידו, "מעולה")

### 1. קירות מורמים של Shed — פינות ממוטרות + שיפוע
- `build_gable_top_mitred!` + `add_mitred_wall_solid!` + `add_top_strip!`
  ב-roof_manager.rb: קיר מורם נבנה פנים-פנים (לא pushpull). הפנים
  החיצוניות רצות עד הפינה, הפנימיות נעצרות עובי-שכן קודם, החתך
  באלכסון — שני קירות של פינה חותכים על **אותו** מישור אלכסוני.
- הראש נדגם על הגג **פעמיים** — קו חיצוני וקו פנימי
  (`wall_line_profile` קיבל `inboard:`). קיר שניצב לשיפוע (הקיר הגבוה)
  יוצא מושפע ומתחבא מתחת לדק. רייק נשאר ישר לרוחבו. `tests/rt93`.
- **צבע לפי זהות, לא לפי נורמל**: `paint_mitred_wall!` — הבונה אומר
  איזו פנים פנימית (לבן), כל השאר חיצוני (טקסטורה). הנורמלים של
  add_face תלויים בסדר הנקודות ובגללם הצבע יצא הפוך בהתחלה.
- `soften_coplanar_edges!` מרכך תפרים בתוך מוצק אחד.
- rt92 עודכנה **בכוונה**: היא הצמידה את חוק ה-butt הישן (קיר מוותר
  על קצה START). הוחלף במיטר לבקשתו המפורשת. אל תחזיר את ה-butt.

### 2. הקצה העליון של Shed — לבוש איב מלא בגובה שלו
- `level_gable_edges` (היה כתוב ולא בשימוש) עכשיו חי:
  `lvl_edges` יוצאות מ-`rakes`, נשארות ב-`gables`.
- `level_edge_band_top` — האנלוג של band_top לקצה העליון: תחתית הגג
  על הקו של הקצה עצמו.
- פשיה + דריפ בגובה שלו, פינות **מרובעות** (square_flags) שרצות עד
  הפנים החיצוניות של לוח/דריפ הרייק — לא 45.
- **הפשיה העליונה עמוקה רבע אינץ' יותר** מהאיב (`- 0.25`) — פלוש עם
  תחתית הרייק בפינה. זו בקשה מפורשת שלו, אל "תתקן" את זה.
- סופיט עליון נבנה בתוך בלוק הסופיט (נצבע ומתרכך עם כולם), ועם
  soffit_slope הוא **משופע במראה** — rise שלילי (ההיפך מהאיב, כי
  פנימה מהקצה העליון זה במורד). `rise2 = -soffit_rise(...)`.
- בלי מרזב למעלה — המים זורמים לצד הנמוך.
- לוח הרייק ותאום-הסופיט שלו **נחתכים** (reach דרך `rake_meet_span`)
  להיגמר על הפשיה/הסופיט העליונים. החוק.

### 3. מטל-אדג' עוטף כל פינה, בכל הגגות
- הדריפ הראשי של האיבים מקבל עכשיו square_flags + square_k
  (`d_in + DRIP_THICK`) — נגמר מרובע על הדריפ של הרייק. תופס גם את
  שתי הפינות הנמוכות של Shed וגם 4 פינות של גייבל. היפ/פלאט — אין
  איבי גייבל, כלום לא זז שם.

### 4. הדק נגמר בסוף
- רצועת דק לאורך כל רייק (Shed בלבד — `lvl_edges` ריק בכל סגנון
  אחר): `build_rake_board!` עם zmap מורם ב-dz, k 0..(פשיה+דריפ),
  צבוע כגג. הדק מכסה את הטרים של הרייק עד הפינה, כמו שסידר ביד.

`tests/rt94` מצמידה את 2–4. נבדקה שהיא נופלת מול הקוד הישן.

---

## מוקשים שעדיין בתוקף

- `roof_manager.rb` הוא **CRLF**. tests/rt*.rb, run_all.sh, CLAUDE.md
  הם **LF**. עריכה שהופכת סופי שורות = דיף ענק.
- שורת קונסולה עם `build_roof!` בלי `replace:`/`level:` **מוחקת את כל
  הגגות**. תמיד:
  `RoofManager.roofs.each { |r| RoofManager.build_roof!(replace: r) }`
- הסטאב בענן לא בונה רכס-קאפ/רעפים/חומרים אמיתיים — צבעים אפשר לבדוק
  **רק אצלו**. לפני "בדקתי בענן" תוודא שהדבר בכלל נבנה בסטאב.
- `X = {...} unless const_defined?` לא נטען מחדש ב-reload!.
- כפתור סרגל חדש → ריסטרט סקצ'אפ. HTML דיאלוג → לסגור ולפתוח.
- `debug_shed_corner.rb` חדש בשורש (מוחרג ע"י debug_*.rb) — פרוב
  קריא-בלבד: קירות, זוויות פינה, ובדיקת חפיפה בין קירות מורמים.
  הפלט `shed_corner_report.txt`. `*_report.txt` עדיין לא ב-gitignore.

## מה עוד פתוח

- הצעד הבא — הוא יגיד.
- rt94 מכסה Shed; לא נכתבה בדיקה לעטיפת הדריפ על גייבל רגיל (אותו
  קוד path, אבל אם נוגעים שם — להוסיף).
- אם יצוץ עוד קרש-בתוך-קרש (למשל סופיט-רייק מול סופיט-איב בפינות
  הנמוכות של גייבל) — לטפל עם אותו `rake_meet_span`, לא בטלאי מקומי.

## איך לעבוד עם דרור — מה שהיום לימד

- עברית פשוטה, משפט-שניים. פקודות בקופסאות עם לאן. git רק דרך CMD שלו.
- **הוא מראה, אתה בונה.** פעמיים היום הוא סידר ביד בסקצ'אפ ואמר
  "ככה" — זו הדרך הכי מהירה להבין אותו. תסתכל טוב על התמונות שלו.
- שרטוט לפני בנייה עבד מצוין (חתך הפשיה). ASCII בצ'אט הוא לא הבין —
  תמונה מרונדרת כן.
- כשלא בטוחים — קובץ מדידה שהוא מריץ בקונסולה. המספרים של
  debug_shed_corner סגרו את חקירת "הקירות הכפולים" בסבב אחד
  (החשד: שאריות ידניות; המספרים הראו שהקוד נקי).
- commit אחרי כל סבב שעובד.
