# Interior Pro — מסירה 2026-08-27 (רבע האינץ' בפינת הרייק)

קרא את זה קודם, ואז `CLAUDE.md`. הקודמת: `HANDOFF_2026-08-26.md`.
**אל תסמוך על הסיכום — תאמת מול הקוד החי.**

בדיקות: בענן בלבד (למשתמש אין רובי): `cd tests` ואז
`LANG=C.UTF-8 bash run_all.sh ..` → **135 ירוקות**.
להעתיק לענן גם `icons/` ו-`textures/` (בלי `textures/floors/`).
`run_all.sh` מעתיק את קבצי המקור לתוך `tests/` — **תמיד run_all.sh**,
אף פעם לא `ruby rt84.rb` לבד, זה רץ על עותק ישן ומשקר.
(נכוויתי מזה היום: תיקנתי את `roof_manager.rb` בשורש, מדדתי, ולא ראיתי
שום שינוי — כי `tests/roof_manager.rb` עוד היה הישן.)

git: ענף `door-stabilize`, קומיט `be9fd57`.
**צעד ראשון חובה: `git status` דרך CMD שלו** — אל תריץ git בעצמך,
אתה תוקע `index.lock`.

---

## מה נסגר היום ואושר על ידו

### הפרש הגובה בפינת הרייק — סגור

זה היה הדבר הפתוח מ-26.08. **סופר-חשוב איך זה נפתר: מדידה, לא ניחוש.**

**מה נמדד** (גג 4:12, איב 18", פאשיה 8", `soffit_slope` דלוק), בקו התפר
שבין הלוח הישר ללוח הרייק:

| לוח | תחתית | עליון |
|---|---|---|
| הסופיט הישר המשופע, בקצה הפנימי (בקיר) | 87.75 | 88.50 |
| סופיט הרייק, באותו מקום | **88.00** | **88.75** |

מדרגה של **0.25"** לאורך כל התפר, בכל ארבע הפינות. הוא אישר במפורש
שזאת המדרגה שהוא סימן באדום.

**מאיפה זה בא.** `soffit_rise` החזיר `slope * (k_out - k_in)` — הטיפוס
של הגג לרוחב הלוח **עצמו**. זה מה ש"מקביל לגג" אומר, וזה נכון בפני
עצמו — אבל הלוח נתלה מתחתית הפאשיה, שנמדדת על קו הפוליגון (k = 0),
בעוד שהלוח עצמו מתחיל עובי-פאשיה פנימה, ב-k_out. סופיט הרייק לא סובל
מההיסט הזה: הוא יושב עומק-פאשיה מתחת ללוח הגג **בקו הפוליגון**. ההפרש
בין השניים הוא בדיוק `slope * FASCIA_THICK` = 0.3333 × 0.75 = 0.25.

**התיקון — שורה אחת** ב-`soffit_rise` (`roof_manager.rb` ~4764):

```ruby
r = slope.to_f * (0.0 - band[:k_in].to_f)   # היה: (k_out - k_in)
```

עכשיו הלוח ננעץ **בשני הקצוות**: החיצוני על תחתית הפאשיה (לא זז,
הפאשיה והדריפ לא יודעים שקרה משהו), והפנימי על אותו קו שסופיט הרייק
כבר יושב עליו. אחרי התיקון שני הלוחות בדיוק על 88.00 / 88.75.

**המחיר, והוא אושר על ידו:** הלוח כבר לא מקביל לגג ב-100% — הוא תלול
ב-4% יותר (6.0 במקום 5.75 על רוחב של 17.25"). זה בערך 1/40 מעלה. הוא
אמר "נראה מספיק טוב".

**אימות** (`tests/check_seam.rb` בענן, לא בגיט): איב 12/18/24/36,
שיפוע 4/6/9/12, עם פאשיה ובלי. לפני התיקון נמדדו **ארבעה** גבהים
שונים בקו התפר בכל שילוב; אחרי — **שניים** בדיוק, תחתית ועליון של לוח
אחד. אין מדרגה בשום שילוב.

**בדיקות שהתעדכנו** (הכלל הישן היה נעול בהן):
- `tests/rt84.rb` — במקום "הלוח מעתיק את הגג אינץ' באינץ'" יש עכשיו
  שלוש: הלוח הישר **נפגש** עם סופיט הרייק; הקצה החיצוני לא זז; והכלל
  הישן קצר בדיוק בעובי פאשיה אחד.
- `tests/rt85.rb` — קצה האיב (`build_end_caps!`) עולה עם הלוח: 88.0
  במקום 87.625. הקוד שם לא נגעו בו, הוא קורא ל-`soffit_rise` ומקבל את
  המספר החדש לבד.

---

## ⚠️ מה פתוח — הדבר הבא

**גייבל קטן מול גייבל גדול: הגדול נשבר.** הוא הראה תמונה ב-26.08,
עוד לא נגענו. זה הדבר הבא לפי הסדר שלו.

---

## מוקשים שעדיין בתוקף

- `roof_manager.rb` הוא **CRLF**, `roof_dialog.rb` ו-`toolbar.rb` הם LF,
  וקבצי `tests/rt*.rb` הם **LF**. עריכה עם סקריפט שמחפש מחרוזות חייבת
  להתאים, אחרת העוגן לא יימצא. (היום זה עצר אותי פעם אחת על rt84.)
- `X = {...} unless const_defined?(:X)` **לא** נטען מחדש ב-`reload!`.
  לכן `roof_setting_keys`, `soffit_textures`, `beam_spec` הן **מתודות**.
- כל פאה עוברת `add_ring!` / rescue — raise אחד מפיל קצה שלם.
- הסטאב בענן זורק על נקודות כפולות כמו סקצ'אפ אמיתי. לא לרכך.
- שינוי HTML בדיאלוג → **לסגור ולפתוח את החלון**, לא רק `reload!`.
- כפתור חדש בסרגל → **הפעלה מחדש של סקצ'אפ**.
- **לסגור את פינות הגייבל עם `build_end_caps!` — נוסה ונכשל (26.08).**
  זה החזיר את הצורה שהורדנו, והוא אמר "רק הרסת". אל תחזור על זה.

## איך לעבוד עם דרור

- עברית פשוטה, **משפט-שניים, כמו לילד בן 5**. הוא ביקש את זה במפורש.
- כל פקודה בקופסה נפרדת עם לאן (Ruby Console / CMD).
- git רק דרך CMD שלו. הוא לא כותב קוד — אתה עורך, הוא מדביק ובודק.
- **תמדוד לפני שאתה משנה, ותסתכל על התוצאה בענן לפני שאתה מוסר לו.**
  היום זה עבד: מדידה אחת בסבב הראשון סגרה פינה שאתמול בזבזה שלושה
  סבבים על ניחושים.
- **שרטוט לפני בנייה.** שני שרטוטים היום (המדידה, ואז התיקון המוצע)
  קיבלו "יאללה לך על זה" מיד. הוא מאשר מהר כשהוא רואה תמונה.
  לציור בענן: SVG + `cairosvg`, ועברית חייבת לעבור `python-bidi`
  (`get_display`) לפני שהיא נכנסת ל-SVG, אחרת היא יוצאת הפוכה.
- צעד אחד, קטן. הוא אמר: "כשלוקח יותר מדי זמן זה לא יוצא טוב".
- אם לא ברור מה הוא רואה — לבקש תמונה מסומנת. הוא מצייר באדום.
- **commit אחרי כל סבב שעובד**, דרך CMD שלו.

## TODO

- גייבל קטן מול גייבל גדול — **הדבר הבא**.
- גאטרס — הוא הזכיר שירצה. הפאשיה כבר נשמרת פנויה בשבילם.
- מ-`CLAUDE.md`: קירות מעוגלים, תוכניות 2D ל-PDF, NOTE מצויר, צבע
  בגיליון, לוגו per-installation, corner boards.
