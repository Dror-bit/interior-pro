# מסירה לצ'אט הבא — Interior Pro, סבב 2026-08-12B (המשך של 2026-08-12)

## 0. לפני הכל
התיקייה החיה (PC):
`C:\Users\rordt\AppData\Roaming\SketchUp\SketchUp 2024\SketchUp\Plugins\interior_pro`

סדר קריאה: **הקובץ הזה** ← `CLAUDE.md` ← `HANDOFF_2026-08-12.md` (הבוקר).
לא לסמוך על סיכומים — לאמת מול הקוד החי.

### בדיקות
```
cd tests
"C:\Program Files\Git\bin\bash.exe" run_all.sh ..
```
אין Ruby אצל המשתמש — מריצים בענן. מצב: **32 JS + 36 רובי, ALL GREEN**
(חדשים: rt32–rt35; rt26 ו-t34 עודכנו; rt18 דורש textures/roof_shingle.jpg).

### כללי עבודה (בתוקף, נאכפו שוב היום)
1. עברית, משפט-שניים, כמו לילד בן חמש.
2. כל פקודה בקופסה נפרדת + לאן (Ruby Console / CMD).
3. העוזר עורך קבצים בעצמו ומעביר ב-device_commit. לא מריץ git — נותן
   פקודות CMD.
4. דבר ויזואלי = לא מנחשים: סימולציה בענן (matplotlib) או debug script
   שמדפיס מספרים. ככה נסגרו היום שן-הפינה ובאג הדלת (2.5").
5. "שום דבר לא השתנה" = הקוד לא נטען. `fix_corners_once.rb` קיים בדיוק
   בשביל זה; יש גם `debug_arc_corner.rb` חדש (מדפיס פינה-פינה).
6. בלי כפתורים חדשים כשאפשר אוטומטי — המשתמש דרש וצדק (ראה align).

## 1. מה נסגר בסבב הזה

### גג מעל קיר מעוגל — שלבים 1–2 מהתוכנית (rt32)
- `outer_offset` ב-roof_manager.rb: קיר מעוגל = קשת אב-מרכזית
  (חצי עובי + overhang), פינות בחיתוך סגור קו-מעגל/מעגל-מעגל
  (עזרים חדשים ב-arc_math.rb: line_circle, circle_circle, retrim,
  reverse, nearest_point, closest_point_on_line, signed_delta).
- ROOF_CURVE_TOL=1.5 (פאסטות גסות לגג). כל פאה נושאת wall_id של הקשת.
- בליטה גדולה שהמעגל בורח מעבר לפינה: הקיר הישר נשאר ישר, הקשת נמתחת
  לנקודה הקרובה (בלי להטות פשיה).
- flat מעל קיר מעוגל — המשתמש אישר בסקצ'אפ. hip עובד (ראה מניפה למטה).

### רידג'-קאפ על עיגול — רק בחיבורים אמיתיים (rt32)
`drop_facet_hips` + `facet_hip_points`: ה-skeleton מרים "מניפה" של
צלעות בין כל שתי פאסטות של אותה קשת — אלה לא פינות אמיתיות ולא מקבלות
קאפ. קאפ נשאר רק בין שני קירות שונים ועל רכסים. המשתמש: "ניראה פצצה".

### הבאג המרכזי: פינת קשת עם שן 5" — תוקן אוטומטית (rt35)
שורש: קשת ושכן שגופם בצדדים שונים של הקו ("ליינים" שונים) — weld לא
יכול לסגור, יוצא כתף/שן. המשתמש התעצבן בצדק: "למה צריך כפתור?!".
- `align_curve_lanes!` ב-wall_tool.rb: רץ אוטומטית בתוך `set_wall_sag!`
  (נקודת הכניסה היחידה של עיקום/קשת). מיישב קודם את הקשת, ואז שכן ישר
  שעדיין מתנגש (רק אם: לא מעוגל, לא center, בלי חלונות, אותה קטגוריה).
  אותו undo. קו-השרטוט לא זז — רק הגוף עובר צד.
- עזרים טהורים: `end_cuts_xy`, `corner_snaps?`, `wall_h_anchor`,
  `MIRROR_H`. הכל נבדק על 6 הקירות האמיתיים של המשתמש.
- כלי ידני נשאר לקצוות: קליק ימני ← `Wall Body to Other Side` (רב-בחירה,
  rt33, `swap_wall_side_multi!`). מסרב לקיר עם חלון. דורש ריסטרט לתפריט.

### דלת/חלון על קיר מעוגל ישבו 2.5" בתוך הבית — תוקן (rt34)
`DoorManager.geo_at` + הענף המעוגל ב-window_tool.rb שכחו את
center_offset של העוגן (מרכז הכיס יושב על קשת-השרטוט, לא על ציר הגוף).
rt25 בדק רק bottom-center ולכן לא ראה. rt34 מצמיד: רציפות ב-sag לכל
עוגן + הצמדה לפאנל על קשת 60".

### Ctrl+Z — פעולה אחת = צעד אחד
חלון היה 5 צעדים, דלת 2. כל תפעולי-ההמשך הפכו transparent
(start_operation ארג' 4): Window Data/Casing/To Component/גופים,
Door Data/גופים/Undo Failed, ו-molding refresh אחרי דלת
(`refresh!(transparent: true)`). stub עודכן בהתאם.

### עוד
- flip פנים/חוץ רב-בחירה: `flip_wall_faces_multi!` + תפריט עם מונה.
- דיאלוג חלונות צומצם — הכל בלי גלילה (rt26 עודכן: padding-bottom>=8).
- t34 תוקן — הטסט טעה, לא הקוד (אומת מול הרובי על אותה גאומטריה).
- `debug_arc_corner.rb` בשורש — מדפיס לכל פינת-קשת: עוגנים, חיתוכים,
  ענף שרץ, סטייה מהטבעי. להשתמש לפני כל ניחוש.

## 2. גיט
branch: `door-stabilize`. המשתמש התבקש להריץ commit בסוף הסבב
("auto-align", "one undo step", "curved eave"). לוודא push.

## 3. הצעד הבא (לפי הסדר של המשתמש)
1. **גג Hip מעל קיר מעוגל — לוודא יציבות/ביצועים** (שלב 3 של התוכנית
   מ-HANDOFF_2026-08-12 §2). ה-skeleton כבר אוכל את הפאסטות; לבדוק על
   הבית האמיתי שלו, במיוחד קשתות גדולות (sag>120) ושני מפלסים.
2. Gable על קיר מעוגל = סירוב מנומס (שלב 4) — עדיין לא ממומש.
3. אחרי שהגג נסגר: **תוכניות 2D ל-PDF** (עדיפות 2 של המשתמש).

## 4. פתוח / לא לאבד
- עורך 2D: התצוגה הכחולה (pending) של weld לא מכירה את היישור האוטומטי —
  זניח כי היישור קורה ב-Apply, אבל אם יצוץ פער 2D/3D להתחיל שם
  (weldPendingEnds ב-plan_editor.rb, t34).
- קשת פנימה: פינת גג בולטת ~35" (מיטר מתמטי). המשתמש אמר שקירות
  מתעגלים תמיד החוצה — לא לתקן אלא אם יבקש.
- מסגרות פינה בסיידינג — "למקסם" (L אחד, סביב פתחים, קאפ עליון).
- זווית בין קירות בעורך 2D — שאלה פתוחה למשתמש (מי זז).
- קיר מעוגל ב-plan_generator (תוכנית 2D) — עדיין כורדה.
- butt joint פנים-אל-קשת — עדיין כורדה.

## 5. דגלי חירום
```ruby
InteriorPro::WallTool::USE_CURVED_WALLS = false
InteriorPro::WallTool::USE_NATIVE_OPENINGS = false
```
