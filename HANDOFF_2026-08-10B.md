# מסירה לצ'אט הבא — Interior Pro, סבב 2026-08-10 (ערב)

## 0. לפני הכל
התיקייה החיה (PC):
`C:\Users\rordt\AppData\Roaming\SketchUp\SketchUp 2024\SketchUp\Plugins\interior_pro`

סדר קריאה: **הקובץ הזה** ← `ROOFS_ROADMAP.md` ← `HANDOFF_2026-08-10.md`
← `HANDOFF_2026-08-08.md` ← `TARGET_PLANS.md` ← `STATUS_handoff.md`.

### בדיקות
```
cd tests
"C:\Program Files\Git\bin\bash.exe" run_all.sh ..
```
אין Ruby במחשב של המשתמש — חבילות ה-rt נותנות FAIL כוזב. ההרצה האמיתית
אצל העוזר בענן. מצב נוכחי: **30 JS + 18 רובי, ALL GREEN**
(`rt18` חדש, 78 בדיקות).

### כללי עבודה (המשתמש חזר עליהם בסבב הזה)
1. תשובות בעברית, **משפט-שניים**, בפשטות. לא להאריך.
2. כל פקודה בקופסה נפרדת עם ציון לאן להדביק (Ruby Console / CMD).
3. **המשתמש כבר לא עובד עם Cursor.** העוזר עורך את הקבצים בעצמו.
4. העוזר לא יכול להריץ git — נותן פקודות ל-CMD.
5. לא לנחש: לאמת מול הקוד החי ומול הצילומים לפני כל תיקון.

---

## 1. מה נעשה בסבב

### א. טקסטורת שינגלס (`roof_manager.rb`, `roof_dialog.rb`, `textures/`)
- `textures/roof_shingle.jpg` — אריח מרוצף שנוצר פרוצדורלית, מייצג
  48"x24" אמיתיים (4 שורות של 6" חשיפה, טאבים של 12"). אפור פחם.
- `ROOF_TEXTURES` — משפחות חומר. `surface_material` מחזיר חומר עם
  טקסטורה בגודל אמיתי, **צבוע** ב-roof_color, אז קובץ אחד = כל הצבעים.
- קובץ חסר או משפחה לא מוכרת → נפילה חזרה לצבע אחיד, הגג תמיד נבנה.
- בדיאלוג: `Surface → Roof material` (Solid color / Shingles).

### ב. עובי ללוח הגג
- `DEFAULT_ROOF_THICKNESS = 0.5`. `slab_lift(t, slope) = t*sqrt(1+slope²)`
  — הרמה אנכית שנותנת עובי אמיתי **מאונך** למישור.
- העיבוי **כלפי מעלה בלבד**: הקליפה הקיימת נשארת התחתית, נבנית קליפה
  שנייה גבוהה יותר, ו-`build_slab_edge!` סוגר את המתאר.
  כך הפשיה, הרייק והקירות הגמלוניים לא זזים בכלל.
- זה גם פתר באג אמיתי: הקיר הגמלוני בקע דרך השינגלס, כי הוא הגיע בדיוק
  לאותו מישור. עכשיו הוא קבור מתחת ללוח.

### ג. רידג' קאפ תלת-ממדי — 4 סבבי תיקון מול צילומים
נבנה מאפס, וכל סבב תיקן דבר שהמשתמש ראה בצילום:
1. **היפים לא קיבלו קאפ.** הבדיקה הראשונה השוותה גובה מרכז פאה — יוצא
   על הגבול בדיוק במדרון שליד ההיפ. עכשיו `face_gradient` מחליט:
   הגג יורד לשני הצדדים = רכס, עולה = עמק (עמק לא מקבל קאפ).
2. **הקאפ היה שקוע חצי בגג.** עכשיו כל קצה יושב על המישור שמתחתיו.
3. **הצורה** — לא משולש חד אלא **קשת** (`RIDGE_CAP_CROWN`, פרבולה
   שמתאפסת בקצוות), בנויה מחתיכות נפרדות שרוכבות זו על זו.
4. **כיוון החפיפה היה הפוך.** מים זורמים למטה, אז העליון חייב לשבת על
   התחתון. עכשיו כל חתיכה מורמת בקצה ה**תחתון** שלה.
5. **חדירה של 0.1125"** בין חתיכות עוקבות (המשתמש מדד!). הסיבה: הרמתי
   בעובי אחד, אבל אז שתי התחתיות יורדות בקצב שונה. הנוסחה הנכונה:
   `cap_head_lift = THICK * LENGTH / EXPOSURE`. אז שתיהן יורדות באותו
   קצב והחפיפה מדויקת לחלוטין. **אל תשנה את זה בלי לחשב מחדש.**
6. **צומת Y** — קו אחד (הכי שטוח, כלומר הרכס) עובר מעל, השאר נעצרים
   `RIDGE_CAP_TUCK` לפניו **ויורדים בעובי אחד** כדי לעבור מתחתיו.
7. **תחתית ההיפ** — שם הגג מתכנס לנקודה, אז קאפ ברוחב מלא תלה באוויר.
   `RIDGE_CAP_END_TAPER` מצמצם את 9" האחרונים לחוד.
- תקרת ביצועים: `MAX_RIDGE_CAP_PIECES` — מעליה הכל נמשך כריצה אחת.

### ד. חלון הגג נפתח במסך מלא
`preferences_key` גרם ל-SketchUp לזכור מצב ממוקסם. עכשיו min/max בקונסטרוקטור
ו-`set_size(340, 640)` בכל פתיחה.

### ה. `ROOFS_ROADMAP.md` (חדש)
רשימת הגגות של המשתמש, מסודרת ומאומתת מול הקוד. **"גג מתחת" = גג מתכת.**

---

## 2. הבא בתור — לפי מה שהמשתמש אמר בסוף הסבב

שני אלה **דחופים לו יותר** מהמשך הגגות:

### עדיפות 1 — קירות מעוגלים (וגג שמתאים להם)
**מצב נוכחי: לא קיים בכלל.** קיר הוא start_x/y → end_x/y ותו לא.
המילה arc בקוד היא רק לפתחי קשת בדלתות.

מה שהמשתמש ביקש (נשאל ונענה במפורש): **גם** עריכת קיר קיים (למשוך את
האמצע כדי לעקם) **וגם** כלי קשת בשלושה קליקים.

מקומות שיצטרכו לדעת על עקומה:
- `rebuild_wall_native_geometry!` — היום בונה מקטע ישר אחד
  (`build_wall_with_openings_oriented`) ואז `apply_native_miter_corners!`.
- פתחים בקיר מעוגל (`read_door_openings` יושב על t לאורך קו ישר).
- `RoofManager.eave_polygon` + השלד הישר — מתאר הגג מניח מצולע ישר.
- `plan_generator.rb` — ציור הקיר בתוכנית.
- `room_manager` — זיהוי חדרים לפי מקטעים.

הצעה לסדר: (1) מתמטיקת קשת טהורה + בדיקות, (2) בניית גוף קיר מעוגל
מפוליליין עם דגל כיבוי, (3) גרירת אמצע, (4) כלי קשת, (5) פתחים על קשת,
(6) פיצול הקשת למקטעים במתאר הגג.

### עדיפות 2 — הפקת תוכניות 2D ל-PDF
**מצב נוכחי: הציור קיים, ההפקה לא.** `plan_generator.rb` כבר מצייר
קירות, סמלי דלתות (כנף/הזזה/אקורדיון/כיס/מוסך), חלונות, שרשראות מידות
ותוויות חדרים — כשכבות במודל.

מה שחסר: מסגרת דף, סקייל קבוע, טייטל בלוק, וייצוא לקובץ PDF.
המשתמש ביקש **קודם את תוכניות החוץ, בלי אליוויישנים בשלב הזה**.

בנוסף ביקש בתוכניות (בסוף הסבב, במפורש):
- **להוסיף תמונות** לדף.
- **מידות**.
- **לקבוע SCALE**.
- **טמפלאט** לדף.
- **לקבוע את גודל הדף** (Arch D, Arch E, A1... — לבחירה).

שים לב — תמונות + סקייל **כבר קיימים חלקית בעורך ה-2D**: יש underlay של
תמונת רקע עם כיול בשתי נקודות (`startCalib`/`calibClick`), אטימות,
סיבוב, ושליחה ל-3D. וגם שרשראות מידות קיימות ב-`plan_generator`.
לבדוק מה מזה חסר בצד ההפקה ולא לבנות פעמיים (כלל 1 של המשתמש).

`TARGET_PLANS.md` מחזיק את מפרט היעד המלא לדפים.

---

## 3. פתוח מסבבים קודמים

1. **גרירת פינות של תמונת הרקע** — המשתמש ביקש את זה בסבב הזה ו**זה לא
   נבנה**. היום התמונה נגררת ומסתובבת, והסקייל נקבע רק בכיול שתי נקודות.
   צריך ידיות בארבע הפינות. הוחלט: לשמור יחס (בלי מתיחה חופשית).
   הקוד הרלוונטי ב-`plan_editor.rb`: `underSizeWorld`, `hitUnderlay`,
   `dragUnder`, `drawUnderlay`.
2. **להב דק בחיבור הפשיה לקיר** — אובחן ולא תוקן: לוח הפשיה השטוח יושב
   0.75" **פנימה** מקו הגג, ולוח הרייק 0.75" **החוצה**. שני הלוחות
   במישורים שונים. ממתין לצילום קירוב מהמשתמש.
3. חתיכת הגג הגדול מאחורי הגג הקטן — ניקיון.
4. המשך הגגות לפי `ROOFS_ROADMAP.md`: שורת רעפים תחתונה על הפשיה,
   3 סוגי טיל, גג מתכת, גאטרס/דאונספוט, ספריית איבס, שד, הולנדי, דורמר.
5. `CLAUDE.md` עדיין פותח ב"דלתות בלבד" מפאזה ישנה — לשאול אם למחוק.
6. **רעש CRLF**: קבצי הדלתות מסומנים כ"שונו" בלי שינוי תוכן. אומת עם
   `git diff --ignore-cr-at-eol` — אפס. הוצע `.gitattributes`, לא בוצע.

---

## 4. git
branch: `door-stabilize`. הקומיט האחרון של הסבב:
`8f74423 roof: shingle texture, slab thickness, 3D ridge caps on ridges and hips`
(נדחף ל-origin).

## 5. קבצים שהשתנו בסבב
`roof_manager.rb`, `roof_dialog.rb`, `textures/roof_shingle.jpg` (חדש),
`tests/rt18.rb` (חדש), `tests/rt15.rb`, `tests/rt17.rb`,
`tests/sketchup_stub.rb`, `tests/run_all.sh`,
`ROOFS_ROADMAP.md` (חדש), והקובץ הזה.

הערה על `rt15`/`rt17`: הן סופרות פאות של קליפת הגג, אז הן בונות אותו
כיריעה — יש בראשן alias שמכריח `thickness: 0` ו-`ridge_cap: false`.
אם מוסיפים שם בדיקה חדשה שכן צריכה עובי, להעביר את הפרמטר במפורש.
