# מסירה לצ'אט הבא — Interior Pro, סבב 2026-08-12D

## 0. לפני הכל

התיקייה החיה (PC):
`C:\Users\rordt\AppData\Roaming\SketchUp\SketchUp 2024\SketchUp\Plugins\interior_pro`

סדר קריאה: **הקובץ הזה** ← `CLAUDE.md` ← `HANDOFF_2026-08-12C.md`.
לא לסמוך על סיכומים — לאמת מול הקוד החי.

### בדיקות
```
cd tests
"C:\Program Files\Git\bin\bash.exe" run_all.sh ..
```
אין Ruby אצל המשתמש — **מריצים בענן**. מצב נוכחי: **32 JS + 38 רובי, ALL GREEN**.
חדשים הסבב הזה: rt37, rt38. `run_all.sh` תוקן (העתיק חסר את
`window_library_dialog.rb`, ולכן rt26 נפל בענן).

### כללי עבודה — נאכפו שוב היום, כולל חדש

1. עברית, משפט-שניים, כמו לילד בן חמש.
2. כל פקודה בקופסה נפרדת + לאן (Ruby Console / CMD).
3. **חדש (2026-08-12D, המשתמש אכף את זה בחריפות): לא מחליטים כלום בלי
   אישור שלו. מביאים אפשרויות → הוא בוחר → רק אז נוגעים בקוד.** בסבב הזה
   העוזר ערך קבצים בלי לעצור לשאול, בהסתמך על CLAUDE.md ("העוזר עורך
   קבצים בעצמו"), והמשתמש התעצבן בצדק. הכלל הזה גובר.
4. העוזר לא מריץ git — נותן פקודות CMD.
5. **דבר ויזואלי = לא מנחשים. מודדים.** בסבב הזה זה סגר שני באגים: 47.840"
   של הדלת על הקשת, ו-6.000" של הדלת בקיר הישר.
6. **"כלום לא השתנה" = הקוד לא נטען.** תוכיח שנטען לפני שאתה מחפש באג.
7. **אל תבקש מהמשתמש לקרוא פלט ארוך.** מספר אחד קצר מהמודל שלו, ואז
   משחזרים בענן ומוצאים שם.
8. **סקריפט בדיקה לא מעתיק את הקוד שהוא בודק — הוא קורא לו.**

---

## 1. מה נסגר בסבב הזה

### א. דלת/חלון עוקבים אחרי הקיר (rt37) — נבדק ואושר בסקצ'אפ

**הבאג:** `set_wall_sag!` בנה מחדש את הקיר, אז **החור** עבר לקשת — אבל גוף
הדלת הוא קבוצה נפרדת ואף אחד לא הזיז אותו. נמדד: קיר 200" שהתעקם ב-48",
הדלת נשארה **47.840"** מאחור.
`door_regen!` לא היה עוזר: `opening_context` מעדיף את `face_x/face_y`
ששמורים על הדלת מהקיר הישר, אז הדלת הייתה חוזרת בדיוק לאותו מקום.

**הבאג השני, שהתגלה כשהמשתמש בדק:** `align_curve_lanes!` קורא ל-
`swap_wall_side!` על **קיר ישר שכן**, כדי שהפינה תיוולד נקי. זה מזיז את
גוף הקיר עובי שלם לצד השני — והדלת נשארה. נמדד **6.000"** על קיר בעובי 6".
זה באג ישן (מהבוקר של 12/08), לא נוצר בסבב הזה. קרה גם בקליק-ימני
"Wall Body to Other Side".

**התיקון — הזזה, לא בנייה מחדש.** הגוף כבר בצורה הנכונה (הוא יושב בכיס
שטוח בשני המצבים), אז הוא רק מסתובב לכיוון הפאנל החדש ומחליק למקום.
הידיות, הקייסינג והזכוכית לא נוגעים בהם בכלל.

- `DoorManager.opening_seat(geo, t, width)` — איפה הגוף יושב על צורת קיר
  נתונה. בדיוק הביטוי שכל שאר הקוד משתמש בו: `cline_start + unit*t + n*n_side`.
- `DoorManager.seat_transform(old, new)` — טהורה. מסתובבת סביב המושב הישן,
  ואז מחליקה. זה החישוב היחיד שיש.
- `DoorManager.reseat_hosted_openings!(wall, old_geo)` — עובר על כל דלת
  וחלון של הקיר. `old_geo` חייב להיקרא **לפני** השינוי.

**סדר הקריאות — קריטי, אל תשנה:**
- `set_wall_sag!` קורא reseat **מיד אחרי `rebuild_wall_geometry`, ולפני
  `align_curve_lanes!`**.
- `swap_wall_side!` קורא reseat בעצמו, עם old_geo משלו.
- ככה כל שינוי מיישב מיד אחרי עצמו, ושום דלת לא זזה פעמיים. rt37 נועל
  גם את הסדר הזה (הוא קורא את הקוד עצמו).

דגל כיבוי: `InteriorPro::WallTool::RESEAT_OPENINGS_ON_CURVE = false`.

### ב. ציור קיר בכל זווית (rt38) — נבדק ואושר בסקצ'אפ

**הבאג:** `detect_auto_snap` תמיד בחר את הציר הקרוב יותר, אדום או ירוק.
לא הייתה בכלל אפשרות שלישית, אז קיר בזווית היה בלתי אפשרי לצייר ביד.

**הכלל עכשיו** (`WallTool.direction_snap`, טהורה):

| הכיוון | מה קורה |
|---|---|
| עד `DRAW_SNAP_ORTHO_DEG` (8°) מאדום/ירוק | נצמד לציר |
| עד `DRAW_SNAP_DIAG_DEG` (5°) מ-45° | נצמד לאלכסון, קו סגול מקווקו |
| כל השאר | חופשי לגמרי |

אדום/ירוק נבדקים ראשונים ועם חלון רחב יותר, אז הם תמיד מנצחים.
`@locked_axis` מקבל ערך חדש `:diag` + `@locked_dir`.
Shift (`axis_lock`) **לא השתנה** — rt16 עדיין נועל אותו.
דגל כיבוי: `DRAW_SNAP_ORTHO_ONLY = true` מחזיר את ההתנהגות הישנה.

המשתמש: "זה ניראה מעולה, הוא נינעל על 45 אדום וירוק ואפשר לשחק איתו לאן
שרוצים."

### ג. הסטאב של הבדיקות שודרג

`geom_stub.rb` ידע רק להזיז, לא לסובב — אז אי אפשר היה לבדוק את סיבוב
הדלת. עכשיו `Geom::Transformation` הוא אפיני אמיתי (סיבוב סביב Z + הזזה)
עם `*`, `identity?`, `inverse`, ו-`Point3d#transform` שמפעיל את המטריצה.
`sketchup_stub.rb`: `Group#transform!` מרכיב באמת במקום להיות no-op.
**כל 36 החבילות הישנות נשארו ירוקות אחרי השדרוג.**

---

## 2. גיט

branch: `door-stabilize`.
המשתמש התבקש להריץ commit על:
`"openings follow their wall when it bends or its body swaps sides (rt37); draw walls at any angle (rt38)"`
**לוודא שזה רץ.** `debug_*.rb` ב-.gitignore.

---

## 3. ההחלטה הגדולה של הסבב — תוכנת התוכניות

### מה המשתמש אמר שהוא בונה (בלשונו)

> "אני רוצה לבנות תוכנה לתוכניות שאוכל להכניס מידות ממה שאני בונה, אוכל
> להסיר שכבות כמו חשמל TILE וכו', אוכל למסור את גודל הדלתות והחלונות,
> אוכל להראות בהמשך גם חיבור לתוכנה שבניתי לעסק שתראה את רשימות החומרים
> לכל חדר, אליוויישנים לכל חדר ולבחוץ, צמחים, רשימות הערות, פריסות של
> חומרים, חישוב כמויות בטבלאות ועוד. כל הדברים שניבנה בפלאגין, כולל עבודה
> עם רינדורים בעתיד, הם יצטרכו לרדת לרמת התוכנית בצורה יפה ומסודרת.
> בסופו של דבר זה הדובדבן בקצפת."

**התוכנית היא המוצר הסופי של כל מה שהתוסף יודע. לא פיצ'ר צדדי.**

### מה נבדק והוכח בסבב הזה

- `Sketchup.is_pro?` = **true**, גרסה **24.0.594**.
- **ה-API של לאיאוט יושב בתוך סקצ'אפ** — תוסף יכול לבנות מסמך לאיאוט
  ולייצא PDF **בלי להפעיל את התוכנה LayOut**.
- הוכח בפועל: `debug_layout_spike.rb` בשורש. הרצה אצל המשתמש החזירה
  `OK: PDF on your Desktop (114 KB). scale=true`.
  החתימות שאומתו בתיעוד: `Layout::Document.new(template_path)`,
  `Layout::SketchUpModel.new(skp_path, Geom::Bounds2d)`, `.perspective=`,
  `.view=`, `.scale=` (דורש אורתוגרפי), `.render`, `doc.export(pdf, opts)`,
  `doc.save`.

### ההחלטה שהמשתמש קיבל

**לא בונים "מייצא PDF". בונים מסמך שרטוט עם שכבה אמצעית.**

```
מסמך שרטוט:  דפים  →  שכבות  →  צורות
              (קו, פוליגון, מילוי, האטצ', טקסט, טבלה, תמונה)

יצרני שכבות → כותבים לתוכו:
   קירות/פתחים (plan_generator כבר מחשב את כל זה)
   אריחים, חשמל, ריהוט, צמחייה
   טבלאות: דלתות, חלונות, כמויות, רשימות חומרים
   שרטוטים מבחוץ — ספסל, מטבח חיצוני, ייבוא מתוכנת שרטוט אחרת
   בעתיד: רינדורים

מנועים → מציירים אותו:
   PDF     ← ראשון. העוזר בונה ובודק בענן ושולח דף מוכן לצ'אט
   LayOut  ← שני. הצינור כבר הוכח
   בעתיד: מסך, SVG, החוצה לתוכנה של העסק
```

**למה ככה ולא לאיאוט לבד** (השיקולים שהוצגו למשתמש והוא בחר):
- העוזר יכול לבנות ולבדוק PDF **בענן** — שולח דף מוכן במקום שהמשתמש
  יריץ בדיקות. בלאיאוט העוזר עובד עיוור.
- **לא דורש Pro** → התוסף מכיר לכל אחד. קריטי אם המשתמש ימכור אותו.
- שליטה מלאה בעובי קווים, פונט, מיקום.
- **שכבות וטבלאות מהיום הראשון** במקום טלאי אחר כך.
- לאיאוט **לא נזרק** — הוא מנוע שני על אותו מסמך.

**מה שהמשתמש אוהב בלאיאוט ולמה זה נשמר:** בניית טמפלייט. הוא יעצב
טייטל-בלוק וישלח תמונה; בשלב א' העוזר מצייר אותו בקוד, בשלב ב' דיאלוג
הגדרות, בשלב ג' טמפלייט/עורך דף.

**מה שמעצבן אותו בלאיאוט ולמה זה נעלם:** שליחת מידע וסינכרון (כאן כל
בנייה מרעננת הכל מהמודל); סקייל (מספר בקוד, הוא לא נוגע); עדכון תמונות
(נטענות מהדיסק בכל בנייה — תרנדר שוב בלומיון עם אותו שם והן שם).

### הצעד הבא — **לא התחיל, טעון אישור המשתמש**

הסדר שהוצע: (1) השכבה האמצעית, קטנה אבל נכונה, עם בדיקה בענן.
(2) דף אחד — תוכנית קומה — שיוצא מושלם ל-PDF. (3) יצרן שכבה אחד בכל פעם.

**עדיין לא סוכם עם המשתמש** (הוא נשאל ולא ענה — לשאול שוב):
- גודל דף: Arch D 24×36 / Arch E 36×48 / Tabloid 11×17 / A1.
- מה קורה כשהבית לא נכנס ב-1/4"=1'-0" — לרדת אוטומטית ל-3/16" ואז 1/8",
  או להישאר קבוע וליחתך.
- מה בדף הראשון: תוכנית בלבד, או גם טבלאות.
- מה בטייטל-בלוק: שם פרויקט/כתובת, לוגו Visualize by Dror, מספר+שם דף,
  תאריך+קנה מידה.

### מה כבר קיים ולא לבנות פעמיים

`plan_generator.rb` כבר בונה תוכנית שטוחה מלאה במודל (תגית IP/2D) + סצנה
"2D Plan" בהיטל מקבילי: poche לקירות לפי קטגוריה, סמלי דלתות (סיבוב,
הזזה, אקורדיון, כיס, מוסך), סמלי חלונות, תגי D101/W101/IN101, שרשראות
מידות, תוויות חדר + שטח, **טבלאות דלתות וחלונות**, מקרא וכותרת.
**זה בדיוק התוכן שנכנס לשכבות.** הוא היום פולט גיאומטריית סקצ'אפ; צריך
שיפלוט גם צורות למסמך השרטוט.

`plan_editor.rb` (404KB) — עורך ה-2D מהחוזה, קיים ועובד.

---

## 4. פתוח / לא לאבד

### מוקפא: מסגרות פינה בסיידינג — פינה כ-L אחד
התוכנית מוכנה **ונמדדה**, רק לא מומשה (המשתמש עצר כדי לעבור לתוכניות):
- שתי פאות החוץ של שני קירות בפינה נפגשות **בדיוק באותה נקודה** (0.0000")
  בכל בית שנבדק — כולל פינה פנימית ופינות בזווית.
- בפינת 90° ה-L האחד מכסה **בדיוק** את אותה צללית ששני הקרשים החופפים
  מכסים היום. אפס הבדל. השינוי הוא ניקוי גאומטרי בלבד.
- בזווית 45°/135° ה-L **קטן** מהיום ב-~4% — מוריד שן שהמסלול הנוכחי יוצר.
- בפינה **פנימית** הנוסחה לא מתאימה (חסר ריבוע הפינה) — **החלטת המשתמש:
  להישאר במסלול הישן שם.**
- **החלטת המשתמש:** גובה שונה בין שני הקירות = מסלול ישן.
- **החלטת המשתמש:** קודם פונקציה טהורה `corner_l_polygon` + rt37... (מספר
  חדש) שמוכיחה זהות לצללית של היום, ורק אז חיבור, עם דגל `SIDING_CORNER_L`.
- נקודות כניסה: `add_horizontal_siding` ו-`add_board_and_batten` ב-wall_tool.rb;
  היום `corner_trim_runs` + `ext_start`/`ext_end` + `build_siding_board!`.
- נשארו גם: טרים סביב פתחים, קרש סיום עליון.

### מהחוזה (CONTRACT_2D.md) — פערים שעדיין פתוחים
- `corners_xy` נשמר כמקור ולא כ-cache. זה הבאג של שינוי עובי קיר.
- לא מתועד חד-משמעית מה `position_along_wall_in` מודד.
- (נסגרו מאז: Tags, ישות חדר, level, רישום חלונות על הקיר, רצפות/תקרות/גגות.)

### שאר הפתוחים
- **הגג לא מתעדכן לבד** אחרי עיקום קיר — נשאר על הקו הישן עד בנייה מחדש.
- קיר מעוגל ב-`plan_generator` — עדיין קו ישר.
- butt joint פנים-אל-קשת — עדיין כורדה.
- זווית בין שני קירות בעורך 2D — מי משניהם זז.
- `WindowManager.update_window` מחשב את נקודת ההצבה **בקו ישר בלבד**
  (מתעלם מ-arc_sag) — באג פוטנציאלי לעריכת חלון על קיר מעוגל. לא נבדק.
- קשת פנימה שנפגשת בזווית רדודה: שן קטנה בחוץ — התקבל כרגע.
- דלת בקיר מעוגל שיושבת בתוך הבית — **סגור. המשתמש: לא לשאול על זה שוב.**

---

## 5. דגלי חירום

```ruby
InteriorPro::WallTool::USE_CURVED_WALLS = false
InteriorPro::WallTool::USE_3D_SIDING = false
InteriorPro::WallTool::USE_NATIVE_OPENINGS = false
InteriorPro::WallTool::RESEAT_OPENINGS_ON_CURVE = false   # חדש
InteriorPro::WallTool::DRAW_SNAP_ORTHO_ONLY = true        # חדש
```

כוונון הצמדת הציור (Ruby Console, שורה אחת):
```ruby
InteriorPro::WallTool.send(:remove_const, :DRAW_SNAP_ORTHO_DEG); InteriorPro::WallTool::DRAW_SNAP_ORTHO_DEG = 5.0
```

---

## 6. כלי debug בשורש

- `debug_layout_spike.rb` — **חדש.** מוכיח את כל צינור הלאיאוט מקצה לקצה
  ומדפיס שורה אחת. `load File.join(InteriorPro::PLUGIN_DIR, 'debug_layout_spike.rb')`
- `debug_ridge_caps.rb`, `debug_curved_door.rb`, `debug_arc_corner.rb`,
  `fix_corners_once.rb` — מסבבים קודמים.

## 7. הוכחות טעינה (רול 6)

```ruby
InteriorPro.reload!; puts(InteriorPro::DoorManager.respond_to?(:reseat_hosted_openings!) && InteriorPro::WallTool.respond_to?(:direction_snap) ? "NEW CODE LOADED" : "OLD CODE - restart SketchUp")
```
