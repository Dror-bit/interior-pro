# Interior Pro — מסירה 2026-08-21 ב' (רעפי רומן, גג מתכת, שינגלס)

קרא את זה קודם, ואז `CLAUDE.md`. הקודמת: `HANDOFF_2026-08-21.md`.
**אל תסמוך על הסיכום — תאמת מול הקוד החי.**

**הבדיקות: 132 ירוקות.** מריצים:
`cd tests` ואז `bash run_all.sh ..`
**חובה `LANG=C.UTF-8`** — בלעדיו rt40 ו-rt49 נופלות על קידוד בלבד, לא על באג.

> **git: לא בוצע commit לאורך כל הסבב.** כל השינויים בדיסק בלבד.
> הרץ `git status` ותכניס אותם לפני שאתה נוגע במשהו.

---

## 0. הדבר הכי חשוב, שוב

§0 של המסירה הקודמת חזר על עצמו היום, אותו נזק:

**הגדלתי את הרעף של רומן ולא אמרתי שהרידג' קאפ נגזר ממנו** (`cap_width_for` =
רוחב הרעף +15%). הקאפ גדל מ-6.9" ל-16.1" בלי שביקש. הוא שאל *"אני לא מבין למה
שינית את צורת הרידג' קאפ?"* — וזה עלה סבב שלם. אחר כך הקטנתי אותו ביוזמתי
וזה עלה עוד סבב: *"עוד הקטנת את זה???"*

**התיקון המבני:** מספרי הקאפ יושבים עכשיו **בטבלת החומר עצמה**
(`cap_w` / `cap_crown` / `cap_round` ב-`RoofTileMath.shapes`), לא נגזרים מהרעף.
שינוי ברעף כבר לא יכול להזיז את הקאפ. **אל תחזיר את זה לגזירה.**

**שתי טעויות נוספות שלי, שתדע לא לחזור עליהן:**

1. **מדידה שקרית.** מדדתי "מרחק קצה הבליטה מקצה הגג" וסיננתי פאות לפי
   `normal.z > 0.1` — זה תפס גם את **הפאות העליונות של הבליטות עצמן**, ולכן
   החזיר 0.25" תמיד ואמרתי לו שהכול תקין. זה לא היה תקין. **מסנן פאות גג חייב
   גם `area >` משהו (2000 in²), אחרת אתה מודד את החתיכות מול עצמן.**
2. **הרצתי אחריו במקום למדוד.** שלוש פעמים אמרתי "תטען מחדש" כשהבעיה הייתה
   אחרת לגמרי (ראה §5).

---

## 1. Roman Tile — פאן + רול

הצינורות חצי-עגולים נכנסו אחד לתוך השני. עכשיו זה רעף אמיתי: **פאן שטוח ברוחב
פיץ' אחד** ששני שכנים נתקעים בו בדיוק בלי רווח, ו**רול מוגבה** שיושב על התפר
ביניהם ומכסה אותו.

| | ערך | מאיפה |
|---|---|---|
| מרווח (pitch) | 10" | הוא: רול 8" + רווח 2" |
| רוחב רול | 8" | `2 × lap` — נוחת בדיוק על התפר |
| רעף מלא | 14" | `pitch + lap` |
| גובה רול | 3.84" | היחס שהוא אישר (0.48) על רול 8" |
| דופן | ⅓" | לא שונה |
| רידג' קאפ | 13" × 1.42" | **מוצהר**, חצי עיגול |
| setback | 4.6" | חצי רוחב הרול המולפף |

- `RoofTileParts.pan_roll?` = הרעף רחב ממרווחו. Barrel (6" על 7.5") **לא** —
  הוא נשאר צינור חלול בדיוק כמו שהיה, ומודל ישן איתו נבנה כרגיל.
- הרול קופץ מ-`run_wall_t` ולא מ-0, ולכן הוא עובר **מעל** הפאן של השכן ולא דרכו.
- `cap_lift_for` מרים את הקאפ בגובה הרעף כך שהוא רוכב עליו. **רק לפאן+רול.**

## 2. Standing Seam — גג המתכת

לפני היום לא הייתה לו שום גיאומטריה: `scallop: 0` ולכן `runs?` החזיר false והוא
היה משטח צבוע שטוח. עכשיו:

| | ערך |
|---|---|
| בליטה | 2" רוחב × 1" גובה, מלבן |
| מרווח | 26" **בין** הבליטות → 28" בין מרכזים |
| פאן | **אין!** ראה למטה |
| מסגרת איב | בר 1"×1", בגובה הבליטות |
| רידג' | 10" (5" לכל צד), **קשת 0** — קיפול שטוח, **פס אחד רצוף** |
| setback | 0 — הבליטות מגיעות עד הקו |

- נכנס דרך דגל חדש `run_seam: true`, **לא** דרך `scallop` — סטנדינג סים אינו
  מעוגל ו-`scallop` פירושו "הפרופיל מעוגל".
- **אין פאן, וזה חשוב.** בהתחלה בניתי פאן+בליטה כמו רומן. הוא אמר *"שהכל יהיה
  משטח אחד גדול כמו השכבה מתחת"* — וגם צדק טכנית: פאן ברוחב 28" נחתך רק לפי
  **קו המרכז** שלו, ולכן כל פאן בלט מההיפ במדרגות. משטח הגג ממילא רציף, אז הוא
  הפאן. הבליטה יושבת ישירות עליו מ-z=0.
- **בלי ריכוך קצוות** (`soften_run?` = false): הוא ביקש במפורש שיראו את הקווים
  והפינות, רק בגג הזה.
- הרידג' נבנה כחתיכה אחת רצופה לכל קו (`cap_continuous_for`) ובלי ריכוך.

## 3. שינגלס

`USE_ROOF_TEXTURES = false` (מאתמול) כיבה טקסטורות בכל הגגות, ולשינגלס אין
תלת-ממד שיחליף אותן — אז הוא נשאר צבע שטוח. עכשיו יש `textured_families`
(**מתודה ורשימה, לא קבוע** — קבוע עם `const_defined?` לא נקרא מחדש ב-`reload!`)
ובה `['shingle']` בלבד. המתג `USE_ROOF_TEXTURES` עדיין גובר ומחזיר את כולם.

הוגברה גם הניגודיות של `textures/roof_shingle.jpg` (סטיית תקן 11 → 22, הבהירות
הממוצעת נשמרה). המקור ב-git.

## 4. מה **לא** נגעתי, בכוונה

**Spanish Tile (`metaltile`), slate, barrel, color** — בדיוק כמו שהיו.
בסבב אחד הם כן זזו בטעות (ספניש הורם 1.31" והפרופיל שלהם הפך לעגול) והוא שם לב
מיד: *"מחקת את כל הרידג' קאפ... גם בשינגלס"*. הוחזרו. `cap_profile_flat` הוא
הפרופיל המקורי מילה במילה, וכל מה שאינו רומן עובר בו.

---

## 5. הבעיה הפתוחה — הבליטות בהיפ

**זה מה שנשאר, וזה הצעד הבא.**

בגג המתכת, הבליטות **חורגות מעבר לקו ההיפ בצד אחד ולא מגיעות אליו בצד השני**.
מילותיו: *"הבליטות יוצאות בצד אחד ועוברות דרך הרידג' והצד השני לא מגיע אליו
בכלל."* יש לו צילום שמראה את זה בבירור.

מה שכן ידוע:
- החישוב ב-`run_slots` **אמור** לתת אורך מדויק עד המתאר: `v_spans_at` חותך על
  המתאר, ה-setback אצל seam הוא 0, וה-overhang מתקזז.
- **המדידה שלי שהראתה 0.25" הייתה שגויה** — ראה §0.
- לכן הסיבה עדיין **לא ידועה**. חשודים: המצולע ב-`plane_uv` לא זהה לפאת הגג
  שרואים; פאות `top_shell` חופפות בין מישורים שכנים; או שהבליטה ברוחב 2" חורגת
  באלכסון (אבל בצילום החריגה גדולה בהרבה מ-2").

**סקריפט המדידה הנכון** (Ruby Console) — הוא לא הספיק להריץ אותו:

```ruby
m = Sketchup.active_model
ribs = []; deck = []
w = lambda { |es, tr| es.each { |e|
  if e.is_a?(Sketchup::ComponentInstance)
    t = tr * e.transformation
    ribs << t if e.definition.name.start_with?('IP_TileRun_seamRib')
  elsif e.is_a?(Sketchup::Group)
    w.call(e.entities, tr * e.transformation)
  elsif e.is_a?(Sketchup::Face) && e.normal.z > 0.1 && e.area > 2000
    deck << [e, tr]
  end } }
w.call(m.entities, Geom::Transformation.new)
out = 0
ribs.each { |t|
  p2 = t.origin.offset(t.yaxis)
  inside = deck.any? { |f, ft|
    q = p2.transform(ft.inverse)
    f.classify_point(q.project_to_plane(f.plane)) == Sketchup::Face::PointInside }
  out += 1 unless inside }
puts "ribs=#{ribs.length}  deck=#{deck.length}  ends OUTSIDE=#{out}"
```

---

## 6. מלכודות מכניות שעלו זמן היום

- **שם ההגדרה חייב לשאת את הצורה, לא רק את המידות.** הורדתי את הפאן מהבליטה,
  אף מספר בשם לא השתנה, `fetch_or_build` החזיר את ההגדרה הישנה עם הפאן, והגג
  נבנה זהה. *"כלום לא השתנה."* עכשיו יש `'Rib'` / `'Seam'` בשם. **כל שינוי במה
  שחתיכה *היא* חייב להזיז את המחרוזת הזאת.**
- **`reload!` לא בונה גג מחדש.** צריך `RoofManager.build_roof!` אחריו. פעמיים
  חשבנו שהקוד לא נטען וזה פשוט לא נבנה.
- **`ridge_cap` בהגדרות היה `false`** ולכן לא היו קאפים בכלל, וזה נראה כאילו
  מחקתי אותם. תמיד לבדוק את ההגדרה לפני שמאשימים קוד.
- **ערך מוצהר של 0 נבלע.** `cap_crown_for` עשה `h > 0.25 ? h : RIDGE_CAP_CROWN`,
  אז קאפ שטוח (0) חזר להיות קשת. עכשיו `cap_crown_stated?` מבדיל בין "הוצהר 0"
  לבין "אין תשובה".
- **בדיקת מקור שמתארת קוד שהחלפת — תעדכן אותה, אל תמחק** (כלל `rt65`).
  rt82 ו-rt18 עודכנו כך לאורך כל היום, כולל תיאור המספרים הישנים.

## 7. הסקריפט שמאמת שהקוד באמת נטען

שווה זהב, השתמשנו בו שלוש פעמים:

```ruby
P = InteriorPro::RoofTileParts
M = InteriorPro::RoofTileMath
R = InteriorPro::RoofManager
s = M.shape('seam')
puts "cap_w=#{R.cap_width_for('seam')} crown=#{R.cap_crown_for('seam')} lift=#{R.cap_lift_for('seam').round(2)}"
puts "setback=#{P.ridge_setback(s)} rib=#{P.seam_w(s)}x#{P.run_height(s)} pitch=#{P.run_pitch(s)}"
puts "run def = #{P.run(Sketchup.active_model, 'seam').name}"
```

## 8. קבצים שהשתנו היום

`roof_tile_math.rb`, `roof_tile_parts.rb`, `roof_tile_place.rb`,
`roof_manager.rb`, `textures/roof_shingle.jpg`,
`tests/rt18.rb`, `tests/rt82.rb`.

## 9. מה פתוח — לפי סדר

1. **הבליטות בהיפ** — §5. הצעד הבא.
2. הוא אמר שיש **גג שני לתקן** ועוד לא הראה אותו.
3. רעפי איב ורייק לרומן — `RoofTileParts.eave` / `.rake` קיימים, אף אחד לא מציב
   אותם. (לגג המתכת כן נבנתה מסגרת איב.)
4. הכל מ-`HANDOFF_2026-08-21.md` §5 שלא נסגר.

## 10. איך הוא רוצה לעבוד

- עברית, **משפט או שניים**, כל פקודת רובי בקופסה נפרדת.
- **בדיוק מה שביקש, לא יותר.** מספר שלא ביקש לשנות הוא באג גם אם הוא נראה נכון.
- אם לא ברור איזה ממד הוא מתכוון — **תשאל**, שאלה אחת קצרה.
- **תמדוד, אל תנחש**, ואל תגיד "זה תקין" לפני שהמדידה נבדקה שהיא מודדת את הדבר
  הנכון.
