ממשיכים את פרויקט **Interior Pro** — תוסף SketchUp 2024, ‏Ruby, ‏`module InteriorPro`.

## 0. לפני הכל — חיבור תיקייה
חבר לסשן את התיקייה החיה של התוסף:
`C:\Users\rordt\AppData\Roaming\SketchUp\SketchUp 2024\SketchUp\Plugins\interior_pro`

**אל תיגע בתיקייה `C:\InteriorPro_Agent\source_readonly`** — זה סנאפשוט ישן (2026-07-14) שמבלבל. גם אם היא מחוברת, התעלם ממנה לגמרי.

## 1. מה לקרוא ראשון (בתיקיית הפלאגין)
1. `STATUS_handoff.md` — קרא לפחות את הפרק העליון "כללי עבודה מחייבים" + "מצב סוף סבב 2026-07-30".
2. `CONTRACT_2D.md` — חוזה שכבת ה-2D.
3. `TARGET_PLANS.md` — מסמך היעד לתוכניות הקבלן.
4. `CLAUDE.md` — ישן וחלקי (מדבר על דלתות בלבד); ה-STATUS גובר עליו.

## 2. כלל עבודה מחייב
**להציע — כן. לממש בלי אישור — לא.** כל שינוי בקוד או תוספת ויזואלית לתוכנית דורשים אישור מפורש ממני קודם. (הרקע: נוסף האצ' לרצפה בלי לשאול, וזה לא מה שרציתי.)

## 3. כיוון המוצר — 2D-first
שלושה שלבים:
1. **סירטוט מלמעלה ב-2D** — שיטת העבודה הראשית שלי, כולל מדידות שטח.
2. **המרה ל-3D בלחיצת כפתור** — ה-3D משמש בעיקר לרינדורים, אליוויישנים וחישוב כמויות.
3. **הוצאת תוכניות קבלן.**

**חובה שיעבוד גם המסלול 2D בלבד → תוכניות, בלי 3D בכלל** (למשל תוכנית מדידות).

## 4. איפה עומדים
גמור ועובד ב-3D: קירות, דלתות, חלונות, מולדינג, ריצפות, חדרים, פאונדיישן, דלתות מוסך, פתחים מקושתים.

שכבת ה-2D:
- `plan_editor.rb` (~123KB) — עורך 2D מלמעלה על קנבס: ציור עם נעילת Shift, אינפרנס, מיטר פינות חי, מיזוג קו-ליניארי, צד-חוץ אוטומטי; מצב Select עם מחיקה, גרירה, עריכת מידות/אורך/הזזה לצדדים, שמירה או ניתוק פינות; קטלוגים אמיתיים של קירות/דלתות/חלונות עם ברירות מחדל ממה שכבר קיים במודל.
- `plan_generator.rb` (~38KB) — מחולל התוכנית: פושה, סימבול 2D נכון לכל סוג דלת וחלון, סימון W101/D101/IN101, שתי שורות מידה (חוץ + נטו פנים), תוויות חדרים, טבלאות WINDOW/DOOR SCHEDULE, מקרא וכותרת.

שניהם רשומים ב-`main.rb` ובתפריט: ‏2D Editor / 2D Plan: Build-Refresh / 2D Plan: Remove.
מקומטים ב-git, ענף `door-stabilize`, קומיט אחרון `dc83f84`.

## 5. מה פתוח — לפי סדר עדיפות שלי
1. **חומר הריצפה בתוכניות** — הריצפה בתוכנית צריכה להיות אותו חומר/דפוס שבחרתי בדיאלוג הריצפות (`FloorManager` — ‏`floor_texture` / FloorPattern). **רק בשלב הוצאת ה-2D PLANS, לא בשלב הסירטוט.** אושר עקרונית, לא מומש.
2. להשלים את ה-2D עד רמת שימוש יומיומי אמיתי — נדבר על מה חסר לפני שמתחילים.
3. מ-`TARGET_PLANS.md` עוד לא נעשו: ‏Area Plan, ריהוט ואביזרים, בועות גריד, אליוויישנים פנימיים, דף שער וטייטלבלוק, ייצוא LayOut/PDF.
4. אחר כך: תקרות וגגות.

## 6. חוב טכני שחשוב להכיר
- באג שורש לא מתוקן: ‏`DoorManager.wall_geometry` מחשב `center_offset` בסימני הניצב השמאלי בעוד ש-`n` שלו הוא הניצב הימני → בקירות "bottom-right" דלתות חוץ משתקפות. **קיימת עקיפה:** העורך יוצר תמיד קירות `bottom-left`. אל תשנה בלי אישור.
- מיזוג קו-ליניארי עובד רק על קירות pending, לא מול קירות שכבר הוחלו.
- העורך עוד לא מחובר לדיאלוגים המלאים של דלת/חלון ("More options").
- ‏`hatch_polygon` קיים ב-`plan_generator.rb` אבל **הקריאה אליו מנוטרלת בכוונה** — לא להפעיל.
- ב-git יש רעש של סופי שורה (CRLF מול LF) בקבצי ה-door ובקובץ `CLAUDE.md` — התוכן זהה, רק ה-EOL שונה. אפשר לנקות פעם אחת עם `.gitattributes` + ‏`git add --renormalize .` — רק באישור שלי.
- המאונט של התיקייה לא מאפשר מחיקת קבצים, אז git משאיר קבצי `.lock` תקועים; יש תיקייה `_to_delete\git_stale_locks` שאפשר למחוק ידנית.

## 7. שיטת העבודה הטכנית
- עורכים **ישירות בתיקיית הפלאגין** (היא החיה). אחרי שינוי: ‏`InteriorPro.reload!` בקונסול של סקצ'אפ.
- שינוי בדיאלוג — מספיק לסגור ולפתוח אותו. שינוי ב-`DOOR_SETTING_KEYS` — **חובה הפעלה מחדש מלאה של סקצ'אפ**.
- קובץ חדש שנוסף ל-`main.rb` לא ייטען עד restart מלא.
- ‏git רק בתיקיית הפלאגין, ענף `door-stabilize`. קומיט אחרי כל צעד שעובד.
- בדיקות תמיד על קובץ חדש לגמרי (File → New) — יש cache של רכיבים במודל.
- אני מדביק פקודות בלבד — תן לי פקודות מוכנות עם פלט ברור, וציין איפה מדביקים (Ruby Console או cmd).

## 8. אזהרה קריטית לגבי `plan_editor.rb`
ה-HTML/JS של העורך יושב בתוך heredoc של Ruby. **escape בודד כמו `\'` בתוך ה-HTML המוטבע הורג את כל ה-JavaScript בשקט** — הקנבס פשוט מת. אחרי כל שינוי בקובץ הזה חובה לאמת: לחלץ את ה-JS מה-heredoc כולל עיבוד ה-escapes של Ruby, להריץ `node --check`, ואז להריץ עם DOM/Canvas-stub לסימולציית אירועי עכבר. ככה נתפסו שלושה באגים לפני שהגיעו אליי.

---

**התחלה:** קרא את הקבצים, תגיד לי בקצרה מה הבנת על המצב, ותציע מה הצעד הבא — ואל תממש כלום לפני שאישרתי.
