# פתיחה לצ'אט הבא — העתק את זה כהודעה ראשונה

פרויקט Interior Pro — תוסף SketchUp (Ruby, מודול InteriorPro).
תיקייה: C:\Users\rordt\AppData\Roaming\SketchUp\SketchUp 2024\SketchUp\Plugins\interior_pro
בקש גישה לתיקייה. קרא בסדר הזה: HANDOFF_2026-09-20.md, אחר כך
VRAY_POC_PLAN.md, אחר כך CLAUDE.md.

איך אנחנו עובדים:
- עברית פשוטה, משפט-שניים. כל פקודה בקופסה נפרדת עם תווית Ruby Console או CMD.
- אני לא כותב קוד — אתה עורך את הקבצים, אני מריץ InteriorPro.reload! ובודק.
- אתה אף פעם לא מריץ git — נותן לי פקודות CMD. commit אחרי כל צעד עובד.
- **אני לא מדביק פלט.** קובץ מדידה שכותב דוח לתיקייה, ואתה קורא אותו בעצמך.
- צעד אחד קטן בכל פעם, בדיוק מה שביקשתי, ודגל כיבוי לכל דבר חדש.
- כשאני אומר "לא טוב" — לא להתווכח, להחזיר לאחור.

בדיקות בענן: cd tests ואז LANG=C.UTF-8 bash run_all.sh .. — 211 ירוקות,
rt51/rt52/rt60 אדומות מסיבה ישנה.

## הצעד הראשון בצ'אט החדש

**לאחד את חישוב פריסת האריחים.** היום יש שניים:

1. `floor_pattern.rb` — מצייר את האריחים על הרצפה. יודע: `pattern_angle`,
   `pattern_center` (ממרכז את הרשת בחדר), ברירות מחדל שונות למידה לפי
   הדוגמה, וגראוט.
2. `tile_count.rb` / `tile_takeoff.rb` — סופר אריחים (נכתב 2026-09-20,
   אומת ביד: 104 על חדר אמיתי). מכיר גראוט ו-ox/oy, **אבל מתעלם
   מ-angle ומ-center**.

הוא תפס את זה בעצמו: "השרטוט חייב להיות בדיוק כמו הרינדור, לא רק דומה".
צודק — שני חישובים תמיד יתפצלו.

**המטרה:** פונקציה אחת שמייצרת את הפריסה (רשימת תאי אריח: שלם / חתוך
ובאיזו מידה), ושלושתם קוראים ממנה — הצייר, המונה, ובהמשך שרטוט הפריסה
והרינדור. זה גם מה ש-`VRAY_POC_PLAN.md` קורא לו "מקור נתונים אחד,
שלושה פלטים".

להתחיל במדידה: לקרוא את `floor_pattern.rb` במלואו ולראות מה בדיוק
`tile_planks` / `draw_tile_lines!` מחשבים, לפני שמזיזים משהו.

## מה עוד פתוח (לא לפני שהאיחוד נסגר)

- **V-Ray לא מותקן** — האתר של Chaos לא עבד, הוא צריך להתקשר אליהם.
  זו החסימה ל-POC. לא לגעת בשמירת טקסטורות/PBR של אריח עד שנמדוד מה
  V-Ray חושף.
- שרטוט פריסת האריחים (אלווישן) — הפלט השלישי של Tile.
- אריחים על קירות — היום הספירה רק לרצפות.
- ייבוא אמיתי של `surface_takeoff_<uuid>.csv` ב-Invoice Studio.
- הגדרות שמלוות אותו בשני המחשבים ("אקאונט").

## אזהרות שנקנו בדם

- **חדר הוא לא קבוצה אחת** — קירות ורצפה נפרדים. לבחירת חדרים יש טבלה
  (`Room Names` בסרגל), לא קליק על גיאומטריה.
- שינוי שם חייב גם **לצייר מחדש** את התווית (`RoomManager.build_label!`).
- לפני שמדווחים באג — למדוד. פעמיים אמרתי "באג" ולא היה: הגראוט כן
  נשמר, ו-"Tile" הוא שם חוקי של דוגמה.
- הרינגבון ושברון **מסורבים** בספירה בכוונה, לא נספרים כרשת.
