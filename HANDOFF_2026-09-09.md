# Interior Pro — מסירה 2026-09-09 (הסופיט הכפול, הפשייה של הגיבל, הגגון)

קרא את זה קודם, ואז `CLAUDE.md`. הקודמת: `HANDOFF_2026-09-08B.md`.
**אל תסמוך על הסיכום — תאמת מול הקוד החי.**

בדיקות: `cd tests` ואז `LANG=C.UTF-8 bash run_all.sh ..` — **174 ירוקות**
(rt51/rt52 אדומות מהסיבה הישנה). חדשה היום: rt127.

## מה נסגר היום (הכל אושר בעין שלו)

### 1. המשולש — לא היה חור, היו שני לוחות סופיט אחד על השני
**המסירה הקודמת שלחה אותי לפינה הלא נכונה.** הכתובת (1011.5, 258) לא
הייתה המקום. הוא לחץ בעצמו (debug_click_probe) ויצא **(872.4, 135.9,
197.0)** — הפינה הדרום-מערבית של האגף. שם נמדדו שתי פאות שטוחות ב-z=197,
שתיהן InteriorPro_Roof_ffffff, מכסות את אותו ריבוע 11.25×11.25.
debug_corner_grid צייר מפת כיסוי: משולש שלם של "2" מעל האלכסון.

הסיבה: הצלע המערבית מסומנת גיבל **לכל אורכה**, אבל מעל האגף היא איבס
רגיל — והלוח שלה שם כן נבנה, ממוזוה, דרך ענף gable_spans של build_band!.
כלל ה"קצה מרובע" קרא את gable_flags הגס (פר-צלע), אז הלוח הדרומי רץ ישר
מעל שכן שהוא בכלל לא רייק. `rake_corners` (פר-קצה, מ-08/09) הוא התשובה
הנכונה — הסופיט השטוח קורא אותו עכשיו.
תיקון: ארגומנט אחד ב-build_band! של הסופיט. rt127 מכסה.

### 2. פשיית הגיבל בלטה 3/4" מהפשייה של האיבס (הוא מדד)
`build_rake_board!` עמדה ב-0..FASCIA_THICK, כלומר **מחוץ** לקו, בזמן
שפשיית האיבס היא -FASCIA_THICK..0. נוספו `RAKE_K_IN = -FASCIA_THICK`
ו-`RAKE_K_OUT = 0.0`, ואיתם זזו כל מי שרוכב על הרייק:
- הרייק של הבית (שני אתרי קריאה) מקבל k מפורש
- המטל-אדג' של הרייק: `d_in` הפך ל-RAKE_K_OUT (שני מקומות)
- הסופיט של הרייק: פרמטר חדש `inset:` (ברירת מחדל 0.0 = ההתנהגות הישנה)
- החיתוך המרובע של הגאטר ושל פשיית הצלע העליונה בשד
- square_k של הסופיט השטוח = RAKE_K_IN
**הדורמר לא זז בכוונה** — שם הוא ביקש ב-09/02 שהגיבל יבוא לפני האנכי
(rt98). לכן ברירות המחדל של build_rake_board! נשארו 0..FASCIA_THICK.
rt15 ו-rt94 עודכנו למספרים החדשים (הם קיבעו את המיקום הישן).

### 3. חלון Edit Roof: שינוי סגנון סופיט לא עשה כלום
`soffit_paint` נותנת לצבע ידני לנצח את הטקסטורה, ו-soffitPickedFlag
נשאר דלוק לנצח אחרי בחירת צבע אחת. לכן "Stucco" לא שינה כלום.
`soffitChanged(true)` מאפס עכשיו את הדגל; הקריאה בטעינה לא.

### 4. הגגון: הרווח בין האיבס לפשייה נסגר
נמדד אצלו: לוח האיבס השטוח מסתיים ב-z=234.01, והסופיט של הגיבל מתחיל
ב-235.76 — מדרגה של 1.75" פתוחה לרוחב. הסיבה: rake_meet_span מושך את
הסופיט של הרייק אוברהנג אחד אחורה, ושם התחתית שלו כבר גבוהה מהלוח השטוח.
נבנה `DormerManager.build_rake_return!` — טריז בין הפאה העליונה של האיבס
השטוח לתחתית לוח הרייק, נגמר בדיוק איפה שהם נפגשים (we - thick/pitch),
נסחב עומק אוברהנג פנימה. **רק כשהסופיט לא משופע.**
`extrude_wz!` הוא העוזר החדש (חתך ב-(w,z) נסחב לאורך s).
**roof_manager לא נגעתי** — לבקשתו המפורשת.

### 5. בורר צבע למסגרת החלון של הגגון (טרם נבדק בעין)
`window_color` חדש: settings → save_settings! → spec_from_settings →
אטריביוט על הגגון → dormer_spec → `paint_window_frame!`, שמחליף חומר רק
על מי שלובש `InteriorPro_Window_Frame` (הזכוכית לא נוגעים בה). '' =
כמו ש-WindowTool בנה, כלומר כל גגון קיים לא משתנה. בחלון: שדה color
בקטע Window + דגל winPickedFlag, בדיוק כמו הסופיט בחלון הגג.

## מה עוד לא נסגר
- **commit אחרון לא רץ**: בורר הצבע + CLAUDE.md + המסירה הזאת.
- **המאק לא מעודכן.** origin מפגר בהרבה. הוא ידחוף כשיגיע למחשב:
  `git push origin door-stabilize` ואז `git pull` במאק.
- מהמסירות הקודמות: רייק/פשיה בפינת המפרץ; איבס הגג הקטן + המשולש הלא
  צבוע ("gable wall top: no wall materials found"); rt51/52; סופיט משופע;
  גאטרס-המשך.

## איך לעבוד איתו — מה שעלה היום
- **הוא לא יכול לבחור קרש בתוך קבוצה, אבל הוא כן יכול ללחוץ.**
  `debug_click_probe.rb` הופך את העכבר לגשוש: לחיצה אחת ומקבלים את
  הנקודה, כל מה שמתחת לסמן, וכל קצה ופאה ברדיוס 8" עם soft/smooth/hidden.
  זה מה שפתר את המשולש אחרי ששני ניסיונות רינדור נכשלו. לחיצה *בתוך* רווח
  לא מחזירה כלום — להגיד לו ללחוץ על הלוח שליד.
- אל תסמוך על קואורדינטה שכתובה במסירה. תבקש שילחץ.
- בדיקה חדשה שעוברת גם על הקוד הישן היא בדיקה מזויפת. rt126 בונה בית עם
  שלוש צלעות מסומנות; אצלו נשארו **שתיים** — בלי דאמפ טרי (debug_house_dump)
  rt127 לא היה משחזר כלום.
- הרצתי git פעם אחת בטעות (`git status`) ופעם אחת באישורו (`push`, נכשל
  על סיסמה). לא נשאר index.lock. **לא להריץ git.**

## קבצי מדידה של היום (בשורש)
debug_soffit_edges.rb (כל קצה ליד פינה: soft/smooth/hidden + הפאות),
debug_click_probe.rb (הגשוש — הכי שימושי), debug_corner_grid.rb (מפת
כיסוי 0/1/2 של הסופיט), debug_aim.rb (קרניים דרך מרכז המסך),
debug_zoom_corner.rb. ב-tests/_to_delete/ יש סקרפצ'ים שלי — אפשר למחוק.
