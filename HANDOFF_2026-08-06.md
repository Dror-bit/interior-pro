# מסירה לצ'אט הבא — Interior Pro, סבב 2026-08-06

## 0. לפני הכל
התיקייה החיה היחידה (PC):
`C:\Users\rordt\AppData\Roaming\SketchUp\SketchUp 2024\SketchUp\Plugins\interior_pro`
סדר קריאה: **הקובץ הזה** ← `HANDOFF_2026-08-05C.md` ← `HANDOFF_2026-08-05B.md`
← `HANDOFF_2026-08-05.md` ← `STATUS_handoff.md`.

בדיקות (חובה לפני כל מסירה) — **מתוך תיקיית tests, עם `..`**:
```
cd tests && bash run_all.sh ..
```
31 חבילות JS + 16 רובי. הרצה עם `.` מהשורש לא מעתיקה קבצים ותיתן FAIL כוזב.

**שני מחשבים מסונכרנים דרך git** (branch: door-stabilize):
- PC (SketchUp 2024) — כאן מפתחים. commit + push.
- Mac (SketchUp 2026) — `~/Library/Application Support/SketchUp 2026/SketchUp/Plugins/interior_pro`, `git pull`.
- העוזר בענן **לא יכול לעשות commit** (`.git/index.lock` — Operation not
  permitted). המשתמש מריץ git ב-CMD. אם יש נעילה: `del .git\index.lock`.
- קובץ skp מהמאק ל-PC: Save As בפורמט **SketchUp 2024**.

## 1. מה נעשה בסבב (הכל אושר ויזואלית ע"י המשתמש)
### גגות — roof_manager.rb / roof_dialog.rb
1. **אין יותר משולש גמלון לבן.** קצה גמלון נשאר פתוח (החלטת משתמש: הקיר
   עצמו יעלה למשולש בהמשך). `framed_tri!` רק רושם גבהים ל-zmap;
   `build_gable_wall_face!` קיים אך לא נקרא (revert מהיר).
2. **רייק בפרופיל מלא** (מסלול framed): קרש אחד לכל *מישור* קצה-גמלון
   (dedup לפי קו) — איבס←קודקוד←איבס, גם כשמישור הקצה מכוסה חלקית ע"י כנף.
   ב-fallback נשאר רייק פר-צלע חתוך.
3. **הרייק נעצר היכן שגג אחר מתחיל**: `framed_cover_z` + `rake_visible_runs`
   (דגימה כל 6" + ביסקציה).
4. **כנף שחוצה את רכס ההורה**: הצלילה שלה כולה מתחת למשטח ההורה (זבל +
   חפיפה מהבהבת). `wing_side_face!` בודק את משולש החדירה מול
   `framed_cover_z`: גלוי → ואלי אמיתי; מוסתר → חיתוך. הכנף החתוכה
   **נכנסת עוד overhang פנימה עד קו הקיר** (`tuck`), לא נעצרת בקו הפה.
5. **מצב Gable מגמלן תמיד את שני קצוות הגוף הראשי** (סימון ישן על קצה אחד
   השאיר את השני היפ). סימונים רק בוחרים ציר.
6. **Apply במצב Hip מנקה את כל סימוני הקליקים** (roof_dialog.rb) →
   היפ מלא נקי. קליקים אחר כך מוסיפים מחדש. **החלטת משתמש.**

### קירות — wall_tool.rb / toolbar.rb / plan_editor.rb
7. **`flip_wall_faces!(wall, wrap_operation: true)`** — מחליף חוץ/פנים:
   מחליף start↔end, ממראה h_anchor, ממראה מיקומי דלתות (t → len-t),
   ובונה מחדש באותו מקום. חשוף ב**קליק ימני** על קיר מסומן:
   "Interior Pro: Flip Wall Faces" (toolbar.rb → דורש restart מלא).
8. **תיקון שורש הבאג**: המחולל 2D→3D העביר את כיוון הציור כמו שהוא,
   והחוץ הוא תמיד הצד הימני של start→end — קיר שצויר הפוך יצא הפוך.
   עכשיו `normalize_exterior_orientation!` רץ ב-`apply_walls`
   **לפני join_corners**: משרשר את קירות החוץ ללולאה, כופה כיוון
   **נגד כיוון השעון** (שם "ימין" = חוץ), ומהפך את מי שלא מסתדר.
   לולאה לא סגורה / קיר מנותק / פחות מ-3 → לא נוגע בכלום.
   הליבה `reversed_loop_segments(segs)` היא גאומטריה טהורה ונבדקת.

## 2. בדיקות חדשות
- `tests/rt16.rb` (חדש, 13 בדיקות) — כיוון קירות חוץ. כולל את התוכנית
  האמיתית של המשתמש מהמאק (6 קירות) שבה קיר `a4a2ed66` צויר הפוך;
  אידמפוטנטיות, לולאה שלמה בכיוון השעון, סדר קירות אקראי, L-shape,
  וכל מקרי הבטיחות (שרשרת פתוחה / קיר מנותק / nil).
  טעינת wall_tool.rb האמיתי בטסטים דורשת
  `InteriorPro.send(:remove_const, :WallTool)` אחרי sketchup_stub.
- `tests/rt15.rb` — רגרסיות הגג על **הבית הרביעי** (h4, 10 פינות):
  פרופיל חתוך מטפס, פרופיל מלא לקודקוד, רייק נעצר ברכס הכנף, קצה חשוף
  לא מושפע, כלום לא צולל מתחת למשטח הראשי, tuck עד קו הקיר,
  Gable עם סימון בודד מגמלן את שני הקצוות, Hip Apply מנקה סימונים.

## 3. הצעד הבא / פתוח
1. **סבב קליקים לא הושלם**: Hip → Apply → Gable Ends הלוך-חזור על קירות
   שונים (ראשי + כנף + כמה ביחד) ולוודא שכל קליק עושה בדיוק מה שמצופה.
2. **gable walls אמיתיים** — הקיר עולה למשולש כקיר. זה מה שמתוכנן במקום
   המשולש הלבן שהוסר. עדיפות גבוהה אצל המשתמש.
3. מגבלה ידועה: שני גמלונים על אותו קו אינסופי (שתי כנפיים סימטריות) —
   המעטפת העליונה תגשר ביניהם. לא קיים בבתים הנוכחיים.
4. Dutch Gable (הובטח), חומרים אמיתיים לגג (המשתמש רוצה שהחומר ייתן את
   העובי), צורות איבס שונות, תקרה עליונה קומה 2, מיטר Merge Wall,
   אייקונים 24px.
5. רעש ב-git: `tests/*.rb` הם עותקים שסקריפט הבדיקות יוצר — שווה
   .gitignore. `debug_roof_edges.rb` (gitignored) = סקריפט אבחון גג
   (מדפיס מתאר, plan, edges) — להריץ לפני כל אבחון גג.

## 4. קבצים שהשתנו בסבב
roof_manager.rb, roof_dialog.rb, wall_tool.rb, toolbar.rb, plan_editor.rb,
tests/rt15.rb, tests/rt16.rb (חדש), tests/run_all.sh, הקובץ הזה.
