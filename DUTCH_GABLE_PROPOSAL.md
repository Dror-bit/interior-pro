# גג הולנדי (Dutch gable) — הצעה, 2026-09-09

## מה זה, במילים שלו
אותו גובה ואותו רכס בדיוק כמו היפ. רק הקצה משתנה: **למטה נשאר משטח היפ
קטן** שעולה מהפשייה פנימה, ומעליו **גמלון אנכי** עד לרכס.
הוא בוחר מספר אחד: **כמה עמוק פנימה, נמדד מהפשייה**, נמצא קו השבירה.

```
        רכס
         /\                    /\
        /  \                  /  \
       /    \      →         /____\   <- הגמלון האנכי (קיר)
      /      \              /      \
     /________\            /___/\___\ <- משטח ההיפ הקטן, עומק d מהפשייה
        היפ                    הולנדי
```

## הפקד — מספר אחד, בלי סגנון חדש
**לא** רדיו חדש ב-Roof Type. בחלון הגג, מתחת לסימוני הגיבל:
`Dutch gable — depth from the fascia   [ 0 ] in`
- `0` = גמלון מלא, בדיוק מה שיש היום. אף גג קיים לא משתנה.
- `> 0` = כל צלע שסומנה כגיבל נבנית הולנדית באותו עומק.
זה מכבד את כלל ה-UI ב-CLAUDE.md: לא בונים פקד שני לאותה עבודה. הסימון
כבר קיים ועובד — רק נוסף לו מספר.

## הגיאומטריה — איך מוציאים את זה מהקוד שיש
מסקנת הסקירה: מודל ה-`speeds` הוא **בינארי לכל צלע** (0 = גיבל, 1 = היפ),
ולכן הוא לא יכול לבטא "חצי היפ". הדרך שכן עובדת איתו:

1. **פוליגון שלד נפרד.** `poly_sk` = `poly` שבו הצלע ההולנדית נדחפת
   פנימה ב-`d` (שתי השכנות מתקצרות בהתאם). על `poly_sk` בונים בדיוק את
   מה שבונים היום לגיבל: `speeds[i] = 0.0`, `straight_skeleton`,
   `roof_cells`, `build_hip_geometry!`. הרכס והמדרונות נגמרים על הקו
   המוסט — וזה בדיוק מה שרואים בתמונה.
2. **הסינר (apron).** בין הפשייה האמיתית לקו המוסט נבנה משטח היפ קטן
   בשיפוע הגג: טרפז שעולה מ-`z0` בפשייה ל-`z0 + d*slope` בקו המוסט,
   ושתי משולשי-פינה שמחברים אותו למישורי הצד. זה החלק החדש היחיד.
3. **`zmap`.** הסינר חייב להיכנס ל-zmap כמו כל משטח, אחרת כל הליפוף
   (`edge_profile_chain`) לא ידע עליו — והפשייה, הדריפ, הסופיט, הגאטר
   וקירות הגיבל כולם יושבים עליו.
4. **הקישוט.** הצלע ההולנדית מקבלת **פשייה רגילה** בפשייה האמיתית (לא
   רייק!), כי שם יש איבס אמיתי. לוחות הרייק עולים על שתי הצלעות
   המשופעות של קיר הגמלון — קווים שאינם צלעות של `poly`, ולכן זו
   גיאומטריה חדשה, לא `build_rake_board!` כמו שהוא.
5. **קיר הגמלון.** `build_gable_wall_tops!` מרים היום את הקיר לצורת הגג
   לפי `zmap` על קו הקיר. בהולנדי הקיר לא מגיע לשם — הגמלון יושב על
   הסינר, לא על הקיר. צריך פאה משלו (`build_gable_wall_face!` כבר קיים
   בקובץ ומושבת מ-2026-08-05B — כאן היא חוזרת לשימוש).

## צעדים, לפי הסדר, כל אחד עם בדיקה
1. **טהור בלבד**: `dutch_poly(poly, i, d)` — מחזיר את `poly_sk`.
   בדיקה: rt חדש, ריבוע ומלבן, בלי SketchUp.
2. **המשטח**: השלד על `poly_sk`, בלי סינר ובלי קישוט. בעין: הגג נגמר
   באוויר מעל הפשייה — מכוער אבל נכון. בדיקה: הרכס לא זז, הגובה לא זז.
3. **הסינר + zmap**. בעין: הצורה שלמה.
4. **קישוט**: פשייה/דריפ/סופיט על הצלע ההולנדית, רייק על שתי הצלעות
   של הגמלון, קיר הגמלון.
5. **הפקד בחלון** + שמירה על הגג (`roof_dutch_depth`).

## מה עלול להישבר — לשמור עליו
- `framed_plan` (המסלול הממוסגר) לא יודע להביע חצי-היפ. **צעד 1 יכבה
  אותו** כשה-depth > 0, כדי שלא ייבנה משהו אחר בשקט.
- `level_gable_edges` / `roof_under_z` מניחים מישור אחד לתא לפי ה-eave
  שלו. הצלע ההולנדית תישאל מהפרופיל, לא מהתא.
- שד ושטוח לא נוגעים בזה (אין להם גיבלים במובן הזה).
- 174 הבדיקות חייבות להישאר ירוקות עם `depth = 0`.

## פתוח לשאלה אליו כשיהיה ליד המחשב
עומק ברירת מחדל — 24"? (בתמונה שלו זה נראה בערך 2 רגל.)

## מצב, סוף 2026-09-09
נבנו צעדים 1–3 והפקד (5). 175 בדיקות ירוקות; `dutch_depth = 0` הוא
ברירת המחדל, ואז שום שורה לא מתנהגת אחרת מאתמול.
- `dutch_poly`, `clip_halfplane`, `dutch_cut`, `dutch_apron_cells` — טהורות.
- rt128 מכסה את `dutch_poly` (כולל הסירובים).
- הפקד: "Dutch gable (0 = full gable)" בחלון הגג, מתחת ל-Pitch.

**שני דברים שהוא חייב לראות בעין לפני שממשיכים:**
1. עומק > 0 **מכבה את מסלול ה-framed** ומעביר את כל הגג לשלד. בסטאב
   הרכס יצא כ-3" גבוה יותר מהמסלול הממוסגר. ייתכן שכל הגג ייראה אחרת —
   זה הסיכון הגדול של הפיצ'ר.
2. בבית שלו הודפס "dutch gable on **1** edge(s)" ולא 2 — צלע אחת
   מהשתיים נדחתה. לבדוק למה (MIN_DUTCH_EDGE? הקצה שברח מהקיר השכן?).

**עוד לא נבנה (צעד 4):** פשייה/דריפ/סופיט על הצלע ההולנדית, לוחות רייק
על שתי הצלעות המשופעות של הגמלון, ופאת קיר הגמלון עצמה.
