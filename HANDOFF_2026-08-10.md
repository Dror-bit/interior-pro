# מסירה לצ'אט הבא — Interior Pro, סבב 2026-08-10

## 0. לפני הכל
התיקייה החיה (PC):
`C:\Users\rordt\AppData\Roaming\SketchUp\SketchUp 2024\SketchUp\Plugins\interior_pro`
מאק: `~/Library/Application Support/SketchUp 2026/SketchUp/Plugins/interior_pro`

סדר קריאה: **הקובץ הזה** ← `HANDOFF_2026-08-08.md` ← `HANDOFF_2026-08-06.md`
← `05C` ← `05B` ← `05` ← `STATUS_handoff.md`.

הסבב הזה נגע ב-`roof_manager.rb`, `wall_tool.rb`, `plan_editor.rb`.

### בדיקות
```
cd tests
"C:\Program Files\Git\bin\bash.exe" run_all.sh ..
```
- **במחשב של המשתמש אין Ruby**, אז חבילות ה-rt נותנות FAIL כוזב ושורות
  "BAD". זה לא באג. ההרצה האמיתית היא אצל העוזר בענן (ruby+node).
- מצב נוכחי: 30 חבילות JS + **17 רובי** (`rt17` חדש) — ALL GREEN.

### git
- העוזר בענן לא יכול להריץ git. המשתמש מריץ ב-CMD. branch: `door-stabilize`.
- שני המחשבים מסונכרנים: PC עושה commit + `git push origin door-stabilize`,
  מאק עושה `git pull origin door-stabilize` ואז `InteriorPro.reload!`.
- קבצי `debug_*.rb` ב-`.gitignore` — `git add` עליהם נכשל ועוצר את כל
  שרשרת ה-`&&`. לא לכלול אותם בפקודות commit.

## 1. כללי עבודה (ממשיכים מ-08-08, נרשמו ב-CLAUDE.md)
1. לא בונים שתי דרכים לאותו דבר.
2. פחות קליקים; כל פעולה מקבלת קיצור מקלדת.
3. אייקונים במקום מילים בפאנלים צפופים.
4. כפתור שנלחץ נראה נלחץ; הסמן מתחלף לפי הכלי.
5. תשובות: עברית, משפט-שניים, כל פקודה בקופסה עם ציון לאן להדביק
   (Ruby Console / CMD / Terminal).
6. **לא לנחש.** לאבחן מול הקוד החי ומול פלט הקונסול לפני כל תיקון.

## 2. מה נעשה בסבב

### א. מידה בעורך ה-2D — זרימת 3 קליקים (`plan_editor.rb`)
הבאג הפתוח מ-08-08 ("הרחקת מידה לא עובדת") **לא היה באג** — זו הייתה
תכונה חסרה. המשתמש רצה זרימת CAD.
- קליק 1 = נקודה א', קליק 2 = נקודה ב', ואז המידה **נגררת עם העכבר**
  וקליק 3 קובע את המרחק. `dimPlace` מחזיק את המידה שבהנחה.
- `dimOffTo(d, p)` — מקור אמת אחד לחישוב המרחק (גם לגרירה וגם להנחה).
- Esc באמצע = ביטול (`cancelDimPlace`); מעבר כלי = השארה במקום
  (`finishDimPlace`); Ctrl+Z מבטל.
- הקיים לא נגעו בו: תפיסת מידה קיימת + גרירה, דאבל-קליק לשינוי המספר.
- בדיקות: 5 חדשות ב-`tests/t32.js`.

### ב. קירות גמלון אמיתיים (`roof_manager.rb`)
הקיר עצמו עולה למשולש — מה שתוכנן מ-08-05B במקום המשולש הלבן שהוסר.
- `build_gable_wall_tops!` — פריזמה בעובי הקיר, על **קו הקיר** (overhang
  פנימה מקצה הגג), מראש הקיר עד צדודית הגג, בתוך קבוצת הגג כתת-קבוצה
  עם `part='gable_wall_top'`. נמחקת ונבנית מחדש עם כל Apply.
- צבועה בחומרי החוץ/פנים של הקיר עצמו (`wall_side_material_names`).
- דגל כיבוי: `InteriorPro::RoofManager.build_roof!(gable_walls: false)`
  (נשמר כ-`roof_gable_walls` על המודל).
- `chain_regions_above` — גאומטריה טהורה, הקטעים שמעל ראש הקיר.

### ג. שלושה באגים שנפתחו בעקבות ב' (כולם ב-`roof_manager.rb`)
1. **המשולש הלבן** — הפאות נצבעו רק ב-`back_material`, אז מבחוץ הן
   נראו לבנות. עכשיו כל פאה נצבעת משני הצדדים.
2. **הפשיה והקיר ריחפו באוויר.** זה היה **שורש אמיתי**: הפילטר
   "מעטפת עליונה" ב-`edge_profile_chain` מחק נקודות ש"שוקעות" מתחת לקו
   בין השכנות — כולל פינת האיבס האמיתית של הגוף הראשי. התוצאה: הפרופיל
   גישר בקו ישר מהפסגה אל הקצה הרחוק, מעל גג הכנף, שם אין גג בכלל.
   **התיקון:** פרמטר `surface:` — נקודה נשמרת אם היא באמת ראש הגג שם,
   ונמחקת רק אם גג אחר עובר מעליה. המקור: `framed_cover_z(..., nil)`.
   בנוסף הפרופיל נחתך למתאר הפוליגון (`full: false` בנתיב החי).
3. **הפשיה נעלמה לאורך קיר מסומן.** הדילוג היה לפי **קצה שלם**
   (`gable_flags`), וקיר מסומן שרץ 3977" גם על הגוף וגם על הכנף איבד
   פשיה על כל אורכו. עכשיו `build_band!` מקבל גם `skip_spans` — דילוג
   לפי **קטע**: רק המקטע שבו הגג עולה מעל האיבס מקבל רייק, השאר מקבל
   פשיה רגילה. עוזרים: `complement_spans`, `lerp2`.
   כמו כן `next if z1 < zmin+0.02 && z2 < zmin+0.02` כבר לא מותנה ב-`full`.

### ד. נעילת Shift — שני באגים (`wall_tool.rb`, `plan_editor.rb`)
המשתמש סרטט קיר למטה, לחץ Shift, והקיר קפץ הצידה. **עבד ב-PC, נשבר במאק.**
1. **קוד המקש שונה בין הפלטפורמות.** `onKeyDown` מקבל 16 ב-Windows, אבל
   במאק `CONSTRAIN_MODIFIER_KEY` — **131072** (נמדד אצל המשתמש,
   SketchUp 2026). הקוד בדק `key == 16` בקשיח, אז במק הנעילה הידנית
   מעולם לא הופעלה. עכשיו `WallTool.shift_key?(key)` משווה מול הקבוע.
   **להשוות תמיד מול הקבוע, לא מול המספר.**
2. **הנעילה נמדדה מהנקודה שכבר יושרה.** `@end_point` עובר `snap_to_axis`,
   אז אם הניחוש האוטומטי היה אופקי — `dy` היה 0 ו-Shift יכול היה רק
   לאשר אופקי. עכשיו נשמר `@raw_point` (הסמן האמיתי) ו-`WallTool.axis_lock`
   (טהורה, נבדקת) מחליטה ממנו.
3. **בעורך ה-2D:** `captureLockDir` העדיף את `parInd` (נעילת מקבילות),
   אבל `parInd` מתעדכן רק בשרשרת הקווים — במצב קיר ערך ישן חטף את
   הנעילה. עכשיו הוא נלקח רק אם הוא בכיוון שמסרטטים (עד 15°),
   ומתאפס ב-`endChain`.

## 3. בדיקות חדשות
- `tests/rt17.rb` (**חדש**, ~40 בדיקות) — קירות גמלון. כולל את התוכנית
  האמיתית של המשתמש (10 פינות, `POLY10`) בשתי הקונפיגורציות:
  סימון דרום+צפון, וסימון **הקיר המערבי הארוך** (רכס 565") שרץ על הגוף
  ועל הכנף. בודק: הקיר נעצר במתאר, הפשיה לא מרחפת, הפשיה חוזרת בקטע
  האיבס, צביעה דו-צדדית, דגל הכיבוי, `chain_regions_above` טהורה.
- `tests/rt16.rb` — נוספו `axis_lock` ו-`shift_key?` (כולל סימולציית מק).
- `tests/t32.js` — נוספו זרימת 3 הקליקים של המידה, ונעילת Shift
  (אינפרנס ישן לא חוטף, אינפרנס תואם כן, `endChain` מנקה).
- `tests/run_all.sh` — כולל `rt17`.
- `debug_gable_sel.rb` (**חדש**, gitignored) — סמן קיר (ואת הגג) והרץ
  `load '.../debug_gable_sel.rb'`. מדפיס את הקיר, קצה הפוליגון, תוכנית
  הגג, ואת הפרופיל המדויק שממנו הפשיה והקיר נבנים (`FULL-LINE` מול
  `CLIPPED`), ומה יש בגג החי. **זה הכלי שפתר את הבאג — להשתמש בו קודם.**

## 4. פתוח / הבא בתור
1. **להב דק בחיבור הפשיה לקיר** — פגם ויזואלי קטן שרואים מקרוב.
   יש צילום קירוב אצל המשתמש; לבקש שוב אם צריך.
2. **חתיכת הגג הגדול מאחורי הגג הקטן** — לא קריטי, ניקיון בלבד
   (המשתמש: "זה צריך להיות נקי").
3. המידות חיות רק בעורך — **לא** נבנות במודל/בתוכניות. לשאול אם צריך.
4. חלונות/פתחים בקיר הגמלון — לא נבנה, לא נשאל.
5. גגות: ליטוש פשיה, Dutch Gable, חומרים אמיתיים לגג.
6. חוב ישן: תקרה עליונה קומה 2, מיטר Merge Wall, אייקונים 24px.
7. `CLAUDE.md` עדיין פותח ב"דלתות בלבד" מפאזה ישנה — לשאול אם למחוק.

## 5. קבצים שהשתנו בסבב
`roof_manager.rb`, `wall_tool.rb`, `plan_editor.rb`,
`tests/rt17.rb` (חדש), `tests/rt16.rb`, `tests/t32.js`, `tests/run_all.sh`,
`debug_gable_sel.rb` (חדש, gitignored), והקובץ הזה.
