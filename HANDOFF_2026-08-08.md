# מסירה לצ'אט הבא — Interior Pro, סבב 2026-08-08 (עורך 2D)

## 0. לפני הכל
התיקייה החיה היחידה (PC):
`C:\Users\rordt\AppData\Roaming\SketchUp\SketchUp 2024\SketchUp\Plugins\interior_pro`
סדר קריאה: **הקובץ הזה** ← `HANDOFF_2026-08-06.md` ← `HANDOFF_2026-08-05C.md`
← `05B` ← `05` ← `STATUS_handoff.md`.

**כל הסבב הזה נגע כמעט רק ב-`plan_editor.rb`** (עורך ה-2D, HTML+JS בתוך heredoc).
הגגות לא נגענו בהם בכלל מאז 08-06.

### בדיקות
```
cd tests
"C:\Program Files\Git\bin\bash.exe" run_all.sh ..
```
- ב-CMD אין `bash` ב-PATH — צריך את הנתיב המלא ל-Git Bash.
- **במחשב של המשתמש אין Ruby מותקן**, אז חבילות ה-rt נותנות FAIL כוזב
  ושורות "BAD" בבדיקת התחביר. זה לא באג. הרצה מלאה ואמיתית נעשית אצל
  העוזר בענן (שם יש ruby+node) — 31 JS + 16 רובי + **t32 חדש**, הכל ירוק.
- `run_all.sh` עודכן וכולל עכשיו את t32.

### git
- העוזר בענן **לא יכול** להריץ git. המשתמש מריץ ב-CMD, עם
  `del .git\index.lock` לפני אם יש נעילה. branch: `door-stabilize`.
- `.gitignore` עודכן: כל עותקי הקבצים ש-run_all.sh מייצר בתוך `tests/`
  (plan_editor.rb, wall_tool.rb, roof_manager.rb, out.js, tmp_heredoc.rb וכו')
  כבר לא ב-git. **קבצי `tests/t*.js` ו-`tests/rt*.rb` כן נשמרים.**

## 1. כללי עבודה שנוספו (חובה — נרשמו גם ב-CLAUDE.md)
1. **לא בונים שתי דרכים לאותו דבר.** סיבוב מכסה היפוך — בונים רק סיבוב,
   בלי כפתורי flip. לפני שמוסיפים פקד — לבדוק אם פקד קיים כבר עושה את זה.
2. **פחות קליקים.** לחיצה על כלי מפעילה אותו מיד ומבטלת את מה שרץ — בלי Esc.
   פאנלים לא נסגרים סתם, וכל פעולה מקבלת קיצור מקלדת.
3. **אייקונים במקום מילים** בפאנלים צפופים.
4. כפתור שנלחץ חייב להיראות נלחץ; הסמן מתחלף לפי הכלי הפעיל.
5. תשובות למשתמש: עברית, משפט-שניים, כל פקודה בקופסה נפרדת עם ציון
   לאן להדביק (Ruby Console / CMD).

## 2. מה נעשה בסבב (הכל אושר ויזואלית חוץ מסעיף 4)
### קיצורי מקלדת (עובדים גם במקלדת עברית — לפי `ev.code`)
`S` בחירה · `D` דלתות · `W` חלונות · `L` קווים · `O` קווי עזר ·
`M` הזזה · `R` סיבוב · `U` הפיכת זווית קו עזר.
קיצור לא נחטף באמצע הקלדת מספר (`5'` נשאר `5'`).

### עורך — התנהגות כללית
- `cancelOps()` — בחירת כל כלי מבטלת מיד move/rotate/offset/guide/calib.
- חלון הצורות נשאר פתוח גם ב-Select; לחיצה על כלי ציור קופצת ישר לציור.
- **חלון עזר אחד בכל פעם**: פתיחת "קווי עזר" סוגרת "תמונת רקע" ולהפך;
  מעבר מצב סוגר את שניהם. סגירת חלון קווי העזר מכבה את הכלי.
- Undo אמיתי: `editHist` + `histPush/histUndo` — Ctrl+Z מבטל קודם
  הזזה/סיבוב/היפוך, ורק אז מוחק צורות; פוליליין נמחק קו-קו.
- **טיוטה**: כל 1.5 שניות `save_draft` שומר על המודל
  (`InteriorPro/plan_draft`) את pending walls + pendingSketches + guides + dims.
  `push_draft` מחזיר בפתיחה. Apply מנקה.
- סמן העכבר מתחלף להזזה/סיבוב/אופסט; כפתור Apply מציג "מחיל…".

### קווים
- **קו בודד** (`lineTool='line'`): כל קליק-קליק = אובייקט נפרד, הריצה
  ממשיכה מהנקודה האחרונה. **פוליליין** (`'poly'`) = ההתנהגות הישנה,
  אובייקט אחד, סגירת צורה יוצרת משטח.
- גרירת קצה קו: `hitSketchVertex` + `dragVert` → הארכה/קיצור/זווית.
- Snap **על גוף הקו** (`kind:'edge'`, סמן סגול), חלש מפינה/אמצע.
  נגיעה בגוף קיר **לא** מבטלת את נעילת המקבילות — הנקודה נופלת בדיוק
  בחיתוך של הכיוון הנעול עם הקצה.

### קווי עזר
אופקי / אנכי / זווית מוקלדת (בלי "2 נק'" בממשק — הקוד נשאר).
קליק אחד מניח קו אינסופי. `O` מחזר בין h → v → 45 → 135.
בחירה, הזזה (M), מחיקה, שכפול.

### תמונת רקע
- בפאנל רק: טען · הסר · כייל לפי מידה · שקיפות · **שלח ל-3D / הסר מ-3D**.
- אין נעול, אין מרכז, אין flip, אין תיבת סיבוב.
- **החלון פתוח = התמונה אובייקט רגיל** (`type:'under'`) — נבחרת, זזה עם M,
  מסתובבת עם R (rotOp.ub). **החלון סגור = נעולה** והקליקים עוברים דרכה.
- תמונה שנשארה מחוץ למסך חוזרת למרכז לבד (`ensureUnderlayVisible`),
  בלי לשנות scale.
- `place_underlay_3d` ב-Ruby מניח `Sketchup::Image` על הרצפה במידות
  ובזווית הנכונות, עם attribute `type='underlay'` ותגית `IP/Underlay`.

### מדידה + מידות
- כלי המדידה (`measure`) ננעל לימין/שמאל/מעלה/מטה (`axisAim`), אדום=X,
  ירוק=Y, Shift כופה. אלכסון חופשי נשאר חופשי.
- **כלי חדש `dim`** (אייקון עם חצים): שני קליקים ומידה **נשארת** על
  הסרטוט. `dims = [{x1,y1,x2,y2,off,txt}]`, נשמרת בטיוטה.

## 3. הבאג הפתוח — לטפל בו ראשון
**המשתמש מדווח: "ההרחקה של המידות מהאובייקט לא עובדת" — גם אחרי שני סבבי
תיקון.** כל הבדיקות האוטומטיות עוברות, כולל סימולציה של הקליק האמיתי דרך
`__listeners.cv.mousedown`. כלומר הבאג הוא בסביבה החיה, לא בלוגיקה.

מה כבר נוסה (ועובד בבדיקות):
1. `hitDim` נבדק **ראשון** ב-mode `sel`, לפני קירות/צורות — כי מידה חדשה
   יושבת בדיוק על הקיר שנמדד.
2. `hitDim` בודק את הקו **המוזז** (`dimGeom(d).a/b`), לא את הבסיס.
3. גם בתוך כלי `dim` עצמו, קליק על מידה קיימת תופס אותה במקום ליצור חדשה.
4. `dragDim` + `dimDragTo` ב-`handleMove`, שחרור ב-`finishDrag`,
   וכל שומרי ה-early-return (`pointermove`/`window mousemove`) עודכנו.
5. `rubberPick` מחזיר עכשיו גם `dim` וגם `guide`.

**השערות שלא נבדקו מול המשתמש (לבדוק לפי הסדר):**
- **א. הוא בכלל לא בכלי הנכון.** יכול להיות שהוא יוצר "מידות" עם כלי
  ה-**מדידה** הישן (סרט זמני) ולא עם כלי ה-**dim** החדש — הם נראים דומה.
  לשאול/לוודא: `Sketchup.active_model.get_attribute('InteriorPro','plan_draft')`
  → האם יש שם `dims` בכלל?
- **ב. הדיאלוג לא נטען מחדש.** `InteriorPro.reload!` **לא** מרענן את
  ה-HTML! חייבים לסגור ולפתוח את חלון העורך. אם הוא רק עשה reload,
  הוא עדיין מריץ JS ישן. לוודא במפורש.
- **ג. אירועי עכבר בדיאלוג.** יש `#dbg` בתחתית שמראה md/mm/mu
  (`evCount`) — לבקש צילום בזמן גרירת מידה ולראות אם mm בכלל עולה.
- **ד. `dblclick` ב-CEF** — לא נבדק חי בכלל.

צעד ראשון מוצע: להוסיף `puts`/console זמני ב-`hitDim` ובענף ה-mousedown,
לבקש צילום של `#dbg` + מה שהודפס, ורק אז לתקן. **לא לנחש.**

## 4. פתוח / הבא בתור
1. הבאג של הרחקת המידות (סעיף 3).
2. המידות חיות רק בעורך — הן **לא** נבנות במודל/בתוכניות. המשתמש עוד
   לא ביקש; לשאול אם צריך.
3. גגות: ליטוש פשיה (המשתמש אמר "יש כמה עניינים עם הפשייה, נחזור").
4. **gable walls אמיתיים** — הקיר עולה למשולש. עדיפות גבוהה, מחכה מ-08-06.
5. `CLAUDE.md` עדיין פותח ב"דלתות בלבד" מפאזה ישנה — לא נגעתי. לשאול
   את המשתמש אם למחוק את החלק הזה.
6. חוב ישן: Dutch Gable, חומרים אמיתיים לגג, תקרה עליונה קומה 2,
   מיטר Merge Wall, אייקונים 24px.

## 5. קבצים שהשתנו בסבב
`plan_editor.rb` (רוב הסבב), `CLAUDE.md`, `.gitignore`,
`tests/t32.js` (חדש), `tests/t2.js`, `tests/t3.js`, `tests/t7.js`,
`tests/t9.js`, `tests/run_all.sh`, והקובץ הזה.
