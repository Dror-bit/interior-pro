# Interior Pro — מסירה 2026-08-18B (סימון ההצמדה בעורך ה-2D)

קרא את זה קודם, ואז `CLAUDE.md`. **אל תסמוך על הסיכום — תאמת מול הקוד החי.**
הקובץ הקודם: `HANDOFF_2026-08-18.md` (ריצפת קומה עליונה, ריתוך פינות, מזהים).
כללי העבודה: `HANDOFF_2026-08-14.md` §0. **תקרא אותם ברצינות.**

התיקייה החיה:
`C:\Users\rordt\AppData\Roaming\SketchUp\SketchUp 2024\SketchUp\Plugins\interior_pro`

**הבדיקות: 117 חבילות ירוקות** (71 רובי + 46 ג'אווהסקריפט), אפס אדומות.
היו 116; נוספה `t48`. ספרתי בעצמי מריצה על התיקייה החיה.

---

## 0. השורה התחתונה של הסבב הזה

סבב קצר ואחד. המשתמש דיווח: *"שאני מצייר קו ואז אני רוצה לחבר לו עוד קו
הוא לא תמיד מסמן לי נקודה בקצה קו או פינה... אני ממש חייב את זה כדי לא
לפספס התחלה של קו."*

זה היה באג אמיתי, והוא נסגר בשורה אחת.

> **ההצמדה עבדה כל הזמן. רק הסימון לא צויר.**

---

## 1. מה נכנס — סימון ההצמדה לא נעלם יותר (`t48`, 13 בדיקות)

**הסיבה:** הדגל `lockedNow` ("כרגע מחזיקה נעילת כיוון") מתאפס **רק בראש
`chainEnd`**. ריחוף רגיל עובר דרך `snapPoint`, שלא נגע בדגל אף פעם. אז
מספיק קו אחד עם Shift — או **נעילה מקבילה אוטומטית אחת** — והדגל נשאר
`true` עד סוף הסשן. מאותו רגע `if (snapInd && !lockedNow)` ב-`draw()` לא
מצייר את העיגול יותר. **הנקודה עדיין נתפסה נכון**; פשוט אי אפשר היה לראות
איפה.

זה מסביר את ה"לא תמיד" במדויק: **לפני** הנעילה הראשונה — עובד. **אחרי** —
נעלם עד שסוגרים ופותחים את החלון.

**התיקון:** `snapPoint` מכבה את הדגל. `chainEnd` מדליק אותו שוב **אחרי**
שהוא קורא ל-`snapPoint`, אז נעילה אמיתית לא נפגעת בכלל.

**איך מדדתי, ולא ניחשתי:** הרצתי את קוד החלון האמיתי ב-node עם קנבס מקליט,
עשיתי בדיוק את מה ש-`handleMove` עושה, והדפסתי את הדגל:

```
A1  תוך כדי קו עם Shift        lockedNow = true
A2  מרחף על קצה קו אחר כך      lockedNow = true   הנקודה נתפסה: {x:120, y:0}
A3  האם מצויר עיגול?           false
```

**השומר נופל:** `t48` על הקוד הישן = **5 נפילות**, בדיוק התסמין. אחרי
התיקון 13/13. ראיתי אותה אדומה במו עיניי לפני שכתבתי את התיקון.

---

## 2. השערה שלי שהתבררה כשגויה — אל תרדוף אחריה

בזמן החקירה ראיתי ש**בכלי הקו** העיגול מצויר **לפני** רצועות הקירות
(שורה ~4284 ב-`out.js`, הקירות ב-~4293), בעוד שבמצב קיר הוא מצויר **אחרי**
(~4362). הסקתי שעיגול על פינת קיר מכוסה בידי הקיר, והצעתי לתקן.

**המשתמש בדק בפועל ואמר: "הוא מופיע ירוק, הכל טוב."**

אז או שהעיגול בולט מעבר למילוי, או שהמילוי לא מכסה אותו בפועל. **לא
נגעתי, ואין כאן באג.** נשאר כאן כתוב כדי שמישהו לא "יתקן" את זה מחדש על
סמך קריאת סדר הקוד בלבד.

---

## 3. קבצים

| קובץ | מה |
|---|---|
| `plan_editor.rb` | `lockedNow = false` בראש `snapPoint`, עם הסבר בהערה |
| `tests/t48.js` | **חדש, 13 בדיקות.** נופלת ב-5 בלי התיקון |
| `tests/run_all.sh` | `t48` נוסף לרשימת ה-JS |

שלושה קבצים, שום דבר אחר לא נגע.

---

## 4. מה פתוח — הצעד הבא

**המשתמש קבע את הסדר: קודם גגות, אחר כך לנדסקייפ/הרדסקייפ.**
הוא אמר במפורש שהוא רוצה ללמוד קצת על הלנדסקייפ לפני שנכנסים אליו.

1. **גגות — `ROOFS_ROADMAP.md`.** סדר העבודה שם: (א) באג הלהב בחיבור
   פשיה-קיר, (ב) רידג' קאפ, (ג) חומרים (שינגלס/טיל/מתכת), (ד) גאטרס,
   (ה) ספריית איבס, (ו) שד/הולנדי/דורמר, (ז) גגות ב-2D.
   **בקובץ יש שלוש שאלות פתוחות למשתמש — תשאל אותן לפני שמתחילים.**
2. **הרדסקייפ/לנדסקייפ** — `LANDSCAPE_ROADMAP.md`. אחרי הגגות.
3. **צבע בתוכניות — הבהרה חשובה.** `HANDOFF_2026-08-17C.md` §4.1 מתאר
   "צבע קו" גורף. **המשתמש תיקן אותי מפורשות (2026-08-18):** מה שהוא ביקש
   הוא **צבע למידות ולטקסטים שהוא כותב** — לא צבע לכל התוכנית. הצעתי לו
   צבע-לפי-שכבה והוא דחה. תכנן לפי מה שהוא אמר, לא לפי הסיכום הישן.
4. הפתוחים מ-`HANDOFF_2026-08-18.md` §4: חיבורי T לא מדויקים, הצמדה בזמן
   ציור (חשד לא מוכח), מזהים כפולים של דלתות/חלונות.
5. הפתוחים מ-`HANDOFF_2026-08-17C.md` §4: לוגו גלובלי, ההערה המכוערת.

---

## 5. מה למדתי הסבב הזה

1. **תריץ את נקודת הכניסה, לא את מה שמתחתיה** — שוב. `snapPoint` עבר
   בבדיקה מושלמת. הבאג היה בתנאי שמצייר, שלוש שורות רחוק משם.
2. **"לא תמיד" זה כמעט תמיד דגל תקוע.** כשמשתמש אומר "לפעמים כן ולפעמים
   לא", תחפש משתנה שנדלק ולא נכבה, לא סף ולא סובלנות.
3. **תן למשתמש בדיקה של שנייה במקום ויכוח.** במקום להתווכח על סדר הציור
   נתתי לו "רחף על פינת קיר ותגיד אם אתה רואה ירוק". הוא ענה תוך רגע
   וחסך תיקון מיותר. §2.
4. **תשאל איפה, לפני שאתה קורא קוד.** "קו" יכול להיות בעורך ה-2D או
   בסקצ'אפ — שני קודים שונים לגמרי. שאלה אחת חסכה חצי שעה בקובץ הלא נכון.
5. **אל תתקן לפי הסיכום של צ'אט קודם.** הצעתי צבע-לפי-שכבה כי כך היה כתוב
   ב-§4.1. המשתמש אמר "זה לא מה שביקשתי". §4.3.
6. **`extract.rb` קורא את `plan_editor.rb` מתוך `tests/`**, לא מהשורש. אחרי
   עריכה חייבים `cp ../plan_editor.rb .` לפני `ruby extract.rb`, אחרת אתה
   בודק את הקוד הישן ומסיק שהתיקון לא עבד. נפלתי בזה פעם אחת.
7. **בדוק זמן שינוי לפני כל כתיבה.** בדקתי `stat -c %Y` על `plan_editor.rb`
   ועל `run_all.sh` רגע לפני שכתבתי, ועצרתי אם השתנה. הפעם לא היה צ'אט
   שני, אבל בסבב הקודם כן היה.
