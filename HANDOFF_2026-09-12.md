# Interior Pro — מסירה 2026-09-12 (יום ארוך: הולנדי, פינות abut, מרזבים)

קרא את זה קודם, ואז `CLAUDE.md`. הקודמת: `HANDOFF_2026-09-11.md` (הגג
ההולנדי — התעלה, הפשייה, תפריט הסיום, הצבעים, הגרעין).
**אל תסמוך על הסיכום — תאמת מול הקוד החי.**

בדיקות: `cd tests` ואז `LANG=C.UTF-8 bash run_all.sh ..` — **180 ירוקות**.
אדומות קבועות: rt51/rt52 (ישנות), ו-**rt60** (טיימר הגיבוי האוטומטי —
נופל גם על הקוד שלפני היום, נבדק; לא קשור לגגות. פתוח).

## מה נסגר בחצי השני של היום — הכל אושר בעין שלו

### 1. פינת abut/רייק (commit "Abut/rake corner", rt132)
גג נמוך שהרייק שלו נכנס בקיר של הקומה שמעל. הוא עיגל שלושה דברים
באותה פינה: דש אפור שבולט מהקיר, פשייה פתוחה, סופיט שנעצר. **נמדד**
(`debug_click_probe.rb` + `debug_abut_corner.rb`): כולם הנחה אחת —
הקוד לקח את השכנה של צלע ה-abut כאיבס ישר.
- `build_end_caps!` קיבל `zmap:`, `rake_flags:`, `surface:`. כשהשכנה
  רייק, הלוח נבנה **בגובה הרייק** (מעל הסופיט עד תחתית הגג) ורק מהפנים
  של לוח הרייק (`RAKE_K_IN`) עד הקיר — הלוח סוגר את ה-3/4 שלו בעצמו.
- `rake_soffit_segments(..., returns)` + `rake_returns(poly, i, abut_flags)`:
  קצה של רייק שחולק פינה עם צלע abut **לא בונה return** — הסופיט רץ ישר
  עד מישור הקיר ונחתך ישר.

### 2. קומה שכולה מתחת לקומה אחרת — בלי גג (commit "A storey fully under", rt133)
`exposed_polygon` מחזיר nil גם כש"שום קיר עליון לא חוצה" וגם כש"שום דבר
לא פתוח לשמיים" — וה-nil נפל ללולאה המלאה ונבנה גג מסביב לכל הקומה
מתחת לקירות שמעל (התמונה הראשונה שלו). **לא מהיום** — הקוד זהה בגיבויים
מ-28.08.
- `storey_covered?(walls, uppers)` → `covered_polygon?(base, up, step=6)`:
  דגימת רשת בחצי-צעד; מכוסה = אף דגימה בתוך הבסיס לא מחוץ לכיסוי.
- ב-`build_roof!`: מכוסה → הודעה בקונסול, גג ישן על הקומה נמחק, `return
  nil` (מסלול All storeys עושה `.compact`). חפיפה חלקית / לא נוגע — כמו קודם.

### 3. חלון הגג נפתח כולו (commit המרזבים)
הוא: "הוא חייב להיפתח כולו ולא רק חלק". `roof_dialog.rb` קיבל את מנגנון
`fit_height` של הדורמר (06.09): הדף מודד את עצמו ב-load והחלון עוקב.
**פתוח:** עוד 9 חלונות עם גובה קבוע (קירות, חלונות, דלתות, רצפה, קרניז,
גיליון, פלאן, גדר, ui_dialogs) — הוא אמר "כל חלון אחר". לא נגעתי, לא
ראיתי איזה חתוך. אותו מנגנון בכולם = צעד אחד.

### 4. דאונספאוט בקומה שנייה יורד למה שבאמת מתחתיו (rt134)
היה: `ground` = בסיס הקומה של הגג → כל צינור בקומה 2 נעצר ברצפת קומה 2
באוויר.
- `downspout_ground_probe(model, floor, ray:, true_ground:)` → lambda
  (x,y)→z. קומה תחתונה: תמיד הרצפה, בלי קרן. גבוה יותר: `model.raytest`
  ישר למטה מ-`floor-0.5` בנקודה שבה עומד הריצה האנכית של הצינור; פגיעה
  מעל `true_ground + DS_LANDING(6")` = גג נמוך → נוחת שם; אחרת → בסיס
  הקומה הנמוכה ביותר.
- `build_downspouts!` מקבל עכשיו גם lambda `(x,y) -> [rings, inner]`
  במקום rings אחד לכולם. `rings` רגיל עדיין עובד.

### 5. הצינור פלאש עם הגאטר
`downspout_path(z_bot, ...)` במקום `z_bot + 1.0` — הצינור התחיל אינץ'
בתוך התעלה ונראה שם כקופסה. עכשיו הראש שלו הוא תחתית הגאטר.

## פתוחים
- 9 החלונות עם גובה קבוע (ראה 3).
- rt60 — טיימר גיבוי, נופל בקביעות מהצהריים. לא גגות.
- הגרעין על הספניש "קצת נראה כמו מתכת" — ראה מסירה 11.
- ההולנדי במסלול השלד עדיין כבוי.

## דברים שנלמדו היום (בדם)
- כשהוא אומר "שברת" — קודם להראות אם זה מהיום או לא (גיבויים מתוארכים
  בשורש: `debug_v0_roof.rb` וכו'), בלי להתווכח, ואז לתקן. עבד.
- הפאה שהוא לוחץ עליה בקליק-פרוב היא ההתחלה, לא הסוף: `debug_abut_corner.rb`
  שמכוון לנקודה מהדוח ומדפיס כל פאה ב-30" סביבה, עם ספירת צלעות פתוחות,
  מצא את הסיפור כולו בריצה אחת. פאה שכל הצלעות שלה `faces=1` היא יתומה.
- בדיקה ישנה שנופלת כי החוזה השתנה — לעדכן, לא לעקוף (rt15/36/18 היום).
