# Interior Pro — מסירה 2026-09-15 (גג לכל מבנה, גג לפי גובה קיר, סקיי לייט)

קרא את זה קודם, ואז `CLAUDE.md`. הקודמת: `HANDOFF_2026-09-14.md` (שם
הסקיי לייט במלואו, ותוכנית ה-POC של V-Ray).
**אל תסמוך על הסיכום — תאמת מול הקוד החי.**

בדיקות: `cd tests` ואז `LANG=C.UTF-8 bash run_all.sh ..` — **196 ירוקות**.
אדומות קבועות: rt51/rt52/rt60 (ישנות, לא קשורות).
חדשות: **rt149** (סקיי לייט) · **rt150** (מבנים וגבהים). אומתו נגד הקוד
הישן ונפלו שם.

## 1. גג לכל מבנה (rt150)

"צריך לייצר גג לעוד בית בצד או ADU — כרגע הוא מייחס את זה רק למבנה
אחד". `eave_polygon` לוקח את הלולאה **הגדולה ביותר** של קירות הקומה,
אז מבנה שני לא קיבל גג.

`RoofManager.buildings_of(walls)` מפצל קירות שנוגעים קצה לקצה
(אותו גרף של `RoomManager.build_graph`) למבנים. `build_roof!(level:)`
בונה **גג לכל מבנה** (קריאה רקורסיבית עם `building:`), `replace:`
נשאר במבנה של הגג הישן (לפי `set_walls`), ומחיקה בבנייה מחדש נוגעת
רק בגגות שעומדים על הקירות של המבנה (`roof_on_walls?`).

**מלכודת שמדדנו:** הוא צייר מבנה חדש בזמן ש-"Work on Level 2" היה
פעיל → הקירות יצאו בקומה 2, צפים. וגם: `storey_covered?` השווה את
קומת הקרקע של הבית מול הלולאה הגדולה של הקומה השנייה — שהייתה המבנה
הצף — ובנה לבית גג נוסף מתחת לקומה שלו. `upper_over(walls, uppers)`
משאיר רק את מבנה-הקומה-העליונה שעומד **מעל** הקירות האלה.

## 2. גג לפי גובה קיר — הגראז' המונמך (rt150)

"אני רוצה שהגג ירד לגובה הקירות, כי אם ארצה להגביה את הגג אגביה את
הקירות". `eave_z` = הקיר הכי גבוה במבנה, אז הגג מעל גראז' שהונמך 18"
צף 18" מעל הקירות שלו.

`storeys_of(groups, pool)`: בתוך כל מבנה, קירות עם אותו גובה-עליון
(±1") = מבנה. הגבוה שומר את הלולאה שלו. כל ריצה **נמוכה** היא מבנה
משלה עם `closers` — קירות גבוהים ממנה שנוגעים בקצוות שלה — שסוגרים לה
את הלולאה והופכים ל-**abut edges** (הגג מת לתוכם, בלי פאשיה, כמו קומה
נמוכה מול קיר גבוה). `own_walls` נותנים את גובה האיב ואת `set_walls`;
ה-closers רק סוגרים.

### שלוש מדידות שהיו חייבות (`buildings_report.txt`, `roof_parts_report.txt`)
- **הגראז' הוא חדר בבית**: הקיר ביניהם **פנימי** (96" מול 102"). בלי
  הפול של כל קירות הקומה (`LevelManager.walls_of_level`) אף לולאה לא
  נסגרה. למבנה הגבוה ה-closers הם קצוות רגילים (`abut: false`), לנמוך
  — abut. כל גובה מתאים לסגירה של הגבוה.
- **הפינה הכמעט-ישרה**: הקיר הפנימי ב-y 2.8 מול קיר החזית ב-y 2.5.
  שני פסי איב במרחק 0.3" — הפינה שלהם 7000" משם, ופאה של פאשיה רצה
  x -7178..2127. `drop_flat_corners(poly, edata, 3.0)` מבטל פינה
  שפונה פחות מ-3° ומאחד את שני הקטעים (שומר את הקיר הארוך). מופעל רק
  כשיש closers — `eave_polygon(walls, oh, flat_tol)`, ברירת מחדל nil.
- `touching` בודק קצה של קיר נמוך **על הקו** של קיר גבוה (T), לא רק
  פינה-לפינה — הגראז' נתלה על צד הבית באמצע הקיר.

**מה הוא רואה עכשיו:** גג הגראז' על הקירות שלו, גג הבית במקומו, פאשיה
וגטרס בסדר. "נראה טוב".

## 3. הצעד הבא — **חיבור שני הגגות** (סעיף 1 המקורי שלו)

"צריך שהגגות יתחברו, כרגע זה שני גגות נפרדים". היום גג הגראז' מת
אנכית בקיר הבית (abut), וגג הבית שומר איב+פאשיה+גטר מעל הגראז'.

מה שצריך: מישורי הגג הנמוך ממשיכים לתוך מישור גג הבית עד **וואלי**,
והפאשיה/גטר/סופיט של הבית **נחתכים** בין שתי רגלי הוואלי. זה שינוי
עמוק: `build_framed_geometry!` בונה main + wings מ-**z0 אחד**
(`st[:z0]`). כיוונים שנבדקו על הנייר בלבד:
- `zmap` (גובה לכל קודקוד איב) כבר קיים ומזין את הפאשיה/סופיט/רייק —
  אז איבים בגבהים שונים **ניתנים לייצוג** במורד הזרם.
- `build_wing_rect!(st, rect, mouth, gabled, cov)` — `cov` הוא
  `framed_cover_z` של ה-main; אם האגף ייבנה עם z0 משלו (עותק של `st`)
  וה-cover יישאר בגובה ה-main, החיתוך אולי ייפול לוואלי לבד. **למדוד,
  לא להניח.**
- אלטרנטיבה: להשאיר שני גגות, ולבנות רק את הוואלי כחיתוך: להאריך את
  מישורי הגראז' מעבר לקיר עד מישור הבית, ולחתוך את חלקי הטרים של
  הבית בין רגלי הוואלי. פחות אלגנטי, פחות סיכון.
בכל מקרה: דגל כיבוי מיד, commit לפני, ולבנות פעמיים (דלוק/כבוי) אם
סקצ'אפ נופל — `debug_apply_probe.rb`.

## 4. סקיי לייט — סיכום (הפירוט ב-HANDOFF_2026-09-14)
- Place / Edit / Move / Delete, קבוצה משלהם בסרגל אחרי Downspout,
  **אייקונים משלהם** (`icons/skylight_*.svg`).
- החלון בתוך החור (משקוף + שפה 1" + זכוכית שקועה 1"), פיר לבן לתקרה,
  שורד בנייה מחדש של הגג, הרעפים נעצרים במסגרת (`hole_th_proc`).
- **פתוח**: פס חשוף ליד רעף שנחתך (ספניש/רומן) — פלאשינג נבנה
  ונפסל (הדגל `USE_SKYLIGHT_FLASHING` כבוי), חיתוך רעף לאורכו נדחה.

## 5. מה שנשאר מהרשימה שלו
1. **חיבור גג נמוך לגבוה** — סעיף 3 למעלה. הבא.
2. גייבל באמצע הגג שהפאשיה שלו ביישור עם הקיר — צריך תמונה מהמודל.
3. אחרי הגגות: **POC של V-Ray** (HANDOFF_2026-09-14, הסעיף האחרון).

## פתוחים ישנים
rt60 · `build_tube!: undefined method 'erase_entity'` · כיוון השד על
גג עם בעלים · קרשים על גמלון הדורמר עצמו · `plan_generator.rb` מצייר
קיר מעוקל ישר.

## קבצי מדידה מהיומיים האלה (בשורש)
`debug_siding_run.rb` · `debug_siding_gap.rb` · `debug_window_head.rb`
· `debug_dormer_spec.rb` · `debug_skylight_layers.rb` ·
`debug_skylight_tiles.rb` · `debug_buildings.rb` (מבנים, לולאות,
closers) · `debug_roof_parts.rb` (ממה כל גג עשוי, פאות שברחו).

## לשימוש
- מבנה חדש: קודם **Level: Work on Level 1**, ואז לצייר.
- גראז' מונמך: Floors → שורת הגראז' → Level (למשל −18) + Drop walls
  with floor → Apply. ואז Build לגג.
- דורמר לא נבנה מחדש עם הגג — לולאת `replace_dormer!` בסוף המסירה
  הקודמת.
