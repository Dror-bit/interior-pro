# Interior Pro — מסירה 2026-08-18C (רעפי גג, סימון הצמדה, חלון בקיר הלא נכון)

קרא את זה קודם, ואז `CLAUDE.md`. **אל תסמוך על הסיכום — תאמת מול הקוד החי.**
הקודמים: `HANDOFF_2026-08-18B.md`, `HANDOFF_2026-08-18.md`.
כללי העבודה: `HANDOFF_2026-08-14.md` §0. **תקרא אותם ברצינות.**

התיקייה החיה:
`C:\Users\rordt\AppData\Roaming\SketchUp\SketchUp 2024\SketchUp\Plugins\interior_pro`

**הבדיקות: 120 חבילות ירוקות** (74 רובי + 46 ג'אווהסקריפט), אפס אדומות.
ספרתי מריצה על התיקייה החיה. היו 116 בתחילת הסבב.

---

## 0. הדבר הכי חשוב במסמך הזה

> **מדדנו איך Instant Roof (Vali Architects) בונה גג רעפים — בפרויקט האמיתי
> של המשתמש — ובנינו על סמך המדידה, לא על סמך ניחוש.**

הכלי: `debug_valiroof.rb` (בשורש). הפלט: `valiroof_report.txt`.
גג של 63×59 רגל בבית שלו. מה שנמצא:

| מה | כמה | פאות לכל אחד |
|---|---|---|
| רצועת שורה (קורס) | ~420, כל אחת ייחודית | **5** |
| `va_BirdStop` בקצה האיב | 408, **קומפוננטה אחת** משוכפלת | **1** |
| הרעף עצמו | טקסטורה `Roofing Tile Spanish.jpg`, 30×24.7" | — |

**~2,500 פאות לגג שלם.** אני הערכתי 40,000 וטעיתי פי 16.

**הכלל שהם עובדים לפיו, וגם אנחנו:**
**בונים 3D רק איפה שרואים את הצללית. השדה שטוח עם טקסטורה.**

---

## 1. מה נכנס

### א. סימון ההצמדה בעורך ה-2D לא נעלם (`t48`, 13 בדיקות)
ראה `HANDOFF_2026-08-18B.md`. הדגל `lockedNow` נשאר תקוע `true` אחרי נעילת
כיוון אחת, וההצמדה עבדה בלי שרואים אותה. תוקן בשורה אחת ב-`snapPoint`.
**המשתמש אישר שזה עובד.**

### ב. `roof_tile_math.rb` — מתמטיקה טהורה לשורות רעפים (`rt72`, 76 בדיקות)
קובץ חדש, **אפס API של סקצ'אפ**, בדיוק כמו `arc_math.rb`.

- `plane_frame(normal)` — מערכת צירים למישור גג: `u` לרוחב (אופקי, לאורך
  האיב), `v` **במעלה השיפוע לפי אורך אמיתי** ולא היטל. `v=0` על האיב.
- `plane_uv(points, normal)` — מישור תלת-מימדי → מצולע דו-מימדי בצירים שלו.
- `spans_at(poly, c)` — סורק אופקי. **זה מה שחותך שורה בהיפ, בעמק ובדורמר.**
  דורמר מחזיר שני קטעים לבד, בלי אף מקרה מיוחד.
- `courses(plane, shape)` — כל השורות, כבר חתוכות.
- `scallop(...)` / `drop_at(...)` — קו הגל בתחתית שורה. **פאזה גלובלית**, כך
  שהרעפים מסתדרים בין שורות וגם משני צידי דורמר.
- `edge_slots(...)` — איפה יושבות חתיכות הקצה התלת-מימדיות.
- `estimate(...)` — כמה פאות זה יעלה, **לפני** שבונים.
- `shapes` — טבלת ארבעת החומרים (רוחב רעף, חשיפה, בליטה, גל, שזרוג).

### ג. ארבעה חומרי גג בתפריט (`rt73`, 54 בדיקות)
`Barrel Tile (Spanish S)` · `Roman Tile (flat pan)` · `Flat Slate Tile` ·
`Standing Seam Metal`. ארבע טקסטורות חדשות ב-`textures/`, **מיוצרות
פרוצדורלית, אפורות, נטולות תפר**. בורר הצבע צובע אותן, כמו שינגלס.

גדלי הטקסטורות אינם שרירותיים: כל אחת היא מספר שלם של רעפים ברוחב ומספר
שלם של שורות בגובה, תואם ל-`RoofTileMath.shapes`. `rt73` נופלת אם זה נשבר.

**המשתמש אישר שהטקסטורה עובדת.** תלת-מימד עוד לא נבנה — ראה §4.

### ד. חלון לא נחתך בקיר שלא לחצת עליו (`rt74`, 34 בדיקות)
**התסמין שלו:** *"שמתי את החלון בצד אחד והוא יצר לי חור במקום שלא ביקשתי."*

**המדידה, מהקונסול שלו:** `[WindowTool] n_offset=87.8809`. בחלון תקין הערך
הזה הוא ~2.5 (חצי עובי קיר). כאן הקיר שנבחר היה **שבעה רגל מאחורי** נקודת
הלחיצה.

**הסיבה:** `PickHelper` מחזיר את **כל** מה שעל קו הראייה, קרוב ורחוק.
`find_wall_under_cursor` לקח את המסלול הראשון שרק **הכיל** קיר — אז כשהדבר
הקרוב לא היה קיר (עמוד, חלון קיים, בליטת גג) הוא המשיך פנימה ותפס קיר בצד
השני של הבית.

**התיקון:** `click_on_wall?` בודק שהנקודה באמת על הקיר, **באותה אריתמטיקה
בדיוק** ש-`cut_window_opening` משתמשת בה — כדי שהמסננת **תנבא** את החיתוך
ולא תנחש אותו. מועמד שנכשל **מדולג**, לא נחתך. סבולת: חצי עובי + 3".
קיר מקושת מקבל את הקשת שלו בנוסף, אחרת כל חלון בחדר עגול היה נדחה.

---

## 2. מלכודת ה-`reload!` — עלתה לנו שוב, בפעם השלישית

```ruby
X = { ... }.freeze unless const_defined?(:X, false)   # ← לא מתעדכן ב-reload!
```

ארבע הטקסטורות היו על הדיסק, התפריט הציג אותן, וכל גג יצא צבע שטוח —
כי `ROOF_TEXTURES` **בזיכרון** עדיין החזיק ערך אחד. המשתמש אמר "שום דבר לא
עבד", ובצדק.

זו אותה מלכודת של `DEFAULT_STATE` ב-2026-08-14, **שכתובה במסירה שקראתי
באותו בוקר.**

**התיקון:** `ROOF_TEXTURES` ו-`SHAPES` הן **מתודות** עכשיו
(`RoofManager.roof_textures`, `RoofTileMath.shapes`). `rt73` נופלת אם מישהו
יהפוך אחת מהן בחזרה לקבוע.

**כלל:** כל טבלה שבן אדם יערוך — מתודה. קבוע רק למספר שבאמת לא משתנה.

---

## 3. שלוש טעויות שלי בסבב הזה — כתובות כדי שלא תחזור עליהן

### א. סיננתי החוצה בדיוק את המקום שנשבר
כשחיפשתי מי משתמש ב-`ROOF_TEXTURES` הרצתי
`grep -rn ... | grep -v "^./tests/"`. `rt18` השתמשה בו ונפלה.
**חבילת הבדיקות תפסה, לא אני.** אל תסנן תיקיות כשאתה מחפש שימושים.

### ב. הסקתי מסדר הקוד במקום למדוד
ראיתי שבכלי הקו העיגול מצויר לפני רצועות הקירות, והכרזתי שעיגול על פינת
קיר מכוסה. נתתי למשתמש בדיקה של שנייה — "רחף על פינת קיר, רואה ירוק?" —
והוא ענה **"הוא מופיע ירוק, הכל טוב"**. **אין שם באג.** לא תיקנתי.

### ג. הערכת עלות מהאוויר
"40,000 פאות יחנקו את סקצ'אפ." המדידה: 2,500. המשתמש שאל *"אז איך הוא עושה
את זה ב-Vali וזה סבבה?"* והשאלה הזאת היא שהחזירה אותנו למציאות.
**אל תיתן מספר הנדסי בלי למדוד או לומר במפורש שזו הערכה.**

---

## 4. מה פתוח — לפי סדר

### 1. גג תלת-מימדי, צעד 2ב — **הצעד הבא**
המתמטיקה מוכנה ובדוקה (`roof_tile_math.rb` + `rt72`). מה שנשאר: לבנות את
הגיאומטריה בפועל ב-`roof_manager.rb`.

**איפה להתחבר:** אחרי בלוק הרידג' קאפ ב-`build_roof!`. שם כבר יש
`cap_faces` — **פאות הקליפה העליונה בלבד**, בדיוק מה שצריך.
**התבנית לחיקוי:** `build_ridge_caps!` / `build_cap_piece!` — תת-קבוצה,
פאות מפורשות, צביעה, `rescue` שמחזיר nil. **ובלם כמות**
(`MAX_RIDGE_CAP_PIECES = 1500` → נופל לרצועה אחת). תעשה אותו דבר.

שלושה חלקים: (א) מדרגה לכל שורה, (ב) חתיכת קצה באיב **וגם ברייק** —
Vali השאיר את הרייק שטוח, זה הפער שלנו, (ג) הטקסטורה בשדה (כבר יש).
דגל כיבוי: `USE_TILE_COURSES`.

### 2. קיר למעלה גורר קיר למטה — **מדוד, לא פתור**
`debug_wall_ids.rb` → `wall_ids_report.txt`. התוצאה:

- **0 מזהים כפולים.** התיקון של 2026-08-18 עבד; ההעתק-הדבק כבר לא הסיבה.
- **44 קצוות שנוגעים בין הקומות, רובם ב-0.000"** — וזה נכון, ככה בית בנוי.

כלומר **המנגנון הוא מיקום, לא מזהה.** בדקתי ושלושה מקומות **כן** מסננים
לפי קומה: `find_neighbor_at`, `weld_drifted_end!`, `wall_stretch_tool`.
**עוד לא נמצא מי כן דולף.** חשודים שלא נבדקו: `plan_editor.rb` (מסלול
ה-Apply), `wall_move_tool.rb`, `RoomManager`.

**הכלי מוכן:** `debug_move_watch.rb`. מריצים אותו (צילום), מזיזים קיר אחד,
מריצים שוב — והוא מדפיס בדיוק איזה קיר בקומה השנייה זז ובכמה.
**צילום כבר נלקח ב-18.8 ולא הושלמה ההשוואה.** תתחיל משם.
**ותשאל אותו קודם: 2D או סקצ'אפ.** זה חוסך חצי שעה בקובץ הלא נכון.

### 3. ידית ירוקה שמסתובבת כמו מחוג — בקשה שלו, 2026-08-18
*"שברגע שאני לוחץ על הירוק לזוז כמו מחוג ימינה ושמאלה."*
היום הידית הירוקה רק **מאריכה ומקצרת על אותו קו**
(`dragLenLimit(d, pinned, sgn, wantLen)` ב-`plan_editor.rb`). הוא רוצה
שהקצה **יסתובב סביב הקצה השני**. גם לקו וגם לקיר.

### 4. צבע בתוכניות — **הבהרה, המסמכים הישנים מטעים**
`HANDOFF_2026-08-17C.md` §4.1 מתאר "צבע קו" גורף. **המשתמש תיקן אותי
מפורשות:** מה שהוא ביקש זה **צבע למידות ולטקסטים שהוא כותב** — לא לכל
התוכנית. הצעתי צבע-לפי-שכבה והוא דחה. תכנן לפי זה.

### 5. שני פערים קטנטנים בקומה 1
`fix_ends_report.txt`: שתי פינות, 0.17" ו-0.09". תצוגה בלבד, לא בוצע.
לסגור: `InteriorPro::FixWallEnds.run(apply: true)`.

### 6. הרדסקייפ / לנדסקייפ
`LANDSCAPE_ROADMAP.md`. **הוא ביקש במפורש להשאיר לסוף** — רוצה ללמוד על
הנושא לפני שנכנסים.

### 7. גגות — השאר
`ROOFS_ROADMAP.md`, כולל החלטות 18.8 בסוף הקובץ (3 סוגי טיל, Standing Seam,
דורמרים). **שים לב: הרוד-מאפ לא מעודכן** — הוא טוען שרידג' קאפ לא קיים,
והוא כן קיים מ-10.8. תאמת מול הקוד.
**שתי שאלות פתוחות אליו:** מה חסר בגג השטוח, ומאיפה מתחילים.

---

## 5. כלי מדידה שנוספו (כולם קריאה בלבד, כותבים דוח לקובץ)

| קובץ | עונה על |
|---|---|
| `debug_valiroof.rb` | איך תוסף אחר בנה גג — קומפוננטות מול גיאומטריה ייחודית |
| `debug_roof_loop.rb` | למה הגג אומר "אין טבעת סגורה". דרגת צומת = התשובה |
| `debug_wall_ids.rb` | מזהים כפולים מול קצוות שנוגעים בין קומות |
| `debug_move_watch.rb` | **תופס בפעולה** מה זז בלי שביקשת. אותה שורה פעמיים |

**המשתמש לא קורא פלט של קוד.** אתה קורא את הדוח בעצמך.

---

## 6. מה למדתי הסבב הזה

1. **§2. טבלה שבן אדם עורך חייבת להיות מתודה.** שלוש פעמים כבר.
2. **§3א. אל תסנן תיקיות כשאתה מחפש מי משתמש במשהו.**
3. **§3ג. אל תיתן מספר הנדסי בלי למדוד.** *"אז איך הוא עושה את זה ב-Vali
   וזה סבבה?"* — שאלה אחת שלו שמוטטה הערכה שלמה שלי.
4. **תמדוד את המוצר המתחרה במקום לתכנן מאפס.** חצי שעה עם
   `debug_valiroof.rb` שינתה את כל הארכיטקטורה, לטובה ולזול.
5. **המספר שבקונסול הוא הרמז.** `n_offset=87.88` פתר את באג החלון לפני
   שקראתי שורת קוד אחת. תלמד מה כל מספר שהתוסף מדפיס אומר.
6. **תוודא שהשומר נופל, בכל תיקון.** בסבב הזה עשיתי את זה ארבע פעמים
   (t48, rt72, rt73, rt74) — כולל להחזיר את הבאג ולראות אדום.
7. **המשתמש עובד על בית אמיתי בזמן אמת.** כשהוא כותב "שום דבר לא עבד" —
   הוא תקוע, לא מתלונן. תעצור, תמדוד, תענה קצר.
8. **צ'אט ארוך מתדרדר.** הסבב הזה נגע בעורך ה-2D, בגגות, בחלונות ובקירות.
   זה יותר מדי לצ'אט אחד. תפצל מוקדם יותר.

---

## 7. מגבלה שהתגלתה בסוף הסבב — קירות חייבים להיות ברמה העליונה

המשתמש הכניס את **כל הבית לקבוצה אחת**, ומאותו רגע "הקירות ב-2D נמחקו לי
לגמרי" ודברים הפסיקו לעבוד — **גם כשעבד מתוך הקבוצה**.

**הם לא נמחקו.** התוסף סורק `model.entities` — **הרמה העליונה בלבד**.
ספירה מהירה: `wall_tool.rb` 3, `room_manager.rb` 3, `level_manager.rb` 4,
`plan_editor.rb` 10. קיר בתוך קבוצה פשוט בלתי נראה לכל אלה.

`LevelManager.walls_of_level` הוא הדוגמה הנקייה:

```ruby
Sketchup.active_model.entities.grep(Sketchup::Group).select { ... }
```

**מה נאמר לו:** לעת עתה לא להכניס את הבית לקבוצה. הוא החזיר את זה אחורה.

**התיקון האמיתי, כשמגיעים אליו:** מקום מרכזי אחד שמחזיר את כל הקירות —
`model.entities` **וגם** `model.active_entities`, ואולי סריקה רקורסיבית —
ואז כל הקוראים עוברים דרכו. **אל תתקן 20 מקומות בנפרד.** ותיזהר: שינוי
כזה נוגע בקירות, בחדרים, בקומות ובעורך ה-2D בבת אחת — כלומר בכל מה
שעובד היום. צעד בפני עצמו, עם בדיקות משלו.

**נקודה פתוחה שלא נמדדה:** `find_neighbor_at` ו-`apply_miter` משתמשים
ב-`model.active_entities` ולא ב-`model.entities`. שני הסורקים האלה לא
מסכימים ביניהם על מה זה "כל הקירות", וזה חשוד בפני עצמו.

---

## 8. הבאג שחוסם אותו עכשיו — חלון לא נכנס בקומה העליונה

**זה הדבר הראשון לעשות. הוא חסום עליו.**

**התסמין (2026-08-18, בסוף הסבב):** *"זה עובד יותר טוב בקומה התחתונה אבל
זה לא עובד בקומה העליונה."* אחרי שנכנס תיקון `rt74`.

**האבחנה — חשד מבוסס, לא הוכח. אל תתקן לפני שמדדת.**

`cut_window_opening` ב-`window_tool.rb` קורא `start_x/start_y/end_x/end_y`
מהאטריביוטים — והם **מקומיים לקבוצה** — ומשווה אותם לנקודת הלחיצה, שהיא
**בקואורדינטות עולם**:

```ruby
drawn_start = Geom::Point3d.new(sx, sy, 0)          # LOCAL
click_xy    = Geom::Point3d.new(picked_point.x, picked_point.y, 0)   # WORLD
to_click    = click_xy - cline_start
n_offset    = to_click.dot(n)
```

קיר בקומה 1 עם טרנספורמציה אחדותית: מקומי == עולמי, הכול עובד.
קיר עם הזזה ב-x/y — קומה 2, או קיר שהועתק — **המספר יוצא שגוי**.

זה גם מסביר למה `n_offset` יצא **87.88** מלכתחילה. ייתכן מאוד שזה היה
הגורם האמיתי, ולא עומק הבחירה של `PickHelper` ש-`rt74` תיקנה. **שני
תיקונים אמיתיים, שני באגים שונים — אל תניח שהראשון היה מיותר.**

זו בדיוק המלכודת מ-`HANDOFF_2026-08-18.md` §2: **מקומי מול עולמי.**

**המדידה שתכריע (שורה אחת ב-Ruby Console, הוא לא הריץ אותה):**

```ruby
puts Sketchup.active_model.entities.grep(Sketchup::Group).select{|g| g.get_attribute('InteriorPro','type')=='wall' && (g.get_attribute('InteriorPro','level')||1).to_i==2}.map{|g| g.transformation.origin.to_a.map{|v| v.round(1)}}.uniq.inspect
```

- `[[0.0, 0.0, 106.0]]` — הרמה בלבד. האבחנה **שגויה**, תחפש במקום אחר.
- מספרים שאינם אפס ב-x או ב-y — **האבחנה נכונה.**

**התיקון, אם אושר:** להמיר את נקודת הלחיצה למרחב **המקומי של הקיר** לפני
כל החישוב, בשני המקומות:

```ruby
inv = wall_group.transformation.inverse
local_pt = picked_point.transform(inv)
```

ואז להשתמש ב-`local_pt.x/.y` במקום ב-`picked_point.x/.y`.
נכון לכל מקרה — אחדותי, הרמה בלבד, הזזה, סיבוב — ו**זהה מתמטית** למה
שקורה היום כשהטרנספורמציה אחדותית או הרמה טהורה. כלומר הוא לא יכול לשבור
את קומה 1.

**שני המקומות:** `wall_click_offsets` (המסננת החדשה) ו-`cut_window_opening`
(החיתוך עצמו). **חייבים להשתנות ביחד** — אם המסננת והחותך יחשבו אחרת,
המסננת חסרת ערך. יש הערה על זה בקוד, אל תמחק אותה.

**להרחיב את `rt74`:** קיר עם `transformation` שיש בה הזזה ב-x/y, ולוודא
שלחיצה על הפאה שלו **מתקבלת**. הבדיקה הזאת נכשלת על הקוד הנוכחי — תריץ
אותה לפני התיקון ותראה אותה אדומה.

**זהירות:** ה-stub של הבדיקות — `tests/sketchup_stub.rb` — `Point3d#transform`
שם הוא `def transform(_t); self; end`, כלומר **לא עושה כלום**. בדיקה
שתסתמך עליו תעבור גם אם התיקון שגוי. **תתקן את הסטאב קודם** — זה כלל
קיים בפרויקט (`HANDOFF_2026-08-14.md` §0: "אם הסטאב מתנהג אחרת מסקצ'אפ,
תקן את הסטאב").
