# Interior Pro — מסירה 2026-08-14

קרא את זה קודם, ואז `CLAUDE.md`. אל תסמוך על הסיכום — תאמת מול הקוד החי.
הקובץ הקודם: `HANDOFF_2026-08-13.md`.

---

## 0. כללי עבודה (לא השתנו — תקרא אותם ברצינות)

- **אתה לא מחליט כלום בלעדיו.** מביא אפשרויות, הוא בוחר, ורק אז נוגעים בקוד.
- **עברית, משפט-שניים, כמו לילד בן חמש.** כל פקודה בקופסה נפרדת עם ציון
  לאן מדביקים (Ruby Console / CMD).
- **הוא לא קורא פלט של קוד.** אל תבקש ממנו להעתיק רשימות. יש סקריפטי
  `debug_*.rb` שכותבים דוח לקובץ בתיקייה — אתה קורא אותו בעצמך.
- **הקבצים שאתה כותב נכנסים ישר לתיקייה החיה.** אתה לא מריץ git, נותן לו
  פקודות CMD.
- **דבר ויזואלי = לא מנחשים. מודדים.**
- **"כלום לא השתנה" = הקוד לא נטען.** תוכיח שנטען לפני שאתה מחפש באג.
- **סקריפט בדיקה לא מעתיק את הקוד שהוא בודק — הוא קורא לו.**
- **הבדיקות רצות בענן, אין לו רובי.** אם הסטאב מתנהג אחרת מסקצ'אפ —
  **תקן את הסטאב.** קרה שוב הסבב הזה (ראה §3).

התיקייה החיה:
`C:\Users\rordt\AppData\Roaming\SketchUp\SketchUp 2024\SketchUp\Plugins\interior_pro`

לא לגעת: גג, דלתות, חלונות, סיידינג. ה-L בפינות הסיידינג מוקפא בכוונה.

---

## 1. מה נסגר בסבב הזה

### א. תמונות אמיתיות ב-PDF (rt47)
`plan_pdf.rb` צייר מלבן ריק במקום תמונה. עכשיו:

- **JPEG** נכנס לקובץ בדיוק כמו שהוא — PDF מפענח DCTDecode לבד. אפס המרות.
- **PNG** נפרס: בלי שקיפות ה-IDAT עובר כמו שהוא עם Predictor 15; עם שקיפות
  הוא מפוענח, מפוצל לצבע ולמסכה, ונדחס מחדש. תמיכה ב-ct 0/2/3/4/6.
- תמונה שומרת על הפרופורציות שלה ומתמרכזת בקופסה (`fit_box`).
- קובץ חסר או פגום → המסגרת הריקה הישנה. הייצוא לא נופל.
- אותה תמונה פעמיים בדף = XObject אחד.

### ב. דף לכל תמונה (rt48)
`PlanDoc.new_image_sheet` — תמונה אחת ממלאת את כל הדף מעל הטייטל-בלוק.
זה בדיוק מה שהוא ביקש: *"הכי פשוט בעולם — תמונה גדולה על כל הדף חוץ
מלמטה שיש את הטמפלט."* הרינדורים נכנסים אחרי התוכנית והטבלאות, אז
A-101 תמיד נשארת תוכנית הרצפה.

### ג. העלאת תמונות — כפתור אחד (rt50, rt51)
`debug_drop.rb` מדד אצלו: גרירה **עובדת**, אבל כל קובץ מגיע עם
`path property: NOT THERE`. החלון מקבל תוכן, לא כתובת. אז הבייטים
עוברים לרובי בחתיכות ונכתבים ל-`InteriorPro_images` ליד קובץ הסקצ'אפ.

- כפתור **אחד**: "העלאת תמונות". משם אחת או עשרים. וגם גרירה לכל החלון.
- הוא ראה שלושה כפתורים ואמר שהם אותו דבר. rt50 נכשל אם מישהו יחזיר עוד אחד.
- קובץ שכבר הועתק (אותו שם, אותו גודל) — מדלגים עליו מיד.

### ד. רשימת דפים אחת (rt48)
בורר-דפים + רשימת תמונות → **רשימה אחת של כל הדפים**. לחיצה = מעבר,
× = מחיקה. לדף התוכנית אין × בכוונה. כל `Page` נושא `kind` ו-`ref`,
כי רק רובי יודע מה "למחוק דף" אומר.

### ה. מידות והערות ידניות (rt52)
שלושה כלים בצד: ✋ גרירה, ↔ קו מידה, A טקסט.

**ההחלטה החשובה:** הסימונים חיים ב**אינצ'ים של המודל על הקנבס**, לא על
הנייר. תשנה קנה מידה, גודל דף, או תזיז את התוכנית — המידה נשארת על הקיר
שהיא מודדת. שכבה `NOTES`, אפשר לכבות.

הטקסט בגובה `DIM_TEXT_H` — אותו גובה של המידות האוטומטיות.

---

## 2. איפה הקוד

| קובץ | מה נוסף |
|---|---|
| `plan_pdf.rb` | `load_image`, `jpeg_info`, `png_info`, `png_unfilter`, `ImageSet`, `fit_box`, `image_object` |
| `plan_doc.rb` | `IMAGE_LAYER`, `new_image_sheet`, `place_image_full!`, `Page#kind/#ref/#sheet_number` |
| `plan_canvas.rb` | `MARK_LAYER`, `apply_marks!`, `draw_mark_dim`, `draw_mark_note`, `mark_text_h` |
| `plan_sheet_dialog.rb` | `default_state` (מתודה!), `file_url`, `drop_dir`, `safe_name`, `free_name`, `delete_page`, כלי המידות |
| `plan_geometry.rb` | דילוג על ישות בעייתית + `skipped` בדוח |

מצב חדש ב-state: `images`, `image_title`, `marks`.

---

## 3. שלוש מלכודות שנפלנו בהן — אל תפול שוב

### א. קבוע מוגן ב-`unless const_defined?` לא מתעדכן ב-`reload!`
`DEFAULT_STATE` היה קבוע. ביום שנוסף המפתח `images`, `save_state` המשיך
לזרוק אותו: המשתמש בחר תיקייה, החלון אמר **"נוספו 18 תמונות"**, וכולן
נמחקו בדרך למודל. רק restart מלא היה מתקן, ושום דבר לא אמר את זה.

**הכלל:** רשימת הגדרות = **מתודה**, לא קבוע. `default_state` + `extra_state_keys`.
rt49 טוען את הקובץ פעמיים (כמו `reload!`) ונכשל אם זה חוזר.

### ב. `Sketchup::ImageRep` ב-SU2024 — אין `resize`, אין `save_as`
בניתי מנגנון תמונות-קטנות שהחזיר `nil` תמיד, והמשתמש ראה מסגרת מקווקוות.
`debug_images.rb` מדד: `responds to resize: false`, `responds to save_as: false`,
אבל `file:/// loaded: YES 3840x2160`. **החלון קורא מהדיסק מצוין.**
זרקתי את כל המנגנון ומצביע ישר על הקובץ.

הנתיבים שלו הם `15723 E La Belle St, La Puente, CA 91745` — רווחים
ופסיקים. `file_url` מקודד לפי **בייטים** (גם עברית). rt50 מכסה.

### ג. קבוע בתוך `class << self` הוא לא `Module::CONST`
כתבתי `PlanGenerator::DIM_TEXT_H`. הוא מוגדר בתוך `class << self`, אז זה
`NameError`, שנבלע ב-rescue והחזיר 5.0 — **במקרה אותו מספר**. ביום שמישהו
היה משנה אותו, המידות הידניות היו נשארות מאחור בשקט.
הכתיב הנכון: `PlanGenerator.singleton_class.const_get(:DIM_TEXT_H)`.

**בשלושת המקרים הבדיקה תפסה טענה שגויה שלי, לא באג של מישהו אחר.**
תכתוב את הבדיקה לפני שאתה מאמין לעצמך.

---

## 3א. סוף הסבב — עורך התוכנית (2026-08-14 ערב)

### מה נסגר
- **מעלות בכל פינה, תמיד** (t39, rt54). אפור = זווית עגולה, אדום = לא.
  לחצן ימני על נייר ריק = להסתיר/להציג.
- **הגדרת מעלות** — לוחצים על המספר, אומרים 90, הקיר מסתובב סביב הפינה.
  `turn_wall!` עובר דרך `stretch_wall!` עם `aim` — **אין עותק שני** של
  עבודת השותף-בפינה, הפתחים, הבנייה מחדש והחיבור.
- **ESC תמיד חוזר ל-Select**, והעורך **נפתח** ב-Select (t38).
  סדר האתחול קריטי: `setLineTool` קופץ למצב Line בכוונה, אז
  `setMode('sel')` חייב לבוא **אחרון**.
- **אורך קיר באינצ'ים** — `parseLen` מקבל `11'-3.5"`, `11 6 1/2`, `3 ft 6 in`.
  מספר לבד בשאלת אורך קיר = **רגל**.
- **Undo כרונולוגי** (t40). היה סדר **העדפות** (קודם שינוי צורה, אחרת
  מחק את הקיר הכחול האחרון...), אז שינוי אורך — שלא היה ברשימה בכלל —
  נפל עד "מחק קיר". עכשיו יש **רשימה אחת לפי סדר הזמן**: `editHist`,
  `histLocal` (מצלם `pending`/`pendingSketches`/`dims`) ו-`histModel`
  (מסמן שהצעד הבא שייך ל-Undo של סקצ'אפ). `histUndo()` לוקח מהראש.
  **הכלל: כל פעולה שמשנה משהו חייבת לרשום שורה. אם לא — Undo יעבור מעליה
  ויקח משהו אחר.** t40 בודק את 15 הקריאות למודל אחת-אחת.

### שני באגים שהוא צעק עליהם, ובצדק — שניהם "נכתב ולא ניתן להגיע אליו" (t41)
הקוד לשני הדברים היה כתוב. אף אחד מהם לא היה נגיש, משתי סיבות שונות,
ו-t39 לא תפס כי הוא בדק רק את צד ה**ציור**.

1. **`draw()` איפסה את `dimTags` אחרי `drawCornerAngles`.** כל קופסת-לחיצה
   של זווית נזרקה באותו פריים שבו נוצרה, אז לחיצה על המספר לא פגעה בכלום.
   הוא ראה את המעלות ולא יכול היה לשנות אותן. **התיקון: מאפסים לפני.**
2. **`hitWallEnd` פתחה ב-`sel.type !== 'wall' → null`.** קיר **כחול** (שעוד
   לא עבר Apply) הוא `sel.type === 'pending'`, אז תפיסת הנקודה הירוקה שלו
   החזירה null ושום גרירה לא התחילה — למרות שהנקודה הירוקה מצוירת על קיר
   כחול בדיוק כמו על אפור.

**הלקח לכתוב על הקיר:** בדיקה שקוראת את המקור של פונקציית הציור **לא**
מוכיחה שאפשר להגיע אליה. t41 מריץ את `drawCornerAngles` באמת ואז שואל את
`hitDimTag` אם נשארה קופסה — וזה מה שתפס. כשהוא אומר "ביקשתי ולא עשית",
תבדוק קודם **נגישות**, לא קיום.

### עוד פתוח בעורך
- **הגדרת מעלות בפינה של קיר כחול** — כרגע אומר "הקיר הזה עוד לא עבר
  Apply to Model". אורך כן עובד על כחול (`setPendingLength`). **לשאול אותו
  אם רוצה שזה יעבוד גם על כחול לפני שכותבים.**
- מספר לבד = רגל גם ב-VCB **בזמן ציור** קיר (הוצע, לא נעשה).
- קיצור "התאם למסך" אחרי שסרגל הזום הוסר (הוצע, לא נעשה).

---

## 4. הצעד הבא

### פתוח ולא נבדק אצלו
הוא היה מחוץ לבית כשזה נכתב. **לא נבדקו בסקצ'אפ:**
- כלי המידות וההערות (§1ה)
- דילוג על ישות בעייתית (§5)

תבקש ממנו לאמת לפני שממשיכים הלאה.

### לפי הסדר שהוא הציב
1. **הטייטל-בלוק האמיתי שלו** — הוא ישלח תמונה, מציירים בקוד, לוגו כתמונה.
   **חסום עד שהוא שולח את התמונה.**
2. **עוד יצרני שכבות** — אריחים, חשמל, ריהוט, צמחייה. כל אחד = צ'קבוקס.
3. **מנוע LayOut** — הצינור הוכח ב-`debug_layout_spike.rb`, לא חובר.
4. **אליוויישנים** — הוא הזכיר אותם כיעד; לא נגענו.

---

## 5. הבאג שעוד לא נפתר

בקונסולה שלו, פעמיים:

```
[Sheet] add_selection: no implicit conversion to Transformation
```

**הסיבה לא ידועה.** ההודעה הגיעה בלי stack trace, אז אין מה לקרוא.
לא ניחשתי תיקון. מה שכן נעשה:

- כל ה-rescue ב-`plan_sheet_dialog` מדפיסים עכשיו `e.class` + 4 שורות backtrace.
- `harvest` עוטף כל ישות בנפרד: אחת בעייתית מדולגת, השאר עוברות,
  והחלון אומר "N דולגו" (rt53).
- `mul` לא מתפוצץ על צירוף לא חוקי — שומר את החיצוני וממשיך.

**בפעם הבאה שזה קורה אצלו — תבקש את שורות הקונסולה. עכשיו הן יגידו איפה.**

---

## 6. בדיקות

```
cd /d "C:\Users\rordt\AppData\Roaming\SketchUp\SketchUp 2024\SketchUp\Plugins\interior_pro\tests"
bash run_all.sh ..
```

חדשות בסבב הזה: **rt47** (תמונות ב-PDF), **rt48** (דף לתמונה + רשימת דפים),
**rt49** (הגדרה חדשה שורדת reload), **rt50** (כתובת קובץ + כפתור אחד),
**rt51** (העברת בייטים + שמות קבצים), **rt52** (מידות והערות),
**rt53** (ישות בעייתית לא מפילה הכל).

בעורך התוכנית: **t38** (ESC ו-Select), **t39** (מעלות — ציור),
**t40** (Undo כרונולוגי), **t41** (מעלות — לחיצה, ונקודה ירוקה על קיר כחול).

בענן נכשלות תמיד **rt7, rt18, rt26, rt43** — חסרים שם `door_tool.rb`,
`textures/`, `icons/`. **זו סביבה, לא קוד.** כל השאר ירוק.
