# Interior Pro — מסירה 2026-08-19 (חלון בקומה 2, קומות שנגררות, וגג הרעפים ההיברידי)

קרא את זה קודם, ואז `CLAUDE.md`, ואז `ROOF_TILES_PROPOSAL.md`.
**אל תסמוך על הסיכום — תאמת מול הקוד החי.**
הקודמים: `HANDOFF_2026-08-18C.md`, `HANDOFF_2026-08-18B.md`.
כללי העבודה: `HANDOFF_2026-08-14.md` §0. **תקרא אותם ברצינות.**

התיקייה החיה:
`C:\Users\rordt\AppData\Roaming\SketchUp\SketchUp 2024\SketchUp\Plugins\interior_pro`

**הבדיקות: 122 חבילות ירוקות** (76 רובי + 46 ג'אווהסקריפט). היו 120 בתחילת
הסבב. נוספו `rt75`, `rt77`; הוסרה `rt76` (ראה §4).

> **rt51 ו-rt52 נופלות רק בענן, לא אצלך.** שתיהן מוחקות קובץ זמני, והכונן
> המשותף שאני עובד דרכו לא מרשה `unlink` (`Errno::EPERM`). על המכונה שלך הן
> ירוקות. **אל תתקן אותן.** אם הן נופלות אצלך — זה באג אמיתי.

---

## 0. הדבר הכי חשוב במסמך הזה

> **שלושה באגים שונים היום נבעו מאותה טעות אחת: הקוד זיהה קיר לפי
> "איפה הוא עומד" במקום "מי הוא".**

הבית של המשתמש בנוי כך שקומה 2 יושבת **בדיוק** מעל קומה 1 — הוא מעתיק את
הקירות. כל השוואה שמשטחת את ה-z ומשווה x/y תמצא את הקיר הלא נכון.
המשפחה הזאת סגורה עכשיו בכל מקום שמזיז קיר. **אם משהו קורה "בקומה הלא
נכונה" — תחפש קודם סריקת מיקום בלי סינון `level`.**

---

## 1. מה נכנס

### א. חלון לא נכנס בקומה העליונה (`rt74`, +9 בדיקות) — §8 של המסירה הקודמת, סגור

**התסמין:** *"עובד יותר טוב בקומה התחתונה אבל לא עובד בקומה העליונה."*

**המדידה שהכריעה** (`debug_win_level.rb` → `win_level_report.txt`):

| קומה | קירות | עם x/y ≠ 0 בטרנספורמציה |
|---|---|---|
| 1 | 22 | **18** (רובם `y = -161.687`) |
| 2 | 12 | **10** (כולם `y = -161.687`, `z = 106`) |

אפס סיבוב. כלומר **רק 4 קירות בכל הבית** היו עם טרנספורמציה אחדותית — הם
היחידים שהחישוב היה נכון עליהם. זה בדיוק למה זה "עבד יותר טוב למטה" ולא
עבד באמת.

**הסיבה:** `start_x/start_y` הם **מקומיים לקבוצה**; נקודת הלחיצה היא
**עולמית**. `window_tool.rb` השווה ביניהם ישירות.

**התיקון:** `wall_local_point(wall_group, world_pt)` — ממיר את הלחיצה למרחב
הקיר. **שתי הפונקציות עוברות דרכו יחד** — `wall_click_offsets` (המסננת)
ו-`cut_window_opening` (החיתוך). אם רק אחת תמיר, המסננת מפסיקה לנבא את
החיתוך והיא חסרת ערך. `rt74` נופלת אם אחת מהן מאבדת את זה.

**בונוס שנמצא באותו מקום:** קבוצת הסימון של החלון נבנתה ב-`(cx, cy)`
מקומיים עם `wall_z` מתווסף ידנית — כלומר במקום שבו הקיר **צויר**, לא איפה
שהוא **עומד**. עכשיו הנקודה עוברת דרך `wall_group.transformation` כולה.
לקיר אחדותי או מורם בלבד זה אותו מספר בדיוק.

**המשתמש אישר: "עובד מעולה".**

### ב. קיר בקומה 2 גרר קיר בקומה 1 (`rt54` +5, `rt75` חדשה)

**התסמין שלו:** *"אני מעתיק קירות מקומה 1 לקומה 2, ואז שינוי בקיר העליון
מתייחס לקיר התחתון — כמו קומפוננט. בעיקר ב-2D."*

**שלוש השערות נבדקו ונפסלו** (`debug_copy_walls.rb` → `copy_walls_report.txt`):

| חשד | מדידה | מסקנה |
|---|---|---|
| הגדרה משותפת (SketchUp משכפל גיאומטריה) | 34 הגדרות ל-34 קירות | **לא** |
| מזהה כפול | 0 כפולים | **לא** |
| **התאמה לפי מיקום** | **7 קווי קיר קיימים בשתי קומות** | **כן** |

`ensure_unique_ids!` באמת מרפא מזהים כפולים, אבל הוא רץ רק בתוך
`sync_rooms!` שרץ **אחרי** עריכה. חשדתי שהעריכה הראשונה אחרי הדבקה פוגעת —
`debug_paste_probe.rb` הראה 0 כפולים. **גם זה נפסל.**

**מה שתפס בסוף:** `debug_change_watch.rb` (כלי חדש, ראה §5). צילום, שינוי
אחד, צילום שוב:

```
THE WALL YOU MEANT  id=1edd95d9  level=2   (שינוי אורך)
COLLATERAL          id=207c63ce  level=1   end_x/end_y -> אותה נקודה חדשה
```

**הדליפה:** סריקת השותפים ב-`stretch_wall!` (`plan_editor.rb`, סביב 1799)
מצאה כל קיר שקצהו על הפינה — בלי לבדוק קומה. הקונסול אמר את זה כל הזמן:
`(moving start, 2 partner(s))` — אחד מהשניים היה מקומה אחרת.

**סקירה מלאה אחרי התיקון.** 11 סריקות בפרויקט מזהות קיר לפי מיקום:

| קובץ | מה עושה | מצב |
|---|---|---|
| `plan_editor.rb:1799` `stretch_wall!` | **מזיז** קיר | **תוקן** |
| `wall_delete_tool.rb:69` | **מרובע ובונה מחדש** שכנים | **תוקן** (§1ג) |
| `floor_manager.rb:582` `square_mixed_base_corners!` | **מיישר** פינות | **תוקן** |
| `wall_tool.rb:3566` `align_curve_lanes!` | **מחליף צד** לקיר | **תוקן** |
| `plan_editor.rb:723` | הדפסת אבחון | קריאה בלבד |
| `wall_tool.rb:573` | הצמדה בזמן ציור | קריאה — **בכוונה חוצה קומות**, ככה מעריכים קומות |
| `wall_tool.rb:749` | מגנט צירים | קריאה בלבד |
| `wall_tool.rb:1147` `wall_end_neighbor?` | קורנר-בורד | קריאה, קוסמטי |
| `wall_split_tool.rb:174` | הצמדת נקודת פיצול | קריאה |
| `floor_manager.rb:524` | מסונן לפי `bounding_wall_ids` | לא לפי מיקום |
| `wall_tool.rb:3710` | קורא `join_corners` שמסנן בעצמו | תקין |

**כל מה שמזיז — מסונן. מה שנשאר קורא בלבד.**

### ג. מחיקת קיר מחקה פינה בקומה מתחת (`rt75`, 12+7 בדיקות — חבילה חדשה)

`delete_wall!` אוסף "שכנים" לפי מיקום ואז **מחזיר להם קצה מרובע ובונה מחדש**.
קיר בקומה מתחת נתפס כשכן — ופינה טובה למטה קיבלה כובע לבן, בלי שנגעת בה.
זה כנראה מה שדיווחת עליו ב-17.8.

הסריקה הוצאה ל-`WallDeleteTool.neighbor_walls(wall, model)` כדי שאפשר יהיה
לבדוק אותה בלי להעיר חצי תוסף. **ל-`wall_delete_tool.rb` לא היו בדיקות
בכלל עד היום** — ולא היה בכלל ברשימת ההעתקה של `run_all.sh`.

### ד. גג רעפים — ראה §4, זה סיפור בפני עצמו

---

## 2. מלכודת שחזרה: קובץ שלא נבדק כי הוא לא הועתק

`run_all.sh` מעתיק רשימה מפורשת של קבצים לתיקיית הבדיקות. `wall_delete_tool.rb`
ו-`wall_split_tool.rb` **לא היו ברשימה**, אז אף חבילה לא יכלה לגעת בהם גם אם
מישהו היה רוצה. הם ברשימה עכשיו.

**כלל:** קובץ חדש → מוסיפים אותו ל-`main.rb` **וגם** ל-`run_all.sh`.
שכחת את השני = הקובץ לא נבדק ואף אחד לא יודע.

---

## 3. שלוש טעויות שלי בסבב הזה

### א. בדיקת מקור חלשה שעברה גם בלי התיקון
כתבתי `ok('...', sq.include?("'level'"))` — והמחרוזת `'level'` הופיעה
בשורה אחרת של אותה מתודה. הסרתי את השומר, הבדיקה נשארה ירוקה.
**החלפתי ל-`.to_i == w_level`** — הביטוי עצמו. אז הסרתי את שלושת השומרים
וראיתי שלוש אדומות.
**בדיקת מקור חייבת לחפש את הביטוי שעושה את העבודה, לא מילה שנמצאת בסביבה.**

### ב. בניתי 20 שורות של מדרגות כשהמדידה אמרה שורה אחת
ראה §4. המדידה הייתה אצלי ביד וקראתי אותה שטחית.

### ג. נתתי מספר הנדסי בלי לחשב אותו
הכרזתי שגל בכל שורה עולה "~2,400 פאות" ורק אחר כך חישבתי: **50,652**.
תפסתי את זה בעצמי לפני שזה הגיע אליך, אבל זו אותה טעות מ-18.8.
**עכשיו זה נעול בבדיקה** — `rt72` נופלת אם המספר משתנה.

---

## 4. גג הרעפים — ניסיון אחד נכשל, וההצעה שנולדה ממנו

### מה בניתי, ולמה זה היה מכוער
`build_tile_courses!` — מדרגה תלת-מימדית בכל שורת רעפים. אתה אמרת
**"ממש לא נראה טוב"** ושלחת צילום: 20 מדפים מרחפים במעלה מדרון 4:12.

`debug_tiles.rb` אישר שהגיאומטריה **תקינה** — u אופקי בדיוק (`u.z = 0.0000`),
המדרגות דקות ובכיוון הנכון. **לא באג. התכנון היה שגוי.**

### מה שהמדידה אמרה כל הזמן
בדוח של Vali שכבר היה אצלי:

| הגדרה | עותקים | פאות | גודל |
|---|---|---|---|
| `va_BirdStop` | **408** | **1** | 5.1" × 0" × 1.9" |

408 עותקים של פאה אחת = **קו האיב בלבד**. מעל האיב הם שטוחים.
הכלל "תלת-מימד רק איפה שרואים את הצללית" — **הצללית של גג היא האיב, הרכס,
ההיפ והרייק.** השדה נראה על רקע הגג עצמו, ושם הטקסטורה עושה את העבודה.

### מה שהוסר
`build_tile_courses!`, `build_plane_courses!`, `build_course_strip!`,
`TILE_COURSE_ROWS`, `TILE_SCALLOP_COURSES`, ו-`rt76`.
**`roof_tile_math.rb` נשאר שלם** — המסגרת, ה-`spans_at`, ה-`edge_slots`
וה-`scallop` הם בדיוק מה שמערכת הרעפים החדשה צריכה, והם בדוקים.
`scallop_amp` ו-`estimate(rows:)` נוספו היום ונשארו — `rt72` מכסה אותם.

### מה נכנס במקום — `roof_tile_parts.rb` (`rt77`, 30 בדיקות)
ארבע הגדרות קומפוננטה דלות-פאות, **6 פאות כל אחת**:

| הגדרה | פאות | הערה |
|---|---|---|
| `IP_TileEave_<shape>` | 6 | אף קצר 4" — **המשתמש בחר** ב-19.8 |
| `IP_TileRake_<shape>` | 6 | |
| `IP_TileRidge_<shape>` | 6 | |
| `IP_TileHip_<shape>` | 6 | זהה לרכס היום, בכוונה |

**24 פאות ייחודיות לכל גג, בכל גודל בית.** קריאה שנייה מחזירה את אותה
הגדרה ולא בונה כלום (`rt77` נופלת אם כן). חומר אחר מקבל הגדרה **חדשה**,
כי השם נושא את המידות — אותה הגנה מהמלכודת של `reload!`.

הפרופיל מודל במסגרת המקומית של המישור: **+X לרוחב, +Y במעלה, +Z החוצה** —
בדיוק ה-u/v/n של `RoofTileMath.plane_frame`, כך שההנחה היא
`Transformation.axes` ותו לא. הבטן **פתוחה בכוונה** — היא יושבת על הגג,
אף אחד לא רואה אותה, וזו פאה פחות כפול כמה מאות.

### הצעד הבא — קרא את `ROOF_TILES_PROPOSAL.md`
המשתמש שלח מפרט מלא למערכת היברידית והוא **תואם למה שמדדנו**. ההצעה ממפה
אותו על הקוד הקיים. הצעדים שנשארו:

| # | מה | סטטוס |
|---|---|---|
| 1 | להסיר את `build_tile_courses!` | **בוצע** |
| 2 | `roof_tile_parts.rb` + `rt77` | **בוצע** |
| 3 | `RoofManager.roof_edges` — עטיפה שמחזירה איב/רייק/רכס/היפ | הבא |
| 4 | **רעפי איב בלבד** + תג — הראשון שנראה בעיניים | |
| 5 | כובעי רכס+היפ → עותקים במקום קבוצות ייחודיות | |
| 6 | רעפי רייק | |

**§3 בהצעה: אל תכתוב מסווג קצוות שני.** `ridge_lines`, `drop_facet_hips`,
`eave_polygon` ו-`gable_spans` כבר עובדים ובייצור. שני מסווגים שלא מסכימים
זו בדיוק המלכודת של החלון מ-§1א.

**§5 בהצעה — הפער האמיתי:** `build_cap_piece!` בונה
`grp.entities.add_group` **לכל חתיכת כובע**. על גג גדול זה מאות קבוצות
ייחודיות — בדיוק ה-"thousands of unique groups" שהמשתמש אמר להימנע ממנו.
זה מה שצריך להפוך לעותקים. **הפרופיל, ה-tuck וה-taper הקיימים טובים —
אל תכתוב אותם מחדש, רק תעטוף אותם בהגדרה.**

---

## 5. כלי מדידה שנוספו (כולם קריאה בלבד, כותבים דוח לקובץ)

| קובץ | עונה על |
|---|---|
| `debug_win_level.rb` | האם לקירות קומה 2 יש הזזה ב-x/y, או רק גובה |
| `debug_copy_walls.rb` | הגדרה משותפת מול מזהה כפול מול התאמת מיקום — **מפריד בין שלושתם** |
| `debug_paste_probe.rb` | מזהים כפולים **מיד אחרי הדבקה**, לפני שמשהו מרפא |
| `debug_change_watch.rb` | **הכלי שתפס.** אותה שורה פעמיים, ואומר איזה קיר השתנה בלי שביקשת ובאיזו קומה |
| `debug_tiles.rb` | האם מסגרת המישור נכונה ומה גודל המדרגות שנבנו |

`debug_move_watch.rb` (מ-18.8) **לא הספיק** — הוא מסתכל רק על שני קצוות
הקיר, ואמר "כלום לא זז" בזמן שהמשתמש ראה קיר זז. `debug_change_watch.rb`
מסתכל על 20 תכונות. **כשכלי אומר "כלום", תשאל קודם מה הוא בכלל מודד.**

**המשתמש לא קורא פלט של קוד.** אתה קורא את הדוח בעצמך.

---

## 6. מה למדתי הסבב הזה

1. **§0. "איפה הוא עומד" זה לא "מי הוא".** שלושה באגים, מנגנון אחד.
2. **§3א. בדיקת מקור שמחפשת מילה ולא ביטוי — עוברת גם כשהתיקון הוסר.**
   תמיד תסיר את השומר ותראה אדום.
3. **§4. המדידה הייתה אצלי ואני קראתי אותה שטחית.** "408 עותקים" זה לא
   קישוט, זו התשובה. תקרא מספר עד הסוף לפני שאתה בונה עליו.
4. **§5. כלי שאומר "לא קרה כלום" צריך אימות בעצמו.** `move_watch` מדד את
   הדבר הלא נכון וכמעט שלח אותנו לכיוון הפוך.
5. **§2. קובץ שלא ברשימת ההעתקה של `run_all.sh` הוא קובץ בלי בדיקות** —
   ואי אפשר לדעת את זה מהסתכלות בו.
6. **תחשב לפני שאתה אומר מספר.** אמרתי 2,400, המחשבון אמר 50,652.
7. **המשתמש מכיר את התחום.** המפרט ששלח על המערכת ההיברידית היה מדויק
   יותר ממה שתכננתי לבד. כשהוא שולח מסמך — תקרא אותו לפני שאתה מציע.
8. **צ'אט אחד נגע בחלונות, קירות וגגות.** שוב. פצל מוקדם יותר.

---

## 7. מה פתוח — לפי סדר

1. **גג רעפים, צעדים 3–6.** `ROOF_TILES_PROPOSAL.md`. הצעד הראשון שנראה
   בעיניים הוא 4 (רעפי איב) — **תעצור שם ותבקש צילום.**
2. **ידית ירוקה שמסתובבת כמו מחוג** — בקשה מ-18.8, לא בוצע.
   `dragLenLimit` ב-`plan_editor.rb`.
3. **צבע למידות ולטקסטים בתוכניות** — לא לכל התוכנית. `CLAUDE.md` מפרט
   ארבעה קבצים שחייבים להשתנות יחד.
4. **שני פערים בקומה 1** — `fix_ends_report.txt`, 0.17" ו-0.09". תצוגה
   בלבד. לסגור: `InteriorPro::FixWallEnds.run(apply: true)`.
5. **קירות חייבים להיות ברמה העליונה** — `HANDOFF_2026-08-18C.md` §7. לא
   נגעתי. עדיין נכון, ועדיין צעד בפני עצמו עם בדיקות משלו.
6. **לוגו per-installation, הערה מצוירת, קורנר-בורדס** — `CLAUDE.md`.
7. **לנדסקייפ** — המשתמש ביקש במפורש להשאיר לסוף.

---

## 8. ניקיון קטן

`tests/_to_delete/` ו-`_to_delete/` בשורש מחזיקים קבצים זמניים שהריצות שלי
בענן לא הצליחו למחוק (אותה מגבלת `EPERM` של rt51/rt52), וגם את `rt76.rb`
שהוסרה ואת הצילום הישן של `move_watch`. **אפשר למחוק את שתי התיקיות ידנית.**
