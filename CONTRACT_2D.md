# חוזה הדו-ממד — Interior Pro (טיוטה 1, 2026-07-14)

מסמך דרישות. לא קוד. מגדיר מה כל אלמנט חייב לשמור כדי שבעתיד נוכל:
א. להפיק ב-LayOut: תוכנית בנייה, אליוויישנים פנימיים (לפי חדר), חזיתות חוץ.
ב. לבנות עורך מלמעלה (סגנון Rayon/Revit): מציירים ומודדים ב-2D → לוחצים "פתור" → ה-3D נבנה מחדש.

---

## 1. העיקרון המרכזי: Attributes = מקור אמת יחיד

**הכלל:** כל גוף תלת-ממדי חייב להיות ניתן לבנייה מחדש (regen) אך ורק מה-attributes שלו. הגיאומטריה היא תוצר — לעולם לא מקור.

זה כבר כמעט נכון היום (קיר נבנה מחדש בעריכה, דלת ב-regen). מה שהחוזה מוסיף:

- **אסור** שמידע יחיה רק בגיאומטריה. אם משהו נראה במודל — יש לו attribute שמגדיר אותו.
- **corners_xy הוא cache, לא מקור.** חייב להיות ניתן לחישוב מחדש מ-start/end + thickness + anchor + שכנים. (היום שינוי עובי לא עובד בדיוק בגלל שהוא נשמר כמקור — הבאג הפתוח הזה הוא הפרת חוזה.)
- מבחן הקבלה: מחיקת כל הגיאומטריה ובנייה מחדש מה-attributes בלבד → מודל זהה.

## 2. זהות ושיוך — חובה לכל אלמנט

כל אלמנט (קיר, דלת, חלון, חדר, רצפה, תקרה, גג, מולדינג) שומר:

| attribute | תפקיד |
|---|---|
| `type` | 'wall' / 'door' / 'window' / 'room' / 'floor' / 'ceiling' / 'roof' / 'molding' |
| `id` | מזהה יציב, לא משתנה לעולם (גם אחרי regen/עריכה) |
| `mark` | סימון לתוכנית: D1, W3, קיר 101… רץ לפי סוג |
| `host_wall_id` / `room_id` | שיוך: פתח→קיר, קיר→חדר(ים), רצפה→חדר |
| `level` | קומה (מכינים מהיום — גם אם כרגע תמיד 1) |

**חוק ה-id:** regen מחליף גיאומטריה אבל משמר id. כל ההפניות (host_wall_id, connected_doors, room_id) שורדות כל עריכה.

## 3. מוסכמות מדידה (הוחלט 2026-07-14)

- **קירות נמדדים לפנים הקיר (face to face)** — מידות נטו של חללים.
- **פתחים נמדדים לצד הפתח** — עד קצה הפתח הגס (rough opening), לא למרכז.
- יחידות פנימיות: אינצ'ים. תצוגה: רגל-אינץ' (כמו היום).

**המשמעות לחוזה:** לכל קיר חייבים להיות ניתנים לחישוב שני קווי face (פנימי/חיצוני) — נגזרים מ-centerline + thickness. לכל פתח — שני קצוות הפתח הגס לאורך הקיר (נגזר מ-position_along_wall_in + width_in + הגדרת נקודת העיגון של position). **נדרש לתעד חד-משמעית מה position_along_wall_in מודד היום (מרכז? קצה?) ולקבע את זה בחוזה.**

## 4. חוזה פר-אלמנט

### 4.1 קיר (קיים — נדרשות תוספות)

**נשמר היום:** start/end (drawn), thickness, height, anchor, corners_xy, wall_category, exterior_material, side_a_color, side_b_color, door_openings, id, mark, length_in, שטח, נפח.

**תוספות נדרשות:**
- `level` (קומה).
- `room_ids` — לאילו חדרים הקיר גובל (נגזר, ראה חדר).
- `base_z` — גובה בסיס (היום תמיד 0; מכינים לעתיד).
- `window_openings` — כמו door_openings (היום חלונות לא רשומים על הקיר — פער!).

**ייצוג בתוכנית (מבט מלמעלה, חתך בגובה 48"):**
- שני קווי face מקבילים + סגירות בקצוות (אחרי מיטר).
- מילוי (hatch) לפי wall_category: קיר חוץ מילוי מלא/כהה, קיר פנים מילוי בהיר.
- הפתחים נחתכים מהקווים (רווח בקיר במקום הפתח).

**ייצוג באליוויישן:** מלבן הקיר + פתחים + מולדינג (קווי בסיס/קראון) + גובה.

### 4.2 דלת (קיים — כמעט מלא)

**נשמר היום:** כל הפרמטרים (סוג, מידות, swing_direction/side, slide_direction, קייסינג, transom, sidelites, host_wall_id, position_along_wall_in, mark…).

**תוספות נדרשות:**
- `rough_width_in` / `rough_height_in` — מידות פתח גס (אם שונות ממידות הדלת + פריים). למדידות ולקבלן.

**ייצוג בתוכנית:**
- דלת ציר: קו כנף בזווית 90° + קשת רבע עיגול (לפי swing_direction + swing_side + clicked_side).
- דלת הזזה: מלבני כנפיים חופפים + חץ כיוון.
- דלת כיס (אם תהיה): כנף בתוך הקיר.
- דלתות ארון (הוחלט 2026-07-14, סטנדרט אמריקאי): bifold = קווים שבורים בזווית (V); הזזה = שני מלבנים חופפים עם חצי כיוון.
- דלת כניסה עם sidelites/transom: הפתח המלא בתוכנית, transom רק באליוויישן.
- מספור: עיגול/משושה עם mark ליד הדלת.

**ייצוג באליוויישן:** מבט חזיתי לפי leaf_style (הפאנלים/זכוכית כמו ב-3D), קייסינג, ידית.

### 4.3 חלון (קיים — נדרשות תוספות)

**נשמר היום:** סוג, מידות, header/sill, host_wall_id, position, clicked_side, mark, קייסינג.

**תוספות נדרשות:**
- רישום החלון על הקיר (`window_openings`, ראה 4.1).
- `rough_width_in` / `rough_height_in`.

**ייצוג בתוכנית:** סימבול חלון סטנדרטי — קו/קווים דקים בתוך עובי הקיר (זכוכית) + קווי משקוף. סוג החלון לא משנה את סימבול התוכנית (משנה רק באליוויישן).

**ייצוג באליוויישן/חזית:** מסגרת + חלוקת כנפיים לפי window_type + גריד + sill/header בגבהים מה-attributes.

### 4.4 חדר — ישות חדשה (הוחלט: ישות מלאה)

**היום לא קיים בכלל. נדרש:**

| attribute | תפקיד |
|---|---|
| `type` = 'room' | |
| `id`, `name`, `number` | "Kitchen", 104 |
| `boundary_xy` | פוליגון גבול החדר (לפנים הקירות) — flat array כמו corners_xy |
| `bounding_wall_ids` | הקירות הגובלים (לשיוך אליוויישנים!) |
| `area_sqft` | נגזר מ-boundary |
| `ceiling_height_in` | ברירת מחדל מגובה הקירות |
| `floor_finish`, `ceiling_finish` | טקסט/חומר — בסיס לכלי רצפות/תקרות |
| `level` | קומה |

**זיהוי חדר (הוחלט 2026-07-14):** אוטומטי — לולאת קירות סגורה = חדר. שם ברירת מחדל אוטומטי ("חדר 1", "חדר 2"…), והמשתמש עורך שם וסוג ("מטבח", "שירותים 1"…). ה-id נשאר יציב גם כששם משתנה.

**ייצוג בתוכנית:** תווית שם + מספר + שטח במרכז החדר.
**תפקיד באליוויישנים:** כל חדר = דף; bounding_wall_ids + כיוונים (N/E/S/W → A/B/C/D) מגדירים את 4 המבטים.

### 4.5 מולדינג (קיים — כמעט מספיק)

**נשמר היום:** type, host_wall_id, profile, side, profile_h, no_molding.
**תוספת:** אין חובה מיידית. בתוכנית מלמעלה מולדינג לא מופיע. באליוויישן: קו בסיס בגובה הבייסבורד + קו קראון, עם שם הפרופיל.

### 4.6 רצפה / תקרה / גג — כלים עתידיים (סעיף 2 במפת הדרכים)

החוזה מגדיר מראש מה הם ישמרו כשייבנו:

- **רצפה:** id, room_id (או boundary עצמאי), חומר/פיניש, עובי, base_z. נגזרת מגבול החדר כברירת מחדל.
- **תקרה:** id, room_id, גובה, סוג (שטוחה/משופעת), חומר.
- **גג:** id, boundary, סוג (gable/hip/shed…), שיפוע (pitch), overhang, חומר. הגג חייב regen מלא מה-attributes כמו קיר.

### 4.7 קשתות/פתחים מעוגלים (עתידי)

כשיגיעו — הפתח יקבל `arch_type` + `radius_in`, והסימבול בתוכנית נשאר מלבן הפתח (הקשת נראית רק באליוויישן).

## 5. Tags (שכבות) — לא קיים היום, חובה לחוזה

כל אלמנט מקבל Tag בעת יצירה/regen:

```
IP/Walls
IP/Doors
IP/Windows
IP/Moldings
IP/Rooms          (תוויות חדר)
IP/Floors  IP/Ceilings  IP/Roofs   (עתידי)
IP/2D              (גיאומטריית סימבולים דו-ממדית, אם תיווצר במודל)
```

מטרה: ב-LayOut מדליקים/מכבים שכבות פר-דף (למשל חזית חוץ בלי תוויות חדרים; תוכנית בלי גג). קבלני משנה בעתיד יקבלו שכבות משלהם (IP/Electric…) — המבנה ההיררכי (IP/) מוכן לזה.

## 6. עורך המלמעלה ("פתור" → 3D)

הדרישה: לצייר ולמדוד פרויקט שלם ב-2D — קירות, דלתות, חלונות, גגות — ואז ה-3D נבנה מחדש, כמו Revit.
**פלטפורמה (הוחלט 2026-07-14):** דיאלוג HTML בתוך SketchUp, כמו הדיאלוגים הקיימים.

**מה זה מחייב (וזו הסיבה שסעיף 1 קשיח):**
1. העורך קורא וכותב **אך ורק attributes** — אף פעם לא גיאומטריה.
2. "פתור" = להריץ את אותם בנאים שקיימים היום (build_wall_group, regen דלת/חלון, MoldingManager.refresh!) על כל האלמנטים לפי סדר תלות: קירות → מיטרים → פתחים → מולדינג → רצפות/תקרות/גגות.
3. כל בנאי חייב להיות דטרמיניסטי: אותם attributes → אותה גיאומטריה.
4. מידות חיות בעורך = אותן נוסחאות face/rough-opening מסעיף 3.

**מסקנה מעשית:** כל פיצ'ר חדש מהיום נבחן בשאלה "האם הוא regen-able מ-attributes בלבד?" אם לא — הוא מפר את החוזה.

## 7. פערים ידועים היום מול החוזה (לתיקון בהמשך, לא עכשיו)

1. חלונות לא רשומים על הקיר (אין window_openings) — דלתות כן.
2. corners_xy נשמר כמקור ולא כ-cache (באג שינוי עובי הוא סימפטום).
3. אין Tags בכלל.
4. אין ישות חדר.
5. אין level/base_z.
6. לא מתועד חד-משמעית מה position_along_wall_in מודד (מרכז/קצה) — לוודא מול הקוד ולקבע.
7. `interior_material` מכיל צבע ולא חומר (שם מטעה) — לתעד, לא לשנות.

## 8. שאלות פתוחות

1. ~~זיהוי חדר~~ — הוחלט: אוטומטי מלולאת קירות + שם ניתן לעריכה (ראה 4.4).
2. גבהים בחזיתות חוץ (grade/plate/ridge): המשתמש טרם החליט — **נשאר פתוח, לסגור כשמגיעים לכלי הגגות.**
3. ~~סימבול ארונות~~ — הוחלט: סטנדרט אמריקאי (ראה 4.2).
4. ~~פלטפורמת העורך~~ — הוחלט: דיאלוג HTML בתוך SketchUp (כמו הדיאלוגים הקיימים).

---

*טיוטה לדיון. לא לגעת בקוד עד אישור המסמך.*
