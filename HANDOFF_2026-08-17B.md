# Interior Pro — מסירה 2026-08-17B (גדר ויניל + חומה / Landscape Pro)

קרא את זה קודם, ואז `CLAUDE.md`. **אל תסמוך על הסיכום — תאמת מול הקוד החי.**
הקובץ הקודם: `HANDOFF_2026-08-17.md`. כללי העבודה: `HANDOFF_2026-08-14.md` §0.

התיקייה החיה:
`C:\Users\rordt\AppData\Roaming\SketchUp\SketchUp 2024\SketchUp\Plugins\interior_pro`

---

## 0. השורה התחתונה של הסבב הזה

שני דברים נסגרו:

1. **הגדר** — הקורא נפל על קובץ הויניל שלו כי היו בו **שני עמודים באותו מקום
   בדיוק**. תוקן. ובנוסף נכנס חוק שלו: **עמוד לעמוד ≤ 8 פיט, תמיד.**
2. **החומה** (`חומה`) — כלי חדש לגמרי, נבנה מאפס בארבעה צעדים שהוא בדק אחד
   אחרי השני: גוף → קאפ → בול-נוז → קו אלווישן.

**הבדיקות: 674 ירוקות** (rt56, rt57, rt61, rt62, rt63, rt64). אפס אדומות.

---

## 1. הגדר — מה תוקן

### א. עמודים כפולים (`parts_mode`)

קובץ הויניל שלו: 198" אורך, 60" גובה, עמודים 6" ב-x = ‎-3.001, **-3.001**,
92.999, 188.999. שני הראשונים אותו עמוד, מודל פעמיים באותו מקום.

הקורא לקח את שני מרכזי העמודים הראשונים כשדה, קיבל **0"**, `parts_mode`
נכשל, והמודל כולו (198") הפך ליחידה. הוא ראה שני עמודים דבוקים.

**התיקון:** שני עמודים במרחק קטן מ-1" הם אותו עמוד. הראשון נשמר, השאר
נזרקים. עכשיו הויניל נקרא כ-**parts mode, שדה 96"**.

### ב. חוק 8 פיט (`layout_for`, `NEVER_STRETCH`)

`MAX_STRETCH = 1.35` אפשר לשדה של 96" להימתח ל-**129"**. חורג מהחוק שלו.

**התיקון:** `NEVER_STRETCH = true` — מעגלים כלפי **מעלה** ומכווצים. מתיחה
לעולם לא עוברת 1.0, אז שדה לעולם לא עובר את היחידה המקורית. הוא בחר **שדות
שווים** על פני "שדות מלאים + אחרון קצר".

**חור שנמצא תוך כדי:** גם ב-cut mode, הנפילה-לאחור "שארית קטנה → תמתח את
השלמים" ייצרה מ-102" על יחידה של 96" **שדה אחד של 102"**. תוקן שם גם.

`NEVER_STRETCH = false` מחזיר את ההתנהגות הישנה. `USE_CUT` נשאר **כבוי**
(הוא תלה את סקצ'אפ על המודל הגולמי) — לא נגעתי בזה.

### ג. rt63 היה אדום לפני שנגעתי

2 נפילות, שאריות ממצב ביניים. עכשיו ירוק, 108 בדיקות.

---

## 2. החומה — הכלי החדש

**קובץ:** `landscape/garden_wall_tool.rb`
**מחלקה:** `InteriorPro::Landscape::GardenWallTool`
**כניסה:** `InteriorPro::Toolbar.open_garden_wall` — הכפתור **Wall** בסרגל
Landscape Pro **והתפריט**, שניהם לאותה מתודה.

### הכלל הכי חשוב בקובץ הזה

**החומה נשמרת תחת `LandscapePro`, לא תחת `InteriorPro`.**

כל מי שמחפש קירות בתוסף — `RoomManager` סוגר חדרים, `FloorManager` /
`FoundationManager` בונים רצפות, `MoldingManager` מריץ פנלים, `RoofManager`
לוקח מרזבים, `plan_generator` מצייר בתוכנית, וכלי Edit/Move/Stretch/Split/
Merge/Delete של הקירות — כולם מוצאים קיר לפי **בדיוק** המילון `'InteriorPro'`
והמחרוזת `'wall'`. חומה שתסומן ככה תיגרר לתוך הכל.

זה בדיוק מה שהגדר כבר עושה נכון, והעתקתי את השיטה. **יש בדיקה ב-rt64 שנופלת
אם מישהו ישבור את זה.** אל תשבור.

מסיבה זו החומה **אינה** יורשת מ-`WallTool`. הדבר היחיד שהיא לוקחת ממנו הוא
`WallTool.apply_axis_magnet` — אותה הצמדה אדום/ירוק שהגדר לוקחת.

### קונבנציית ימין/שמאל (קריטי)

**ימין = יד ימין של הכיוון שהוא צייר (לחיצה 1 → לחיצה 2).**

שלושה דברים יושבים על ימין, בכוונה:

- הבול-נוז (`One side`)
- הבליטה של הקאפ (`One side`)
- ו**הפוך מהם**: קו האלווישן, שרץ בגב = שמאל

זה אומר שהפאה שרואים מקבלת גם עיגול וגם בליטה, והקו יושב מאחור. **כדי להפוך
— הוא מצייר את החומה הפוך.** אין כפתור ימין/שמאל, בכוונה: זו אותה פעולה בשתי
דרכים, וזה נגד כלל ה-UI שלו.

### מה יש בדיאלוג (12 שדות, `UI.inputbox`)

| שדה | מה |
|---|---|
| Thickness | עובי החומה |
| Height | גובה החומה |
| Finish | חומר גמר — **רשימה משלה**, לא של הקירות |
| Cap width (side to side) | **הרוחב הכולל** של הקאפ, לא בליטה |
| Cap thickness (0 = no cap) | גובה הקאפ. **0 = אין קאפ.** זה כפתור הכיבוי, אין צ'קבוקס |
| Cap finish | חומר של הקאפ |
| Cap units | `Continuous` / `Split` |
| Stone length | אורך כל אבן (Split בלבד) |
| Joint gap | מרווח בין אבנים (Split בלבד) |
| Bullnose | `None` / `One side` / `Both sides` |
| Cap overhang | `Both sides` (ממורכז) / `One side` (פלאש בשמאל) |
| Elevation line along the back | `Yes` / `No` |

### איך זה בנוי

- **גוף:** מלבן ממורכז על הקו (חצי עובי לכל צד), נדחף למעלה. סדר הפינות
  נגד כיוון השעון כדי שהפאה תפנה למעלה, **אבל** הקוד שואל את
  `face.normal` לפני ה-pushpull ולא מניח. חומה שנבנתה כלפי מטה בלתי נראית
  ונקראת "כלום לא קרה".
- **קאפ:** **חתך שנמשך לאורך החומה**, לא לוח שנדחף למעלה. זו הדרך היחידה
  שבול-נוז יכול להתקיים, ואותו מסלול קוד משרת גם קאפ ישר — כדי ששניהם לא
  יתפצלו.
- **Split:** כל אבן היא **קבוצה משלה** בתוך קבוצת הקאפ. אבן שאי אפשר לבחור
  לבד היא לא אבן, ופאות חופשיות באותה קבוצה היו נדבקות ומוחקות את המישקים.
  **האחרונה נחתכת קצרה** (הוא בחר את זה במפורש). שארית קטנה מ-`MIN_CAP_PIECE`
  (1/8") נזרקת.
- **בול-נוז:** חצי עיגול אמיתי, רדיוס = חצי עובי הקאפ, **תמיד**. אין שדה
  רדיוס — משהו קטן יותר הוא eased edge, דיטייל אחר. הוא **אוכל פנימה**
  מהרוחב שהוא הקליד, לא מוסיף לו.
- **הקווים על העיגול ירדו:** 12 קטעים, וכל קצה ששתי הפאות שלו כמעט באותו
  כיוון (< 30°) מקבל `soft`+`smooth`. פינת 90° נשארת קו. **יש בדיקה שנופלת
  אם מישהו יוריד את מספר הקטעים לרמה שבה הקווים חוזרים.**
- **קו האלווישן:** קצה אחד, בגב, בקצה העליון של **החומה** — מתחת לקאפ, לא
  עליו. קבוצה משלו + **תג משלו** `LP/Wall Elevation Lines`, כדי שאפשר יהיה
  לכבות את כולם בקליק.

### תכונות שנשמרות (מילון `LandscapePro`)

`type='garden_wall'`, `thickness`, `height`, `material`,
`start_x/y`, `end_x/y`, `length_in`,
`cap_width`, `cap_height`, `cap_material`, `cap_mode`, `cap_unit`, `cap_joint`,
`bullnose`, `overhang`, `back_line`, `overall_height`,
`ground_start`, `ground_end` (אפס היום — מוכנים לשיפועים, בדיוק כמו בגדר),
`created_at`.

קבוצות פנימיות: `LandscapePro_GardenWallCap`, `LandscapePro_CapUnit_<n>`,
`LandscapePro_GardenWallBackLine` (עם `type='garden_wall_back_line'`).

---

## 3. קבצים

| קובץ | מה |
|---|---|
| `landscape/garden_wall_tool.rb` | **הכלי החדש.** כל החומה |
| `landscape/fence_ref_tool.rb` | הגדר. `parts_mode` (עמודים כפולים) + `layout_for` (8 פיט) תוקנו |
| `tests/rt64.rb` | החומה. 182 בדיקות |
| `tests/rt63.rb` | הגדר. 108 בדיקות. היה אדום, עכשיו ירוק |
| `tests/sketchup_stub.rb` | נוספו `SB_PROMPT` / `SB_VCB_LABEL` / `SB_VCB_VALUE` — קיימים בסקצ'אפ, חסרו בסטאב |
| `tests/run_all.sh` | `garden_wall_tool.rb` + `rt64` |
| `main.rb` | `landscape/garden_wall_tool.rb` ב-`plugin_files` |
| `toolbar.rb` | כפתור `Wall` + תפריט `Landscape: Garden Wall` + `open_garden_wall`. **`setup_landscape_toolbar` השומר עלה מ-`>= 1` ל-`>= 2`** — צריך להעלות אותו בכל כפתור חדש, אחרת הכפתור פשוט לא מופיע |

---

## 4. מה פתוח — הצעד הבא

1. **רשימת חומרי הגמר.** הוא אמר במפורש שזו **רשימה בפני עצמה**, לא של
   הקירות. היום ב-`MATERIALS`: `Stucco`, `Stack Stone`, `Block Wall`. הוא
   הזכיר גם "אבנים מיוחדות". **הוא חייב לשלוח את הרשימה המלאה.**
2. **טקסטורות.** רק ל-`Stucco` יש `.jpg`. השאר מקבלים **צבע זמני**
   (`FALLBACK_COLOR`) כדי שלא ייצאו לבן שטוח שנראה כמו באג. צריך
   `textures/stack_stone.jpg`, `textures/block_wall.jpg`.
3. **שיפועים ופינות** — פתוח גם בגדר וגם בחומה. `ground_start/ground_end`
   כבר שמורים בשניהם, אותה צורה, כדי שאותו קוד ירים את שניהם.
4. **קשתות בחומה** — כשזה יגיע, `back_line_points` הוא **המקום היחיד**
   שצריך להשתנות בשביל שהקו יעקוב. זה נכתב ככה בכוונה.
5. **דיאלוג HTML** במקום `UI.inputbox` — 12 שדות זה כבר על הגבול. לא לפני
   שהוא מבקש.
6. **אייקון** ל-`icons/garden_wall_tool.png` — אין, והכפתור עולה בלעדיו.
7. הפתוחים הישנים (גדר: מלכודת המתיחה על מודל גולמי; חיתוך במקום) — ראה
   `HANDOFF_2026-08-17.md` §5.

---

## 5. מה למדתי הסבב הזה

1. **שני צ'אטים על אותה תיקייה.** באמצע הסבב `fence_ref_tool.rb` **השתנה
   תוך כדי שדיברנו** — 28KB → 34KB, עם קוד שלא היה שם. צ'אט שני שלו עבד על
   אותם קבצים. עצרתי ושאלתי במקום לכתוב. **תמיד תבדוק mtime/גודל של קובץ
   רגע לפני שאתה כותב עליו**, ואם הוא זז — תעצור ותשאל.
2. **הוא לא הריץ reload!.** "לא נטען" היה הזיכרון של סקצ'אפ, לא הדיסק.
   הסקריפט שהוכיח את זה (`File.exist?` + `plugin_files.include?` + `load`
   ישיר) לקח 30 שניות וחסך חיפוש באג שלא היה.
3. **הוא ביקש קופסה לכל פקודה, גם כשזה נראה מובן מאליו.** "תסגור ותפתח את
   סקצ'אפ" בלי קופסה = תלונה. כל דבר שהוא צריך להקליד → קופסה + לאן.
4. **"נראה טוב אבל..." זה שני באגים.** בשתי הפעמים שהוא אמר את זה הוא הצביע
   על משהו אמיתי (בליטה בצד אחד, קווים על העיגול). לא לקרוא את זה כאישור.
5. **"אני חושב שהורדת את X" — תמדוד, אל תסביר.** נתתי לו קופסה שמדדה את
   הקאפ במודל. התשובה הייתה "טעות שלי". הסבר היה מבזבז שני סבבים.
6. **צעד אחד, בדיקה, commit.** ארבעת הצעדים של החומה עברו ככה, וכל תיקון
   שלו נכנס על גוף עובד ולא על ערימה.
7. **תקן את הסטאב, לא את הקוד.** `SB_PROMPT` קיים בסקצ'אפ וחסר היה בסטאב.
   הפיתוי לעקוף אותו בקוד היה שם. זה הכלל של `HANDOFF_2026-08-14.md` §0.
