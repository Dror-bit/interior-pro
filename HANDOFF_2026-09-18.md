# Interior Pro — מסירה 2026-09-18 (החיתוך במפגש: נסגר)

קרא את זה קודם, ואז `CLAUDE.md`. הקודמת: `HANDOFF_2026-09-17.md`.
**אל תסמוך על הסיכום — תאמת מול הקוד החי.**

בדיקות: `cd tests` ואז `LANG=C.UTF-8 bash run_all.sh ..` — **202 ירוקות**
(rt51/rt52/rt60 אדומות ישנות). חדשות: **rt153**, **rt154**, **rt155**,
**rt156**.

## דגל

`InteriorPro::RoofManager::USE_ROOF_VALLEY` — **false בקובץ בכוונה**.
הדלקה + בנייה מחדש (קופסה אחת ל-Ruby Console):
```
InteriorPro.reload!; InteriorPro::RoofManager.send(:remove_const, :USE_ROOF_VALLEY); InteriorPro::RoofManager.const_set(:USE_ROOF_VALLEY, true); InteriorPro::RoofManager.roofs.each { |r| InteriorPro::RoofManager.build_roof!(replace: r) }; puts "valley ON"
```

## מה נסגר היום — סעיף 1 מהמסירה הקודמת ("החיתוך במפגש")

הכלל שלו, עכשיו בקוד ולא בטלאים:
> גג הגראז' קיים רק איפה שהוא **מעל מישור גג הבית**, או **מדרום לקו
> הקיר של הבית**. כל השאר — בפנים, נמחק.

הוא אישר את זה על תוכנית מרונדרת (מבט על עם המידות האמיתיות) לפני
שנגענו בקוד, ואחר כך את התוצאה בסקצ'אפ: "מעולה".

### `valley_inside_regions` (טהור פרט ל-`roofs`, rt153)
לכל אחד משני המדרונות של הגג הנמוך, הפוליטון שלו נחתך בשלושה חצאי
מישור עם `clip_halfplane` הקיים:
1. מעבר לקו הקיר של הגג הגבוה (האיב שלו דחוף פנימה באוברהנג שלו) —
   מדרום לקו הזה הגג ממשיך מתחת לאיב ו**נשאר**;
2. **מתחת** למישור הגג הגבוה — הגבול הזה הוא בדיוק רגל הוואלי
   (`clip_under`, עוזר חדש: איפה מישור א' נמוך ממישור ב');
3. בצד שלו של הרכס, כדי ששני האזורים לא יחפפו.

במודל שלו יוצאים בדיוק שני הטרפזים: מערב
(-618.51,0)(-534.51,0)(-517.51,17)(-618.51,17), מזרח במראה סביב הרכס
x=-459.975. **84" על קו הקיר ו-101" בקצה** — לא 72"/89" של המסירה
הקודמת: האזור רץ עד קצה הגג ובולע גם את רצועת האוברהנג ש-
`valley_low_deck_cuts` כבר מורידה. אותו חיתוך, פעם אחת.
רגלי הוואלי: מערב `y = x + 534.51`, מזרח `x = -385.44 - y`. שתיהן
נפגשות עם מישור הבית ב-z=110 על קו הקיר.

התוצאה נכנסת למכונה הקיימת: `valley_cut_deck!` (0.35 לפני הרעפים, 0
אחריהם) + `valley_cull_pieces!`.

### `valley_sweep_under!` — "המסגרת של השכבה מתחת לרעפים"
הוא צילם: הדק נחתך, אבל **רצועת החצי אינץ' של הקצה נשארה עומדת בקו
הישן**. `debug_valley8.rb` מדד בדיוק: שתי פאות ב-y=17, x -606.51..
-313.44, z 92..129.15, בחומר הרעפים. `valley_cut_deck!` לוקח רק קליפות
**מקבילות** למישור — היא לא נגעה בהן.

אותו כלל, על הפאות שאינן מקבילות: הן נחתכות **על מישור גג הבית**
(`v[:hp]`, נשמר ב-hash של האזור יחד עם `wall` ו-`inw`) וכל מה שתלוי
מתחתיו בתוך האזור נמחק. ליד הרכס הרצועה גבוהה ממישור הבית — היא נשארת,
וזו הגיאומטריה האמיתית. פאות שיושבות **על קו הקיר** לא נוגעים בהן —
אלו לוחות הסגירה, בדיוק כמו ב-`valley_low_strip_sweep!`.
מבחן פנים-האזור עם סובלנות 0.1" על המתאר, כי הרצועה יושבת בדיוק על
הקו — `point_in_poly?` לבד לא עונה שם לא כן ולא לא.

### עוד שינוי קטן
`valley_cut_deck!` מחשב את מרכז ה-inset על **כל** קודקודי הפוליגון ולא
על שלושת הראשונים — האזורים החדשים הם מרובעים, לא משולשים.

## מה נסגר היום — סעיף 2: הפאשיה של הבית (rt154)

בין שתי הרגליים האיב של הבית כבר מדולג. **מחוץ** להן הוא נבנה — ושם
מישור הגראז' עבר דרך הפאשיה: מערב x -578.51..-546.51, מזרח
x -373.44..-341.44, בדיוק 32" בכל צד, מ-z=99 (הסופיט) עד z=107 (ראש
הפאשיה). ממערב לזה (40") המישור נמוך מהסופיט והכל נשאר שלם — זו
הגיאומטריה האמיתית, לא באג.

- `valley_cuts` מקבל ארגומנט שמיני **אופציונלי** `oh` (האוברהנג של הגג
  הזה) ומחזיר בנוסף ל-`span`/`plane`/`over`/`tri` גם `lo` (שני מישורי
  הגג הנמוך) ו-`strip` (רצועת האיב מעל הגג הנמוך: 8" החוצה מקצה הגג
  בשביל הפאשיה/הדריפ/המרזב, והאוברהנג פנימה בשביל הסופיט). קריאה עם
  שבעה ארגומנטים מתנהגת כמו קודם — rt151 לא זז.
- הסוויפ רץ **בסוף הבנייה**, ממש לפני צביעת הפאות חסרות החומר: הפאשיה,
  הדריפ, הסופיט והמרזב נבנים אחרי בלוק הוואלי, אז חיתוך מוקדם יותר לא
  מוצא מה לחתוך. כל מישור של הגראז' לוקח את החצי שלו מהרצועה
  (`clip_under` על הרכס של הגראז') ומוריד את מה שתלוי מתחתיו.

## מה נסגר היום — סעיף 3: החור המשולש ליד קיר הבית (rt155)

`debug_hole.rb` (חדש, בשורש → `hole_report.txt`) מוציא כל צלע פתוחה
(פאה אחת) בחלון שסביב המפגש. משם: בקו הקיר יש פאה סוגרת רק ב-12"
שמחוץ לכל קיר של הגראז'.

מה שנסגר בפועל: **המשולש שמעל גג הגראז'** — במישור קו הקיר, מפני הקיר
של הגראז' (אוברהנג אחד פנימה מקצה הגג, כדי שייפגש עם הכיסוי הקיים ולא
ייכנס לתוכו) ועד רגל הוואלי. במודל שלו: 72" אורך, 18" גובה בקיר
(z=92 של הגראז' עד z=110 של מישור הבית), נסגר לנקודה ברגל. בחומר של
הקירות שהגג עומד עליהם (אותו דפוס כמו ה-Dutch gable, שורה ~3360).
`valley_wall_end_pair` + `valley_wall_end_tri` טהורים; `valley_close_wall_end!`
רק בונה. נקרא בסוף הבנייה, על `icuts`.

**הטעות של הסבב, לזכור:** סבב ראשון סגר את הצד ההפוך — **מתחת** לגג
הגראז', מ-z=81 עד פני הגג. הוא אישר את זה על החתך שציירתי, ואז ראה
בסקצ'אפ שני לוחות אפורים בתוך הגראז' וסימן X: "זה צריך להיות מחוץ לגג
של הגראז' ואתה עשית את זה מתחת לגג ואני אישרתי". כלומר **"כן" על ציור
שלי הוא לא אימות** — הציור היה נכון במידות ושגוי בצד. מה שסגר את זה:
הוא סימן באדום על אותו ציור בדיוק את המשולש הנכון. כשמציירים חתך,
לצייר את **שני** הצדדים של הפאה ולשאול איזה מהם.

## מה נסגר היום — סעיף 4: רידג' קאפ ורעפים עד הקודקוד (rt156)

`debug_ridge.rb` (חדש → `ridge_report.txt`) מוציא לכל גג את קו הרכס
מהמישורים שלו עצמו, את הקודקוד, וכל מה שיושב תוך 12" מהרכס. המדידה:
הקאפ של הגראז' נגמר ב-y=17 (קצה הפוליגון) והקודקוד ב-y=74.54 —
**57.54"** של רכס בלי כלום. (אמרתי לו קודם 91.5" בטעות ותיקנתי.)

הסיבה: `valley_extend!` רץ בסוף `build_roof!`, הרבה אחרי מהלך הרכסים
ואחרי מניח הרעפים — שני המשולשים פשוט לא היו בקליפה שהם קראו.

התיקון, שתי קריאות חוזרות על אותן מכונות, מיד אחרי `valley_extend!`:
- `ridge_lines(ext[:faces])` — שני המשולשים חולקים צלע אחת בדיוק, הרכס
  מקו הפוליגון עד הקודקוד, ושני הצדדים יורדים ממנה ⇒ חוזר קו רכס אחד,
  ו-`build_ridge_caps!` מניח עליו. `RIDGE_CAP_OVERSHOOT` הוא 0, אז הוא
  **נפגש** עם הקאפ הקיים ולא נכנס לתוכו.
- `RoofTilePlace.place_runs!` על `planes_from_faces(ext[:faces])` — הוא
  צילם את הקאפ רץ מעל שני משולשים חשופים. הריצות נחתכות למתאר של כל
  פאה, אז שום רעף לא עובר את רגלי הוואלי. התנאים מועתקים מילה במילה
  מהשדה (חומר/סגנון/`USE_ROOF_TILE_RUNS`).

rt156 טהורה: הקודקוד (-459.975, 74.54, 128.63), חיתוך הרכס עם קו
הפוליגון (-459.975, 17), 57.54" ביניהם, והרכס מפולס בין שניהם.

## מה נשאר

1. הדאון-ספאוט של הגראז' ליד הקיר — אינץ'-שניים דרומה. **זה האחרון.**

## מלכודות (עדיין בתוקף)

1. **זיהוי מתמונה נכשל שוב ושוב.** מה שעבד היום: תוכנית מרונדרת עם
   המידות שלו לפני הקוד, ו-`debug_valley8.rb` אחרי. הוא צילם, הדוח אמר
   איזו פאה — ורק אז נגענו.
2. `PickHelper#point_at` / `picked_point` לא קיימים ב-SU2024.
3. `def dump(L, ...)` — L באות גדולה = קבוע, SyntaxError.
4. הרגל "כן" שלו על ניסוח שלי ≠ הבנה. לשאול על החלק, לא על הפעולה.
5. יש כבר `clip_halfplane` בקובץ (שורה ~8500). אל תכתוב עוד אחד — כמעט
   קרה היום.

## קבצי מדידה (בשורש)
`debug_ridge.rb` (קו הרכס, הקודקוד, ומה יושב על הרכס) →
`ridge_report.txt` · `debug_hole.rb` (צלעות פתוחות = חורים, בחלון סביב המפגש) →
`hole_report.txt` · `debug_valley8.rb` (מה נשאר עומד באזורי החיתוך) →
`valley8_report.txt`
· `debug_pick.rb` → `pick_report.txt` · `debug_valley5..7.rb`
· `valley_report.txt` (המספרים של שני הגגות).

## לשימוש
תג: `before-valley` (git). גיבוי: `C:\Users\rordt\interior_pro_backup_2026-09-03.zip`.
