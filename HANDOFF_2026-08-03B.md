# מסירה לצ'אט הבא — Interior Pro, סבב 2026-08-03 ב' (קומות ותקרות)

## 0. לפני הכל
התיקייה החיה היחידה:
`C:\Users\rordt\AppData\Roaming\SketchUp\SketchUp 2024\SketchUp\Plugins\interior_pro`
להתעלם לגמרי מ-`C:\InteriorPro_Agent\source_readonly`.

סדר קריאה: **הקובץ הזה** ← `HANDOFF_2026-08-03.md` (הסבב הקודם, שכבת ה-2D)
← `STATUS_handoff.md` ← `CONTRACT_2D.md` ← `TARGET_PLANS.md`.

## 1. כללי עבודה (מהמשתמש, מחייבים)
- **אישור לפני מימוש.** מותר להציע, אסור לכתוב שורת קוד לפני אישור מפורש.
- עברית בלבד, הודעות קצרות, הסבר כמו לילד בן 7. בלי ז'רגון.
- **כל פקודה שהמשתמש צריך להדביק — בקופסת קוד נפרדת, תמיד**, עם ציון לאן
  (Ruby Console / טרמינל). זה כלל שהמשתמש הקפיד עליו במפורש בסבב הזה.
- כל כלי נבנה **כמו בסקצ'אפ** — לבדוק את הכלי המקביל ולעבוד לפיו.
- העוזר כותב את הקבצים לדיסק; המשתמש מריץ `InteriorPro.reload!`.
  **קובץ חדש או פריט תפריט/כפתור חדש = restart מלא לסקצ'אפ.**
- צעד אחד בכל פעם, בדיקה אצל המשתמש, ואז commit.
- git מהטרמינל של המשתמש; תמיד `del ".git\index.lock"` לפני `git add`.

## 2. בדיקות — חובה בכל שינוי
```
bash tests/run_all.sh .
```
מריצה `node --check` על ה-JS שחולץ מה-heredoc של `plan_editor.rb`, ואז
26 חבילות JS ו-**10 חבילות רובי** (rt ... rt10). הכל היה ירוק במסירה.
**אזהרה קבועה:** escape בודד בתוך ה-heredoc של plan_editor.rb הורג את כל
ה-JS בשקט. הבדיקות תופסות את זה.

## 3. מה נבנה בסבב הזה

### 3.1 תקרות — `ceiling_manager.rb` (קובץ חדש)
נבנות מ-`boundary_xy` של החדר, בגובה `floor_level + ceiling_height_in`.
ברירת מחדל לגובה = **הקיר הנמוך ביותר** מבין הקירות הגובלים (מינימום, כדי
שקיר משותף גבוה של הבית לא ידחוף למעלה את תקרת הגראז'), פחות מפלס הרצפה.

**שני סגנונות (`ceiling_style`) — החלטת המשתמש:**
- `'flat'` — **ברירת המחדל.** פאה יחידה, **אפס עובי**, לבנה משני הצדדים.
  שם יתווספו בעתיד תאורה/ספוטים. תקרה ישנה בלי attribute הופכת ל-flat.
- `'framed'` — עם עובי (גבס 0.5"), לקומה שנייה.

Tag ‏`IP/Ceilings`. כפתור Ceilings בסרגל Floors (הגארד עלה מ-3 ל-4) +
תפריט Build/Remove. `refresh!` אחרי Sync חדרים **בונה מחדש רק תקרות
קיימות** — חדר בלי תקרה לא מקבל אחת בכוח.

עוזרי קונסול: `set_ceiling_height!('Room 1', 108)`,
`set_ceiling_style!('Room 1', 'framed')`.

### 3.2 קומות — `level_manager.rb` (קובץ חדש)
**רשימת הקומות** (מספרים על המודל, ניתנים לשינוי):
גובה קיר 96" + קורות **9.25" ‏(2x10, ברירת מחדל שאושרה)** + סאבפלור 0.75"
→ `level_base(2) = 106"`. מעבר ל-2x12:
`LevelManager.set_structure!(joist: 11.25)`.

**המודל שהמשתמש בחר במפורש: אין אלמנט rim נפרד.** קיר החוץ עצמו מזהה שיש
קומה מעליו ופשוט **עולה עד רצפת קומה 2** — הסטאקו/לבנים ממשיכים לבד כי זה
אותו קיר.

- `build_level2_structure!` — כל קיר חוץ של **קומה 1** עולה ל-`level_base(2)`.
  לפני העלייה הוא זוכר **פעם אחת** את גובה התקרה שלו ב-attribute
  **`ceiling_h`** (הרצה שנייה לא דורסת). בסוף נבנה הסאבפלור.
- `remove_level2_structure!` — מחזיר כל קיר עם `ceiling_h` לגובה המקורי,
  מוחק את ה-attribute ואת הסאבפלור, מרענן מולדינג.
- **סאבפלור** (`type='subfloor'`, Tag ‏IP/Floors): הפאה העליונה ב-`level_base(2)`,
  עובי 0.75" כלפי מטה, חומר עץ. **הגבול שלו = הלולאה של קירות קומה 2** אם
  קיימת (החלטת משתמש: קומה 2 לא תמיד חופפת לקומה 1, ואיפה שאין קומה 2 יבוא
  **גג** ולא רצפה). אין עדיין קירות קומה 2 → נבנה על כל שטח קומה 1 כברירת מחדל.

**קומה פעילה:** `active_level` על המודל + תפריט
"Level: Work on Level 1 / Work on Level 2". `place_wall_on_active_level!`
נותן לקיר חדש `level` ומרים אותו ל-`level_base` דרך `set_wall_base!` הקיים.

### 3.3 ציור על מישור הקומה (`wall_tool.rb`)
כל הציור עובר למישור הקומה הפעילה — לא "קופץ" אחרי הקליק:
`active_base` + `pick_point` (נקודה חופשית מוטלת על מישור הקומה),
`snap_to_axis`, `apply_length_input`, `raw_cursor_position`,
ו-`compute_wall_points` (z1/z2 מהבסיס). קומה 1 = התנהגות ישנה בדיוק.

`create_wall` קורא ל-`place_wall_on_active_level!` **לפני** `join_corners`.

### 3.4 הפרדת קומות — שומרי level שנוספו
קירות של קומות שונות חולקים אותם x/y, וכל ההשוואות מושטחות ל-z=0, ולכן:
- `wall_tool.rb ‏find_neighbor_at` — מיטר **רק בין קירות מאותה קומה**.
  הסינון לפי attribute ‏`level`, **לא** לפי base_z — הגראז' (level 1 עם
  בסיס מוזז) שומר על פינות ה-mixed-base שלו.
- `ui_dialogs.rb ‏Move Wall` — שותפי פינה ו-T רק מאותה קומה + **רענון
  פאונדיישן אוטומטי אחרי הזזה** (הבטון היה נשאר מאחור).
- `wall_stretch_tool.rb` — זיהוי partners רק מאותה קומה (זה גם המסלול של
  העורך הדו-ממדי).
- `foundation_manager.rb` — `exterior_walls` ו-`interior_walls` רק level 1
  (אחרת נוצר בטון תלוי באוויר מתחת לקירות קומה 2).
- `room_manager.rb ‏wall_list` — **שומר זמני:** רק קירות level 1 משתתפים
  בזיהוי חדרים, עד שיהיו חדרים פר-קומה.

### 3.5 מולדינג — שני תיקונים
- **קראון נעצר בתקרה:** `build_crown_on_edge!` קורא `ceiling_h` ומשתמש בו
  במקום בגובה הקיר המורם. בלי זה הקראון טיפס ל-106".
- **בייסבורד עובר מתחת לחלונות:** פתח קוטע את הבייסבורד רק אם
  `floor_offset` שלו נמוך מגובה הפרופיל. דלת (0) קוטעת; חלון (32") לא.
  זה היה באג ויזואלי שהמשתמש זיהה בצילום.

### 3.6 בדיקות חדשות
- `tests/rt8.rb` — תקרות: שני הסגנונות, גובה מהקיר הנמוך, גראז' יחסית
  לרצפה שלו, legacy → flat, build/refresh/remove.
- `tests/rt9.rb` — קומות: הרשימה, קירות שעולים, `ceiling_h` שורד הרצה
  שנייה, הסאבפלור (גובה, כיוון, גבול), הצטמצמות לפי קירות קומה 2, החזרה.
- `tests/rt10.rb` — קומה פעילה: מיקום קיר, שומרי ה-level בכל הקבצים,
  ציור על המישור, פאונדיישן level-1.
- `run_all.sh` מעתיק עכשיו גם ceiling_manager, level_manager, molding_tool,
  wall_tool, foundation_manager, ui_dialogs, wall_stretch_tool.

## 4. הצעד הבא (סוכם עם המשתמש)
**לחבר את העורך הדו-ממדי (`plan_editor.rb`) לקומות** — המשתמש אמר במפורש
שהוא יסרטט את קומה 2 ב-2D. התוכנית שסוכמה:
1. בורר קומה בפאנל העורך.
2. קיר שנוצר ב-Apply נולד בקומה הפעילה (אותו `place_wall_on_active_level!`).
3. הקנבס מציג **רק את קירות הקומה הפעילה** (אחרת שתי הקומות מצטיירות אחת
   על השנייה). אפשרות עתידית: הקומה שמתחת ברקע אפור בהיר, כמו בתוכנות
   אמיתיות.

**זהירות:** זה נוגע ב-heredoc של plan_editor.rb — לעבוד קטן ולהריץ את
חבילת הבדיקות אחרי כל שינוי.

## 5. מה נשאר פתוח
1. **חדרים / רצפות / תקרות לקומה 2** — מנוע זיהוי החדרים משטח הכל ל-z=0.
   צריך זיהוי פר-קומה, ואז הרצפות והתקרות של קומה 2 יעבדו לבד.
2. **כלי גג** — איפה שקומה 2 לא מכסה את קומה 1 (הסאבפלור כבר עוצר שם).
3. **דלתות/חלונות על קירות קומה 2** — לא נבדק בכלל בסבב הזה.
4. **`plan_generator.rb` לא מכיר קומות** — התוכניות עדיין של קומה 1.
5. **הרפאים בזמן הציור** — עובד על מישור הקומה, אבל לא נבדק לעומק מול
   הצמדות לקירות של הקומה שמתחת.
6. מהסבב הקודם: קירות בקשת, חיבור ל"דיאלוגים המלאים" של דלת/חלון,
   Area Plan, ריהוט, בועות גריד, אליוויישנים פנימיים, LayOut/PDF.

## 6. חוב ידוע שלא נגענו בו
- אזהרות `already initialized constant` מ-`door_library_dialog.rb` בקונסול —
  רעש ישן של טעינה כפולה, **לא שגיאה**.
- באג שורש `DoorManager.wall_geometry` (center_offset בסימני הניצב השמאלי
  מול `n` ימני). עקיפה: העורך יוצר תמיד קירות `bottom-left`. **לא לשנות
  בלי אישור.**
- `hatch_polygon` ב-`plan_generator.rb` מנוטרל בכוונה.
- רעש CRLF/LF ב-git בקבצי ה-door וב-CLAUDE.md.

## 7. קבצים שהשתנו/נוספו בסבב
**חדשים:** `ceiling_manager.rb`, `level_manager.rb`, `icons/ceiling_tool.svg`,
`tests/rt8.rb`, `tests/rt9.rb`, `tests/rt10.rb`.
**שונו:** `main.rb`, `toolbar.rb`, `wall_tool.rb`, `room_manager.rb`,
`molding_tool.rb`, `foundation_manager.rb`, `ui_dialogs.rb`,
`wall_stretch_tool.rb`, `tests/run_all.sh`.
