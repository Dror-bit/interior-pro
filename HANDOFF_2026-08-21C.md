# Interior Pro — מסירה 2026-08-21 ג' (גג המתכת נסגר)

קרא את זה קודם, ואז `CLAUDE.md`. הקודמת: `HANDOFF_2026-08-21B.md`.
**אל תסמוך על הסיכום — תאמת מול הקוד החי.**

**הבדיקות: 132 ירוקות.** מריצים בענן (אין לו רובי):
`cd tests` ואז `bash run_all.sh ..` — **חובה `LANG=C.UTF-8`**, בלעדיו rt40
ו-rt49 נופלות על קידוד בלבד.

> **git:** נתתי לו פקודת commit בסוף הסבב. תריץ `git status` (דרך CMD שלו,
> לא בעצמך!) לוודא שהיא באמת רצה לפני שאתה נוגע במשהו.

---

## 0. הדבר הכי חשוב שנלמד היום

**הסטאב מקבל פאה עקומה; SketchUp האמיתי זורק `ArgumentError`.**

חיתוך הפינה של הקאפ + ה-tuck בצומת עיקמו פאות ב-0.0115" — מעבר לסבילות —
וב-SketchUp האמיתי החתיכה מתה חצי-מצוירת בזמן שכל הבדיקות ירוקות. הוא ראה
שני היפים בלי קאפ: *"רידג' קאפ המקביל לאדום ירד."*

**התיקון:** ב-`build_cap_piece!` פאה שנדחית מפוצלת למשולשים (משולש תמיד
מישורי) והתפרים של הפיצול מוחלקים. **המנגנון:** בזנב `rt18.rb` יש עכשיו
harness עם `add_face` קפדני שמחקה את SketchUp — כל קוד חדש שבונה פאות חייב
לעבור גם בו, לא רק בסטאב הרגיל.

ולקח שני מאותו ערב: *"לא עובד לי"* התברר כ**הגדרות גג שהתאפסו במודל**
(style=flat, חומר metaltile) — לא באג בקוד. לפני שמאשימים קוד:
`RoofManager.settings.inspect`.

---

## 1. מה נסגר היום — הכל בגג המתכת (seam) בלבד

כל שינוי מגודר ב-`seam?`. רומן, ספניש, סלייט ושינגלס לא זזו, ויש בדיקות
שנופלות אם יזוזו.

### הבליטות (roof_tile_place.rb)

1. **נמדדות איפה שהן מצוירות.** `seam_profile` יושב על `run_pitch/2` ימינה
   מהמקור של הסלוט, אבל האורך נחתך על המתאר במקור. בהיפ זה נתן 6 בליטות
   קצרות ב-10.8" ו-6 ארוכות ב-9.5" (נמדד על הגג שלו). עכשיו `u_off` מוסט
   לכל הקריאות ל-`v_spans_at`. **rt82 עצמו מדד את אותה הנחה שגויה** ולכן
   עבר כשהגג שבור — עודכן למדוד ברמקום הנכון + בדיקה שהקריאה הישנה נכשלת.
2. **גריד עולמי אחד** (`seam_slots`): כל המישורים שמים את הבליטות על אותם
   קווים במרחב — בליטה מצד אחד של הרידג' פוגשת את התאומה שלה. קו שנופל
   בדיוק על קצה תחום נזרק בשני הקצוות (half-open חד-צדדי שם בליטה על רייק
   אחד ולא על השני).
3. **נחתכות לפני הכל:** `ridge_setback: 4.0` **מוצהר** בטבלת seam (היה 0),
   כלל "מוצהר גובר" כמו `cap_crown_stated?`. למעלה — מתחת לקאפ (מכסה 5"),
   למטה באיב — נצמדות לפנים הפנימיות של המסגרת (`run_height` במקום
   overhang), ורגל שנחתכה באמצע שיפוע (ואלי/דורמר) — אותו setback מהצד
   השני, מתחת לתעלת הוואלי.

### המסגרת (eave bar)

- יושבת **על הדק, צמודה לקצה** (v=0). קודם ריחפה 1.25" מעבר עם רווח אוויר
  0.25" — הקו האדום שסימן.
- שתי מסגרות שנפגשות בפינה נחתכות **בגרונג על חוצה-הזווית** — שני פני
  החיתוך תמונת-ראי ומתלכדים נקודה-בנקודה (נמדד). בגלל זה כל מסגרת נבנית
  כקבוצה של 6 פאות ולא כ-instance נמתח — אי אפשר לגרונג קופסה משוכפלת.

### הרידג'/היפ קאפ (roof_manager.rb)

- **בלי taper** לקצה התחתון (רק חימר שומר אותו) — החוד שגלש על הפינה.
- **חיתוך פינה** (`miter_lo` ב-`build_cap_piece!`): כל נקודת פרופיל נסוגה
  לאורך הקו לפי היסט התכנית שלה — על פינה ישרה זה נוחת בדיוק על שני קווי
  האיב. *"פינה שלא יעבור את ה-90 מעלות של הגג."*
- **חצאית** (`skirt`): הקאפ רוכב בגובה בליטה, ומאז שהן נעצרות לפניו נראה
  חלול — חצאית יורדת מכל שפת התחתית עד הדק. חימר: skirt=0.
- **הקמט מכבד crown מוצהר**: `cap_profile_flat` דחף באמצע את הקבוע
  `RIDGE_CAP_CROWN` במקום הפרמטר — קיפול "0" בלט אינץ'. חימר לא זז
  (שם crown == הקבוע ממילא).

### תעלת הוואלי

*"זה לא מכסה זה יותר מוליך מים."* `ridge_lines(faces, valleys: true)` —
אותה הליכה עם הפילטר הפוך (שני הצדדים מטפסים). `build_roof!` מוסיף את
קווי הוואלי לרשימת הקאפים **רק ל-seam**, וב-`build_ridge_caps!` קו ואלי
מזוהה (`sides` חיוביים) ומקבל: **בלי הרמה, בלי חצאית, בלי גרונג** — פס
שטוח בתוך ה-V על הדק, חתיכה אחת רצופה.

## 2. קבצים שהשתנו היום

`roof_tile_place.rb`, `roof_tile_math.rb`, `roof_tile_parts.rb`,
`roof_manager.rb`, `tests/rt18.rb`, `tests/rt82.rb`.

## 3. מלכודות מכניות שעלו זמן

- **פאה לא-מישורית** — §0. הכי חשוב.
- **הגדרות המודל מתאפסות** — §0. גם: אחרי reload חייבים `build_roof!`,
  ו-`[Roof] flat over level 1` בקונסול אומר שאתה במודל/הגדרות לא נכונים.
- **סקריפט אבחון לקונסול חייב להיות טיפשי-פשוט.** שלחתי שורה עם `rescue`
  בתוך סוגריים — SyntaxError, סבב שלם של בלבול. בלי modifiers מתחכמים.
- **בדיקה שמודדת את אותה הנחה כמו הקוד לא שווה כלום** — rt82 והמדידה
  ב-u של המקור. למדוד את מה שמצויר.

## 4. מה פתוח — לפי סדר

1. **הגג השני** — הוא אמר שיש עוד גג לתקן ועוד לא הראה אותו. תבקש צילום.
2. **רעפי איב ורייק לרומן** — `RoofTileParts.eave` / `.rake` קיימים, אף
   אחד לא מציב אותם.
3. מה שנשאר פתוח מ-`HANDOFF_2026-08-21.md` §5.
4. ה-TODO הקבועים ב-`CLAUDE.md`: ה-NOTE המצויר, צבע קו בגיליון, לוגו
   פר-התקנה, ליטוש corner boards.

## 5. איך הוא רוצה לעבוד

- עברית, **משפט או שניים**, כמו לילד בן 5. כל פקודה בקופסה נפרדת עם ציון
  איפה להדביק (Ruby Console / CMD).
- **בדיוק מה שביקש, לא יותר.** מספר שלא ביקש לשנות הוא באג גם אם הוא
  נראה לך נכון יותר.
- לא ברור? **שאלה אחת קצרה** במקום לנחש.
- **תמדוד לפני שאתה משנה, ותוודא שהמדידה מודדת את הדבר הנכון.** פעמיים
  היום מדידה נכונה חסכה סבב שלם, ופעם אחת בדיקה עיוורת עלתה אחד.
