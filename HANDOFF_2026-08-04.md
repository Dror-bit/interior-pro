# מסירה לצ'אט הבא — Interior Pro, סבב 2026-08-03/04 ג' (עורך + קומות)

## 0. לפני הכל
התיקייה החיה היחידה:
`C:\Users\rordt\AppData\Roaming\SketchUp\SketchUp 2024\SketchUp\Plugins\interior_pro`
להתעלם לגמרי מ-`C:\InteriorPro_Agent\source_readonly`.

סדר קריאה: **הקובץ הזה** ← `HANDOFF_2026-08-03B.md` (תקרות/קומות, הסבב הקודם)
← `HANDOFF_2026-08-03.md` ← `STATUS_handoff.md` ← `CONTRACT_2D.md` ← `TARGET_PLANS.md`.

## 1. כללי עבודה (מהמשתמש, מחייבים — לא השתנו)
- **אישור לפני מימוש.** מותר להציע, אסור לכתוב קוד לפני אישור מפורש.
  תיקון באג שהמשתמש דיווח = מותר לתקן ישר; פיצ'ר חדש = לשאול קודם.
- עברית בלבד, הודעות קצרות, הסבר כמו לילד בן 5-7. בלי ז'רגון.
- **כל פקודה להדבקה — בקופסת קוד נפרדת**, עם ציון לאן (Ruby Console / cmd).
- העוזר כותב קבצים לדיסק; המשתמש מריץ `InteriorPro.reload!`.
  שינוי HTML בעורך = לסגור ולפתוח את הדיאלוג. קובץ/כפתור חדש = restart מלא.
- צעד אחד, בדיקה אצל המשתמש, commit (מהטרמינל של המשתמש,
  `del ".git\index.lock"` לפני `git add`).
- אחרי כל כתיבה מרחוק לאמת גודל קובץ מול device_list_dir.

## 2. בדיקות — חובה בכל שינוי
```
bash tests/run_all.sh .
```
עכשיו: 29 חבילות JS (‏t1–t31, נוספו t29/t30/t31) + **14 חבילות רובי**
(‏rt–rt14, נוספו rt11–rt14). הכל היה ירוק במסירה.
**אזהרה קבועה:** escape בודד ב-heredoc של plan_editor.rb הורג את כל ה-JS בשקט.

## 3. מה נבנה בסבב הזה (הכל נבדק ע"י המשתמש ועובד)

### 3.1 העורך הדו-ממדי מחובר לקומות
- **בורר קומה 1/קומה 2** בתחתית הפאנל (callback ‏`set_level`,
  ‏`loadLevel` ב-JS, מסונכרן עם `LevelManager.active_level`).
- הקנבס מציג **רק את הקומה הפעילה** (`walls_payload` מסנן לפי `level`).
- קיר מ-Apply נולד בקומה הפעילה: `apply_walls` קורא
  `place_wall_on_active_level!` לפני `join_corners` (כמו create_wall).
- **קומת רקע ("רוחות")**: כשעובדים על קומה 2, קומה 1 מצוירת אפורה-שקופה
  (`ghost_walls_payload` ← `loadGhosts`), לא ניתנת לבחירה, אבל **נצמדים
  אליה**: פינות/אמצעים ב-snapPoint + אינפרנס מקבילים ב-segDirs.
  מיטרים של רוחות מחושבים רק בין הרוחות לבין עצמן.

### 3.2 Shift + דקירת נקודה בגרירת קיר הצידה
בזמן גרירת קיר הצידה, החזקת Shift + כיוון לנקודה (כולל פינת רוח של
הקומה שמתחת) מקפיצה את **הפאה הקרובה** של הקיר בדיוק על הנקודה — ככה
מניחים קיר קומה 2 בדיוק על קיר קומה 1. יישור זעיר (<1/4") עדיין נשמר
(עוקף את סף הרבע אינץ' כשיש snapPt). ‏t30.

### 3.3 חלונות על קירות מורמים — תוקן
`window_tool.rb cut_window_opening`: גוף החלון מקבל את ה-Z של
טרנספורמציית הקיר (`wall_z`) — החור היה נחתך נכון (native), אבל הגוף
נשאר בגובה קומה 1. ‏`bottom_z`/`top_z` נשמרים עכשיו עולמיים.
חלונות ישנים תקועים למטה: למחוק ולהציב מחדש.

### 3.4 תקרות אוטומטיות לקומה שמתחת
`LevelManager.ensure_ceilings_below!` — נקרא מ-`place_wall_on_active_level!`
כשהקומה הפעילה >1: כל חדר בקומות שמתחת בלי תקרה מקבל אחת (flat).
תקרה שנמחקה חוזרת עם הקיר הבא בקומה 2 (החלטת משתמש: אוטומטי תמיד).

### 3.5 מבנה בין-קומות אוטומטי — "בלי רווח"
`LevelManager.ensure_structure_below!` — רץ **אחרי** ה-commit של יצירת
קיר (גם ב-wall_tool.create_wall וגם ב-plan_editor.apply_walls) כשהקומה
הפעילה >1: קירות החוץ של קומה 1 עולים ל-`level_base(2)=106` (זוכרים
`ceiling_h` פעם אחת) + נבנה הסאבפלור. סוגר את הרווח של 10" בין הקופסאות.

### 3.6 מולדינג מודע לקומות
`molding_tool.rb`: ‏`wall_lvl` חדש; ‏`resolve_miters!`/`resolve_tees!`/
`resolve_flush_butts!` מזווגים רק בתוך אותה קומה (הקומות חולקות x/y
והחישוב שטוח). ‏`sides_for` משתמש ב-`room_polys(level)` של קומת הקיר;
אין חדרים בקומה → היוריסטיקת מרכז. גובה כבר היה תקין (base_z). ‏rt12.

### 3.7 חדרים ורצפות פר-קומה (השומר הזמני הוסר!)
`room_manager.rb`: ‏`wall_list(level=1)`, ‏`wall_levels`, ‏
`detect_rooms!(level:)`, ‏sync רץ פר-קומה, התאמת centroid **רק בתוך אותה
קומה** (חדר קומה 2 יושב בדיוק מעל חדר קומה 1 ואסור שיגנוב לו את ה-id).
תווית חדר בגובה `level_base+0.25`. ‏`floor_manager.rb`: חדר בקומה >1
מקבל `floor_level` ברירת מחדל = בסיס הקומה (106) ו-attr ‏level נכון.
העורך: תוויות חדרים ושטח חי פר-קומה פעילה. ‏rt13.

### 3.8 כלי Move עובד על הכל
דרישת משתמש מפורשת: "הוא צריך לעבוד על הכל — חומות, גדרות, מטבחים,
בריכות בהמשך". ‏`startFreeMove` אוסף עכשיו קירות מוחלים + קירות כחולים +
צורות; הכל זז ביחד (אחיזה ← נעילת ציר ← הצמדה ← הקלדת מרחק). ב-commit:
צורות דרך `update_sketches`, קירות דרך callback חדש **`move_selection`** ←
**`translate_walls!`**: מזיז start/end + ‏corners_xy (שטוח!), בונה מחדש,
מזיז גופי דלתות/חלונות של הקיר, ואז join_corners + sync חדרים + מולדינג +
פאונדיישן. כפתור ה-Move (ebMove) דולק גם לקירות. ‏t31, rt14.

### 3.9 באג שנתפס ותוקן — לקח חשוב
**corners_xy הוא מערך שטוח [x1,y1..x4,y4], אבל `read_corners_attr`
מחזירה זוגות [[x,y]×4].** ערבוב ביניהם קרס בשקט בתוך rescue וההזזה
התבטלה (הקירות "חזרו למקום"). ‏rt14 היא בדיקת הרגרסיה. כשמזיזים
attributes של פינות — לקרוא את ה-attribute הגולמי, לא דרך ההלפר.

## 4. מה נשאר פתוח (לפי סדר)
1. **תקרה עליונה לחדרי קומה 2** (מעל קומה 2) — המשתמש טרם ביקש/אישר
   אוטומציה; כפתור Ceilings ידני אמור לעבוד כבר (החדרים קיימים).
2. **דלתות על קירות קומה 2** — לא נבדק (חלונות תוקנו; לדלתות יש כנראה
   base-awareness מהגראז', לוודא).
3. **חפיפת פאות**: רצפת גימור של קומה 2 (top=106) קו-פלנרית עם הסאבפלור
   (top=106) — עלול להבהב (z-fight). אם מפריע: להזיז/להחריג.
4. **הזזה חלקית**: Move של חלק מחיבור משאיר מיטר ישן אצל השכן שנשאר.
5. **plan_generator.rb לא מכיר קומות** — תוכניות עדיין של קומה 1.
6. **כלי גג**; חזיתות; מהסבבים הקודמים: קירות בקשת, דיאלוגים מלאים
   מהעורך, Area Plan, ריהוט, LayOut/PDF.

## 5. חוב ידוע שלא נגענו בו
- באג שורש `DoorManager.wall_geometry` (עקיפה: העורך יוצר bottom-left).
  **לא לשנות בלי אישור.**
- `hatch_polygon` מנוטרל בכוונה; אזהרות `already initialized constant`
  מ-door_library_dialog = רעש, לא שגיאה; רעש CRLF/LF ב-git.

## 6. קבצים שהשתנו/נוספו בסבב
**שונו:** `plan_editor.rb` (הרבה), `level_manager.rb`, `wall_tool.rb`,
`window_tool.rb`, `molding_tool.rb`, `room_manager.rb`, `floor_manager.rb`,
`tests/run_all.sh`, `tests/rt9.rb`.
**חדשים:** `tests/rt11.rb`–`tests/rt14.rb`, `tests/t29.js`–`tests/t31.js`.
