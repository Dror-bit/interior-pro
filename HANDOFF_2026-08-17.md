# Interior Pro — מסירה 2026-08-17 (גדרות / Landscape Pro)

קרא את זה קודם, ואז `CLAUDE.md`. אל תסמוך על הסיכום — תאמת מול הקוד החי.
הקובץ הקודם: `HANDOFF_2026-08-16.md`. כללי העבודה: `HANDOFF_2026-08-14.md` §0.

התיקייה החיה:
`C:\Users\rordt\AppData\Roaming\SketchUp\SketchUp 2024\SketchUp\Plugins\interior_pro`

---

## 0. השורה התחתונה של הסבב הזה

**גדר = מודל skp שהוא בונה, מונח לאורך קו.** לא מספרים, לא מנוע.
הוא ניסה את המנוע הפרמטרי פעמיים ואמר "זה אפילו לא דומה". הוא צודק —
מספרים לא מייצרים את מה שהוא מצייר. **אל תחזור לבנות גדרות מגאומטריה.**

הכלי: `landscape/fence_ref_tool.rb`. הכפתור **Fence** בסרגל Landscape Pro
פותח רשימת קבצים מ-`landscape/reference/*.skp`, ואז שתי לחיצות.

## 1. מה עובד (אושר אצלו: "יופי")

- הקובץ הראשון שלו (`cable railing`, גרסה 1) — **3 יחידות של Component#2**
  (עמוד+פאנל, 78.7") + עמוד סוגר. הכלי זיהה, סובב מ-Y ל-X, הציב, מתח
  באחוזים בודדים. **נראה בדיוק כמו המודל שלו.**
- `rt63` — 85 בדיקות ירוקות. שלושה מצבי קריאה (ראה §2) + מצב CUT.
- הפאנל של הקירות/דלתות/חלונות — לא נגעתי.

## 2. שלושה מבנים של קובץ, וזה קריטי

הקורא (`shape_of`) מזהה לפי סדר:

| מצב | מתי | מה מוצב |
|---|---|---|
| **unit** | קומפוננט גדול (≥50% גובה, ≥12" אורך) שחוזר ≥2 | הקומפוננט, כל pitch |
| **parts** | עמודים (גבוהים, צרים, אותו definition ≥2) + חלקים חופשיים | עמוד + כל מה שבשדה הראשון, מורכב |
| **whole** | **יש פאות חופשיות ברמה העליונה**, או שכלום לא זוהה | **כל ה-definition** כיחידה אחת. pitch = מעמוד לעמוד |

**הגרסה השנייה של הקובץ שלו = מודל גולמי:** 57 חלקים + **5,868 פאות
חופשיות**. עמוד ראשון ואחרון definitions שונים. עמודים אמצעיים = פאות.
→ מצב whole. יחידה = 180" (3 שדות). **מתיחה גדולה** אם הקו לא כפולה
של 180. הוא ראה את זה: "אם אני מציירת יותר ארוך זה מסתדר וזה בעייתי".

**מה הוא ביקש ולא נסגר:** להוריד את המוטות האמצעיים ולחתוך לשדה אחד.
ניסיתי בסקריפט (`debug_fence_trim.rb`) — **הרס את המודל** (מחק את
העמודים האמצעיים כי הם פאות דקות). שוחזר מ-`cable railing - original.skp`.
**אל תנסה לזהות חלקים במודל גולמי בסקריפט.** הוא אמר שיכין "גדר נורמלית" בעצמו.

**מה כן נסגר אחרי זה (הוא אישר: "תעשה מה שרצית"):** מצב **CUT** ב-`layout_for`.
יחידות שלמות בגודל אמיתי, והשארית = יחידה אחת **חתוכה במישור** (`cut_stub!`
— סכין, לא זיהוי: explode לעותק, `intersect_with`, מחיקת מה שמעבר למישור).
מתיחה עד 6% נשארת מתיחה (בלי תפר). `USE_CUT = false` = ההתנהגות הישנה.
**נבדק אצלו ונכשל:** על ה-cable railing הגולמי הסכין (`intersect_with` על
20k פאות, חלקן מוסתרות) **תקע את סקצ'אפ** + הודעה "visible geometry merged
with hidden". **`USE_CUT = false` בקובץ.** הקוד נשאר, `rt63` בודק אותו עם
דגל דלוק ומכבה בסוף. להדליק רק אחרי שראית אותו על קובץ קטן ונקי.
כרגע ההתנהגות = מתיחה, כמו שהוא אישר קודם ("יופי").

## 3. מה לבקש ממנו כשהוא שולח קובץ גדר

- **שדה אחד:** עמוד – פאנל – עמוד. אז היחידה קטנה והמתיחה זניחה.
- עדיף שהחלקים יהיו קומפוננטים/קבוצות, לא פאות חופשיות. אבל **גם גולמי
  עובד** (מצב whole) — רק המתיחה גדולה יותר.
- הכיוון (X או Y) לא משנה — הקורא מסתדר.
- Z נשאר כמו שהוא ציירת (בכוונה, 2026-08-17 — לא מזיזים לאפס).

## 4. קבצים

| קובץ | מה |
|---|---|
| `landscape/fence_ref_tool.rb` | **הכלי.** שתי לחיצות. `shape_of` = הקורא. `place!` = ההצבה. `layout_for` = כמה יחידות ומתיחה |
| `landscape/fence_math.rb`, `fence_tool.rb`, `fence_library.rb`, `fence_library_dialog.rb` | המנוע הפרמטרי. **לא בסרגל.** נגיש מהקונסולה. rt56/57/61/62 ירוקים. **אל תמחק, אל תחבר** |
| `landscape/reference/*.skp` | הגדרות שלו. 6 מהסבב הראשון + `cable railing` + `cable railing - original` |
| `debug_fence_parts.rb` | מפרק קובץ ומראה מבנה. **תריץ על כל קובץ חדש לפני שנוגעים בקוד** |
| `debug_fence_placed.rb` | מה הוצב במודל + מה הקורא מסיק מכל קובץ |
| `debug_fence_ref.rb` | המדידה של ששת הקבצים הראשונים → `fence_ref_report.txt` |
| `debug_fence_trim.rb` | **לא לשימוש.** הרס מודל. נשאר לתיעוד |
| `tests/rt63.rb` | הכלי, שלושת המצבים, סטאבים לשלושת הקבצים שלו |

`toolbar.rb`: `open_fence_library` = הכניסה היחידה, מהכפתור ומהתפריט.
`main.rb`: טוען `landscape/fence_ref_tool.rb`.
סטאב: `definitions.load` דרך `register!`, `add_instance`, `Group#definition`
לא קיים בסטאב (בסקצ'אפ כן — הקוד משתמש ב-`respond_to?`).

## 5. הצעד הבא — הוא בחר: **ויניל**

- הוא ישלח קובץ ויניל. תריץ `debug_fence_parts.rb` עליו **קודם**, תקרא
  את המבנה, ורק אז תצייר.
- ואז: **שיפועים** (ground_start/ground_end כבר שמורים — למלא ולסובב
  יחידה, או מדרגות) ו**פינות** (שתי גדרות נפגשות, עמוד משותף).
- מצב CUT בנוי אבל **כבוי** (§2) — תקע לו את סקצ'אפ על מודל גולמי. אל תדליק
  בלי קובץ קטן ונקי לבדיקה.

## 6. מה למדתי הסבב הזה (כדי שלא תחזור על זה)

1. **"אני לא סומך עליך" = תראה לו קודם על המודל שלו, ואז תגע בקוד.**
   הסקריפט שהציב שדה אחד מהקובץ שלו לפני שכתבתי כלי — זה מה שקנה אמון.
2. **מספרים לא מייצרים את מה שהוא רואה.** שני סבבים על זה. די.
3. **אל תחתוך גאומטריה גולמית בסקריפט.** 20,000 פאות = אין "עמוד".
4. **פחות מילים.** הוא התעצבן פעמיים על הסברים ארוכים ושאלות מיותרות.
   קופסה אחת, משפט אחד, לאן להדביק.
5. כשהוא כותב "לא עובד" — צילום מסך + `debug_fence_placed.rb`. לא לנחש.
