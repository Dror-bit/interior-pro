# Interior Pro — מסירה 2026-08-19B (גג רעפים, דפים ל-PDF, מידות ומחק)

קרא את זה קודם, ואז `CLAUDE.md`. הקודמת: `HANDOFF_2026-08-19.md`.
**אל תסמוך על הסיכום — תאמת מול הקוד החי.**

התיקייה החיה:
`C:\Users\rordt\AppData\Roaming\SketchUp\SketchUp 2024\SketchUp\Plugins\interior_pro`

**הבדיקות: 131 חבילות ירוקות** (81 רובי + 50 ג'אווהסקריפט). היו 122.
נוספו `rt78`–`rt81`, `t49`–`t53`. עודכנה `rt65`.

> **rt51/rt52 ירוקות אצלו.** אם הן נופלות אצלך — אתה מריץ מתוך כונן משותף
> שלא מרשה `unlink`. תעתיק את התוסף לדיסק מקומי ותריץ משם. **אל תתקן אותן.**

---

## 0. הדבר הכי חשוב במסמך הזה

> **בזבזתי שלושה סבבים על ניחושים במקום למדוד, על באג שהיה מספר אחד.**

המשתמש אמר "אי אפשר לבחור מידה" ו"המחק לא מוחק מידות". ניחשתי שלוש פעמים —
פעם שהוא בעורך הדו-ממדי (לא היה), פעם שהמחק לא מכיר מידות (נכון אבל לא הסיבה),
פעם שהמיקום מחושב לא נכון (לא). בסוף הוספתי **גשש לחיצות** שכותב לקובץ מה כל
לחיצה קיבלה ומה בדיקת הפגיעה ענתה. תוך לחיצה אחת:

```
הלחיצות שלו נחתו 11–43 פיקסלים מקו המידה.  רדיוס התפיסה: 10.
```

שום דבר לא היה שבור. **המטרה הייתה ברוחב שערה** על דף של 15,876 פיקסלים, עם
1622 קווים חופשיים פרושים מעליה שהעכבר כן תפס במקום.

**הכלל:** לפני שאתה משנה שורת קוד בגלל "זה לא עובד" — תמדוד איפה הלחיצה נחתה.
הגשש נשאר בקוד, כבוי:
`InteriorPro::PlanSheetDialog.probe = true` ← כותב ל-`sheet_probe.txt`.

**וכלל שני, שעלה לי הכי הרבה זמן:** *תשאל באיזה חלון הוא עובד לפני שאתה כותב.*
יש שניים — `PlanSheetDialog` (דפים, Export PDF) ו-`PlanEditor` (עורך דו-ממדי).
בזבזתי סבב שלם על ההנחה שהוא בשני. הצילום שלו הכריע, לא הטיעון שלי.

---

## 1. גג רעפים — צעדים 3 ו-4 (`ROOF_TILES_PROPOSAL.md` §6)

### צעד 3 — `RoofManager.roof_edges` + `rt78`
עטיפה דקה מעל המסווגים הקיימים, **לא מסווג שני**. מחזירה
`{ eave:, rake:, ridge:, hip: }`, כל קטע `{kind:, a:[x,y,z], b:[x,y,z], edge:, wall_id:}`.
- רכס↔היפ: קטע מפולס = רכס, משופע = היפ. זו ההחלטה החדשה **היחידה**.
- קצה עם גמלון נחתך בדיוק כמו שהפאסיה כבר נחתכת (`chain_regions_above`).
- `rt78` נופלת אם `ridge+hip` לא שווה בדיוק ל-`drop_facet_hips(ridge_lines(...))`.

### צעד 4 — `roof_tile_place.rb` + `rt79`
הנחת רעפי איב בלבד, אפס גיאומטריה בקובץ.
- **המלכודת:** `roof_edges` קורא z מ-`zmap`, שהוא **תחתית** הרובד (משם תלויה
  הפאסיה). רעף שיושב שם קבור בתוך הגג. אז ה-z נפתר **על מישור פאת הגג**,
  והקצה מספק רק x ו-y. `rt79` נועלת את זה.
- מדידה מקצה לקצה על תוכנית ה-L: **353 עותקים, הגדרה אחת, 6 פאות ייחודיות**.
- מתג כיבוי: `InteriorPro::RoofManager::USE_ROOF_TILE_EDGES = false`.

**המשתמש ראה את זה ואמר "נראה הכי גרוע בעולם".** לא קיבלתי ממנו צילום ולא
המשכתי. **צעד 4 לא מאושר — אל תמשיך לצעד 5 לפני שראית תמונה.** הכיבוי למעלה.

---

## 2. דפים: מה יוצא ל-PDF, ומחיקת כל דף (`rt80`, `t49`)

המשתמש ביקש **שניהם במפורש**, וזה שני דברים:
- **וי** ליד כל דף — הדף נשאר בסט ובחלון ורק לא נכנס להדפסה (`st['pdf_skip']`).
- **×** על כל דף — מחיקה אמיתית, **כולל דף התוכנית** (`st['drop_plan']`).

`PlanPDF.export` תמיד ידע לקבל `opts[:pages]` — פשוט אף אחד לא העביר לו.
- מפתחות לפי `page_key` (`plan` / `schedules` / `image:N`), **לא** לפי מספר דף
  ולא לפי מיקום — שניהם זזים ברגע שדף נוסף או נמחק.
- `shift_image_keys` מזיז את המפתחות כשתמונה יוצאת. בלעדיו הוי היה מצביע
  בשקט על דף אחר.
- אף פעם לא נשארים בלי דפים: המחיקה מסרבת על הדף האחרון, ו-`layout_pages!`
  בונה את דף התוכנית מחדש אם איכשהו נשאר סט ריק.
- דף התוכנית חוזר עם כפתור "החזר את דף התוכנית", שמופיע רק כשהוא חסר.

---

## 3. מידות (`rt81`, `t50`) — ובעיקר: איפה הן יושבות

### `off` — קו המידה יוצא מהאובייקט
תלונת המשתמש: **"המידה יושבת על האובייקט"**. `dim_line(m)` מזיז את קו המידה
לרוחב מהשתי נקודות שנלחצו, עם **קווי עזר** מהנקודות אל הקו. סימן חיובי = שמאלה
מ-start→end, כלומר הצד נגזר מהכיוון שבו הוא צייר. `off` ריק = 0 = בדיוק איפה
שכל מידה ישבה עד היום, ואז אין קווי עזר בכלל.
שדה בפאנל: **"מרחק מהאובייקט"** — עם מידה מסומנת משנה אותה, אחרת את הבאה
(`st['dim_off']`).

### שלוש לחיצות — כמו בכל תוכנת סרטוט
המשתמש: *"תבדוק איך כפתור מידות עובד בסקאצ'אפ וגם בליאאוט וגם ב-Rayon ותראה
שזה לא עובד ככה. באותו זמן שאני יוצר את המידה אני יכול לבחור איפה היא והקו שלה
יהיו ממוקמים."* וצדק. הכלי היה שתי לחיצות, ואז המיקום היה עבודה נפרדת עם כלי
נפרד — כלומר **פעמיים כדי להגיע לתוצאה פשוטה**, בדיוק מה שכלל "פחות קליקים,
תמיד" ב-`CLAUDE.md` קיים כדי למנוע.

עכשיו: `PEND` → `PEND2` → משיכת הקו. בשלב השלישי `offsetToward` הופך את מיקום
העכבר ל-`off`, והתצוגה המקדימה מציירת את **המידה האמיתית** עם קווי העזר — לא
רמז שלה. הלחיצה השלישית שומרת גם את המרחק כברירת מחדל לבאה. `Esc` מבטל.
`PEND2` מאופס בכל מקום ש-`PEND` מאופס (שלושה מקומות; `rt65` נועלת את זה).

### ההערה — שלוש לחיצות גם היא (`t52`)
צילום מ-Rayon ששלח: חץ → אלכסון → **כתף ישרה שהוא קובע את אורכה** → ריבוע.
אצלנו זו הייתה לחיצה אחת והתוסף בחר לו את שתי הנקודות האחרות (תמיד למעלה
וימינה), והוא גרר אותן אחר כך — שוב שתי עבודות להערה אחת.
`PEND` → `PEND2` → `PEND3` ואז הטקסט. `noteMark(tip, knee, end, ...)` הוצאה
מ-`saveNote` (שיושבת בתוך מטפל הטעינה ואף חבילה לא מגיעה אליה) כדי ש-`t52`
תבדוק את הקוד שנשלח ולא העתק שלו.
**הכתף נשמרת מפולסת עם הברך** — הוא בוחר אורך, לא עקמומיות.

### `oa`/`oc` — המספר עצמו
נמדדים במסגרת של **הקו**, לא של הדף: לאורך ולרוחב. ככה המספר שומר על מקומו
כשהמידה נגררת או נמתחת. ברירת מחדל `oc = h*0.8` = בדיוק כמו קודם.

### שתי מימושים לנוסחה אחת — וזה מטופל
רובי מצייר את המספר, והחלון חייב לדעת איפה הוא כדי לאפשר לתפוס אותו.
`tests/dim_label_cases.json` הוא **מקור אמת אחד** ששתי החבילות קוראות.
בדוק במוטציה: היפוך הנורמל ברובי → `rt81` אדומה ו-`t50` ירוקה, ולהיפך.
**אל תכניס את המספרים האלה לתוך אחת החבילות.**

### הזזת כל כיתוב על הדף
`apply_text_overrides!` מקבל עכשיו `dx`/`dy`, אז גם מידה שהתוסף מייצר לבד,
שם חדר או תג — לחיצה אחת בוחרת, גרירה או חיצים מזיזים, וזה עובר ל-PDF.
**המלכודת:** התוכנית נבנית מחדש מהמודל בכל שינוי. המיקום שנבנה נשמר ב-`x0/y0`
בדיוק כמו ש-`h0` שומר את הגובה — בלי זה הכיתוב היה נודד על הדף בכל ציור מחדש.

### הרמה־והנחה במקום גרירה עם ידיות (`t53`)
המשתמש: *"הוא כאילו מזיז לי שתי נקודות כחולות, ובתכלס אני רוצה שהוא יזיז את
הקו או כל דבר אחר בלייב, ואז עוד לחיצה זה יקבע אותם שם... מספיק עם העיגולים
הכחולים האלו."*

`CARRY` / `TCARRY`: לחיצה **מרימה**, הדבר נע עם העכבר בלי כפתור לחוץ, ולחיצה
שנייה **מניחה**. `Esc` מחזיר למקום שממנו הורם (יש `snap`). גרירה רגילה
(לחוץ־הזז־שחרר) עדיין עובדת — מה שמכריע זה אם העכבר זז לפני השחרור.
**הידיות הכחולות הוסרו לגמרי**; מה שמסומן מצויר על עצמו בכחול.

### ריבוע בחירה וסמן חץ (`t53`)
*"כמו בסקאצ'אפ אני רוצה למתוח נגיד ריבוע לבחירת דברים ומחוק ביחד, וגם תהפוך
את המצביע של היד לחץ שחור."*

- `BAND` — גרירה על נייר ריק **במצב בחירה** מותחת ריבוע; `SELS` מקבל כל סימון
  שנוגע בו. `SEL` נשאר האחד שהפאנלים מדברים עליו.
- `Delete` מוחק את **כל** `SELS` (מהאינדקס הגבוה לנמוך), והרמה מזיזה את כולם
  יחד ובשלמותם (`part='all'`), עם `snaps` ל-`Esc`.
- `arrowCursor()` — חץ שחור inline. אין יותר `'grab'` בקובץ.

> **שינוי התנהגות שצריך לדעת עליו:** גרירה על נייר ריק **הייתה** מזיזה את
> התוכנית על הדף. עכשיו היא מותחת ריבוע בחירה, והזזת התוכנית עברה ל-**Alt+גרירה**
> (Shift עדיין גולל). כתוב בשורת הרמז. אם הוא יתלונן — זה המקום.

> **מלכודת ב-`t53`:** התנועה והשחרור מחוברים ל-`window` (`onMove`/`onUp`), לא
> ל-SVG — `svg.onmousemove` הוא רק הריחוף. חבילה שתקרא לו לא תזיז כלום.
> ובנוסף `render()` בונה מחדש את `VIEW`, אז החבילה מנטרלת אותו כדי לשמור על
> מסגרת של 1 אינץ' = 1 פיקסל.

---

## 4. המחק — כמו בסקאצ'אפ (`t51`, `rt65` עודכנה)

המשתמש שלח צילומי סקאצ'אפ: המחק עובר על מידה, היא נצבעת כחול, הסמן הוא מחק.
- `hitErase` — **מידות והערות קודם**, קווים חופשיים אחר כך.
- `ERASWEEP` — לחיצה, גרירה על רצף, ובעזיבה הכל הולך. קווי SITE נשלחים
  ב**הודעה אחת** (`drop_site_line` מקבל `{is:[...]}`), לא אחת לכל קו.
- מחיקה **מהאינדקס הגבוה לנמוך**, אחרת הוצאת אחד מזיזה את הבא.
- `eraserCursor()` — אייקון מחק inline. **לא קובץ**: קובץ חסר היה נופל בשקט לחץ.
- הצבע כחול (#1f6feb) ולא אדום — "המחק עליו", לא "הוא כבר נמחק". ככה בתמונה שלו.
- **המחק לעולם לא נוגע במודל.** מידות יושבות בהגדרות הגיליון, ו-`siteLayer`
  עדיין מכיר רק את SITE.

**`rt65` עודכנה, לא נמחקה.** שלוש בדיקות מקור שלה תיארו את המחק הישן מילה
במילה. שוכתבו, וההתנהגות שהן ייצגו מונעת עכשיו באמת ב-`t51`.

---

## 5. הצמדה — `snapPoint`

בציור מידה הלחיצה ננעלת על הפינה הקרובה: כל קצה של קו וכל קודקוד של פוליגון
בשכבה גלויה, ועוד הקצוות של מידות שכבר צוירו כדי שרצף מידות יסתדר.
- נמדד ב**פיקסלים של מסך** (12), לא באינצ'ים — כך המשיכה מרגישה זהה בכל זום.
- שכבה כבויה לא מציעה כלום: מה שהוא לא רואה, הוא לא נצמד אליו.
- ריבוע ירוק מראה לאן זה ייתפס **לפני** הלחיצה.

---

## 6. רדיוס התפיסה — `MARK_GRAB = 26`

ראה §0. `GRAB` נשאר 10 לקווים חופשיים; מידה נתפסת מ-26. מידה תמיד מנצחת קו
ששוכב מתחתיה, כי היא נבדקת קודם. `t51` נועלת גם את המרחקים האמיתיים שלו.

---

## 7. מה פתוח — לפי סדר

1. **גג רעפים צעד 4 לא אושר.** המשתמש אמר "נראה הכי גרוע בעולם" ולא שלח צילום.
   **תבקש תמונה לפני שאתה נוגע.** מתג כיבוי ב-§1. צעדים 5–6 ממתינים.
2. **המידות — אושרו.** אחרי רדיוס 26 ואחרי שלוש הלחיצות הוא בדק ואמר
   "עובד מצוין". בחירה, הזזה, מחיקה והצמדה — סגור.
3. הידית הירוקה, צבע למידות בתוכניות, שני הפערים בקומה 1, קירות ברמה העליונה,
   לוגו per-installation, לנדסקייפ — כולם מ-`HANDOFF_2026-08-19.md` §7, לא נגעתי.

---

## 7ב. הערה רחבה שלו, וכדאי לקחת אותה ברצינות

> *"התוכנה של התוכניות, וגם שמתי לב לזה ב-2D, לא כל כך נכונה וזורמת לשימוש. יש
> שם המון דברים שצריך לעשות פעמיים כדי להגיע לתוצאה פשוטה, כאילו הדברים לא
> מספיק אינטואיטיביים."*

הוא אמר את זה אחרי שכלי המידה — **אחד הדברים הבסיסיים ביותר בתוכנת סרטוט
דו-ממדי** — דרש שתי לחיצות ואז כלי שני. הוא צודק, וזה לא היה מקרה בודד:
`מרחק מהאובייקט` נבנה כשדה בפאנל לפני שנבנתה המשיכה בעכבר, והמחק לא ידע למחוק
את מה שהוא מצייר.

**לפני שמוסיפים עוד פיצ'ר לחלון הדף או לעורך הדו-ממדי — לעבור על הזרימות
הקיימות ולשאול על כל אחת: כמה פעולות זה לוקח, וכמה זה לוקח ב-SketchUp /
LayOut / Rayon.** זה כנראה שווה סבב שלם בפני עצמו, והוא ביקש אותו בפועל.

---

## 8. מה למדתי

1. **§0. שלושה סבבים של ניחוש מול לחיצה אחת של מדידה.** הגשש עלה 40 שורות.
2. **תשאל באיזה חלון הוא עובד.** שני חלונות, ובניתי בשגוי סבב שלם.
3. **"זה לא עובד" הוא לא אבחנה.** הוא אמר את זה שלוש פעמים ובכל פעם על סיבה
   אחרת, כי אף אחד מאיתנו לא ידע. המדידה ידעה.
4. **בדיקת מקור שמתארת קוד שהחלפת — תעדכן אותה, אל תמחק אותה** (`rt65`).
5. **המשתמש התוסכל, ובצדק.** דבר פשוט לקח שעות כי מדדתי מאוחר מדי.
6. **§7ב. כשהוא אומר "תראה איך זה עובד בסקאצ'אפ" — זו לא בקשת עיצוב, זו
   אבחנה.** הכלי שבניתי היה נכון טכנית ושגוי בזרימה, והוא זיהה את זה מיד.
