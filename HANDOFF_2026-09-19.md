# Interior Pro — מסירה 2026-09-19 (חומרים, כמויות, והגשר ל-Invoice Studio)

קרא את זה קודם, ואז `VRAY_POC_PLAN.md`, ואז `CLAUDE.md`.
הקודמת: `HANDOFF_2026-09-18.md` (הוואלי — סגור).
**אל תסמוך על הסיכום — תאמת מול הקוד החי.**

בדיקות: `cd tests` ואז `LANG=C.UTF-8 bash run_all.sh ..` — **208 ירוקות**
(rt51/rt52/rt60 אדומות ישנות). חדשות היום: **rt158–rt162**.

## מה קרה היום, לפי הסדר

1. **V-Ray לא מותקן** (`debug_vray_api.rb` → `vray_api_report.txt`):
   27 תוספים, אף אחד לא V-Ray, אפס קבצים, אפס קבועים. יש לו קובץ
   התקנה ישן (6.20.04) בשולחן העבודה ומנוי בתוקף. הוא יוריד את הגרסה
   האחרונה מ-my.chaos.com/download. **ה-POC של V-Ray מחכה לזה.**
2. **מזהה קבוע לחומר** — `material_ids.rb`, rt158/rt159.
3. **כמויות** — `material_takeoff.rb` (rt160) ואז `surface_takeoff.rb`
   (rt161), שהוא זה שנשאר.
4. **הגשר ל-Invoice Studio** — `sync_bridge.rb`, rt162.

## 2. מזהה קבוע לחומר (rt158, rt159)

שם של חומר זז; תווית לא. מילון `InteriorPro_Material`, מפתח `id`,
ערך `IP_MAT_0001`. במודל שלו סומנו 106 חומרים, אפס כפילויות.
מספר לא ניתן לאחר גם אחרי מחיקה. חיפוש **רק** לפי תווית.
כל חומר חדש נתפס ע"י מאזין אחד (`Watcher < MaterialsObserver`) —
לא נגעתי ב-17 המקומות שיוצרים חומרים. **הסימון לא נעשה בתוך ההודעה**
אלא בסבב מיד אחריה, כי שינוי מודל מתוך מאזין מפיל את סקצ'אפ.
דגל: `USE_MATERIAL_IDS`.

**באג שנתפס:** זיהיתי מודל לפי `model.guid` — הוא משתנה בשמירה, ואז
`watching?` התהפך ל-false והיינו מחברים מאזין שני. עכשיו `object_id`.

## 3. כמויות

`material_takeoff.rb` סופר שטח לכל חומר בכל המודל — **ולא בזה משתמשים**:
הוא סופר כל פאה בגיאומטריה (הרעפים לבדם: 10,564 sq ft על 144,376 פאות).
נשאר בקוד כי הוא נכון למה שהוא מודד, אבל אי אפשר להזמין לפיו.

`surface_takeoff.rb` הוא הדוח האמיתי, **לפי משטח, נטו** ("כן רק השטח
נטו"):
- **רצפות**: שם החדר, סוג, שטח (גבול החדר = כבר נטו), ומידת אריח
  (`unit_l`/`unit_w`) — זה מה שספירת החתיכות תצטרך.
- **קירות**: פעמיים, `outside`/`inside`, כל צד עם החומר שלו, ושטח
  **נטו** — כל דלת וחלון יורדים משני הצדדים.
- העמודות `category` (איזה סוג קיר) ו-`face` (איזה צד) הופרדו אחרי
  שקראו שתיהן "exterior/interior" ואי אפשר היה להבדיל.

**המודל שלו אין בו דלתות וחלונות בכלל** (נמדד, `openings_report.txt`),
לכן ברוטו=נטו שם. זה לא באג.

## 4. הגשר ל-Invoice Studio (rt162)

Invoice Studio היא אפליקציית ווב שהוא בנה. אין API — **גשר קבצים**.
תיקייה: `C:\Users\rordt\OneDrive\InteriorPro` (ווינדוס מציג לה
"dror - Personal", ולכן הצד השני חשב שאין OneDrive; יש).
**הנתיב הוא הגדרה עם בוחר, לא קוד** — `SyncBridge.choose_folder!`,
נשמר פר-התקנה.

- **קוראים** `rooms.json`: 14 פרויקטים, 115 חדרים.
  **החוק:** `room-0001` קיים כמעט בכל פרויקט ("Kitchen" ב-12 מהם).
  המפתח הוא **הזוג** (project id, room id), ושניהם נשמרים על הקבוצה.
  **אסור לנקות שמות** — יש רווח כפול אמיתי ויש "scond floor" עם שגיאת
  כתיב. אין `strip`, אין תיקונים. ההתאמה אצלם היא מחרוזת מדויקת.
- **כותבים** `surface_takeoff_<project-uuid>.csv` — קובץ לכל פרויקט,
  ה-UUID מועתק מילה במילה. מודל בלי פרויקט **לא כותב כלום**.
  UTF-8, CRLF, כתיבה אטומית, שמונה עמודות תמיד.
- בוחרים: `pick_project!` ואז `pick_room!(group)`. עובד אצלו בפועל —
  הוא ראה את רשימת הפרויקטים האמיתית בסקצ'אפ.

## הצעד הבא (מחר)

1. **בדיקה מקצה לקצה** (התחלנו ולא סיימנו): לקשור חדר, `export_takeoff!`,
   ואז **לייבא בפועל ב-Invoice Studio**. זה המבחן היחיד שסופר.
2. אחרי זה: **ספירת חתיכות אריח** — שלמים / חתכים מזווגים / חתיכות
   מתחת ל-20% בשורה נפרדת. הכלל וה-20% כתובים ב-`VRAY_POC_PLAN.md`.
3. V-Ray — כשיותקן.

## פתוח / לא נבדק

- `Sketchup.write_default` של נתיב התיקייה **נעלם פעם אחת** בין הרצות
  (חזר nil). לא שוחזר מאז. אם יחזור — להחזיק את הנתיב בקובץ.
- קירות לא מקושרים לחדר בכלל; רק לרצפות יש חדר. שורות הקיר ייובאו
  בלי קישור, וזה נמסר לצד השני.
- `README-sample-files.txt` ו-`surface_takeoff_8f3a1c22-...csv`
  בתיקייה הם שאריות דוגמה — הצד השני אמר למחוק.

## קבצי מדידה שנוספו היום (בשורש)
`debug_vray_api.rb` · `debug_matids.rb` · `debug_takeoff.rb` ·
`debug_openings.rb` · `debug_bridge.rb` (מה באמת בתיקיית ההחלפה)
