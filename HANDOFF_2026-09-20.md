# Interior Pro — מסירה 2026-09-20 (טבלת החדרים וספירת האריחים)

קרא את זה קודם, ואז `VRAY_POC_PLAN.md`, ואז `CLAUDE.md`.
הקודמת: `HANDOFF_2026-09-19.md`.
**אל תסמוך על הסיכום — תאמת מול הקוד החי.**

בדיקות: `cd tests` ואז `LANG=C.UTF-8 bash run_all.sh ..` — **211 ירוקות**
(rt51/rt52/rt60 אדומות ישנות). חדשות: **rt163, rt164, rt165**.

## 1. טבלת החדרים (rt163) — `room_link_dialog.rb`

הבחירה חדר-אחרי-חדר בעכבר לא עבדה, ובצדק: **חדר הוא לא קבוצה אחת** —
הקירות והרצפה שלו הם ישויות נפרדות. בלשונו: "זה פשוט מלא קבוצות".

מה שיש עכשיו: חלון אחד, שורה לכל חדר במודל, ולכל שורה או חדר מהפרויקט
ב-Invoice Studio או שם שהוא כותב. לחיצה על שם מסמנת את החדר במודל.
כפתור **Room Names** בסרגל, עם אייקון משלו.

- בחירה מהרשימה מנצחת טקסט שהוקלד.
- מעבר משם חופשי לקישור ובחזרה עובד לשני הכיוונים.
- השם תמיד **מועתק** מ-`rooms.json`, אף פעם לא מוקלד מחדש.
- **השם משנה גם את התווית המצוירת על הרצפה** (`rename_room!` קורא
  ל-`RoomManager.build_label!`). בלי זה הטבלה אמרה "Bathroom 2"
  והמודל המשיך להראות "Room 2" — הוא צילם את זה.

**כפתור "Sync Rooms" נמחק.** הוא נראה שבור כי לא היה לו מה לעשות:
`RoomManager.sync_rooms!` נקרא אוטומטית מ-13 מקומות (כל בנייה/מחיקה/
פיצול/מתיחה/עיקום של קיר, העורך הדו-ממדי, דיאלוג הרצפות).

## 2. ספירת אריחים (rt164) — `tile_count.rb`

**חתיכות, לא רגל מרובע.** אריח 24x48 שנחתך לשניים ושני החצאים בשימוש
הוא **אריח אחד שנקנה**. טהור לגמרי, בלי SketchUp API.

- פריסת רשת על **מתאר החדר האמיתי** (Sutherland-Hodgman מול תא האריח,
  אז חדר בצורת L נספר נכון).
- חתך מזווג לשארית של אריח שכבר נחתך — הגדולים קודם, כמו רצף אמיתי.
- **מתחת ל-20% מהאריח = פחת**: לא מזווג, לוקח אריח משלו, ומדווח
  **בשורה נפרדת** ("כדי שאני יכול לשאול את הקבלן כמה לקנות רק בשביל
  ה-20% האלה"). הסף ניתן לשינוי.

## 3. חיבור לרצפות (rt165) — `tile_takeoff.rb`

לכל רצפה: מתאר החדר, מידת האריח (`unit_w`/`unit_l`), הפוגה, נקודת
ההתחלה והדוגמה. רצפה בלי מידת יחידה — שטח בלבד, בלי להמציא מספר.

**אומת ביד על חדר אמיתי:** Room 2, 283.07"x362.70" = 713 sq ft, אריח
48x24 בלי פוגה ⇒ 75 שלמים, 44 חתכים, מתוכם 14 מתחת ל-20%; 15 רצועות
25.5" ו-15 של 17.56", וכל רצועת 25.5" משאירה 22.5" שממנה יוצאת ה-17.56"
⇒ 15 נחתכים, 15 משאריות. **104 לקנות.** התוסף אמר 104. אותו מספר.

**הרינגבון ושברון מסורבים במפורש** — פריסה אלכסונית, וספירת רשת שם היא
מספר שגוי. הדוח אומר "not counted yet" במקום לנחש.

## שתי הנחות שלי שהתבררו כשגויות (לזכור)

1. חשבתי שהפוגה לא נשמרת — היא כן; הוא פשוט בחר None לחדר הזה.
2. חשבתי ש-`pattern` מחזיק בטעות את סוג הרצפה — לא. הדיאלוג מציע
   None / Tile / Straight / Herringbone / Chevron, ו-"Tile" הוא שם
   חוקי של דוגמה. **אין "running bond" בתוסף בכלל**, ולכן ה-stagger
   שחיפש אותו לא נורה מעולם.
   לפני שמדווחים באג — למדוד מה הדיאלוג באמת שומר.

## מה נשאר

1. **V-Ray עדיין לא מותקן.** זו החסימה האמיתית ל-POC. הוא מוריד
   מ-my.chaos.com/download.
2. **שרטוט פריסת האריחים (אלווישן)** — אותם נתונים של `tile_count`,
   רק מצוירים. לא התחלנו.
3. **ייבוא אמיתי ב-Invoice Studio** של `surface_takeoff_<uuid>.csv` —
   נכתב נכון ואומת מהצד שלנו, לא נוסה מהצד שלהם.
4. הפוגה עדיין לא נכנסת לחישוב הרוחב של הרצועות בדוגמת "Tile" מעבר
   להזזת הרשת — נבדק ועובד, אבל לא נבדק מול פוגות גדולות.
