# Interior Pro — מסירה 2026-09-11 (הגג ההולנדי — סגור)

קרא את זה קודם, ואז `CLAUDE.md`. הקודמת: `HANDOFF_2026-09-10.md`,
והתוכנית: `DUTCH_GABLE_PROPOSAL.md`.
**אל תסמוך על הסיכום — תאמת מול הקוד החי.**

בדיקות: `cd tests` ואז `LANG=C.UTF-8 bash run_all.sh ..` — **175 ירוקות**
(rt51/rt52 אדומות מהסיבה הישנה).

## מה נסגר הפעם — אושר בעין שלו ("מדהים", "נראה מעולה")
התעלה בין הפשייה לקיר הגמלון — הפתוח היחיד שנשאר מהמסירה הקודמת.

**קודם מדדתי** (`debug_dutch_gap.rb`, הדוח ב-`dutch_gap_report.txt`).
במודל שלו, dutch_depth=35, fascia_depth=8, pitch 5, thickness 0.5:
- קיר הגמלון על `x = -364.52` = `x0 + dw` כש-`dw = 35 + DUTCH_SET_IN(8) = 43`.
- לוח הפשייה המטפס על `x = -372.52` = `x0 + dwf` (dwf = 35).
- ברצועה של 8" ביניהם הדבר הכי גבוה היה **לוח האיבס**, 8" מתחת לקו
  הגג (236.19 מול 228.9). כלומר: תעלה ברוחב 8" ובעומק 7.25", פתוחה
  לשמיים, לאורך שתי הצלעות המטפסות, בשני צדי הגג.

### צעד 1 — `dutch_cap!` (commit adcb30f)
פונקציה חדשה ב-`roof_manager.rb`, נקראת מ-`build_main_rect!` בארבעת
ענפי ההולנדי (w/e ב-axis :x, s/n ב-axis :y). לכל צלע מטפסת פאה אחת:
- **באותו מישור** של מדרון הגג הצמוד (אותו lift בדיוק).
- מקו הקיר (`xw`) החוצה עד קו הפשייה (`xf = x0 + dwf`).
- הקצה התחתון נופל **בדיוק על קו המפגש עם הסינר** — הפאה נגמרת על
  `(xf, y0 + dwf)`, שם מישור המדרון ומישור הסינר שווים. לכן אי אפשר
  להיפער שם חור.
- במעבר הסלאב בלבד נסגרת גם הדופן בגובה `dz` בקצה החיצוני, אחרת נשאר
  חריץ פתוח של חצי אינץ' מעל לוח הפשייה.

**שום מישור קיים לא זז.** זה ההבדל מהניסיון שנכשל במסירה הקודמת (שם
שינו את `xw` ל-`dwf` והשאירו את פינת הסינר על `dw`, ונפערו שני
משולשים).

### צעד 2 — הפשייה יורדת עד הסינר (commit 76035f6)
`build_dutch_rake!` קיבל פרמטר `above` (אותו lambda שכבר חתך את לוחות
האיבס), ו-`framed_gable_face!` דוחף עכשיו נקודת בסיס שהוארכה ב-
`DUTCH_SET_IN` לאורך קו הלוח עצמו (`reach`), פלוס ה-lambda, לתוך
`@dutch_rakes` (טאפל של 4 במקום 3 — גם אתר הקריאה ב-`build_roof!`
עודכן).
הלוח מגיע עכשיו עד קו המפגש עם הסינר ונחתך עליו, אז הוא **מתחדד לחוד**
בערך 19" לפני הסוף במקום להיקבר בתוך הסינר. חוק "לוחות נפגשים".

## צעד 3 — בחירת סוג הסיום לכל צלע (commit 09f6251)
הוא ביקש שהבחירה "הולנדי" תעבוד כמו הפיכת היפ לגיבל, ואז חידד: **תפריט
שנפתח על הצלע** ובוחרים בו את סוג הגימור.

- רשימה שנייה על המודל: `roof_dutch_wall_ids` — **תת-קבוצה** של
  `roof_gable_wall_ids`. צלע הולנדית היא סוג של גיבל, אז היא נשארת גם
  ברשימת הגיבלים וכל מה שקורא אותה ממשיך לעבוד בלי שינוי.
- `RoofManager.roof_end_of(id)` → `:hip` / `:gable` / `:dutch` — מקום
  אחד לשאול. `set_roof_end!(wall, kind, click)` הוא הסטר;
  `toggle_gable_wall!` נשאר ומסובב hip→gable→dutch→hip.
- `RoofGableTool`: לחיצה פותחת `UI.inputbox` עם dropdown לאותה צלע,
  פתוח על המצב הנוכחי; Cancel לא משנה כלום. נקודת הקליק נקראת **לפני**
  שהתפריט נפתח, ומתרעננת בכל בחירה (קיר ארוך). הדגשה: כתום היפ, כחול
  גיבל, ירוק הולנדי (`COLORS`/`NAMES` בראש המחלקה).
- `framed_plan`/`assemble_framed_plan` קיבלו ארגומנט חמישי
  `dutch_marked = []` ומחזירים `plan[:d]` (בוליאני לכל צלע, דרך
  `side_gabled?` — אותה פונקציה בדיוק) ו-`plan[:dutch_edges]`.
- `build_main_rect!(st, rect, g, d = nil)`: `dutch_reach` נשאל עכשיו
  `g[:w] && d[:w]`. **כנפיים אף פעם לא הולנדיות** — ל-`build_wing_rect!`
  אין ענף הולנדי.
- ב-`build_roof!`: במקום `gables = []` עכשיו
  `gables -= framed[:dutch_edges]` — כדי שצלע גיבל רגילה שעומדת ליד
  צלע הולנדית תשמור על לוח הרייק שלה.

**המספר בחלון הגג הוא רק העומק** ומופעל רק על צלעות שסומנו הולנדיות.
בלי סימונים — הגג נבנה בדיוק כמו לפני הפיצ'ר.

**בדיקות:** rt129 חדשה (טהורה, מכסה: בלי סימונים כלום לא הולנדי; סימון
צלע אחת מתוך שתיים; סימון על צלע שאינה גיבל לא עושה כלום;
`dutch_edges` הנכונות). rt15/rt36 עודכנו לחוזה הסבב החדש. **176 ירוקות.**

## הצעד הבא
אין באג פתוח בגג ההולנדי. הבאים בתור לפי `CLAUDE.md`:
קירות מעוגלים (גרירת אמצע קיר), ואז תוכניות 2D ל-PDF.
וגם: קוד ההולנדי במסלול השלד (`dutch_poly`/`dutch_apron_cells`/rt128)
עדיין **כבוי** (`dutch_d = 0.0` בענף השלד) — הקישוט לא נבנה שם.

## דברים שנלמדו הפעם
- בדיקה ישנה שנופלת היא לא תמיד "תיקון רחב מדי" — rt15/rt36 נפלו כי
  **החוזה עצמו השתנה** בבקשתו. לעדכן את הבדיקה, לא לעקוף אותה.
- הפאה של קיר הגמלון **לא נשארת משולש** במודל אמיתי — הלוחות שנוגעים
  בה מפצלים אותה (11 קודקודים). כלי מדידה שמחפש "פאה עם 3 קודקודים"
  יחזיר 0. לחפש לפי צורה (אנכית, שני קודקודים תחתונים באותו גובה).
- הגג שלו נושא ~39,000 פאות. כלי מדידה חייב לסנן לפי bounding box
  לפני כל חיפוש, אחרת הוא נתקע ולא כותב דוח בכלל.
- כל קובץ מדידה צריך `rescue` שכותב את השגיאה לדוח — אחרת נפילה
  נראית בדיוק כמו "לא רץ".
