# Interior Pro — מסירה 2026-09-14 (הקרשים על הדורמר, והגג שעולה מעל החלון)

קרא את זה קודם, ואז `CLAUDE.md`. הקודמת: `HANDOFF_2026-09-13.md`.
**אל תסמוך על הסיכום — תאמת מול הקוד החי.**

בדיקות: `cd tests` ואז `LANG=C.UTF-8 bash run_all.sh ..` — **195 ירוקות**.
אדומות קבועות: rt51/rt52/rt60 (ישנות, לא קשורות).
חדשות היום: **rt147** · **rt148** · **rt149**. כולן אומתו נגד
הקוד הישן ונפלו שם.

## 1. הקרשים על לחי הדורמר נעצרו באוויר מעל הגג (rt147)

הוא צילם את הלחי וצייר קו אדום לאורך קצות הקרשים: מדרגות של קצוות
מרובעים תלויים באוויר, עם אור בין הקרש לגג. `gable_course_runs` זרק כל
ביס ברגע שהגג טיפס מעל **תחתית** השורה, אז כל שורה מתה מדרגה שלמה מוקדם.

עכשיו הביס נשמר כל עוד **חלק ממנו עדיין מעל הגג**, והקרש פשוט נמשך
לתוך הגג — הגג מכסה אותו. שורה שהגג בלע לגמרי עדיין לא נבנית, אז שום
דבר לא תלוי בחדר מתחת.

### שתי גרסאות אלכסוניות נזרקו — אל תחזיר אותן בלי לשאול

1. הפיסות האלכסוניות כפאה זקופה שנדחפת החוצה: **"זה נראה איום ונורא"** —
   בלי הלאפ הן עמדו מהקיר כמו מדפים ליד השכנות שלהן.
2. אותו טריז בדיוק, בנוי ביד משש פאות שטוחות עם רגל אלכסונית: **"לא
   טוב"**.

הוא ראה את שתיהן על המסך ופסל. rt147 מוודא ששום בנאי אלכסוני לא חוזר.

## 2. הגג ישב על החלון — עכשיו הוא עולה 6" (rt148)

"תרים את הגג 6 בערך אינץ כדי שהוא לא ישב על החלון". נשאל מה משנים,
הגג או החלון, וענה: **"להעלות את הגג למעלה"**.

`DormerManager.dormer_headroom` = 6.0, נוסף לאורך בדיוק כמו העקב, אז הגג
והקירות עולים יחד. **החלון לא נגעו בו:** הגודל שלו עדיין נמדד מהקצה
האמיתי של הקיר, ורק המרכז שלו נמדד מהקצה **פחות** ההדרום — אז החלון
נשאר איפה שהיה וכל ששת האינצ'ים נוחתים מעליו.

**הגרסה הראשונה הורידה גם את הגודל** והחלון שלו חזר 10" נמוך יותר —
"הורדת את החלון ואני ביקשתי להעלות את הגג". בוטלה.

דגל כיבוי: `InteriorPro::DormerManager::USE_DORMER_HEADROOM = false`.
שבע חבילות ישנות (rt95 · rt99 · rt100 · rt101 · rt102 · rt119 · rt122)
מכבות אותו אצלן, בדיוק כמו שהן מוציאות את העקב מהנוסחאות.

נמדד אצלו: `dormer_spec_report.txt` — קיר 215.93→269.43, חלון 24" כמו
שביקש, **16.5" של קיר מעליו** (בלי התכונה: 10.5").

## 3. מלכודת שעלתה לנו סבב שלם

בדקנו על **גגון אחר** — נמוך, `z0` 202.6 מול 213.88 — והמספרים לא זזו,
אז נראה היה שהתכונה לא עושה כלום ואני ביטלתי אותה. היא עבדה כל הזמן.
**לפני שמכריזים ש"כלום לא קרה" — לוודא שמודדים את אותו גגון.**
`debug_dormer_spec.rb` בשורש נותן את זה בשורה אחת: התכונות של הקבוצה, מה
ש-`dormer_spec` מוסר, מה ש-`frame()` מחזיר, ומה באמת עומד.

וגם: **בניית גג מחדש לא בונה את הדורמר מחדש.** צריך את הלולאה עם
`replace_dormer!` (למטה), אחרת רואים גיאומטריה ישנה וחושבים שהקוד לא נטען.

## פקודות היום

```
InteriorPro.reload!; d=[];f=lambda{|e,n| e.grep(Sketchup::Group).each{|g| d<<g if g.get_attribute('InteriorPro','type')=='dormer'; f.call(g.entities,n+1) if n<4}};f.call(Sketchup.active_model.entities,0);d.each{|g| InteriorPro::DormerManager.replace_dormer!(g,{})};puts "rebuilt #{d.length}"
```

## 4. סקיי לייט — נבנה בשלמותו, **לא נבדק אצלו עדיין** (rt149)

"משהו שהוא כמו חלון תמונה רק להחליף צבעים... צריך להיכנס ולעשות חור
בתקרה שעובר עד לגג... שיהיה אפשר לבחור לו צבע אבל החלון עצמו פשוט".
הוא לא היה ליד המחשב: "תסיים הכל ביחד ותראה שאתה לא שובר את הגגות".

שלושה קבצים חדשים, אף שורה בקוד הגג לא השתנתה חוץ משתי תוספות שמורות:
- `skylight_manager.rb` — המתמטיקה והבנייה. `roof_frame` של הדורמר נותן
  את המסגרת מתחת לקליק, `plan_rect` את המלבן, `cut_rect!` חותך את שני
  העורות של הגג בדיוק כמו `cut_roof!` של הדורמר (טבעת על כל עור, סגירת
  השפה, ורק אז סחיפה), `build_body!` בונה מסגרת נמוכה (4") לאורך הנורמל
  של הגג עם זכוכית בתוכה (`inset_ring` — מסגרת שווה מכל צד גם על לוח
  צר וארוך). `build_shaft!` מוצא את התקרה הגבוהה ביותר שעדיין מתחת לגג
  שם, חותך בה את אותו מלבן (`cut_level!`), ומעמיד ארבעה קירות לבנים
  מהתקרה עד לתחתית הגג.
- `skylight_tool.rb` — רחף על שיפוע, מלבן כחול, קליק. אותו pick כמו
  `DormerTool`.
- `skylight_dialog.rb` — רוחב, גובה, בוחר צבע, "Place on roof". הכפתור
  **Skylight** בסרגל פותח אותו (ליד הדורמר). **צריך ריסטארט מלא** לסקצ'אפ
  כדי לראות את הכפתור.

**שורדים בנייה מחדש של הגג** — `build_roof!` קורא ל-`harvest` לפני
שהגג הישן נמחק ול-`replant!` אחרי הדורמרים, בדיוק באותו דפוס. שתי
התוספות היחידות ב-`roof_manager.rb`, שתיהן מוגנות ב-`defined?`.

תכונות על הקבוצה: `type='skylight'`, `width`, `height`, `color`,
`curb`, `at_xy` (נקודת הקליק בתוכנית), `shaft` (כמה תקרות נחתכו).

דגלי כיבוי: `USE_SKYLIGHTS`, `USE_SKYLIGHT_SHAFT` (ראה CLAUDE.md).

**מה לא נבדק, כי אין סקצ'אפ בענן:** שהחור באמת נפתח (הסטאב לא מפצל
פאות), שהזכוכית מקבלת את החומר שלה, שהפיר נוחת על התקרה. אם משהו לא
נראה — `[Skylight]` בקונסול אומר כמה עורות נחתכו ולכמה תקרות הגיע הפיר.
אין עדיין Edit/Move/Delete לסקיי לייט.

### סקיי לייט — מה הוא ראה ומה ביקש (2026-09-14 בלילה, אחרי הריסטארט)

"ואוו נראה מעולה". ואז:
- **החלון בתוך החור** — נעשה: המסגרת יורדת לתוך הפתח כמו משקוף (מתחתית
  הלוח עד שפה של 1" מעל הגג), הזכוכית שקועה 1" מתחת לפני הגג. במקום
  ה-curb של 4" שעמד על הגג.
- **הוא חותך רק את השינגלס, לא את שאר שכבות הגג** — פתוח, לתקן.
  `cut_rect!` בוחר פאות עם |nz|>0.2 שמכסות את מרכז הפתח **בקבוצת הגג
  עצמה**; שכבות שיושבות בקבוצות-בנות (רעפים? דק?) לא נחתכות. למדוד
  קודם: `debug_*` שמדפיס כל פאה/קבוצה מתחת לנקודת הקליק עם z והנורמל.
- **Edit** ו-**Move** לסקיי לייט, ו**קבוצה משלו בסרגל** עם קו מפריד —
  לבנות, באותו דפוס של הדורמר.

## מה שהוא ביקש להמשך (הרשימה שלו, לא לאבד)

1. גג נמוך (גראז' על קירות בלי פאונדיישן) שמתחבר לגג גבוה ממנו ב-18"
   — שניהם קומה ראשונה. איך הם נפגשים.
2. ~~סקיי לייט~~ — נבנה, ראה סעיף 4. לבדוק אצלו.
3. גג לבניין שני / ADU — היום `build_roof!` מייחס הכל למבנה אחד.
4. גייבל באמצע הגג שהפאשיה שלו ביישור עם הקיר (הקיר באותו לוול).

## פתוחים מקודם
- rt60 — טיימר גיבוי, נופל בקביעות. לא גגות.
- `[Roof] build_tube!: undefined method 'erase_entity'` בבניית הגג.
- כיוון השד אם הוא חוזר על גג שיש לו בעלים.
- קרשים על משולש הגמלון של הדורמר עצמו — לא נבדק.
- CLAUDE.md: רשימת הקירות המעוקלים רועננה מול הקוד ב-2026-09-13B. רק
  `plan_generator.rb` (התוכנית המודפסת) עדיין מצייר קיר מעוקל כקו ישר.

## קבצי מדידה שנוצרו היום (בשורש)
`debug_siding_run.rb` (האם הקוד החדש באמת נטען) · `debug_siding_gap.rb`
(כל קרש והמתאר שהוא נמדד ממנו) · `debug_window_head.rb` (חור החלון מול
קצה הקיר) · `debug_dormer_spec.rb` (spec → frame → מה שעומד).

## הצעד הגדול הבא (סוכם 2026-09-14 בערב): POC של V-Ray — אחרי שהגגות נסגרים

הוא הביא מסמך מלא (שמור אצלו בצ'אט). התמצית, כדי שלא יאבד:

**המטרה:** Interior Pro → Claude API → V-Ray לסקצ'אפ → רינדור.
הוא מעצב (גיאומטריה, חומרים, אריחים, ריהוט, מצלמות). קלוד מכין את
V-Ray (roughness, reflection, bump, תאורה, שמש, חשיפה, איכות). V-Ray
מרנדר. **קלוד לא מעצב מחדש ולא מחליף חומרים.**

**העיקרון:** קלוד מחזיר **מספרים מובנים** (material_id, roughness, ...),
לא קוד. התוסף בודק אותם ומפעיל פונקציות V-Ray מוגדרות מראש. שום דבר
לא רץ בעיוורון.

**הסדר:** קודם להוכיח תוסף ↔ V-Ray. רק אז קלוד. **רק אז Tile** — כי
מערכת האריחים צריכה לשמור מפות PBR (roughness/normal/displacement)
בצורה ש-V-Ray קורא, ואסור לגלות את זה אחרי שהיא בנויה.

**הצעד הראשון, כמו תמיד אצלנו:** קובץ מדידה שרק כותב דוח — מה V-Ray
חושף ל-Ruby **במחשב שלו** (גרסת V-Ray, מודולים, פקודות רינדור, גישה
לחומרים). לא מהתיעוד. יש "V-Ray Script Access" בתיעוד של Chaos —
לאמת מולו.

**ה-POC הקטן ביותר:** פאה אחת, חומר סקצ'אפ אחד עם מזהה קבוע (attribute,
לא שם) → למצוא אותו ב-V-Ray → לשנות roughness/reflection → להריץ
רינדור מהתוסף. אחרי זה: קריאת API אחת לקלוד עם תיאור סצנה, שמחזירה
מספרים, שהתוסף מחיל.

**עלות:** 2–4 סנט לקריאה, כמה דולרים בחודש. דורש מפתח API על המחשב.

**איכות:** האיכות היא של V-Ray. קלוד מכוון. הפער מאמן V-Ray נסגר
בשניים: לולאת משוב (קלוד רואה את הרינדור) — **לא לבנות עכשיו** — וקובץ
כללי בית לרינדור שגדל מכל הערה שלו ("יותר חם", "המנורה מפריעה").
