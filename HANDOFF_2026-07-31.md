# מסירה לצ'אט הבא — Interior Pro, סבב 2026-07-31

## 0. לפני הכל
חבר את התיקייה החיה:
`C:\Users\rordt\AppData\Roaming\SketchUp\SketchUp 2024\SketchUp\Plugins\interior_pro`
והתעלם לגמרי מ-`C:\InteriorPro_Agent\source_readonly`.

קרא: `NEXT_CHAT_PROMPT.md`, ‏`STATUS_handoff.md`, ‏`CONTRACT_2D.md`, ‏`TARGET_PLANS.md`,
ואת הקובץ הזה.

## 1. כללי עבודה (לא השתנו)
- **אישור לפני מימוש.** מותר להציע, אסור לכתוב קוד בלי אישור מפורש.
- כתיבה בעברית, הודעות קצרות. הסברים — **שני משפטים לכל היותר**, בשפה פשוטה.
- העוזר כותב את הקבצים לדיסק בעצמו; המשתמש מריץ `InteriorPro.reload!` ו**סוגר ופותח את הדיאלוג**.
- git מהטרמינל של המשתמש. יש קובץ נעילה תקוע חוזר: `del ".git\index.lock"` לפני `git add`.

## 2. אזהרה קריטית — plan_editor.rb
כל ה-HTML/JS יושב ב-heredoc `<<~HTML` בתוך רובי. **escape בודד הורג את כל ה-JS בשקט.**
אחרי כל שינוי חובה:
1. לחלץ את ה-heredoc ולתת ל**רובי עצמו** לעבד את ה-escapes (לכתוב `s = <<~HTML ... HTML` לקובץ ולהריץ),
2. `node --check` על ה-`<script>`,
3. הרצה עם DOM/Canvas-stub וסימולציית עכבר/מקלדת.
בסבב הזה נבנתה חבילת בדיקות של ~67 תרחישים בשיטה הזו — היא תפסה כל באג לפני המשתמש.

## 3. מה נבנה בסבב הזה (הכל נבדק ואושר ע"י המשתמש)

### סרגל וכפתורים
- **סרגל חדש "Interior Pro 2D"** עם כפתור 2D Editor (`toolbar.rb`, ‏`icons/plan_2d.svg`).
  נדרש **restart מלא** של סקצ'אפ. סרגל נפרד בכוונה: לסרגל הראשי יש
  `return if toolbar.length >= TOOLBAR_ITEM_COUNT` (=9) שהיה מבליע כל כפתור חדש.

### Undo
- כפתור Undo בפינה הימנית התחתונה של הפאנל + **Ctrl+Z**.
- **Ctrl+Z לא מגיע ל-HtmlDialog בשום צורה** (נבדק: גם Ctrl וגם Alt נבלעים, וגם ה-Undo
  של סקצ'אפ עצמו לא רץ). הפתרון שעובד: אלמנט `contenteditable` מוסתר (`#hiddenUndo`)
  שתופס את פקודות ה-**עריכה של הדפדפן** — `beforeinput` עם `inputType==='historyUndo'`.
  אותו מסלול משמש גם ל-**Ctrl+C / Ctrl+V** (אירועי `copy`/`paste` על document).
- `undoAction()` מבטל לפי סדר: נקודה בקו שמצויר ← צורה חצי־מתחילה ← צורה אחרונה ←
  קיר ממתין ← ה-Undo של סקצ'אפ.
- `UndoRefreshObserver` (ModelObserver) מרענן את הקנבס אחרי כל undo/redo במודל.

### בחירה ועריכה
- `selList` = הבחירה האמיתית, `sel` = האחרון שנבחר (כל הקוד הישן ממשיך לעבוד).
- Shift+קליק מוסיף/מוריד, גרירת מלבן בוחרת לפי **מרכז** האלמנט, Esc מנקה.
- מחיקה קבוצתית: `delete_many` — פתחים קודם, אז קירות, ב-operation אחד.
- שינוי **עובי** לכמה קירות: `set_thickness`. משתמש באותו מסלול של דיאלוג ה-3D
  (`ui_dialogs.rb` ~102-109). **הערה: ההערה ב-STATUS ש"שינוי עובי שבור" מיושנת — זה עובד.**
  גופי דלתות/חלונות לא נבנים מחדש אחרי שינוי עובי — **החלטת המשתמש, לא באג.**
- שכפול (ghost שנצמד לעכבר), מראה ⇄/⇅, ‏Ctrl+C/Ctrl+V — לקירות ולפתחים.
  הדבקת פתח מציבה אותו על הקיר שתחת העכבר דרך `place_door`/`place_window`.
- **סרגל עריכה כהה** (`#editBar`) שמופיע רק כשיש בחירה, ומציג רק פעולות רלוונטיות.
- סיבוב: ↺/↻ לפי מספר בשדה, ו**סיבוב חופשי** (`startFreeRotate`) — גרירה בכל זווית,
  Shift = קפיצות 15°, הקלדת מעלות + Enter, ‏Esc מחזיר.

### כלי סירטוט חופשי (אלמנט חדש `type='sketch2d'`)
- מצב **Line** עם סרגל כהה בסגנון סקצ'אפ: קו, קשת, ריבוע, עיגול, מצולע, מדידה.
- שורת סגנון קו: רגיל / מקווקו / עבה — **בחירה אחת בלבד** (`setLinePreset`).
- קו: קליק-קליק, סגירת צורה בחזרה לנקודה הראשונה → **פאה אמיתית במודל**.
- קשת: מיתר אפור + קו קימור ירוק מקווקו עם המידות (כמו סקצ'אפ). הקלדת מיתר ואז קימור.
- ריבוע: הקלדת `רוחב,גובה` (גם `;` ו-`x`), מספר יחיד = ריבוע.
- מצולע: מרכז+רדיוס, והקלדת מספר ואז **S** קובעת מספר צלעות. **`ev.code === 'KeyS'`
  חובה** — במקלדת עברית אותו מקש שולח "ד".
  שינוי מספר הצלעות באמצע ציור לא מאפס את המרכז (`refreshToolUi` בלי `setLineTool`).
- קשת/עיגול נבנים במודל כ-`add_curve` + `soft/smooth` → עקומה חלקה, לא שרשרת קווים.
- אופסט (חיובי=החוצה, שלילי=פנימה, לפי שטח מסומן), קיבוץ לקבוצה דרך attribute `gid`.
- הצורות מופיעות גם ב-`plan_generator.rb` (`draw_sketches`) — קו בלבד, בלי מילוי.

### הצמדות
- **מקביל**: קו שקרוב למקביל לקיר/קו ננעל מקביל בדיוק, עם קו עזר סגול.
- **Shift נועל את הכיוון המקביל** (`captureLockDir` לוקח את `parInd` אם קיים).
- **מגנט**: כשהכיוון נעול, ריחוף על נקודה כלשהי מותח את הקו עד מול אותה נקודה
  (`chainEnd` → `aimFrom`), עם קו תכלת מקווקו והכיתוב "נעול לנקודה".
- **סנאפ למודול**: כבוי / 3" / 6" / 1'. **הקלדה תמיד גוברת על המודול.**
- **קווי עזר**: שתי נקודות → קו אינסופי שהכל נצמד אליו. רק בעורך.

### תמונת רקע (`pick_underlay`)
- טעינת JPG/PNG כ-base64 מאחורי הקנבס, שקיפות, נעילה/גרירה, ו**כיול לפי מידה ידועה**
  (שתי נקודות + הקלדת המרחק האמיתי). הנתיב נשמר על המודל ונטען מחדש.
- **המשתמש אמר שהכיול "מטורף"** — לא לגעת.

### עיצוב הפאנל
- ההגדרות הכלליות (סנאפ, קווי עזר, תמונת רקע) יושבות ב-`#genBox` עם כותרת
  "כללי — לא תלוי בכלי שנבחר", כי קודם הן נראו כשייכות לקירות.

## 4. הבאג הפתוח — הצעד הראשון בצ'אט החדש

**התסמין:** מציירים קו/מלבן ומתחילים **מהפינה החיצונית של קיר קיים**. הקו נראה נעול
על הפינה בזמן הציור, אבל התוצאה הסופית **לא נדבקת לבית** — הצורה יוצאת מוזזת הצידה.
המשתמש אישר במפורש: **זה קרה על ציור חדש, אחרי reload ואחרי פתיחה מחדש של הדיאלוג** —
לא שריד של ציור ישן.

**מה כבר תוקן ולא פתר:** `snapPoint` היה מכיר רק את הקו המצויר של הקיר (פאה אחת).
נוספו **כל ארבע פינות ה-band** כיעדי הצמדה — מ-`w.corners` לקירות שהוחלו, ומ-`bandQuad`
לקירות ממתינים. בבדיקות ה-stub זה עובד מושלם (ריחוף ליד פינה חיצונית נועל עליה בדיוק).
**ובכל זאת בשטח התוצאה מוזזת.**

**כיווני חקירה מוצעים, לפני שנוגעים בקוד:**
1. **חשד ראשי — מערכות קואורדינטות.** `walls_payload` מחזיר `corners` אחרי
   `transform` לעולם, אבל `sx/sy/ex/ey` גם הם עולמיים. לבדוק שההזזה אינה בדיוק
   חצי עובי קיר או עובי מלא — זה יצביע על פאה לא נכונה.
2. **לבדוק מה באמת נשמר.** להשוות בקונסול את נקודות ה-`sketch2d` שנוצרו מול
   `corners_xy` של הקיר, ולראות אם ההפרש קבוע.
3. **חשד שני — `build_sketch_group`.** הוא מריץ dedup של נקודות קרובות
   (`clean.last.distance(pt) > 0.01`) ו-`add_curve`. לוודא שהוא לא מזיז שום נקודה.
4. **חשד שלישי — `moduleIn`.** אם סנאפ למודול היה דלוק, הוא מעגל תזוזה חופשית;
   נקודה שנצמדה לפינה אמורה לעקוף אותו — לוודא שזה באמת המצב בכל המסלולים.

**פקודה לאבחון (Ruby Console):**
```ruby
w = Sketchup.active_model.entities.grep(Sketchup::Group).find { |g| g.get_attribute('InteriorPro','type')=='wall' }
puts "corners: #{w.get_attribute('InteriorPro','corners_xy').inspect}"
Sketchup.active_model.entities.grep(Sketchup::Group).select { |g| g.get_attribute('InteriorPro','type')=='sketch2d' }.each do |g|
  puts "sketch #{g.get_attribute('InteriorPro','id')}: #{g.get_attribute('InteriorPro','pts').inspect}"
end
```
להשוות את הנקודה הראשונה של ה-sketch מול ארבע הפינות של הקיר ולראות מה ההפרש.

## 5. מה נשאר ברשימה
1. **הבאג למעלה** — ראשון.
2. חדרים ושטחים חיים בקנבס של העורך (המידע קיים, פשוט לא מצויר).
3. קומות (הכל level 1).
4. קירות בקשת/עיגול.
5. מ-`TARGET_PLANS.md`: ‏Area Plan, ריהוט, בועות גריד, אליוויישנים פנימיים,
   דף שער, ייצוא LayOut/PDF.

## 6. חוב ידוע שלא נגענו בו
- באג שורש `DoorManager.wall_geometry` (‏center_offset בסימני הניצב השמאלי מול `n` ימני).
  עקיפה: העורך יוצר תמיד קירות `bottom-left`. **לא לשנות בלי אישור.**
- מיזוג קו-ליניארי עובד רק על קירות pending (`mergeCollinear` רץ רק על `pending`).
- העורך לא מחובר לדיאלוגים המלאים של דלת/חלון ("More options").
- `hatch_polygon` קיים ב-`plan_generator.rb` אך **מנוטרל בכוונה** — לא להפעיל.
