# Interior Pro — מסירה 2026-08-29 (גאטרס ודאונספאוטים)

קרא את זה קודם, ואז `CLAUDE.md`. הקודמת: `HANDOFF_2026-08-27.md`.
**אל תסמוך על הסיכום — תאמת מול הקוד החי.**

בדיקות: בענן בלבד (למשתמש אין רובי): `cd tests` ואז
`LANG=C.UTF-8 bash run_all.sh ..` → **139 ירוקות** (היו 136; rt87, rt88,
rt89 חדשות). להעתיק לענן גם `icons/` ו-`textures/` (בלי
`textures/floors/`). **תמיד run_all.sh**, אף פעם לא `ruby rt89.rb` לבד.

git: ענף `door-stabilize`. חמישה קומיטים היום:
`dc11a6e` (חתך הגאטר + הסחיפה), `32a76c5` (הגאטר על הגג),
`f0a4fa1` (הדיאלוג + הצבעים), `0c1c1d4` (הדאונספאוטים),
והאחרון — הכלי והכפתור.
**צעד ראשון חובה: `git status` דרך CMD שלו** — אל תריץ git בעצמך,
אתה תוקע `index.lock`.

---

## מה נסגר היום ואושר על ידו

### 1. גאטרס — סגור

**מה הוא בחר בעצמו, אחרי שרטוט:** כל שלוש הצורות (K-Style, חצי עגול,
מרובע), ו**רק על האיב** — אף פעם לא על הרייק.

**איך זה בנוי.** `build_band!` סוחף מלבן (ארבע פינות, `add_prism!`).
גאטר הוא שוקת: חלול, ובשתיים משלוש הצורות מעוגל. לכן נוסף
`build_profile_band!` — סוחף **חתך סגור כלשהו** סביב אותו `poly`, דרך
אותו `offset_polygon`, אז הפינות מתמזוות לבד בדיוק כמו של הפאשיה.

- `gutter_path(profile, w, h, drop)` — הקו החיצוני של השוקת, **פתוח**.
- `offset_path_left(path, t)` — צועד כל נקודה עובי-מתכת אחד שמאלה,
  ושמאלה על המסלולים האלה זה **פנימה**. הפינות נשארות חדות (חוצה זווית
  מוארך ב-1/cos).
- `gutter_section` = החיצוני + הפנימי הפוך. לולאה סגורה אחת, פשוטה.
- k נמדד **החוצה** מקו האיב, בדיוק כמו ב-`build_band!`: הפאשיה היא
  `-FASCIA_THICK..0.0`, אז **k=0 זה פני הפאשיה** והגאטר תלוי עליו
  וגדל החוצה. לכן הוא לא יכול לאכול את הסופיט או את הקיר.
- **רק באיב:** `gable_flags` / `gable_spans` נמסרים כמו שהם ל-
  `build_profile_band!`, ואותם דגלים שוב כ-`square_flags` — אז הריצה
  נחתכת **ישר** על פני הרייק במקום לרוץ מעבר לפינת הבניין באלכסון 45.
  זה בדיוק האלכסון שהורדנו מהסופיט ב-24.08.
- **מכסה בקצה:** במקום שבו הריצה נגמרת מול רייק נוספת פאה שהיא
  הצללית החיצונית סגורה על פי השוקת (`gutter_outer_len`).
- **החלקה:** `soften_shallow_edges!` — כל תפר עם זווית ≤ 35° נעשה
  soft+smooth. 0° נספר בכוונה (המכסה יושב במישור הטבעת).
- **צבע:** `gutter_color`, ברירת מחדל `''` = ללכת אחרי הפאשיה, בדיוק
  כמו `soffit_color`.

הגדרות: `gutter`, `gutter_profile`, `gutter_width`, `gutter_color`.
ברירת מחדל **כבוי**, אז שום גג ישן לא גדל גאטר מאחורי הגב.

### 2. דאונספאוטים — סגור

**מה הוא בחר:** אוטומטי, **ולחיצה כדי להוריד אחד**.

- `downspout_profile` — מלבן 3"×2', ועגול כשהגאטר עגול. צורה אחת פחות
  לבחור (כלל ה-UI הקבוע). 3" לאורך הקיר, 2" לרוחב — כמו 2x3 אמיתי.
- `downspout_path` — קו המרכז ב-2D: ישר למטה מהשוקת, מרפק 45 לקיר,
  ירידה צמודה לקיר, בעיטה החוצה. כל הצינור יושב ב**מישור אנכי אחד**,
  ולכן זה 2D ולא 3D.
- `tube_rings` — טבעת נקודות לכל נקודת מסלול, עם אותו מיזוו.
- `build_tube!` — שני עורות (חיצוני ופנימי) ובכל קצה טבעת מתכת עם חור
  (פאה חיצונית + פאה פנימית + מחיקת הפנימית). **חלול.**
- `downspout_spots` — אחד בכל פינה של הבית שיש גאטר על לפחות אחד משני
  הצדדים שלה. גייבל מקבל ארבעה; היפ גם, כי הגאטר שלו רץ סביב ואין לו
  קצוות.
- כל צינור הוא **קבוצה משלו** בתוך הגג, עם `ds_key` = נקודת התוכנית.
- `downspout_tool.rb` + כפתור בסרגל הגגות + שורה בתפריט.
  לחיצה על צינור מורידה אותו **לתמיד**; לחיצה על הגג ליד מקום שפונה
  מחזירה אותו. שניהם עוברים דרך `build_roof!(replace:)`.

הגדרה: `downspouts`, ברירת מחדל **דלוק** — אבל היא לא יכולה להראות
כלום בלי גאטר, אז שוב, שום גג ישן לא השתנה.

---

## מה עלה ביוקר היום — אל תחזור על זה

1. **`downspout_spots` מדד מפינת הגג ולא מפינת הבית.** `poly` הוא קו
   האיב, והפינה שלו רחוקה **אוברהאנג שלם** לאורך הצלע מפינת הקיר. שישה
   אינץ' מפינת הפוליגון = הצינור תלוי באוויר ליד הבניין. הוא צילם את
   זה **פעמיים** לפני שתפסתי. `downspout_spots` מקבל עכשיו `overhang`
   ומוסיף אותו להיסט. `rt89` נועלת בדיוק את זה.

2. **פקודת קונסולה `build_roof!(gutter: true)` בלי `level:` מוחקת את
   כל הגגות.** כתוב במפורש בהערה בקוד: "The plain no-argument call still
   clears everything". שברתי לו את הגג של הקומה התחתונה. **תמיד
   `replace:` או `level:`** כשנותנים לו שורת קונסולה:
   `RoofManager.roofs.each { |r| RoofManager.build_roof!(replace: r) }`

3. **המוצא במרכז הגאטר יוצא דרך הדופן הקדמית של K-Style** — התחתית
   השטוחה שלו היא רק 42% הראשונים. `gutter_outlet_k` מוצאת את אמצע
   התחתית האמיתית לכל צורה. הוא ראה את זה מיד בתמונה.

4. **עקומה שנדגמת גס נראית כמו מדרגות.** ה-K-Style הראשון היה תשעה
   קטעים ישרים והוא אמר "נטו כמו מדרגות" ברגע הראשון. עכשיו שני בזייה
   קוביים, 24 נקודות; הפנייה החדה ביותר ירדה מ-60° ל-22°. `rt87` נועלת
   ≤30°.

5. **חצי עיגול חשוף מטה את השפה הפנימית שלו למעלה.** ההיסט נמדד מה
   **קטע**, והקטע הראשון של קשת נדגמת כבר מוטה חצי צעד — השפה הפנימית
   יצאה 0.015" מעל החיצונית והגאטר בצבץ מעל קצה הגג. נוספה שפה ישרה
   (`GUTTER_LIP`) בשני הקצוות. זה גם מה שיש בחצי-עגול אמיתי.

6. **סקריפט פייתון שמחליף מחרוזת בקובץ CRLF** — אם המחרוזת החדשה כבר
   מכילה `\r\n` ואז מריצים `.replace("\n","\r\n")`, מקבלים `\r\r\n`
   ורובי מזהיר. תבדוק `ruby -c` **ו**-`cat -A` אחרי כל עריכה מסוקריפט.

---

## מוקשים שעדיין בתוקף

- `roof_manager.rb` הוא **CRLF**, `roof_dialog.rb`, `toolbar.rb`,
  `main.rb`, `downspout_tool.rb` הם LF, וקבצי `tests/rt*.rb` הם **LF**.
- `X = {...} unless const_defined?(:X)` **לא** נטען מחדש ב-`reload!`.
  לכן `roof_setting_keys`, `soffit_textures`, `beam_spec` הן **מתודות**.
- כל פאה עוברת `add_ring!` / rescue — raise אחד מפיל קצה שלם.
- הסטאב בענן זורק על נקודות כפולות כמו סקצ'אפ אמיתי. לא לרכך.
- **הסטאב לא יוצר Edges מ-`add_face`.** לכן `soften_shallow_edges!` ואת
  החור במכסה **אי אפשר לבדוק בענן** — רק בסקצ'אפ. תגיד לו את זה, אל
  תטען שבדקת.
- שינוי HTML בדיאלוג → **לסגור ולפתוח את החלון**, לא רק `reload!`.
- כפתור חדש בסרגל → **הפעלה מחדש של סקצ'אפ**. `setup_roofs_toolbar`
  מדלג על סרגל מאוכלס (`return if tb.length >= 2`), וזה בכוונה.
- **לסגור את פינות הגייבל עם `build_end_caps!` — נוסה ונכשל (26.08).**
- `full: true` ב-`edge_profile_chain` — נוסה ונכשל (27.08), שובר rt17.

## איך לעבוד עם דרור

- עברית פשוטה, **משפט-שניים, כמו לילד בן 5**. הוא ביקש את זה במפורש.
- כל פקודה בקופסה נפרדת עם לאן (Ruby Console / CMD).
- git רק דרך CMD שלו. הוא לא כותב קוד — אתה עורך, הוא מדביק ובודק.
- **שרטוט לפני בנייה, ושאלה אחת קצרה.** היום כל שרטוט קיבל תשובה
  מיידית: על שלוש הצורות הוא ענה "את כולם", על המיקום "רק באיב", על
  הדאונספאוטים "אוטומטי ולחיצה אם אני רוצה להוריד". הוא מאשר מהר כשהוא
  רואה תמונה.
  לציור בענן: SVG + `cairosvg`, ועברית חייבת לעבור `python-bidi`
  (`get_display`) לפני שהיא נכנסת ל-SVG. לציור 3D מהסטאב: אקסונומטרי +
  מיון פאות לפי עומק, **ותמיד ללכת גם על תת-הקבוצות** — הדאונספאוטים
  הם תת-קבוצות ולא יופיעו ב-grep הרגיל.
  לצילום דיאלוג: להוציא את `build_html` לקובץ ולצלם עם playwright
  (chromium כבר מותקן בענן). ככה ראיתי את הפאנל לפני שמסרתי.
- **תמדוד לפני שאתה משנה, ותסתכל על התוצאה בענן לפני שאתה מוסר לו.**
- אם משהו לא נראה לו — **תבקש תמונה ותסתכל טוב**. שלוש מהתקלות היום
  נמצאו מהתמונות שלו, לא מהקוד. הוא מסמן באדום.
- הוא כותב לפעמים בעברית עם מקלדת אנגלית ("zv gshhi kt huac gk vehr" =
  "זה עדיין לא יושב על הקיר"). תפענח, אל תשאל.
- צעד אחד, קטן, ולתת לו לאשר כל אחד. הגאטרס התפרקו ל-4 והדאונספאוטים
  ל-3 (והשלישי ל-3א כלי / 3ב כפתור, כדי לחסוך לו הפעלה מחדש באמצע).
- **commit אחרי כל סבב שעובד**, דרך CMD שלו, ואז לעדכן את קובץ המסירה.

## TODO

- **צבע נפרד לדאונספאוט** — כרגע הוא לוקח את צבע הגאטר. הוא לא ביקש,
  אבל זה הדבר הבא המתבקש אם יבקש.
- **גאטר על גג מעוגל** — `eave_polygon` עדיין מתייחס לקיר מעוגל כקטע
  ישר, אז הגאטר ירוץ ישר גם הוא.
- מ-`CLAUDE.md`: קירות מעוגלים, תוכניות 2D ל-PDF, NOTE מצויר, צבע
  בגיליון, לוגו per-installation, corner boards.
