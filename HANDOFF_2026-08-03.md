# מסירה לצ'אט הבא — Interior Pro, סבב 2026-08-01/03

## 0. לפני הכל
חבר את התיקייה החיה:
`C:\Users\rordt\AppData\Roaming\SketchUp\SketchUp 2024\SketchUp\Plugins\interior_pro`
והתעלם לגמרי מ-`C:\InteriorPro_Agent\source_readonly`.

קרא: `STATUS_handoff.md`, ‏`CONTRACT_2D.md`, ‏`TARGET_PLANS.md`, ואת הקובץ הזה.
‏`HANDOFF_2026-07-31.md` הוא הסבב הקודם — הבאג שבסעיף 4 שלו **נסגר**.

## 1. כללי עבודה

- **אישור לפני מימוש.** מותר להציע, אסור לכתוב קוד בלי אישור מפורש.
- כתיבה בעברית, הודעות קצרות, הסברים בשפה פשוטה מאוד. המשתמש ביקש
  במפורש: "כאילו אני ילד בן 7".
- **כלל חדש (2026-08-02): כל כלי נבנה כמו בסקצ'אפ.** לפני שינוי בכלי —
  לבדוק איך הכלי המקביל עובד בסקצ'אפ ולעבוד לפיו. ציטוט: "כל הקטע שזה
  אותה התוכנה רק ב-2D".
- **כלל חדש (2026-08-02): כל פקודה שצריך להדביק ל-Ruby Console נכתבת
  בקופסת קוד נפרדת.**
- העוזר כותב את הקבצים לדיסק בעצמו; המשתמש מריץ `InteriorPro.reload!`
  וסוגר/פותח את הדיאלוג.
- ‏git מהטרמינל של המשתמש. יש קובץ נעילה תקוע חוזר: `del ".git\index.lock"`
  לפני `git add`.

## 2. אזהרה קריטית — plan_editor.rb
כל ה-HTML/JS יושב ב-heredoc `<<~HTML` בתוך רובי. **escape בודד הורג את כל
ה-JS בשקט.** יש עכשיו חבילת בדיקות שתופסת את זה — ראה סעיף 4.

## 3. מה נבנה בסבב הזה

### עורך — כלים בשיטת סקצ'אפ
- **מחק קטע** (כפתור במצב Line): מוחק קטע אחד בין שתי הצטלבויות במקום את
  כל הצורה. הצטלבות = צורה אחרת, פאת קיר, או פינה של צורה ישרה. **קו
  שנגמר על קו אחר נחשב הצטלבות** — בלי זה, שארית שנייה של קשת לא ניתנת
  למחיקה. צורה בקבוצה נמחקת שלמה.
- **הזזה**: כלי ← נקודת אחיזה על האובייקט ← הזז. נעילת ציר אוטומטית קרוב
  לאופקי/אנכי (אדום/ירוק), Shift כופה, יעד נצמד לפינות, הקלדת מרחק+Enter.
- **סיבוב**: כלי ← מרכז ← נקודת התחלה ← סובב. גם זרוע האפס וגם הסיבוב
  ננעלים על ימין/שמאל/מעלה/מטה. Shift = 15°. פינה אמיתית גוברת על ציר.
- **אופסט**: כלי ← קליק על קו המתאר ← הזז לצד שרוצים. הצד לפי העכבר,
  המידה לפי גרירה או הקלדה.
- **היפוך** הופך את האובייקט עצמו במקום, בלי לשכפל (שינוי מכוון).
- **ריבוע בחירה**: שמאל→ימין תופס רק מה שנכנס שלם (מסגרת מלאה),
  ימין→שמאל תופס כל מה שנגע (מסגרת ירוקה מקווקוות). קודם נבדק רק המרכז.

### עורך — מידות
- כל קיר מציג **שתי מידות**, חיצונית ופנימית, כל אחת צמודה לפאה שלה,
  נמדדות מהפינות הממוטרות האמיתיות.
- **קווי מידה אמיתיים**: קו רץ, קו עזר וסימן 45° בכל עצירה, המספר בתוך
  פער בקו.
- קיר עם פתחים מקבל **שרשרת**: פינה → רוחב הפתח → מרווח → רוחב → פינה.
  רוחבי הפתחים מודגשים בירוק. הסה"כ עובר שורה אחורה.
- כל מספר **מסתובב עם האלמנט** ולעולם לא הפוך.
- **צורות מקבלות מידות**: כל צלע את אורכה, עיגול מקבל קוטר, קשת נשארת
  נקייה.
- מתג בתחתית הפאנל: חוץ · פנים · הכל · ללא.

### עורך — חדרים ושטחים
- לולאת קירות סגורה מקבלת תווית עם שם, מספר ושטח נטו ב-SF. קליק על
  התווית = שינוי שם, נשמר על המודל.
- **שטח חי גם לפני Apply**: `PlanEditor.preview_rooms` מריץ את המנוע של
  `RoomManager` על קירות המודל + הקירות הכחולים דרך `PendingWallStub`,
  בלי לכתוב כלום למודל. אותו מנוע = אותו מספר בסירטוט ובתוכנית.
- **תווית שטח לכל צורה סגורה** בכפתור ייעודי (attribute `area_on`,
  `area_name`). לא אוטומטי — לפי בקשת המשתמש.

### עורך — הצמדות וציור
- הנקודה הירוקה מופיעה **גם לפני הקליק הראשון** בציור קיר.
- **אמצע הקיר נתפס משני צידיו** (פאה פנימית וחיצונית), בקירות פנים וחוץ.
- **קיר פנים נעצר לבד על קיר חוץ** (`clampToBoundary`). קיר חוץ לא נחתך,
  קיר פנים לא נעצר על קיר פנים, והקלדת מידה גוברת תמיד.

### עורך — פאנל
- קווי עזר ותמונת רקע הפכו לחלונות **מקופלים קבועים בתחתית** + מתג
  המידות. כותרת כל חלון מראה את מצבו כשהוא מקופל. שום דבר לא זז כשמחליפים
  כלי.
- **כיול תמונת הרקע נשמר על המודל** ולא מתאפס בפתיחה מחדש. אחרי כיול
  התמונה ננעלת לבד. תמונה חדשה מאפסת את הכיול.
- סנאפ 3"/6"/1' הוסר לבקשת המשתמש.

### תוכניות (plan_generator.rb)
- **שרשראות מידות ברמת הבניין** על ארבעת הצדדים: פתחים (קצוות, לא
  מרכזים) → קטעים בין קירות חוצים → סה"כ. רצועה מופיעה רק אם יש לה מה
  להגיד. פאות של קירות היקף ליד הקצוות נזרקות כרעש.
- **שורות המידה שהיו על כל קיר בצד החוץ הוסרו** (החלפה מכוונת, באישור).
  המידה הפנימית בחדרים נשארה.

### פתח ללא דלת
- סוג חדש `'Cased Opening'` ב-`INTERIOR_TYPES`, ברירת מחדל 48×84.
- ב-`door_tool.rb`: `cased_opening?` + `return true` מיד אחרי
  `build_u_jamb` — משקוף בלבד, בלי כנף/בולם/ידית. מסלולי ארון וכיס לא
  נגעו.
- סימבול נקי בעורך ובתוכנית (`kind = 'opening'` / `:opening`).
- **סוג חדש בספרייה = הפעלה מחדש מלאה של סקצ'אפ.**

### באג ישן שנמצא ותוקן
מזהי `sketch2d` נבנו מ-`שנייה + rand(10000)`. בבדיקה של 200 צורות באותה
שנייה התקבלה כפילות. מחיקה וחיתוך מחפשים **לפי מזהה** ולוקחים את הראשון,
כלומר אפשר היה לפגוע בצורה הלא נכונה. עכשיו `next_sketch_id` משתמש במונה
רץ + בדיקה מול מה שקיים במודל.

## 4. חבילת הבדיקות — `tests/`

**להשתמש בה בכל שינוי.** היא מריצה את הקוד האמיתי מחוץ לסקצ'אפ:

```
bash tests/run_all.sh .
```

מחלצת את ה-JS מה-heredoc (רובי מעבד את ה-escapes), מריצה `node --check`,
ואז 26 חבילות JS מול DOM/Canvas מזויף ו-7 חבילות רובי מול סטאב של
SketchUp API — כולל `room_manager.rb` ו-`plan_generator.rb` האמיתיים.

בסבב הזה היא תפסה שישה באגים לפני המשתמש: המזהים הכפולים, שתי מידות
שנדחפו לאותו צד, שדה HTML עם id כפול ששבר את Move sideways של הקירות,
ציוני רעש בשרשראות, וקווי חוץ/פנים הפוכים.

## 5. מה נשאר פתוח

**סירטוט — סגור לעכשיו** לפי החלטת המשתמש (2026-08-03).

1. **מיזוג קו-ליניארי מול קירות שהוחלו** — נבדק: `join_corners` מדלג על
   קירות קו-ליניאריים (`return if cross_z.abs < 1e-6`) כי אין מה למטר,
   והם נראים רציפים. הם נשארים שני אלמנטים; הסימפטום היחיד הוא שתי
   שרשראות מידה. **המשתמש החליט שזה לא מפריע.**
2. **קירות בקשת/עיגול** — המשתמש צודק שזה מתחיל ב-3D: צריך attributes של
   רדיוס/כיוון ו-`build_wall_group` שיודע לבנות, למטר ולהריץ מולדינג.
3. **קומות** — גם כן מתחיל ב-3D: `level` ו-`base_z` אמיתיים על כל אלמנט.
4. חיבור העורך לדיאלוגים המלאים של דלת/חלון ("More options").
5. מ-`TARGET_PLANS.md`: ‏Area Plan, ריהוט, בועות גריד, אליוויישנים
   פנימיים, דף שער, ייצוא LayOut/PDF.
6. חומר הריצפה בתוכניות — אושר עקרונית מזמן, לא מומש. המשתמש הבהיר
   שזה שייך לתוכניות/3D ולא לסירטוט.

**הצעד הבא לפי המשתמש: מעבר לעבודה על ה-3D.**

## 6. חוב ידוע שלא נגענו בו
- באג שורש `DoorManager.wall_geometry` (‏center_offset בסימני הניצב
  השמאלי מול `n` ימני). עקיפה: העורך יוצר תמיד קירות `bottom-left`.
  **לא לשנות בלי אישור.**
- `hatch_polygon` קיים ב-`plan_generator.rb` אך **מנוטרל בכוונה**.
- רעש CRLF/LF ב-git בקבצי ה-door ובקובץ `CLAUDE.md`.
