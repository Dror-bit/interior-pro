# מסירה לצ'אט הבא — Interior Pro, סבב 2026-08-04/05 (כלי גגות)

## 0. לפני הכל
התיקייה החיה היחידה:
`C:\Users\rordt\AppData\Roaming\SketchUp\SketchUp 2024\SketchUp\Plugins\interior_pro`
להתעלם לגמרי מ-`C:\InteriorPro_Agent\source_readonly`.

סדר קריאה: **הקובץ הזה** ← `HANDOFF_2026-08-04.md` ← `HANDOFF_2026-08-03B.md`
← `STATUS_handoff.md`. הסבב הזה עסק כמעט רק בגגות.

## 1. כללי עבודה (לא השתנו — מחייבים)
- אישור לפני מימוש; באג שדווח = מתקנים ישר.
- עברית בלבד, קצר, פשוט. כל פקודה להדבקה בקופסת קוד נפרדת עם ציון לאן.
- העוזר כותב קבצים לדיסק; המשתמש מריץ `InteriorPro.reload!`.
  קובץ/כפתור חדש = restart מלא. HTML של דיאלוג = לסגור ולפתוח את החלון.
- צעד אחד ← בדיקת משתמש ← commit (`del ".git\index.lock"` לפני git add).
- אחרי כל כתיבה מרחוק לאמת גודל מול device_list_dir.
- **לא לסמוך על צ'אט קודם — לאמת מול הקוד החי. תמיד לבקש פלט קונסול
  וצילום לפני אבחון.**

## 2. בדיקות — חובה בכל שינוי
```
bash tests/run_all.sh .
```
30 חבילות JS + 15 רובי. `rt15.rb` = כל בדיקות הגג (~60 בדיקות, כולל
רגרסיות על הצורות האמיתיות של המשתמש). הכל ירוק במסירה.

## 3. מה נבנה בסבב — מערכת גגות (קבצים חדשים)

### קבצים
- **`roof_manager.rb`** — המנוע. **`roof_dialog.rb`** — חלון הגדרות.
- **`roof_gable_tool.rb`** — כלי Gable Ends (קליק על קיר).
- סרגל כלים "Interior Pro Roofs" (כפתור Roof + כפתור Gable Ends),
  תפריט Roof: Build/Gable Ends/Remove. אייקונים roof_tool.svg, roof_gable.svg.
- `tests/rt15.rb`; `run_all.sh` מעתיק גם floor_manager/roof_manager/roof_dialog.

### איך הגג עובד (מודל Revit)
- מתאר אמיתי מלולאת קירות החוץ של הקומה העליונה: `eave_polygon` —
  centerlines (RoomManager) ← offset חוצה (עובי/2 + איבס). מחזיר גם
  wall_id לכל צלע.
- **straight skeleton** (`straight_skeleton(pts, speeds)`) עם מהירות
  פר-צלע: 1.0 = משופעת, 0.0 = גמלון. מטפל ב: edge events, split events
  (פינות שקועות), **corridor annihilation** (כנף שמתה לתוך הגוף — חזיתות
  מקבילות פנים-מול-פנים, כולל סגירה חד-צדדית בשרשרת), ו-**tear junctions**
  (גמלון שנגמר באמצע קיר ארוך — צלע וירטואלית אנכית, קודקוד אחד נשאר
  וקודקוד תאום מטפס פנימה).
- `roof_cells` — גרף מכל הקטעים (צלעות + קשתות), **כל קטע מפוצל בכל צומת
  שיושב עליו** (קריטי — ואלי יכול לעבור דרך צומת), trace_faces של
  RoomManager, תא לכל צלע משופעת.
- **הגג הוא משטח בלבד — אפס עובי** (החלטת משתמש). פאה לכל תא: צבע גג
  מלמעלה, לבן מלמטה (paint_surface! מיישר normal).
- **heel**: המשטח מוזז ב-delta = ‑slope*overhang כך שהוא פוגש את ראש הקיר
  בדיוק על הפאה החיצונית — בתוך הבית תמיד מעל התקרה.
- **פשיה** (ברירת מחדל 8", צבע לבן) + **מטל אדג'** (0.1"): קופסאות
  שתחובות מתחת לקצה הגג (הפאה החיצונית של הפשיה בקו קצה הגג). על גמלון:
  **rake board** מטפס עם הפרופיל + **משולש גמלון** אנכי שסוגר את הקצה
  (build_gable_wall_face! — נוסף ממש בסוף הסבב, נבדק בטסטים, המשתמש עוד
  לא ראה אותו במודל!).
- **דיאלוג**: Hip / Gable / Flat, שיפוע 2:12–12:12, איבס עם/בלי + מידה,
  פשיה + עומק, מטל אדג', צבע גג + צבע פשיה. הכל נשמר על המודל
  (roof_* attributes). Flat = משטח על ראשי הקירות.
- **Gable Ends (קליק)**: toggle_gable_wall!(wall, click_xy). נשמר לפי
  wall id + **נקודת הקליק** ('roof_gable_wall_ids' + 'roof_gable_click_xy').
  בקיר ארוך הגמלון הולך לרצועת-העומק שמתחת לקליק (gable_subinterval_at);
  בלי נקודה (סימון ישן) — הרצועה העמוקה. הקיר מתפצל בפוליגון
  (split_gable_edges) בגבולות הרצועה.
- מצב Gable אוטומטי (בלי סימונים) = שתי הצלעות הקצרות הלא-סמוכות —
  **גרוע בבתים מורכבים** (בחר את שתי דפנות אותה כנף אצל המשתמש). בבית
  מורכב להשתמש בקליקים.

## 4. המצב מול המשתמש — חשוב לקרוא
המשתמש בונה בית אמיתי עם 3 כנפיים (מתאר 10 פינות — שמור כרגרסיה ב-rt15
בשם zshape). הוא התלונן על "שברים": רוב הבעיות תוקנו (חורים = corridor
annihilation; תא נעלם = פיצול קשתות בצמתים; גבהים מפלצתיים = קליק קובע
את רצועת הגמלון). **החורים הלבנים המשולשים בצילומים שלו = משולשי הגמלון
שלא נבנו — התיקון נכתב ונבדק אבל טרם נמסר לו לבדיקה במודל!**

### הצעד הראשון של הצ'אט הבא
1. לוודא שהקבצים אצלו עדכניים (roof_manager.rb ~41KB, rt15 ~16KB) ולהריץ:
   reload + Apply, ולבקש צילום. ייתכן שמשולשי הגמלון פותרים את רוב התלונה.
2. אם עדיין "נשבר": לבקש את שלוש השורות (footprint_xy, gable_wall_ids,
   roof_gable_click_xy) + צילום + על מה לחץ — ולשחזר בדיוק בבדיקות לפני
   כל תיקון. ככה נפתרו כל הבאגים עד עכשיו.

## 5. פתוח / הבא בתור
1. **בדיקת משתמש למשולשי הגמלון** (ראה 4).
2. בצילום הידני שלו נראה עוד משטח מרחף עם "שן" ליד חיבור הכנף הימנית —
   לא אובחן (חסרו הנתונים). לאסוף נתונים ולשחזר.
3. **Dutch Gable** (Setback כמו במדייק) — הובטח, טרם נבנה.
4. חומרים אמיתיים לגג (רעפים/שינגלס — טקסטורות) — המשתמש רוצה שהחומר
   ייתן את העובי.
5. אוטו-Gable חכם יותר או הסרתו לטובת קליקים בלבד — לשאול את המשתמש.
6. gable walls אמיתיים (הקיר עולה למשולש כקיר, לא רק פאה לבנה) — בהמשך.
7. מהסבבים הקודמים: תקרה עליונה לקומה 2, plan_generator לא מכיר קומות,
   z-fight רצפה/סאבפלור, מיטר Merge Wall, אייקונים 24px.

## 6. חוב ידוע שלא נגענו
- באג שורש DoorManager.wall_geometry — לא לגעת בלי אישור.
- hatch_polygon מנוטרל; אזהרות already initialized = רעש; CRLF/LF ב-git.

## 7. קבצים שהשתנו/נוספו בסבב
**חדשים:** roof_manager.rb, roof_dialog.rb, roof_gable_tool.rb,
icons/roof_tool.svg, icons/roof_gable.svg, tests/rt15.rb, הקובץ הזה.
**שונו:** main.rb, toolbar.rb, tests/run_all.sh.
