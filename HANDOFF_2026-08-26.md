# Interior Pro — מסירה 2026-08-26 (סופיט משופע + Edit Roof + כמה גגות)

קרא את זה קודם, ואז `CLAUDE.md`. הקודמת: `HANDOFF_2026-08-24C.md`.
**אל תסמוך על הסיכום — תאמת מול הקוד החי.**

בדיקות: בענן בלבד (למשתמש אין רובי): `cd tests` ואז
`LANG=C.UTF-8 bash run_all.sh ..` → **135 ירוקות** (rt85 חדשה היום).
להעתיק לענן גם `icons/` ו-`textures/` (בלי `textures/floors/`).
`run_all.sh` מעתיק את קבצי המקור לתוך `tests/` — **תמיד run_all.sh**,
אף פעם לא `ruby rt85.rb` לבד, זה רץ על עותק ישן ומשקר.

git: ענף `door-stabilize`. **צעד ראשון חובה: `git status` דרך CMD שלו** —
אל תריץ git בעצמך, אתה תוקע `index.lock`.

---

## מה נסגר היום ואושר על ידו

### 1. סופיט משופע (`soffit_slope`)
צ'קבוקס חדש בדיאלוג הגג: **Sloped (follows the roof)**. הלוח נשכב
במקביל לגג, **רק בצד הפיטש** — הרייק של הגייבל לא נגעו בו בכוונה
(הוא ביקש: "לא בגיבל אלא רק בפיטש").

- `soffit_rise(band, slope, sloped)` — טהורה, מספר אחד: כמה הקצה
  הפנימי (בקיר) גבוה מהחיצוני (בפאשיה) = `slope * (k_out - k_in)`.
  0.0 = בדיוק הלוח הישר של אתמול. הקצה **החיצוני לא זז** — הפאשיה
  והדריפ לא יודעים שקרה משהו.
- `build_band!` קיבל `rise` (ארגומנט אחרון) → `add_prism!` קיבל `lift`,
  מערך dz לכל פינה. כל נקודה שהבנאי מייצר יושבת על קו k_in או k_out
  (גם הפינה המרובעת — היא חיתוך **עם** אחד מהם), ולכן מספר אחד לצד מספיק.
- `band_corner_seams` קיבל `rise` כדי שהריכוך ימצא את התפרים.
- הקורות: `tilt_profile(profile, tilt, k_ref)` — **גזירה, לא סיבוב**.
  כל נקודה נדחפת ישר למעלה, שום דבר לא זז הצידה. הקצוות נשארים
  אנכיים (ככה חותכים קורה אמיתית), והגובה נמדד אנכית אז `h` ב-`beam_spec`
  עדיין אומר מה שאמר. הציר: `k_out` של **הלוח**, לא `ko` של הקורה —
  הספניש נעצרת אינץ' לפני בשביל הגאטרס, וציר שם היה מרחיק את העץ מהלוח.

### 2. Apply לא מוחק יותר גייבלים
היה: כל Apply והרדיו על Hip → מחיקת כל סימוני הגייבל. עכשיו רק
**החלפת סגנון** ל-Hip מוחקת. `tests/rt15.rb` נועל את שני הכיוונים.

### 3. Edit Roof — הגג הפך לאובייקט (5 צעדים, כולם בקומיט)

1. **הגג נושא את ההגדרות שלו.** `roof_setting_keys` (מתודה!),
   `save_roof_settings!`, `roof_settings(grp)`. גג בלי חותמת נופל
   חזרה להגדרות המודל — **ככה כל מודל ישן ממשיך לעבוד בלי הגירה**.
2. **הגג נושא את הקירות והגייבלים שלו.** `save_roof_marks!`,
   `roof_wall_ids`, `roof_gable_ids`, `roof_gable_points`,
   `roof_of_wall_id(id)`. סימון על קיר של גג אחר לא נכנס לרשימה שלו.
3. **בנייה מוחקת רק את מה שהיא בונה.** `build_roof!(level:, replace:)`.
   בלי ארגומנטים = כמו תמיד, מוחק הכל. לחיצת גייבל בונה מחדש רק את
   הגג שמחזיק את הקיר. `walls_of(lvl)` נחתך מ-`top_walls`.
4. **כפתור Edit Roof** — `roof_edit_tool.rb`, אייקון `icons/roof_edit.svg`,
   בסרגל הגגות ובתפריט. קליק על גג → החלון נפתח עם ההגדרות **שלו**,
   Apply בונה מחדש רק אותו, Remove מוחק רק אותו. הפאנל עוקב אחרי
   הקבוצה החדשה, אז Apply שני מאותו חלון פוגע באותו גג.
   **הכפתור מופיע רק אחרי הפעלה מחדש של סקצ'אפ** (הגארד
   `return if tb.length >= 2` בסרגל).
5. **בורר קומה** בחלון הרגיל (לא בעריכה — שם הגג כבר יודע איפה הוא).
   מופיע רק אם יש יותר מקומה אחת. **ברירת מחדל: All storeys.**
   החלון הוגדל ל-820 גובה כדי שהכל ייראה בלי גלילה.

### 4. הגג התחתון נחתך מתחת לבית העליון
`exposed_polygon(walls, upper_walls, overhang)` — לא בוליאני על
פוליגונים, אלא **מכונת החדרים**: הקירות העליונים שחוצים את הקומה
נזרקים לאותו גרף קירות, הפאות נמצאות כמו חדרים, והפאה שלא מתחת לבית
היא החלק החשוף. קיר עליון שרק **עומד על** קיר של הקומה יושב על קו
הלולאה ומסונן מראש.

- קצה כזה (`abut`) מקבל **אוברהנג שלילי**: נגמר אינץ' אחד **בתוך**
  הקיר מהפאה החשופה (`[1.0, th/2].min - th`). זה לא קישוט — בפאה
  הרחוקה כל האיב בלט מעבר לקיר כסטאב פתוח.
- הקצה הזה מקבל `gable_flags[i] = true` (בלי פאשיה/סופיט/קורות) אבל
  **לא** נכנס ל-`gables` — שום דבר שגייבל *בונה* לא צומח שם.
- מהירות 0 בשלד → הגג מטפס אל הקיר כמו סככה.
- `framed_plan` מדולג כשיש abut (האוברפריימינג לא מכיר גג שמת בקיר).
- **`build_end_caps!`** — לוח אנכי שסוגר את קצה האיב איפה שהוא נכנס
  לקיר. בצבע הטרים, נמשך עד `k = 0` (הפאה החיצונית של הפאשיה), ותחתיתו
  עולה עם הסופיט המשופע.

### 5. פינות נקיות בסופיט משופע
בפינות הרייק נשארו שאריות. שתי קולות, **שתיהן רק כשהשיפוע דולק**:
- `build_rake_beams!(clear:)` — הלוקאאוט הנמוך ביותר בכל פינה יורד
  (`clear = spec[:h] + 0.02` במקום 0.02).
- `build_rake_soffit!(sloped:)` — המדרגה והמשולש בפינה לא נבנים.

---

## ⚠️ מה פתוח — הדבר הבא

**הפרש גובה בפינת הרייק.** בכל פינה שבה מתחיל האיב של הגייבל, סוף
האיב המאונך וסוף האיב של הגייבל לא באותו גובה (התמונות שלו,
26.08). הוא סימן את זה באדום בכל ארבע הפינות.

**מה כבר נוסה ונכשל — אל תחזור על זה:** לסגור את פינות הגייבל עם
אותו `build_end_caps!` שסוגר את הקיר. זה החזיר בדיוק את הצורה
שהורדנו בסעיף 5, והוא אמר "רק הרסת". הקוד הוחזר לאחור והתגובה
מתועדת בהערה מעל `build_end_caps!` ב-`build_roof!`.

**הכיוון שלא נבדק עדיין:** למדוד את שני הגבהים במספרים לפני שנוגעים.
הלוח המשופע יוצא מ-`z_bot = band_top - fascia_depth` ועולה ב-`rise`;
הסופיט של הרייק (`rake_soffit_quad`) מוחזק **מאוזן** לרוחב הלוח
בכוונה. שני החישובים לא מדברים זה עם זה — שם ההפרש, ושם צריך
להתחיל. סקריפט מדידה, לא ניחוש.

**איך לבדוק את זה בענן בלי להטריד אותו:** הסטאב בונה גג אמיתי.
לרנדר אלבציה של פינת הגייבל ל-SVG (להקרין y,z של הפאות שם, למיין
לפי x), להמיר ל-PNG עם cairosvg, ולהסתכל. ככה מצאתי היום מה קורה
בפינה בלי לבזבז לו סבב.

---

## מוקשים שעדיין בתוקף

- `roof_manager.rb` הוא **CRLF**, `roof_dialog.rb` ו-`toolbar.rb` הם LF.
  עריכה עם סקריפט שמחפש מחרוזות חייבת להתאים, אחרת העוגן לא יימצא.
  (העבודה שלי היום: להמיר ל-LF, לערוך, ולהחזיר ל-CRLF לפני המסירה.)
- `X = {...} unless const_defined?(:X)` **לא** נטען מחדש ב-`reload!`.
  לכן `roof_setting_keys`, `soffit_textures`, `beam_spec` הן **מתודות**.
- כל פאה עוברת `add_ring!` / rescue — raise אחד מפיל קצה שלם.
- הסטאב בענן זורק על נקודות כפולות כמו סקצ'אפ אמיתי. לא לרכך.
- שינוי HTML בדיאלוג → **לסגור ולפתוח את החלון**, לא רק `reload!`.
- כפתור חדש בסרגל → **הפעלה מחדש של סקצ'אפ**.

## איך לעבוד עם דרור

- עברית פשוטה, **משפט-שניים, כמו לילד בן 5**. הוא ביקש את זה במפורש.
- כל פקודה בקופסה נפרדת עם לאן (Ruby Console / CMD).
- git רק דרך CMD שלו. הוא לא כותב קוד — אתה עורך, הוא מדביק ובודק.
- **תמדוד לפני שאתה משנה, ותסתכל על התוצאה בענן לפני שאתה מוסר לו.**
  היום בזבזתי לו שלושה סבבים על פינה אחת כי ניחשתי במקום למדוד.
- צעד אחד, קטן. הוא אמר: "כשלוקח יותר מדי זמן זה לא יוצא טוב".
- אם לא ברור מה הוא רואה — לבקש תמונה מסומנת. הוא מצייר באדום.
- הוא אוהב שרטוט לפני בנייה. שני שרטוטים היום חסכו בנייה מיותרת.
- **commit אחרי כל סבב שעובד**, דרך CMD שלו.

## TODO

- הפרש הגובה בפינת הרייק (למעלה) — **הדבר הבא**.
- גייבל קטן מול גייבל גדול: הגדול נשבר (הוא הראה תמונה 26.08, לא נגענו).
- גאטרס — הוא הזכיר שירצה. הפאשיה כבר נשמרת פנויה בשבילם.
- מ-`CLAUDE.md`: קירות מעוגלים, תוכניות 2D ל-PDF, NOTE מצויר, צבע
  בגיליון, לוגו per-installation, corner boards.
