# מסירה לצ'אט הבא — Interior Pro, סבב 2026-08-13 (תוכנת התוכניות)

## 0. לפני הכל

התיקייה החיה (PC):
`C:\Users\rordt\AppData\Roaming\SketchUp\SketchUp 2024\SketchUp\Plugins\interior_pro`

סדר קריאה: **הקובץ הזה** ← `CLAUDE.md` ← `HANDOFF_2026-08-12D.md`.
לא לסמוך על סיכומים — לאמת מול הקוד החי.

### בדיקות
```
cd tests
"C:\Program Files\Git\bin\bash.exe" run_all.sh ..
```
אין Ruby אצל המשתמש — **מריצים בענן**. חדשים הסבב הזה: **rt39–rt45**.

### כללי עבודה — נאכפו שוב, ובחריפות

1. עברית, משפט-שניים, כמו לילד בן חמש.
2. כל פקודה בקופסה נפרדת + לאן (Ruby Console / CMD).
3. **לא מחליטים כלום בלי אישור שלו.** מביאים אפשרויות → הוא בוחר.
4. העוזר לא מריץ git — נותן פקודות CMD.
5. **דבר ויזואלי = לא מנחשים. מודדים.**
6. **"כלום לא השתנה" = הקוד לא נטען.**
7. **אל תבקש ממנו לקרוא פלט ארוך.** בסבב הזה זה הפך לשיטה: `debug_sheet.rb`
   כותב דוח לקובץ, והעוזר קורא אותו בעצמו מהתיקייה. **זו הדרך. אל תחזור
   לבקש ממנו להעתיק שורות.**
8. סקריפט בדיקה לא מעתיק את הקוד שהוא בודק — הוא קורא לו.
9. **חדש: הוא לא מוצא קבצים ב-Desktop** (יש OneDrive, שתי תיקיות Desktop).
   פלט לבדיקה — כתוב לתיקיית התוסף, לא לשולחן העבודה.

---

## 1. מה נבנה בסבב הזה — תוכנת התוכניות עומדת

הסדר שנקבע ב-12D מומש במלואו: שכבה אמצעית → דף אחד ל-PDF → יצרני שכבות.

### א. `plan_doc.rb` — מסמך השרטוט (rt39, 68 בדיקות)

רובי טהור, **בלי שום API של סקצ'אפ**, ולכן נבדק כולו בענן.

```
Document
  canvases  → קנבס במידות אמיתיות של הבית (אינצ'ים)
    layers → shapes: line / polyline / polygon / hatch / text / table / image
  pages     → דף נייר, באינצ'ים של נייר
    views   → חלון שמראה קנבס בקנה מידה. הסקאל שייך לחלון, לא לדף.
    layers → shapes שנמצאים ישר על הנייר (טייטל-בלוק, טבלאות)
```

- קואורדינטות נייר: אינצ'ים, ראשית בפינה **שמאלית תחתונה**, Y למעלה.
- `PAGE_SIZES`: ARCH D / ARCH C / TABLOID / LETTER (המשתמש ביקש ארבעה
  ולהוסיף בהמשך). `page_size` מקבל גם `[w, h]` ידני.
- `SCALES`: כל 16, מ-12" עד 1/128" — בדיוק הרשימה מ-Rayon.
- `View#fit_scale` **מדווח בלבד**. הוא לא משנה שום דבר לבד.
- `build_title_block!` — הטייטל-בלוק של המשתמש: כתובת מעל סקאל משמאל,
  מספר+שם דף ותאריך באמצע, VISUALIZE by Dror מימין. דף בלי חלון = `N.T.S.`

### ב. `plan_canvas.rb` — התוכנית נכנסת למסמך (rt40, 46 בדיקות)

**הרעיון המרכזי, אל תשבור אותו:** אין תוכנית שנייה. `plan_generator.rb` הוא
שמצייר. `plan_canvas` נותן לו **Recorder** — אובייקט שמתחזה ל-`Sketchup::Entities`
ורושם צורות במקום לבנות גיאומטריה. אז מה שהמשתמש בודק בסקצ'אפ הוא בדיוק מה
שיוצא ל-PDF.

- ה-Recorder מיישם רק את מה ש-plan_generator באמת קורא לו:
  `add_edges`, `add_face`, `add_circle`, `add_group` (+ `add_3d_text`,
  `bounds`, `transform!`).
- `RecTextGroup` עוקב אחרי הסיבוב וההזזה ש-`add_text` עושה, ולכן התוויות
  נוחתות במקום הנכון.
- שכבות: WALLS / DOORS / WINDOWS / DIMENSIONS / ROOMS / SKETCH.
- `layout_pages!(doc, st)` — בונה **את כל הדפים מחדש** מתוך בחירות המשתמש.
  זול, ולכן החלון קורא לו על כל קליק ואף פעם לא נפרד מה-PDF.
- `export_pdf!(path, opts)` — הצינור המלא בשורה אחת.

### ג. `plan_pdf.rb` — מנוע ה-PDF

רובי טהור, בלי ג'מים, **בלי לאיאוט ובלי Pro**. PDF 1.4, שלושה פונטי
Helvetica בסיסיים (בלי הטמעה). טבלת רוחבי תווים בפנים — בלעדיה כל תווית
הייתה יוצאת לא ממורכזת. תומך: קו, פוליגון עם מילוי, טקסט מסובב, טבלה,
מסגרת לתמונה. חיתוך (clip) לפי החלון.

### ד. `plan_sheet_dialog.rb` — חלון הדף (rt41, 30 בדיקות)

מה שהמשתמש ביקש: לראות לפני שממירים, כמו בכל תוכנה.

- בורר דפים, גודל דף, לרוחב/לאורך, קנה מידה, צ'קבוקס לכל שכבה,
  שדות טייטל-בלוק, Export PDF.
- **גרירה** = מזיז את התוכנית. **גלגלת** = זום. **Shift+גרירה** = מזיז את הדף.
- כפתור כחול "התאם את התוכנית לדף" — הוא לוחץ, ולכן **מותר** לו לשנות סקאל.
- **פתיחה ראשונה על מודל** (אין מיקום שמור) = מתאים לבד. מרגע שהוא בחר —
  הבחירה שלו נשמרת על המודל (`InteriorPro/sheet_state`).
- `show` מאפס `@doc` — קורא את המודל מחדש בכל פתיחה. זה היה באג: דלת שהוא
  הוסיף לא הופיעה.
- אם המיקום השמור מוציא את התוכנית לגמרי מהדף — חוזר לאמצע לבד.

### ה. `plan_tables.rb` — טבלאות דלתות וחלונות (rt42, 40 בדיקות)

המספרים של `plan_generator` (אותם תגים, אותו `fmt_feet`, אותו
`door_type_label`); הקובץ הזה רק מחליט איפה הן יושבות על הנייר.
**ברירת מחדל: דף נפרד** (A-102 SCHEDULES, גדולות פי 1.7). צ'קבוקס
"טבלאות בדף נפרד" מחזיר אותן לפינה של דף התוכנית, ואז חלון התוכנית מצטמצם.

### ו. כפתורים במקום תפריט (rt43, 18 בדיקות)

- כפתור בסרגל "Interior Pro 2D" עם אייקון `icons/sheet_pdf.svg`.
- כפתור **📐 תוכניות** בשורה העליונה של עורך ה-2D → `open_sheet`.
- פריט תפריט Extensions נשאר כגיבוי.

### ז. `plan_geometry.rb` — גיאומטריה שנבנתה ביד (rt46, 40 בדיקות)

**למה זה קיים:** למשתמש יש באק-יארד שהוא בנה ביד בסקצ'אפ, והוא רוצה להוציא
ממנו תוכניות **עכשיו**, הרבה לפני שיהיו כלים לספסלים וריצוף. הוא שאל אם זה
יפגע בעתיד. התשובה: לא, כי זה נכנס כשכבה נפרדת בשם **SITE**.

- הוא מסמן בסקצ'אפ ולוחץ "הוסף את מה שנבחר בסקצ'אפ" בחלון הדף.
- הקוד **קורא בלבד**. לא פותח undo, לא מוסיף כלום למודל.
- משטח לרצפה, מוריד קווים כפולים (1/16"), מדלג על קווים רכים, נכנס לקבוצות
  מקוננות, ומדלג על מה שהתוסף כבר מצייר לבד (`OWN_TYPES`).
- **החלטה חשובה (2026-08-13):** לא שומרים מזהים ומחפשים אותם אחר כך.
  ניסינו — `find_entity_by_persistent_id` החזיר **0 מתוך 4632** על המודל
  האמיתי שלו. עכשיו הכפתור לוקח את הקווים **ברגע הלחיצה**, ושומר את הקווים
  עצמם ב-`site_lines` על המודל. זה **צילום מצב** — שינה את הבאק-יארד, ילחץ שוב.
- הכפתור **מדווח**: נבחרו / קווים במודל / נוספו / על הדף, ואם משהו התאפס הוא
  אומר איפה. אל תחזיר כפתור שקט.
- מה שזה **לא** נותן: מידע. קווים בלבד. בלי טבלאות, בלי מידות שמתעדכנות.

**עדיין לא נבדק אצל המשתמש** — הגרסה עם הצילום נשלחה ולא הספיק לנסות.
זה הדבר הראשון לבדוק בסבב הבא.

### ח. reload בלי לסגור את סקצ'אפ (rt44, 16 בדיקות)

`InteriorPro.reload!` היה טוען רק את רשימת הקבצים הישנה, ולכן קובץ חדש לגמרי
לא נטען אף פעם. עכשיו הוא **טוען את `main.rb` עצמו** ואז מרענן את שלושת
הסרגלים הקטנים (לכל אחד יש שומר אורך, אז זה idempotent).
**התפריט לא מרוענן בכוונה** — סקצ'אפ לא יודע להסיר פריט תפריט.
פריט חדש: Extensions ← Interior Pro ← **Reload Interior Pro**.
פריט תפריט חדש = עדיין צריך הפעלה מחדש. שום דבר אחר לא.

---

## 2. הבאג של הסבב — `Length` הופך למחרוזת ב-JSON (rt45)

**הכי חשוב בקובץ הזה.** החלון נפתח ריק, וכל קנה מידה אמר "לא נכנס".

סקצ'אפ לא מחזיר מספרים. `Point3d#x` ו-`get_attribute` מחזירים `Length`.
ב-Ruby הוא מתנהג כמו Float ולכן הכל נראה תקין — אבל `JSON.generate` הפך אותו
למחרוזת `"~ -60' 1 13/16\""`, וה-JavaScript חיבר מילים. יצא `NaN`, אף סקאל
לא "נכנס", והמיקום היה NaN — דף ריק.

התיקון, בשלוש נקודות:
- `PlanDoc::Layer#numbers!` — כל מפתח מדידה עובר `to_f`. נקרא מ-`add` ומ-
  `Recorder#flush_into`.
- `Canvas#bounds` מחזיר Floats.
- `RecTextGroup#transform!` ו-`Recorder#add_circle` ממירים במפורש.

`rt45` מזייף מחלקת `FakeLength` שמתנהגת בדיוק ככה, ונופל עם הקוד הישן.
**כלל להמשך: כל מספר שיוצא מסקצ'אפ ונוסע ל-JS חייב `to_f` מפורש.**

---

## 3. גיט

branch: `door-stabilize`. המשתמש התבקש לעשות commit אחרי כל צעד — **לוודא שרץ.**
הודעת ה-commit האחרונה:
`"a SketchUp Length turns into a string in JSON and made the sheet blank - force plain numbers (rt45)"`

---

## 4. הצעד הבא

**קודם כל: לוודא שהגיאומטריה החופשית עובדת אצלו.** זה נשלח ולא נבדק.
אם השורה מתחת לכפתורים מראה `נוספו 0` — לקרוא את המספרים שהיא נותנת,
לא לנחש.

הוא אמר שהוא רוצה להתחיל להשתמש בתוכנה לעבודות אמיתיות כבר עכשיו, כולל
תמונות רינדור ומידות לעבודות קיימות. שני הדברים שחסרים לו לשם כך:

1. **תמונות אמיתיות בדף** — רינדור מלומיון. היום `plan_pdf` מצייר רק מסגרת
   ריקה במקום תמונה. צריך JPEG/PNG אמיתי ב-PDF (XObject).
2. **מידות והערות ידניות על הדף** — למתוח קו מידה ולכתוב טקסט איפה שרוצים.
   בלי זה גיאומטריה חופשית היא רק ציור.

ואז, לפי מה שהוצג לו קודם:

3. **הטייטל-בלוק האמיתי שלו** — הוא ישלח תמונה, נצייר אותו בקוד, לוגו כתמונה.
4. **עוד דפים** — A-101, A-102, A-103... עם רשימת דפים ובחירת תוכן לכל דף.
   (התשתית קיימת: `layout_pages!` כבר בונה שני דפים.)
5. **עוד יצרני שכבות** — אריחים, חשמל, ריהוט, צמחייה. כל אחד = צ'קבוקס.
6. **מנוע LayOut** — הצינור הוכח ב-`debug_layout_spike.rb`, לא חובר.
7. **אליוויישנים** — הוא הזכיר אותם כיעד; לא נגענו.

---

## 5. פתוח / לא לאבד

### מהסבב הזה
- טקסט של טבלה בתצוגה קטן מ-1.5px לא מצויר. ב-PDF תמיד מצויר.
- טבלת רוחבי התווים ב-`plan_pdf` היא של Helvetica רגיל; מודגש מוכפל
  ב-`BOLD_BUMP = 1.06`. קירוב.
- `draw_schedules` ו-`draw_legend_and_title` של `plan_generator` **לא** נקראים
  מ-`plan_canvas` — הטבלאות נבנות בנייר. המקרא (legend) עדיין לא על הדף.
- אין עדיין תמונות אמיתיות ב-PDF (רק מסגרת). הלוגו מצויר כטקסט.
- `plan_geometry` שומר את הקווים כ-JSON על המודל. בבית גדול זה יכול להיות
  מאות קילובייטים. לא נמדד בפועל.
- `PlanGeometry.entities_for` נשאר בקוד כדרך משנית. **הוא לא עובד על המודל
  של המשתמש** — אל תסמוך עליו.

### מוקפא: מסגרות פינה בסיידינג — פינה כ-L אחד
התוכנית מוכנה ונמדדה, לא מומשה. פרטים מלאים ב-`HANDOFF_2026-08-12D.md` §4.
**לא לגעת בגג, בדלתות, בחלונות ובסיידינג.**

### שאר הפתוחים (מ-12D, עדיין פתוחים)
- הגג לא מתעדכן לבד אחרי עיקום קיר.
- קיר מעוגל ב-`plan_generator` — עדיין קו ישר. **זה יגיע גם לדף.**
- butt joint פנים-אל-קשת — עדיין כורדה.
- `WindowManager.update_window` מתעלם מ-`arc_sag`.
- דלת בקיר מעוגל שיושבת בתוך הבית — **סגור. לא לשאול על זה שוב.**

---

## 6. דגלי חירום

```ruby
InteriorPro::WallTool::USE_CURVED_WALLS = false
InteriorPro::WallTool::USE_3D_SIDING = false
InteriorPro::WallTool::USE_NATIVE_OPENINGS = false
InteriorPro::WallTool::RESEAT_OPENINGS_ON_CURVE = false
InteriorPro::WallTool::DRAW_SNAP_ORTHO_ONLY = true
```

לאפס את זיכרון הדף אם הוא מתנהג מוזר (Ruby Console, שורה אחת):
```ruby
Sketchup.active_model.delete_attribute('InteriorPro','sheet_state'); InteriorPro::PlanSheetDialog.show
```

## 7. כלי debug בשורש

- `debug_sheet.rb` — **חדש והכי שימושי.** כותב `sheet_report.txt` עם כל מה
  שהחלון מקבל: גבולות, שכבות, סוגי צורות, ה-payload עצמו. המשתמש מריץ שורה
  אחת, העוזר קורא את הקובץ.
  `load File.join(InteriorPro::PLUGIN_DIR, 'debug_sheet.rb')`
- `debug_layout_spike.rb` — מוכיח את צינור הלאיאוט מקצה לקצה.

## 8. הוכחת טעינה

```ruby
InteriorPro.reload!; puts(defined?(InteriorPro::PlanSheetDialog) && defined?(InteriorPro::PlanTables) ? "NEW CODE LOADED" : "OLD CODE - restart SketchUp")
```
