# Interior Pro — מסירה 2026-09-08 (הפינה העקומה, והקו האלכסוני שנמצא ברגע האחרון)

קרא את זה קודם, ואז `CLAUDE.md`. הקודמת: `HANDOFF_2026-09-07.md`.
**אל תסמוך על הסיכום — תאמת מול הקוד החי.**

בדיקות: `cd tests` ואז `LANG=C.UTF-8 bash run_all.sh ..` — הכל ירוק,
rt51/rt52 אדומות מהסיבה הישנה. חדשות היום: rt123, rt124, rt125.

## מה נסגר היום

### 1. פרישת רעפים אחת ב-Edit/Move של גגון, אחרי החתך (rt123)
`remove_dormer!(g, no_relay: true)` — Edit פרש על גג בלי גגון ואז חתך
בלי לספר; Move פרש פעמיים. עכשיו פעם אחת בסוף. מחיקה רגילה לא השתנתה.

### 2. הגג חזר לצורה — הקיר היה עקום, לא הקוד
הוא שלח "שברת לי את הגג" עם גיבלים. נמדד: **הקיר הצפוני נגמר ב-
y=520.104 במקום 520.0**. `rectilinear?` עם RECT_TOL=0.05 נכשל →
strip-gable fallback → כל השבירות. חמש גרסאות git נבדקו בסטאב — זהות.
**המקור:** `[WallTool] closed a corner that missed by 0.104"` — Apply
מעורך ה-2D סגר פינה בקו עקום (בלוג שלו). `debug_fix_skew_corner.rb`
יישר (מתכון WallStretchTool), הגג התפרק. **AUTO_WELD_ENDS שווה בדיקה
בצ'אט הבא — הוא זה שסוגר פינות כאלה בקו עקום במקום ליישר.**

### 3. המשולש נעצר על פינות הקירות (rt124)
במסלול המפורק `lo,hi` של קיר-הגיבל היו אינסוף; עכשיו נחתכים
**מקצוות המלבן הבעלים (rch)** באוברהנג אחד מכל צד — לא מקצוות הצלע,
כי שרשרת של קיר מגשר רצה מעבר לצלע שלו (הניסיון הראשון שבר rt17/rt86).

### 4. דופן לוואלי (rt125) + קצוות רכים
ארבעת קצוות הצלילה של אגף לתוך ההורה היו פתוחים (פאה אחת). עכשיו
`wing_side_face!` רושם כל קצה צלילה שנשמר (`st[:valleys]`),
ו-build_roof! סוגר עליהם רצועה כמו של ההיקף, עם soft+smooth שלא
יצויר קו. אגף tucked לא רושם כלום.

## הבאג הפתוח — נמצא ברגע האחרון, זה הצעד הבא

**"הקו האלכסוני" שהוא צילם שוב ושוב הוא לא הוואלי. הוא רצועת slab-edge
שגויה.** נמדד בסטאב (dbg_faces_at.rb, tests/) על הבית שלו המיושר:

    (866,395,262.08) (866,129.5,205.00) (866,129.5,205.54) (866,395,262.62)

רצועת סגירה על הצלע המערבית שנבנתה **בקטע ישר אחד מהפינה הדרומית
(129.5, גובה איבס) עד נקודת הרכס (395)** — מדלגת על נקודת השבירה
**(866,258)** שבה הפרופיל האמיתי עובר מאיבס שטוח לרייק עולה. התוצאה:
קו אלכסוני על כל הצד המערבי + טריז חלול מעל הרצועה עד הפרופיל האמיתי
(הירוק שהוא צבע: "הדפנה הזאת חלולה").

איפה לחפור: `build_slab_edge!` (~שורה 2441) קורא
`edge_profile_chain(poly, i, zmap)` על צלע המתאר המערבית
(129.5→532). ה-zmap כן מכיל את (866,258) (שני המדרונות שומרים אותו).
למה השרשרת מדלגת עליו — זו השאלה. יש שם גם קצה 0.54" ב-(866,258)
ורצועה כפולה חופפת — סימני אותה שרשרת שבורה.
כשזה יתוקן: הרצועה תלך 129.5→258 שטוח ואז 258→395 עולה — הקו נעלם,
הטריז נסגר, **והפשיה האלכסונית תיפגש עם האופקית** (החוק שלו) כמעט
מעצמה. אחרי זה לבדוק אם ה-soft+smooth של סעיף 4 בכלל עוד נחוץ.

## נשאר פתוח (סדר שלו)
1. **הקו האלכסוני** = הרצועה השגויה למעלה. קודם.
2. רייק נפגש עם פשיה בפינת המפרץ (אולי נסגר מאליו עם 1).
3. איבס הגג הקטן תואם לשאר + המשולש לא צבוע
   ("gable wall top: no wall materials found" גם על קירות Stucco —
   לבדוק את wall_side_material_names מול framed).
4. רידג' קאפ: ההגדרה השמורה false; להחזיר —
   `build_roof!(replace: r, ridge_cap: true)`.
5. מהמסירה הקודמת: צבע חלון גגון, סופיט משופע, גאטרס, rt51/52.

## קבצי מדידה של היום (gitignored, בשורש/tests)
debug_relay (ספירת פרישות), debug_gable_break (קצוות פתוחים),
debug_wall_gap, debug_roof_shape (כל פאה+קואורדינטות),
debug_house_dump (הקירות וההגדרות — ממנו משחזרים את הבית שלו בסטאב),
debug_fix_skew_corner (חד-פעמי, שינה מודל), debug_long_edges,
debug_pick_part, tests/debug_wing_open + tests/dbg_faces_at (פאות
הצלע המערבית — שם רואים את הרצועה השגויה), tests/debug_roofdiff
(בונה את הבית שלו עם כל גרסת roof_manager להשוואה).

## איך לעבוד איתו — מה שעלה היום
- שוב: "שברת" ≠ הקוד נשבר. פעמיים היום הסיבה הייתה נתונים (קיר עקום,
  הגדרה כבויה) והקוד היה זהה בין גרסאות. diff בין גרסאות git בסטאב
  הכריע בדקות.
- הודעת ביניים בקובץ חי = מסוכן. הוא עשה reload באמצע פאץ' דו-שלבי
  וקיבל גרסה שבורה. לא משאירים את הקובץ שבור בין הודעות, ואומרים
  במפורש "מוכן ל-reload".
- כל בית משוחזר בסטאב קיר-קיר מ-debug_house_dump — ואז משווים גרסאות
  בלי לגעת בסקצ'אפ שלו. זה מה שסגר את היום.
