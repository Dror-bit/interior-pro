# מסירה לצ'אט הבא — Interior Pro, סבב 2026-08-12

## 0. לפני הכל
התיקייה החיה (PC):
`C:\Users\rordt\AppData\Roaming\SketchUp\SketchUp 2024\SketchUp\Plugins\interior_pro`

סדר קריאה: **הקובץ הזה** ← `CLAUDE.md` (עודכן היום — יש בו את כלל
ההלחמה החדש) ← `HANDOFF_2026-08-11.md`.

### בדיקות
```
cd tests
"C:\Program Files\Git\bin\bash.exe" run_all.sh ..
```
אין Ruby במחשב של המשתמש — ההרצה האמיתית אצל העוזר בענן.
מצב נוכחי: **32 JS + 31 רובי, ALL GREEN** (t33, t34, rt30, rt31 חדשים).

### כללי עבודה (המשתמש אכף אותם שוב היום, בתקיפות)
1. עברית, משפט-שניים, כמו לילד בן חמש. לא להאריך.
2. כל פקודה בקופסה נפרדת עם ציון לאן (Ruby Console / CMD).
3. העוזר עורך קבצים בעצמו (לא Cursor) ומעביר עם device_commit.
4. העוזר לא מריץ git בכלל — נותן פקודות CMD, אחת לשורה.
5. **דבר ויזואלי = לא מנחשים.** מרנדרים סימולציה בענן (matplotlib) או
   מבקשים צילום/trace. זה מה שסגר את הפינות אחרי שלושה ניחושים כושלים.
6. **"שום דבר לא השתנה" = כנראה הקוד לא נטען.** פעמיים היום התיקון "לא
   עבד" כי סקצ'אפ החזיק קוד ישן. `fix_corners_once.rb` נבנה בדיוק בשביל
   זה — הוא טוען, מוכיח שהקוד נטען, מתקן ומוודא. להשתמש בתבנית הזאת.
7. המערכת חייבת להישאר פשוטה למשתמש — בלי כפתורים חדשים בלי צורך.

## 1. מה נסגר היום (הכל בקומיט `curved corners: always weld seam...`)

### סימן הקשת בעורך 2D
פלוס = החוצה מהבית. `uiSagToModel/modelSagToUi` ב-plan_editor.rb (מינוס
פנימי — הצד השמור הפוך מהאינטואיציה). t33 שומר.

### פינות קשת-קיר — כלל ההלחמה (הפיצ'ר המרכזי של היום)
- **פינה עם קשת תמיד מולחמת (`weld_corner!`), לעולם לא miter.** ה-miter
  מודד היסטים מהקו הישר ועל קשת זה עיקם את הקיר / יצר חוד 64".
- הקיר הישר שומר אורך מלא + חיתוך ישר; הקשת נצמדת אליו: חיתוכים כמעט
  זהים = הצמדה מדויקת; גופים בצדדים הפוכים = "כתף" קטנה שמכסה את קצה
  הקיר. חצי-עיגול מדויק (מקבילים) נתפס גם הוא.
- rt31 שומר על שני הענפים + על "אף פינה לא בורחת יותר מ-2 עוביים".
- העורך שולח fp עם ה-overrides + מסמן קשת לפי הקשת (לא הקיר הישן),
  והכחול (pending) מולחם באותו כלל (`weldPendingEnds`, t34).
- מקרה קטן ידוע שהתקבל: קשת פנימה בזווית שטוחה משאירה שן קטנה בחוץ.
- `fix_corners_once.rb` בשורש — שורת קונסול אחת: reload + הוכחת טעינה +
  join מחדש + דוח פר-פינה. המשתמש אישר: 2D ו-3D מעולה.

## 2. הצעד הבא — גג מעל קיר מעוגל (המשתמש ביקש, לפי הסדר שקבע)

מה שמופה היום מהקוד החי לפני שנעצרנו:

- `RoofManager.eave_polygon` (roof_manager.rb:174): בונה את מתאר הגג
  מ-centerlines ישרים (`RoomManager.centerline` = כורדה, מתעלם מ-sag),
  גרף ← לולאה ← `outer_offset` (חצי עובי + overhang החוצה).
- `build_roof!` (roof_manager.rb:654): flat בונה ישר מהפוליגון;
  hip/gable עוברים דרך `straight_skeleton(poly, speeds)` ← `roof_cells`.
- לכן קשת = קו ישר בגג היום.

### התוכנית המוצעת (מדורגת, כל שלב נסגר לבד)
1. **rt חדש + עזרי קשת ב-eave_polygon**: זיהוי קיר מעוגל בלולאה; חישוב
   קשת ה-eave = הקשת הקונצנטרית של ה-centerline מוסטת החוצה
   (חצי עובי + overhang, סימן לפי צד הבליטה, `ArcMath.offset`).
   הפינות: חיתוך מעגל-קו מול קווי ההיסט של השכנים הישרים (מתמטיקה
   סגורה, בלי ניחושים) ← דגימת הקשת בין שתי זוויות החיתוך.
   כל פאה מקבלת את wall_id של הקשת. טסט טהור על המספרים של החדרים
   מהסבב הזה (יש ב-rt31 את הגאומטריות המוכנות).
2. **גג שטוח** מעל קיר מעוגל — הצרכן הפשוט: build_flat_geometry! כבר
   מקבל פוליגון כלשהו. לבדוק בסקצ'אפ, commit.
3. **Hip** — ה-straight_skeleton יקבל את פאות הקשת כרצף צלעות קצרות
   (מניפת משטחים, מראה חרוט מפוסל). לבדוק ביצועים/יציבות; יש תקרת
   MAX_SEGMENTS ב-ArcMath — לשקול tol גס יותר לגג (כמו הסיידינג,
   SIDING_CURVE_TOL=1.5).
4. **Gable על קיר מעוגל = סירוב מנומס** עם הודעה, כמו קיר-עם-פתחים
   שמסרב להתעקם. לא לבנות.
5. לרנדר את התוצאה בענן (matplotlib) לפני שמטריחים את המשתמש.

### פתוח מעבר לזה (לא להתחיל לפני שהגג נסגר)
- הפקת תוכניות 2D ל-PDF (עדיפות הבאה של המשתמש).
- זווית בין שני קירות מסומנים בעורך 2D — המשתמש ביקש לראות ולערוך;
  נשאלה שאלה (מי זז כשמשנים זווית) וטרם נענתה. לשאול שוב קצר.
- קיר מעוגל ב-plan_generator (תוכנית 2D) — עדיין כורדה.
- מסגרות פינה בסיידינג — "למקסם" (מה-11.8), L אחד במקום שני קרשים.
- butt joint של קיר פנים אל קשת — עדיין לפי הכורדה.

## 3. git
branch: `door-stabilize`. הקומיט של היום עבר. לוודא push.

## 4. דגלי חירום
```ruby
InteriorPro::WallTool::USE_CURVED_WALLS = false   # קירות חוזרים ישרים
InteriorPro::WallTool::USE_3D_SIDING = false      # סיידינג חוזר שטוח
```
