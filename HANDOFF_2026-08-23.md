# Interior Pro — מסירה 2026-08-23 (רעף שטוח: נסגר; לשון קבורה בגיבל: פתוח)

קרא את זה קודם, ואז `CLAUDE.md`. הקודמת: `HANDOFF_2026-08-21C.md`.
**אל תסמוך על הסיכום — תאמת מול הקוד החי.**

**הבדיקות: 133 ירוקות** (rt83 חדשה). מריצים בענן (אין לו רובי):
`cd tests` ואז `bash run_all.sh ..` — **חובה `LANG=C.UTF-8`**.

> **git:** לא ידוע אילו מהקומיטים של הסבב רצו אצלו. `git status` דרך CMD שלו
> (לא בעצמך!) לפני הכל.

---

## 0. שלושת הלקחים שעלו הכי ביוקר

1. **הסטאב סלחני, SketchUp לא — פעם שלישית.** פינת רעף שיושבת בדיוק על קו
   חיתוך מייצרת נקודה כפולה; הסטאב בלע, SketchUp זרק את הפאה בשקט, וכל
   הרעפים החתוכים לאורך ההיפים לא נבנו. התיקון: `RoofTileMath.clean_poly`
   על **כל** פוליגון חתוך, ובדיקות נקודה-כפולה/מישוריות ב-rt83. כל קוד חדש
   שבונה פאות — להריץ גם מול החוקים האלה, לא רק מול הסטאב.
2. **פאה קו-מישורית עם הדק נבלעת (z-fighting).** רעף שטוח שנח על הדק לא
   נראה — SketchUp מצייר את הדק. נפתר מבנית: הרעף הוא טריז שנוגע בדק רק
   בקו, לא בפאה.
3. **לחתוך לפי המתאר בלבד.** רעף שנחתך קודם אופקית לפי גובה קו-האמצע שלו
   (v_spans_at) השאיר מדרגות חשופות לאורך הוואלי (נמדד: פער 6.2"). היום
   המלבן המלא של הרעף נחתך על המתאר — אופקי ברכס, אלכסוני בהיפ/ואלי — 
   `clip_to_poly`, כולל בדיקת הכלה (מרכז התוצאה חייב להיות בפנים —
   בלעדיה עמודת רעפי רפאים חזרה "שלמה" מחוץ לגג).

## 1. מה נסגר — הרעף השטוח (slate, "Flat Tile") כולו

- **טריז 13×13, 0.7" עובי** (`RoofTileParts.flat_tile`, 4 פאות): שיפוע ישר
  מהאף לזנב, האף יושב על הרעף שמתחתיו, הראש על הדק — כלום לא נערם.
- **סטאגר** חצי רעף כל שורה שנייה — `flat_slots` הולך שורות-בחוץ
  (עמודות-בחוץ לא יכול להסיט), `course_phase` סוף סוף בשימוש.
- **נחתך באיב** (overhang 0), **נחתך במדויק** על היפ/ואלי/רייק: רעף גבול =
  אותו טריז חתוך (`build_flat_cut!`), ממשיך עד הקו ומראה את העובי בחתך.
  שני צדי ואלי נפגשים על הקו (נמדד ברובי: 0.2‑0.35", רק מרווח הפוגה).
- **קאפ**: cap_w 6.9, **cap_crown 1.0** (המספר שלו, ירד פעמיים: 2.622→2→1),
  setback 0 (הרעפים נחתכים על הקו — נסיגה תפתח פער). קצה מתכת: בלי חוד,
  גרונג + חצאית (cap_flat ב-`build_ridge_caps!`).
- **מישור קבור לא מקבל רעפים** (`drop_covered_planes`, דגימה מרובת נקודות —
  מרכז בלבד פסל שני מישורים מצטלבים הדדית), ורעפים נחתכים על קו-החיתוך
  התלת-ממדי עם מישור שמעליהם (`cover_clips`, שער: כל נקודה קבורה).
- מונה: הגג שלו ~1,600 מופעים של הגדרה אחת + ~160 חתוכים. ספניש/רומן/סים
  לא זזו — יש בדיקות שנופלות אם יזוזו.

## 2. הפתוח היחיד: הלשון הקבורה בגיבל

**התופעה:** כנף גיבל מעל השיפוע הראשי. בהיפ — הוואלי מפצל את המעטפת,
הלשון הקבורה היא פאה נפרדת ו-`drop_buried_faces!` מוחקת אותה. עובד.
בגיבל — אין ואלי, הפאה הראשית נשארת אחת, הלשון שלה רצה מתחת לגג הכנף
ורואים אותה דרך הגיבל הפתוח. הרעפים עליה כבר נחתכים; **הדק עצמו נשאר**.

**מה נוסה אחרון (לא אושר ב-SketchUp):** "הסכין" — `buried_split_segments`
מחשב את קטע קו-החיתוך של שני המישורים (נכון מתמטית, rt83 מצמיד: קצוות על
שני המישורים, תחום לפי המישור המכסה), `drop_buried_faces!` מוסיף
`entities.add_line` על המעטפת כדי ש-SketchUp יפצל את הפאה, ואז המחיקה
הרגילה. **הצעד הבא: לבדוק ב-SketchUp במצב גיבל.** אם לא עובד — לבדוק אם
ה-add_line בכלל מפצל (אולי הקצוות לא נוחתים בדיוק על הפאה בגלל עובי
המעטפת ±0.5 — ה-info נבנה מהפאות עצמן אז אמור להיות מדויק).

**הוא שוקל לוותר** — "זה לא כל-כך מפריע כי גם ככה לא באמת רואים אותה".
אם מוותרים: הקוד שנוסף לא מזיק (ירוק בכל הבדיקות), אפשר להשאיר.

## 3. איפה מה

- `roof_tile_math.rb` — טבלת slate (run_flat), clip_to_poly / clean_poly /
  poly_contains? / clip_band, runs?/run_flat?.
- `roof_tile_parts.rb` — flat_tile (הטריז), build_flat_cut! (טריז חתוך),
  flat_joint, soften_run? (שטוח לא מרוכך).
- `roof_tile_place.rb` — flat_slots (שורות-בחוץ, מתאר חותך הכל),
  drop_covered_planes, cover_clips + solve3, flat_valley_clips (קיים,
  לא מוזן כרגע — הוואלי נפתר בחיתוך, לא בפס).
- `roof_manager.rb` — drop_buried_faces! + buried_split_segments (הסכין),
  face_cover_info, cap_flat (קצה/חצאית/ואלי-שטוח), טקסטורת slate 52×52.
- `tests/rt83.rb` — הכל מוצמד שם, כולל חוקי SketchUp הקפדניים.

## 4. איך הוא רוצה לעבוד (חשוב, זה התפוצץ הסבב)

- עברית, משפט-שניים, כמו לילד בן 5. קופסה נפרדת לכל פקודה (Ruby Console /
  CMD). בלי git בעצמך.
- **בדיוק מה שביקש.** מספר שלא ביקש = באג גם אם "יותר נכון".
- **למדוד ברובי לפני שהוא פותח SketchUp** — הוא ביקש את זה במפורש אחרי
  שבזבזנו סבבים. לבנות את הסצנה בסטאב, למדוד את הדבר הנכון (מרחק לקצוות,
  לא לקודקודים!), ורק אז למסור.
- סבב = תיקון אחד. לא לערבב.
