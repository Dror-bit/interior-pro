# Interior Pro — מסירה 2026-09-12B (חלונות, קצות גג, סיידינג על המשולש)

קרא את זה קודם, ואז `CLAUDE.md`. הקודמת: `HANDOFF_2026-09-12.md`.
**אל תסמוך על הסיכום — תאמת מול הקוד החי.**

בדיקות: `cd tests` ואז `LANG=C.UTF-8 bash run_all.sh ..` — **184 ירוקות**.
אדומות קבועות: rt51/rt52/rt60 (ישנות, לא קשורות).

## 1. כל חלון נפתח כולו (rt26 עדיין ירוק)

11 חלונות בשבעה קבצים קיבלו את מנגנון `fit_height` של הגג והדורמר: הדף
מודד את `scrollHeight` ומודיע ל-Ruby, ו-Ruby עושה `set_size` עם חסמי
min/max. הוספתי גם `ResizeObserver` על ה-body, כך שפאנל שנפתח בתוך החלון
מגדיל אותו מיד.

קירות · חלון + עריכת חלון + חלון דורמר · דלתות (דרך `resize_to_fit`
שכבר היה שם) · רצפה · קרניז · גדר · וחמשת החלונות של `ui_dialogs`.

**לא נגעתי:** `plan_editor.rb` ו-`plan_sheet_dialog.rb`. נמדד — הם
`height:100vh` עם קנבס במסך מלא, לא טפסים; המנגנון הזה היה מקטין להם
את הבד.

## 2. Hip על צלע בודדת באמת עובד (rt135)

נמדד (`roof_end_report.txt`): שני גגות בסגנון gable, 7 מתוך 8 קירות
`end=hip`, והגגות בכל זאת גמלונים. הסיבה: היפ היה רק *היעדר* סימון
גמלון, ו-`assemble_framed_plan` בסגנון Gable מגמלן את הקצוות בעצמו כשהוא
לא מוצא סימונים.

עכשיו יש `roof_hip_wall_ids` — סימון מפורש. הגמלון האוטומטי מדלג על צלע
שסומנה Hip (`hip_side` ב-`assemble_framed_plan`, ו-reject ב-fallback של
`pick_gable_edges`). סימון גמלון שהמשתמש עשה בעצמו עדיין מנצח.

## 3. Shed הוא הקצה הרביעי, הכפתור ירד (rt136)

נמדד: קיר `817ec169` היה ברשימת shed **וגם** gable **וגם** dutch בו-זמנית,
ואף בחירה לא תפסה. שתי דרכים לסמן את אותו קיר — בדיוק מה ש-CLAUDE.md אוסר.

- תפריט Roof Ends: Hip / Gable / Dutch gable / **Shed (low eave)**.
- `roof_end_of` שואל shed ראשון (הוא הבעלים של כל הגג).
- בחירת Shed מנקה gable/dutch/hip מאותו קיר ובונה עם `style: 'shed'`.
- בחירת אחד מהשלושה על איב של שד מורידה את הסימון **וגם את הסגנון**
  (`style_override`) — המלכודת שהוא נתקע בה: הסימון הלך, הסגנון נשאר shed.
- הכפתור Shed Roof ירד מהסרגל. `roof_shed_tool.rb` נשאר על הדיסק ועובד.

## 4. הסיידינג התלת-מימדי היה קומה גבוה מדי (rt138) — **הבאג הכי חשוב כאן**

נמדד (`gable_siding_report.txt`): קיר בגובה 96" על בסיס 106" — גבולות
`z 106..308`. 308 = 202 + 106.

`group.bounds` הוא בקואורדינטות של **ההורה**; כל קרש נבנה בקואורדינטות של
**הקבוצה עצמה**. בקומת קרקע שני המספרים זהים וזה ישב בקוד מאז שנולדו
קומות. `WallTool.siding_z_range(min, max, origin_z)` + `local_z_range` הם
הכלל עכשיו, ושני הבנאים קוראים אותו.

חצי שני של אותו באג: הקרשים הישנים הם חלק מהקבוצה, אז המדידה כללה אותם
וכל בנייה מחדש הגדילה את הקיר שוב. `clear_siding!(group)` רץ **לפני**
המדידה.

`fix_siding_now.rb` בשורש = שורה אחת שמחזירה את הקרשים למקום על מודל קיים.

## 5. המשולש מעל הקיר הוא המשך של הקיר (rt137)

נמדד (`gable_wall_mat_report.txt`): החומר `"Board and Batten"` במודל היה
`NO TEXTURE, rgb(0,0,0)` — אין `board_and_batten.jpg`, אז
`load_or_create_material` ייצר חומר ריק שחור. ובכל המשולשים היו **אפס**
תת-קבוצות של קרשים.

- `RoofManager::SIDING_3D_NAMES` — Board and Batten ו-Horizontal Siding
  אינם טקסטורות אלא קרשים אמיתיים.
- `paint_gable_top!` צובע אותם **לבן**, בדיוק כמו שהקיר צובע את פאתו.
  אותו דבר ב-`paint_mitred_wall!` (פינת שד — לבן, עדיין בלי קרשים).
- `build_gable_top_siding!` בונה את הקרשים. הכל נמדד מהמתאר של הפאה
  החיצונית עצמה (`tz_top_at`), אז זה עובד גם לפריזמה וגם לפינה ממותרת.
- בטנים מיושרים ל**מקור של הקיר שמתחת** (`corners_xy[6..7]` בעולם), לא
  לקצה של המשולש — אחרת הרצועות קופצות הצידה בקו האיב.
- סיידינג אופקי נחתך לרייק במדרגות של שורה (`gable_course_runs`), ואף
  קרש לא עולה מעל הגג.

### הקריסה, ולמה — לקרוא לפני שנוגעים כאן שוב

הגרסה הראשונה **הפילה את סקצ'אפ** בלחיצה על Apply. `debug_apply_probe.rb`
בנה את הגגות פעמיים ורשם כל צעד לדיסק עם flush:

```
pass1: roof #0 DONE          ← בלי הקרשים
pass2: roof #0 start ...     ← איתם, ומת כאן
```

הסיבה: קרשים שכנים באותה שורה, במקום שבו הרייק חותך אותם, נוגעים פאה
בפאה על אותו מישור בדיוק. סקצ'אפ ממזג פאות חופפות, ו-pushpull לתוך
המיזוג הזה מפיל את התוכנה. הקיר עצמו לא נתקל בזה כי השורות שלו חתוכות
על ידי חלונות ולא מונחות זו לצד זו.

**הכלל שנשאר: כל קרש בקבוצה משלו.** קבוצה היא החומה של סקצ'אפ עצמו בין
שני גופים. אל תאחד אותם חזרה "כדי לחסוך קבוצות".

דגל כיבוי: `InteriorPro::RoofManager::USE_GABLE_SIDING = false`.

## פתוחים
- קרשים על פינת שד ממותרת (mitred) — יש לבן, אין קרשים.
- 9 החלונות: נסגר. הפלאן והגיליון עדיין בגובה קבוע במכוון.
- rt60 — טיימר גיבוי, נופל בקביעות. לא גגות.
- הגרעין על הספניש, וההולנדי במסלול השלד — ראה מסירות קודמות.

## קבצי מדידה שנוצרו היום (בשורש)
`debug_roof_end.rb` · `debug_gable_wall_mat.rb` · `debug_gable_siding.rb` ·
`debug_apply_probe.rb` · `fix_siding_now.rb`
