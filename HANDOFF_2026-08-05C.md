# מסירה — Interior Pro, סבב 2026-08-05C (ליטוש גגות: גמלונים פתוחים, רייק, חדירות)

## 0. לפני הכל
התיקייה החיה היחידה:
`C:\Users\rordt\AppData\Roaming\SketchUp\SketchUp 2024\SketchUp\Plugins\interior_pro`
סדר קריאה: **הקובץ הזה** ← `HANDOFF_2026-08-05B.md` ← `HANDOFF_2026-08-05.md` ← `STATUS_handoff.md`.
כללי העבודה לא השתנו. בדיקות: `cd tests && bash run_all.sh ..`
(שים לב: להריץ מתוך tests עם `..` — הרצה עם `.` מהשורש לא מעתיקה את הקבצים!)
git מהענן חסום (אי אפשר למחוק index.lock) — commits דרך CMD של המשתמש,
עם `del .git\index.lock` לפני.

## 1. מה נעשה בסבב (הכל roof_manager.rb + tests/rt15.rb, אושר ויזואלית)
1. **אין יותר משולש גמלון לבן.** קצות גמלון נשארים פתוחים (החלטת משתמש:
   הקיר עצמו יעלה בהמשך). `framed_tri!` רק רושם גבהים ל-zmap;
   `build_gable_wall_face!` לא נקרא (נשמר לצורך revert).
2. **רייק בפרופיל מלא** (מסלול framed): קרש אחד לכל *מישור* קצה-גמלון
   (מזוהה לפי קו, dedup) — איבס←קודקוד←איבס — גם כשמישור הקצה מכוסה
   חלקית ע"י כנף והצלע החיצונית מכסה רק חלק מהפרופיל. שרשרת על הקו
   האינסופי + מעטפת עליונה. ב-fallback נשאר רייק פר-צלע חתוך.
3. **הרייק נחתך היכן שגג אחר מתחיל**: `framed_cover_z` (גובה משטחי שאר
   המלבנים בנקודה) + `rake_visible_runs` (דגימה כל 6" + ביסקציה) —
   הקרש נבנה רק בקטעים שמעל הגגות האחרים.
4. **כנף שחוצה את רכס ההורה** (הפה שלה משני צדי הרכס): הצלילה שלה כולה
   מתחת למשטח ההורה = זבל פנימי + חפיפה קופלנרית מהבהבת. עכשיו
   `wing_side_face!` בודק את משולש החדירה מול `framed_cover_z`: גלוי →
   קוואד מקורי (ואלי אמיתי, כמו הכנף הימנית); מוסתר → חיתוך בקו הפה.

## 2. רגרסיות חדשות ב-rt15 (הבית הרביעי של המשתמש, h4 — 10 פינות)
פרופיל חתוך מטפס (248.66), פרופיל מלא מגיע לקודקוד (265.31) וחוצה את
כל המישור, רייק עם cover נעצר ברכס הכנף (y=742.94), קצה חשוף לא מושפע,
midpoints של צלעות לא צוללים מתחת למשטח הראשי, טיפ הוואלי של הכנף
הרגילה נשאר ((607.52, 258.84)). הכל ירוק: 30 JS + 15 רובי.

## 3. פתוח / הבא בתור
- המשתמש רוצה **פשיה גם בקצוות גמלון** — סופק כ-rake; קרש אופקי בקו
  האיבס לא נדרש (נשאל ונענה: rake בלבד).
- מגבלה ידועה: שני גמלונים על אותו קו אינסופי (שתי כנפיים סימטריות) —
  המעטפת העליונה תגשר ביניהם. לא קיים בבתים הנוכחיים.
- חוב מהסבבים הקודמים: Dutch Gable, חומרים אמיתיים לגג, gable walls
  אמיתיים (הקיר עולה למשולש — זה המתוכנן במקום המשולש הלבן), תקרה
  עליונה קומה 2, מיטר Merge Wall, אייקונים.
- debug_roof_edges.rb (gitignored) — סקריפט אבחון: מתאר, plan, edges.

## 4. קבצים שהשתנו
roof_manager.rb, tests/rt15.rb (+ הקובץ הזה).
