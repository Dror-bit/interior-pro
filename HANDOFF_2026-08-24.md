# Interior Pro — מסירה 2026-08-24 (סופיט: עובד, נשאר קו אלכסוני אחד)

קרא את זה קודם, ואז `CLAUDE.md`. הקודמת: `HANDOFF_2026-08-23C.md`.
**אל תסמוך על הסיכום — תאמת מול הקוד החי.**

בדיקות: בענן בלבד (למשתמש אין רובי): `cd tests` ואז `LANG=C.UTF-8 bash run_all.sh ..`
במצב השמור: **134 ירוקות** (rt84 חדש). להעתיק לענן גם `icons/` ו-`textures/`
(בלי `textures/floors/`), אחרת rt43/rt73 נופלים בכזב.
git: ענף `door-stabilize`. ה-commit האחרון של המשתמש: `304af7a` (המסירה
הקודמת). **כל העבודה של היום עוד לא בקומיט** — זה הדבר הראשון לסגור אם
המצב על המסך טוב, או git checkout לקבצים אם רוצים לחזור אחורה.

## מה נבנה היום — סופיט (תחתית לאיבס), `soffit: 'boxed'`

המשתמש ביקש 5 סוגי תחתיות: אופקית סגורה, קורות גלויות, עץ, סטאקו שטוח,
ספרדי (סטאקו + קורות עגולות). התחלנו רק את הראשונה. ברירת מחדל `'none'`
= בדיוק ההתנהגות הישנה. הכל ב-`roof_manager.rb`, טסטים ב-`tests/rt84.rb`.

מה עובד ואושר ויזואלית:
- לוח אופקי סוגר את האיב מהקיר עד הפאשיה (`soffit_band` + `build_band!`).
- לוח משופע סוגר את הרייק של הגייבל (`build_rake_soffit!`).
- בפינה: "box return" — הלוח נשאר ישר לאורך overhang אחד ואז מדרגה של
  90° (`rake_soffit_segments`, `rake_soffit_step`), לא חיתוך 45°.
  המשתמש שלח תמונת ייחוס ואישר את הצורה.
- משולש סוגר את צד הקופסה מתחת לרייק (`rake_return_skirt`).

## הבאג הפתוח: קו אלכסוני על התחתית ליד הפינה

המשתמש רואה קו אלכסוני (תפר מיטר של `offset_polygon` בפינת הסופיט
השטוח, ואולי גם תפרים של הרייק) ורוצה שייעלם. שלושה סבבים של ריכוך
(soft+smooth) לא סגרו את זה עד הסוף:
1. `soften_seams!` לפי קווי תפר ידועים — פספס כי פאות שנוספו אחר כך
   מפצלות קצוות. תוקן חלקית (`on_segment?` תופס חצאים).
2. `soften_flush_seams!` — הכלל הכללי: קצה שבין שתי פאות קו-מישוריות
   בתוך פאות הסופיט החדשות → soft+smooth. זה הקוד האחרון שנמסר.
**המשתמש בדק את הסבב האחרון: הקו עדיין שם. שלושת הניסיונות נכשלו.**
מסקנה כמעט ודאית: זה בכלל לא עניין של ריכוך — כנראה שתי הפאות לא
באמת קו-מישוריות (View > Hidden Geometry יגלה אם הריכוך תפס בכלל),
או שהקו שהוא רואה הוא בכלל קצה של פאה אחרת (סקרט/מדרגה/פאשיה).
צעד ראשון בסשן החדש: **למדוד, לא לרכך.** לבקש תמונה עם סימון, לכתוב
סקריפט דיבאג שמדפיס את כל הקצוות באזור הפינה (נקודות, פאות מכל צד,
נורמלים, soft?) ולקרוא את הפלט לפני כל שינוי. הסתרת פאות לא מסתירה
את הקצוות שלהן (לקח מ-2026-08-23) — יכול להיות שזה קצה יתום/גבול.

חשוד אם עוד רואים קו: ריכוך משנה רק תצוגה — אם שתי פאות לא בדיוק
קו-מישוריות (סטיית float), הקצה יישאר. למדוד עם `debug_soffit.rb`
(בשורש) או המתג `RoofManager.debug_soffit = true` (מדפיס סגמנטים).

## מוקשים שכבר דרכנו עליהם (לא לחזור)

- SketchUp זורק "Points are not planar" וגם "Duplicate points in array".
  raise אחד בתוך `build_rake_soffit!` מפיל את כל הקצה (rescue אחד סביב
  הלולאה). לכן: כל פאה עוברת `add_ring!` (מנקה כפולים ב-tolerance של
  1/1000", בולע כישלון של פאה בודדת). לא לעקוף את `add_ring!`.
- הסטאב בענן (`tests/sketchup_stub.rb`) הוחמר: `add_face` זורק על נקודות
  כפולות כמו SketchUp אמיתי. בזכות זה הבאג נתפס בענן. לא לרכך את זה.
- פינה ממוטרת על לוח ברוחב overhang בדיוק = משולש (שתי פינות מתלכדות).
- `rake_soffit_quad`: פינה שנמשכת לאורך רייק מטפס חייבת לעלות איתו.
- ריכוך תפרים חייב לרוץ **אחרי** שכל הפאות נבנו, אחרת פאה חדשה מפצלת
  קצה מרוכך והחצאים חוזרים קשים.

## איך לעבוד עם דרור (זה מה שהפיל את הסשן הזה)

- עברית פשוטה, משפט-שניים. כל פקודה בקופסה עם לאן (Ruby Console / CMD).
- git רק דרך CMD שלו. הוא לא כותב קוד — אתה עורך, הוא מדביק ובודק.
- צעד אחד, מדידה, תשובה שלו, ורק אז הצעד הבא. commit אחרי סבב שעובד.
- **לקרוא את ההודעות שלו לאט.** פעמיים בסשן הזה הוכרז "סיימנו" בטעות
  כשהוא עוד ראה בעיה — זה מה שהרס את האמון. אם לא ברור מה הוא רואה,
  לבקש תמונה עם סימון, לא להניח.
- לבדוק בסטאב בענן לפני מסירה. `debug_*.rb` בסוף סבב → `_to_delete/`.

## TODO אחרי שהקו נסגר

- commit של כל היום (roof_manager.rb, tests/rt84.rb, tests/sketchup_stub.rb,
  tests/run_all.sh; debug_soffit.rb ל-_to_delete/).
- להסיר את המתג `debug_soffit` ואת ההדפסה שלו (זמניים).
- המשתמש ביקש: מטל פלשינג (drip edge) גם בגייבל — לא התחלנו.
- רעיון שלו שנשמר: סופיט משופע במקביל לגג כאופציה נוספת.
- 4 סוגי הסופיט הנוספים.
- חיבור ל-RoofDialog (עכשיו רק דרך `build_roof!(soffit: 'boxed')`).
