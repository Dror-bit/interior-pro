# Interior Pro — מסירה 2026-09-13 (קרשים על הדורמר, ושני מספרים שקרו)

קרא את זה קודם, ואז `CLAUDE.md`. הקודמת: `HANDOFF_2026-09-12B.md`.
**אל תסמוך על הסיכום — תאמת מול הקוד החי.**

בדיקות: `cd tests` ואז `LANG=C.UTF-8 bash run_all.sh ..` — **192 ירוקות**.
אדומות קבועות: rt51/rt52/rt60 (ישנות, לא קשורות).
חדשות היום: rt139 · rt140 · rt141 · rt142 · rt143 · rt144 · rt145 · rt146.
כולן אומתו נגד הקוד הישן ונפלו שם.

## 1. קרשים על פינת שד ממותרת (rt137 הורחבה)

הפתוח מהמסירה הקודמת. `paint_mitred_wall!` רק צבע לבן — ההערה בקוד
אמרה במפורש "still to come". עכשיו הבנאי שומר את הפאה החיצונית
(`outer_f`) ומעביר אותה הלאה, ו-`build_gable_top_siding!` בונה עליה
בדיוק כמו על הפריזמה.

נמדד אצלו: שתי פינות שד, 24 פאות קרשים בכל אחת, z 106–137 מול משולש
שמגיע ל-140.46 — לא בורח מעל הגג.

**מלכודת שנתקלנו בה:** שתי הפינות שלו היו Stucco, ואז "אין קרשים" זו
התנהגות נכונה. צריך קיר עם Board and Batten או Horizontal Siding כדי
לראות את הבאג בכלל.

## 2. הדורמר היה שחור (rt140)

`house_wall_material` מחזיר את השם, ו-`paint_wall!` צבע לפי השם.
נמדד (`dormer_finish_report.txt`): החומר `"Board and Batten"` במודל הוא
`rgb(0,0,0)` בלי טקסטורה — אין `board_and_batten.jpg`, כי אלה קרשים ולא
תמונה שלהם. אותו באג של משולש הגמלון, קובץ אחד הלאה.

`DormerManager.siding_3d?` + פנים לבנות. **וגם:** שם החומר נכתב עכשיו
כתכונה `ext_name`/`int_name` על חלק הקיר, ו-`wall_names_of` קורא אותה
ראשונה — אחרת צביעה בלבן הייתה מוחקת את הידיעה מה הגגון לובש, ובניית
חלון הבאה הייתה משאירה אותו לבן לתמיד.

## 3. אפשר לבחור לגגון חומר משלו (rt141)

בפאנל יש קטע **Walls** עם **Outside finish**: "Same as the house" או כל
אחד משבעת חומרי הקיר (`DormerDialog.wall_materials` שואל את
`WallLibraryDialog::MATERIALS` בשם, כדי ששתי הרשימות לא ייפרדו).

הבחירה נשמרת בתכונה `wall_material` על הדורמר, `dormer_spec` מחזיר אותה
כ-`wall_names`, ו-`''` עדיין אומר "כמו הבית" — אז שום דורמר קיים לא
משתנה.

## 4. קרשים על שלושת קירות הדורמר (rt142, rt143)

`DormerManager.build_wall_siding!` לא כותב בנאי שני — הוא משאיל את
`RoofManager.build_gable_top_siding!`, שמודד הכל מהמתאר של הפאה עצמה.
`outer_skin` בוחר את הפאה **לפי מיקום ולא לפי נורמל** (אותה מלכודת
ש-`paint_wall!` מתעד: שתי הלחיים נבנות מאותו סדר נקודות ואחת יוצאת
מלופפת הפוך).

### שתי בעיות שנמדדו ותוקנו תוך כדי

**הקרשים נמשכו לתוך הבית.** הבנאי נכתב למשולש גמלון, שיושב **שטוח** על
הקיר, אז הוא התחיל כל קרש מה-z הכי נמוך של המתאר. לחי של דורמר יושבת
**על הגג** — הרצפה שלה משופעת. `RoofManager.tz_bot_at` (המראה של
`tz_top_at`) נותן את הרצפה בכל t, ו-`gable_course_runs` גם לא מניח שורה
מתחתיה. במתאר עם רצפה שטוחה זה אותו מספר, אז הגמלון לא זז.

**רגל מרובעת השאירה משולש אוויר.** הקרש נבנה עכשיו כפאה זקופה שנדחפת
החוצה (`BATTEN_DEPTH`), עם z נפרד לשני קצות הרגל — חיתוך אלכסוני על
שיפוע הגג, פלוס `BATTEN_FOOT_TUCK = 0.25` פנימה כדי שלא ייראה תפר. רק
כשיש אלכסון.

### החלון

הקרשים לא צריכים שיספרו להם איפה החלון. `punch_window!` משאיר את הפתח
כ**לולאה פנימית** בפאה החיצונית, ו-`face_holes_tz` קורא אותה. לכן
הקרשים של החזית נבנים **אחרי** `punch_window!` ולא בתוך `paint_wall!`
עם הלחיים. `subtract_span` ו-`batten_bands` הם החותכים, שניהם טהורים.

נמדד אצלו: 3 קרשים על החזית, האמצעי נחתך ל-10" מתחת לחלון ו-13" מעליו.

דגל כיבוי: `InteriorPro::DormerManager::USE_DORMER_SIDING = false`.

## 5. ההודעה על הרכס אמרה מספר שהיא עצמה סירבה לו (rt139)

הוא הקליד 25 וקיבל "הכי גבוה שנכנס זה 32". שני המשפטים נכונים בו-זמנית,
וזה לא הודעה. `ridge_cap_check` חישב את התקרה **בלי העקב** (עומק הפאסיה,
אצלו 8"). שלושה תיקונים:
- `h_max -= heel`.
- מעוגל **למטה** (`%d` עם `.floor`) — `%.0f` עיגל 36.6 ל-37, וההודעה
  הציעה גובה שהיא מסרבת לו.
- גגון **שטוח** חויב ב-`half * pitch` למרות שאין לו רכס. עכשיו `room`.

## 6. הגובה גדל בכל עריכה (rt144) — אותו באג של rt122, תכונה אחת הלאה

"הוא מראה לי 30 ואז שאני לוחץ APPLY הוא אומר לי לשנות את זה ל-22".
נמדד נגד הקוד הישן: 38 → 46 → 54 → 62, שמונה בכל סבב.

`build_dormer!` שמר את הגובה ה**בנוי** (מוקלד + עקב), הפאנל החזיר אותו
כמוקלד, והעקב נוסף שוב. עכשיו `frame` מחזיר `height_asked` והקבוצה
שומרת אותו — בדיוק כמו `length_asked` מ-2026-09-07. הגיאומטריה עדיין
משתמשת בגובה הבנוי; רק מה שנרשם השתנה.

## 7. גג שד לא יצא מ-shed (rt145)

"יצרתי גג שד מהתחלה וסימנתי צלע של הגג כדי להפוך אותה לגייבל והוא לא
משנה אותה". עד היום רק בחירת Hip/Gable/Dutch על **האיב של השד עצמו**
הורידה את הסגנון. סימון צלע אחרת נשמר, הסגנון נשאר `shed`, וגג שד
**הופך** כל סימון שהוא מקבל — אז הסימון החדש לא עשה כלום.

לשד יש איב אחד. בחירת סוג אחר בכל מקום עליו היא החלטה להפסיק להיות שד.
`set_roof_end!` שואל עכשיו את הסגנון של הגג **שבבעלותו הקיר** לפני שהוא
כותב משהו, ומשחרר רק את סימוני השד של אותו גג — לא של גג אחר.

## 8. הגג הנעלם מאחורה, וה"קפיצה" (rt146) — **הכי חשוב כאן**

נמדד מהקונסול שלו, לא מתמונה:

```
[Roof] wall 12e16034...: GABLE end (1 gable, 1 dutch)
[Roof] ignoring 1 stale/off-loop gable mark(s)
[Roof] shed over level 2: ...
```

הוא סימן קיר של **קומת הקרקע**. לאותה קומה לא היה גג, `roof_of_wall_id`
לא מצא בעלים, והנפילה-לאחור הישנה קראה ל-`build_roof!(style:)` — בלי
`replace:` ובלי `level:`. הקריאה הזאת **מוחקת את כל הגגות** ובונה אחד על
הקומה העליונה. זה גם הקפיצה שהוא ראה במקום אחר על המסך, גם הסיבה ששום
דבר לא השתנה איפה שהסתכל, וגם — זה מה שמחק לו את הגג של קומת הקרקע
("לפעמים הוא מחסיר לי את הגג מאחורה"). האזהרה הזאת יושבת ב-CLAUDE.md
מזמן; היא פשוט הייתה בתוך הקוד עצמו.

עכשיו: כשיש גגות שכן מחזיקים קירות, וקיר שאף גג לא מחזיק — **לא נבנה
כלום**, והוא מקבל הודעה. הסימון נשמר, אז הוא תופס ברגע שנבנה גג לאותה
קומה. מודל ישן שגגותיו לא מחזיקים קירות בכלל ממשיך לקבל את הבנייה
הכוללת, בדיוק כמו קודם.

**איך לעבוד עם זה:** מסמנים את הקיר של הקומה שהגג שלה מעניין אותך.
לקומת קרקע צריך גג משלה, שנבנה עם `level:` מהפאנל.

## פתוחים
- rt60 — טיימר גיבוי, נופל בקביעות. לא גגות.
- הגרעין על הספניש, וההולנדי במסלול השלד — ראה מסירות קודמות.
- קרשים על הגמלון של הדורמר עצמו (המשולש מעל החזית) — לא נבדק.
- `[Roof] build_tube!: undefined method 'erase_entity'` — הופיע פעמיים
  בקונסול שלו היום בזמן בניית הגג. לא נחקר. גם
  `[Roof] plan not decomposable - strip-gable fallback` באותה בנייה.
- כיוון השד: הוא דיווח ש"השד יוצא הצידה במקום לכיוון שבחרתי". חלק
  מהמקרים היו הגג הלא-נכון (סעיף 8). אם זה חוזר על גג שיש לו בעלים —
  `shed_low_edge` נופל לצלע הארוכה ביותר כשהסימון לא בתוך `loop_ids`,
  ושם צריך למדוד את `loop_ids` מול הסימון.

## קבצי מדידה שנוצרו היום (בשורש)
`debug_shed_boards.rb` · `debug_dormer_finish.rb` · `debug_dormer_boards.rb` ·
`debug_end_decide.rb` (מסמנים קיר ומריצים — אומר איזה גג בבעלותו ומה
הסגנון היה נדחף). `debug_roof_end.rb` הורחב: הוא מדפיס עכשיו גם את
`set_walls` ואת סימוני ה-HIP/SHED לכל גג.
