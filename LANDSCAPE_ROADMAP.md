# Landscape Pro — מפת דרכים: גדרות / חומות / ריילינג

נכתב 2026-08-17. מקור: רשימת הדרישות של המשתמש בצ'אט של 2026-08-17.
הסטטוס נבדק **מול הקוד החי**, לא מול סיכומים.

התיקייה החיה:
`C:\Users\rordt\AppData\Roaming\SketchUp\SketchUp 2024\SketchUp\Plugins\interior_pro`

**זה מסמך תכנון בלבד. שום שורת קוד לא נכתבה בעקבותיו.**
העבודה בפועל עברה לתוכניות (2D/PDF) ולגגות.

---

## 0. מה קיים היום (מאומת מול הקוד, 2026-08-17)

| נושא | מצב |
|---|---|
| גדר | `landscape/fence_ref_tool.rb`, `fence_tool.rb`, `fence_math.rb`, `fence_library.rb`, `fence_library_dialog.rb`. **ספרייה מלאה** — JSON, שמירה/מחיקה, 7 פריסטים, דיאלוג HTML |
| חומה | `landscape/garden_wall_tool.rb`. **אין ספרייה בכלל** — רק `UI.inputbox` עם 12 שדות. אין שמירה |
| ריילינג | **לא קיים כקטגוריה.** יש פריסט אחד של גדר בשם `Deck Railing` |
| שיפועים / מדרגות | **המתמטיקה כבר קיימת בגדר**: `fence_math.rb` תומך ב-`:rake` (משופע, טופ מקביל לשיפוע) ו-`:step` (כל פאנל ישר, מדרגה בכל עמוד). **לא מחובר לדיאלוג.** בחומה אין כלום — `ground_start`/`ground_end` נשמרים כאפס |
| תפריט לחיצה-ימנית | **אין בכל התוסף.** `add_context_menu_handler` לא מופיע באף קובץ |
| שערים | לא קיים |
| קשתות בגדר/חומה | לא קיים. `back_line_points` בחומה נכתב מראש כנקודת השינוי היחידה |

---

## 1. הסדר המומלץ

### 1. ספרייה מאוחדת — שלוש קטגוריות: חומות / גדרות / ריילינג
הבסיס. הכל תלוי בזה: גם שמירת חומות (כמו בקירות), גם ההחלפה בין סוגים.
היום הגדר לבד עם ספרייה; החומה בלי; הריילינג בכלל לא קטגוריה.

**מה צריך להיסגר לפני שנוגעים:** האם `fence_library.rb` מתרחב לשלוש
קטגוריות, או שנבנה `landscape_library.rb` חדש שהגדר עוברת אליו.

### 2. עריכה + החלפת סוג (חומה → גדר), בסגנון Profile Builder
לסמן אובייקט קיים → כפתור עריכה → לשנות פרמטרים או **להחליף קטגוריה**.
יושב ישר על סעיף 1 — בלי ספרייה מאוחדת אין למה להחליף.

### 3. גבהים: שיפוע / מדרגות
בגדר: **חיווט בלבד** — `:rake` / `:step` כבר עובדים ב-`fence_math.rb`,
צריך לחשוף אותם בדיאלוג ולמלא `ground_start` / `ground_end` אמיתיים.
בחומה: פורט של אותה שיטה. `ground_start`/`ground_end` כבר שמורים באותה
צורה בדיוק בשני הכלים — זה נעשה מראש כדי שאותו קוד ירים את שניהם.

### 4. קשתות, עיגולים, צורות אורגניות + פינות
בחומה: `back_line_points` הוא **המקום היחיד** שצריך להשתנות כדי שקו
האלווישן יעקוב אחרי הקשת. נכתב ככה בכוונה.
כדאי לבדוק מה `arc_math.rb` (של הקירות המעוקלים) כבר נותן בחינם.

### 5. שערים
לעבוד **בדיוק כמו דלת בקיר**: ריבוע רפאים → מניחים על הגדר → נחתך חור
בגדר → מושתל שער.
חייב לבוא אחרי 4, כי הגדר צריכה קודם לדעת להיחתך.

### 6. ריוורס-פייס בלחיצה ימנית
"עשיתי את הבול-נוז לצד הלא נכון" → קליק ימני → הופך צד.
קטן, עצמאי, אפשר להכניס בכל שלב.
**שים לב:** היום אין תפריט לחיצה-ימנית בכל התוסף — זו תשתית חדשה
(`UI.add_context_menu_handler`).
מבטל את הכלל הקיים "כדי להפוך — תצייר הפוך" (HANDOFF_2026-08-17B §2).

---

## 2. הקו הירוק בסרטוט

הרעיון: לצייר קו בעורך ה-2D (`plan_editor.rb`), לסמן אותו, וללחוץ כפתור
שהופך אותו לגדר / חומה / ריילינג.

**זה לא סעיף בפני עצמו — זו דרך כניסה לסעיפים 1+2.**
מתחבר יפה, אבל רק אחרי שהספרייה המאוחדת קיימת, אחרת אין למה להפוך אותו.
הגבהים שם מתחברים לסעיף 3, ובסוף לפלאגין הטופוגרפיה.

---

## 3. כללים שאסור לשבור בדרך

1. **חומה וגדר נשמרות תחת המילון `LandscapePro`, לא `InteriorPro`.**
   כל כלי הקירות (Edit/Move/Stretch/Split/Merge/Delete), `RoomManager`,
   `FloorManager`, `FoundationManager`, `MoldingManager`, `RoofManager`
   ו-`plan_generator` מוצאים קיר לפי בדיוק `'InteriorPro'` + `'wall'`.
   יש בדיקה ב-`rt64.rb` שנופלת אם מישהו ישבור את זה.
2. **ימין = יד ימין של הכיוון שצויר** (לחיצה 1 → לחיצה 2).
   הבול-נוז והבליטה של הקאפ שם; קו האלווישן בגב = שמאל.
3. **עמוד לעמוד ≤ 8 פיט, תמיד.** `NEVER_STRETCH = true`.
4. **החומה לא יורשת מ-`WallTool`.** הדבר היחיד שהיא לוקחת ממנו הוא
   `WallTool.apply_axis_magnet`.
5. אין כפילות ב-UI: לפני כפתור חדש, לבדוק אם כפתור קיים כבר עושה את זה.

---

## 4. פתוחים קטנים שנשארו מ-HANDOFF_2026-08-17B §4

- רשימת חומרי הגמר של החומה — היום `Stucco`, `Stack Stone`, `Block Wall`.
  המשתמש הזכיר "אבנים מיוחדות". **הרשימה המלאה עוד לא הגיעה.**
- טקסטורות חסרות: `textures/stack_stone.jpg`, `textures/block_wall.jpg`.
  בינתיים `FALLBACK_COLOR` נותן צבע זמני.
- אייקון חסר: `icons/garden_wall_tool.png`.
- דיאלוג HTML לחומה במקום `UI.inputbox` — 12 שדות זה על הגבול.
  ממילא ייבלע בסעיף 1.
- `setup_landscape_toolbar` — השומר עלה מ-`>= 1` ל-`>= 2`.
  **צריך להעלות אותו בכל כפתור חדש**, אחרת הכפתור לא מופיע.
