# Interior Pro — מסירה 2026-08-14B (ערב, סבב שני)

קרא את זה קודם, ואז `CLAUDE.md`. אל תסמוך על הסיכום — תאמת מול הקוד החי.
הקובץ הקודם: `HANDOFF_2026-08-14.md` (שם §0 כללי העבודה — לא השתנו, תקרא אותם).

---

## 1. מה נסגר בסבב הזה

### א. קיר פנים נעצר בקיר חוץ — גם בגרירה (t42)
הוא מתח קיר פנים בנקודה הירוקה והקיר נכנס לתוך קיר החוץ.
`clampToBoundary` היה קיים מ-03/08 אבל נקרא **רק** ממסלול הציור
(`currentEnd`). שוב "נכתב ולא נגיש".
- `dragLenLimit` חדש ב-`plan_editor.rb` — הופך אורך לנקודה, שואל את
  **אותו** `clampToBoundary`, מחזיר אורך. אין עותק שני של החוק.
- `clampToBoundary(a, bpt, whoWith)` — פרמטר שלישי: הקטגוריה של **הקיר**
  שזז, לא של הכלי ביד. בלי הפרמטר = ההתנהגות הישנה בדיוק.
- **בכוונה לא נחסם:** הקלדת אורך בתיבה. הוא בחר להשאיר ("הקלדה = הוראה
  מפורשת"). אל תוסיף חסימה שם בלי שהוא מבקש.
- אושר אצלו בסקצ'אפ: "עובד מעולה הקיר פנים נעצר בקיר חוץ".

### ב. תווית המעלות בזמן ציור אומרת אמת (t43)
הוא צייר בניין ב-179.7666°; התווית עיגלה ל-180 והוא האמין לה. התוצאה:
כל הבניין 0.2334° עקום, 2 אינץ' סחף על 41 רגל, וכל פינה 90.0000 מושלמת
(כי 90 זה **בין** קירות, לא מול ציר). הוא קרא לזה "טעות טרגית" — צדק.
- הוסר ה-`Math.round` מהתווית; עכשיו עשירית מעלה + `fmtAngle`.
- `isRoundBearing(deg)` חדש — אותו `ANGLE_ROUND` ואותו `ANGLE_TOL` של
  מעלות הפינות, משוקף לכל רבע. כיוון לא-עגול נצבע **אדום** (#e0392b).
- אושר אצלו: "עובד טוב הצבעים".

### ג. מגנט צירים בבניית קיר (rt55)
בחירה שלו: קיר שסוטה **פחות ממעלה אחת** מציר נוחת עליו בדיוק, אוטומטית,
בלי לשאול. חלון של מעלה אחת — לא 5 ולא 10 (נשאל ובחר).
- `apply_axis_magnet(start, end, model)` ב-`wall_tool.rb`, נקרא בראש
  `build_wall_group` — הדלת האחת שכל קיר עובר בה (כלי הקיר, כלי הקשת,
  Apply מהעורך). `AXIS_MAGNET_DEG = 1.0`.
- קצה שיושב על קצה קיר אחר = נעוץ ולא זז (`end_is_pinned?`,
  `AXIS_PIN_TOL = 1.0"`); שני קצוות נעוצים = לא נוגעים בקיר בכלל.
  15/22.5/30/45/60 לא נתפסים. rescue רחב — המגנט לעולם לא מפיל בנייה.

### ד. לחצן ימני על קיר: זווית מול הציר האדום (t44b)
לחצן ימני על קיר (אפור **או** כחול) בעורך → נבחר אוטומטית → תפריט עם
"זווית מול הציר האדום…" → prompt עם הזווית הנוכחית → הקיר מסתובב.
- בחירה שלו: המעלות **מול הציר האדום** (0=ימינה, 90=למעלה), לא מול שכן.
- הנקודה האדומה נשארת, הירוקה מסתובבת. Keep/Detach משפיע.
- מבין שני הכיוונים של אותו קו נבחר הקרוב לכיוון הנוכחי (לא מתהפך).
- אפור: callback חדש `set_wall_aim` ב-Ruby — ה-JS שולח **נקודת יעד**
  (לא מעלות), והרובי רק קורא ל-`stretch_wall!` עם aim. אין דיאלקט שני
  של חשבון זוויות. כחול: סיבוב מקומי + `histLocal`.
- `showMenu(px, py, extra)` — התפריט הקטן מקבל שורות נוספות.
- **טרם אושר אצלו בסקצ'אפ** — נמסר, הוא עוד לא דיווח.

### ה. שלושה סקריפטים חד-פעמיים בשורש (הרצות שהיו)
- `debug_axis.rb` — מודד זווית כל קיר מול הצירים → `axis_report.txt`.
- `straighten_axis.rb` — סובב את כל המודל שיישב על הצירים. **עבד אצלו.**
- `normalize_axis.rb` — ראה מלכודת §2. **עבד אצלו** (דוח: הכל אפסים).
- `debug_floors.rb` — מודד מה עדיין עקום → `floor_report.txt`.

---

## 2. המלכודת הגדולה של הסבב — טרנספורמציה חייבת להישאר Z בלבד

`straighten_axis.rb` הראשון סובב את ה**טרנספורמציות** של הקבוצות. על
המסך הכל התיישר — ואז הוא בנה רצפות והן יצאו עקומות כמו קודם.

הסיבה: התוסף כולו נשען על ההנחה (כתובה ב-`floor_manager.rb` שורה ~498)
ש-transformation של קיר הוא **הזזת Z בלבד**, ולכן local XY == world XY.
`start_x/y`, `corners_xy` נקראים בכל מקום כאילו הם world. סיבוב
בטרנספורמציה משאיר את המספרים במקום הישן — והרצפות נבנות מהמספרים.

התיקון: `normalize_axis.rb` מכניס את הסיבוב **פנימה** (לגאומטריה
ולמספרים) ומחזיר את הטרנספורמציה ל-Z בלבד, עם בדיקה עצמית ששום דבר לא
זז (הדוח שלו: worst 0.000000"). **כלל לצ'אט הבא: לעולם אל תשים סיבוב
בטרנספורמציה של קיר.**

ומדידה חשובה מהאירוע: אחרי יישור קירות החוץ, שני קירות **הפנים** נשארו
עקומים (0.2337° ו-0.0958° — `floor_report.txt`). הוא בחר לבנות מבנה
חדש במקום לתקן אותם אחד-אחד. אם הוא חוזר למודל הישן — זה עוד פתוח,
והלחצן הימני החדש (§1ד) הוא הכלי לזה.

### מלכודת קטנה: Keep joined הפתיע אותו
הוא האריך קיר והפינה נגררה. זה לא באג — כפתורי Keep joined / Detach ✂
בקיפול "פינות". הוא מצא ומרוצה. הוא **השני** שנתקל בזה — אם יעלה שוב,
לשקול ברירת מחדל או כפתור בולט יותר.

---

## 3. בדיקות

חדשות: **t42** (עצירה בקיר חוץ — כולל בדיקת נגישות דרך handleMove),
**t43** (תווית מעלות כנה + אדום), **t44b** (לחצן ימני — מריץ את
setWallAxisAngle באמת עם prompt מזויף), **rt55** (מגנט הצירים — על
המספרים האמיתיים שלו 179.7666/89.7666).

כולן נרשמו ב-`run_all.sh`. בענן: כל חבילות ה-JS ירוקות; ב-rt חלק נופל
בענן על קבצים חסרים בסביבה (rt7, rt18, rt26, rt43 — ידוע, סביבה ולא
קוד). ההרצה הקובעת: אצלו עם git-bash.

שים לב: `rt55.rb` ו-`t44b.js` בודקים גם **נגישות** (שהקריאה קיימת
בקוד החי), לא רק קיום. אל תוריד את החלקים האלה.

---

## 4. הצעד הבא

### לאמת אצלו (נמסר וטרם דווח)
- לחצן ימני על קיר → "זווית מול הציר האדום…" → 90 (§1ד).
- אחרי אישור: commit (הוא מריץ, אתה נותן CMD).

### לפי הסדר שהוא הציב (לא השתנה מהמסירה הקודמת)
1. **הטייטל-בלוק האמיתי שלו** — חסום עד שהוא שולח תמונה.
2. **עוד יצרני שכבות** — אריחים, חשמל, ריהוט, צמחייה.
3. **מנוע LayOut** — הצינור הוכח ב-`debug_layout_spike.rb`, לא חובר.
4. **אליוויישנים.**

### עוד פתוח (הוצע, לא הוחלט)
- הגדרת מעלות בפינה גם על קיר כחול (מהמסירה הקודמת; הלחצן הימני החדש
  עוקף חלקית — עובד על כחול מול הציר, לא מול שכן).
- מספר לבד = רגל ב-VCB בזמן ציור קיר.
- קיצור "התאם למסך".
- באג פתוח: `[Sheet] add_selection: no implicit conversion to
  Transformation` — עדיין בלי stack trace; ה-rescue כבר מדפיסים הכל,
  בפעם הבאה שזה קורה לבקש את שורות הקונסולה.
- שני קירות פנים עקומים במודל הישן (§2) — אם הוא חוזר אליו.
