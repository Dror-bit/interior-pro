# Interior Pro — מסירה 2026-08-23C (שני הקווים על השיפוע: תוקן ✓)

קרא את זה קודם, ואז `CLAUDE.md`. הקודמת: `HANDOFF_2026-08-23B.md`.
**אל תסמוך על הסיכום — תאמת מול הקוד החי.**

הבדיקות: מריצים בענן (למשתמש אין רובי): `cd tests` ואז `LANG=C.UTF-8 bash run_all.sh ..`
במצב השמור: **133 ירוקות.**
שים לב: `rt43` ו-`rt73` קוראים קבצים מהדיסק — כשמעתיקים את הקוד לענן צריך
להעתיק גם את `icons/` ואת `textures/` (בלי `textures/floors/`), אחרת ייפלו
בכזב.

---

## 1. מה נסגר — שני הקווים הדקים על השיפוע ✓

**האבחנה (נמדדה, לא נוחשה):** הקווים היו **שני קצוות חופשיים בלי אף פאה**
(faces=0, אורך 344", אחד ב-z=92.0 ואחד ב-92.5 — שתי שכבות הדק) לאורך קו
המפגש עם הכנף. מחיקת פאות הלשון הקבורה (התיקון של מסירה B) השאירה את
קצוות השפה שלהן תלויים באוויר, ו-SketchUp מצייר קצה בלי פאות תמיד.
דרך האבחון: הסתרת פאות בלבד לא העלימה כלום (הסתרת פאה לא מסתירה את
הקצוות שלה!); הסתרת 4 קצוות הסכין הקו-מישוריים — כלום; הסתרת שני הקצוות
החופשיים — הקווים נעלמו.

**התיקון (commit d2e1a42):** `drop_orphan_edges!` ב-`roof_manager.rb` —
אוספים את קצוות הפאות שנמחקות (הקבורות + התאומות התחתונות דרך פרמטר
שלישי אופציונלי ל-`drop_buried_twins!`), ואחרי המחיקה מוחקים כל קצה
שנשאר **בלי אף פאה**. קצה שעדיין מחזיק פאה חיה = גבול אמיתי, לא נוגעים.
בדיקות ב-`tests/rt83.rb` (סעיף THE ORPHAN EDGES). המשתמש אישר ויזואלית.

הערה: `drop_knife_leftovers!` (מחיקת קצוות קו-מישוריים) נכתב בסבב קודם,
לא פתר את מה שרואים, ובוטל סופית. לא להחזיר.

## 2. אין משימה פתוחה

דרור יגיד מה הבא. כיוונים אפשריים מ-`CLAUDE.md` (לא לבחור לבד):
גג מעל קיר מעוגל, NOTE בשרטוט, צבע בגיליון, לוגו פר-התקנה.

## 3. איך לעבוד איתו (קרא באמת — זה מה שמפיל סבבים)

- עברית. משפט-שניים, כמו לילד בן 5. כל פקודה בקופסה נפרדת עם לאן
  להדביק (Ruby Console / CMD). בלי git בעצמך — רק דרך CMD שלו
  (index.lock נתקע).
- **הוא לא עורך קבצים ולא כותב קוד.** אתה עורך את הקבצים (בענן ואז
  commit_files לתיקייה החיה); הוא רק מדביק קופסאות, מריץ
  `InteriorPro.reload!` ובודק ב-SketchUp. אל תשכח להגיד לו reload
  אחרי כל שינוי קובץ.
- **בדיוק מה שביקש.** מספר שלא ביקש לשנות = באג.
- **צעד אחד, מדידה אחת, ואז לחכות לתשובה שלו.** סבב שעובד נגמר
  ב-commit דרך CMD שלו לפני שנוגעים בדבר הבא.
- למדוד לפני שמשנים, ולהריץ את הסטאב בענן לפני שמוסרים לו.
- מדידת תצוגה: הסתרת פאה לא מסתירה את הקצוות שלה — להסתיר גם קצוות.
- סקריפטים ארוכים לא מדביקים בקונסול (נחתכים) — כותבים קובץ בתיקייה
  ו-`load` אותו. בסוף הסבב להעביר קבצי דיבאג ל-`_to_delete/`.
