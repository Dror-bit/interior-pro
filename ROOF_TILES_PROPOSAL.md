# Interior Pro — מערכת רעפים היברידית: הצעה לפני יישום

נכתב 2026-08-19 מול **הקוד החי**, אחרי מדידה של Vali/Instant Roof
(`valiroof_report.txt`) ואחרי ניסיון כושל אחד היום (§0). אין כאן שורת קוד —
זו התשובה לחמש השאלות ששאלת, ואז סדר צעדים.

---

## 0. מה כבר ניסינו היום, ולמה זה נכשל

בניתי מדרגה תלת-מימדית בכל שורת רעפים. אתה אמרת "ממש לא נראה טוב" וצדקת —
20 מדפים מרחפים במעלה מדרון 4:12.

הטעות לא הייתה בקוד אלא בקריאה של המדידה. בדוח של Vali כתוב שחור על לבן:

| מה | כמה עותקים | פאות לכל אחד | גודל |
|---|---|---|---|
| `va_BirdStop` | **408** | **1** | 5.1" × 0" × 1.9" |

408 עותקים של פאה אחת = **קו האיב בלבד**. מעל האיב הם שטוחים לגמרי.
הכלל "תלת-מימד רק איפה שרואים את הצללית" — הצללית של גג היא האיב, הרכס,
ההיפ והרייק. השדה נראה על רקע הגג עצמו, ושם הטקסטורה עושה את העבודה.

**זה בדיוק מה שהמסמך שלך אומר.** ההצעה למטה היא המסמך שלך, ממופה על הקוד
שכבר קיים.

---

## 1. אילו קבצים חדשים — שניים, לא יותר

### `roof_tile_parts.rb` — הגדרות הרעפים (ComponentDefinition)
בונה **פעם אחת למודל** ארבע הגדרות דלות-פאות ושומר אותן במטמון לפי
שם+מידה, כך שקריאה שנייה מחזירה את אותה הגדרה ולא בונה כלום:

| הגדרה | תפקיד | תקציב פאות |
|---|---|---|
| `IP_TileEave_<shape>` | רעף הקצה התחתון | 6–8 |
| `IP_TileRake_<shape>` | רעף גמלון/רייק | 6–8 |
| `IP_TileRidge_<shape>` | כובע רכס | 6–8 |
| `IP_TileHip_<shape>` | כובע היפ | 6–8 |

**סה"כ גיאומטריה ייחודית בכל המודל: ~30 פאות.** כל השאר עותקים.
הקובץ הזה לא יודע כלום על גגות — רק פרופיל → הגדרה.

### `roof_tile_place.rb` — ההנחה
מקבל את הקצוות המסווגים ואת מסגרת המישור, מחשב `Geom::Transformation`
לכל חריץ, וקורא `add_instance`. **אפס גיאומטריה בקובץ הזה.**
כאן גם יושבים התגים ובלם הכמות.

### מה **לא** נוגעים בו
`wall_tool.rb`, `door_tool.rb`, `window_tool.rb`, `plan_editor.rb` — אפס
שינוי. ב-`roof_manager.rb` שינוי אחד: הקריאה ל-`build_tile_courses!`
(הניסיון הכושל של היום) מוחלפת בקריאה ל-`RoofTilePlace`. `roof_tile_math.rb`
נשאר כמו שהוא — הוא כבר עונה על ספייסינג ועל מסגרת המישור.

---

## 2. איך נבחר פני הגג — לא נבחרים, אנחנו בנינו אותם

בתוספים אחרים המשתמש לוחץ על פאה. אצלנו לא צריך: `build_roof!` בונה את הגג
בעצמו, ומאז היום יש שם משתנה אחד שמחזיק **רק את הקליפה העליונה**:

```ruby
top_shell = dz > 0.001 ? (grp.entities.grep(Sketchup::Face) - shell_before)
                       : shell_before
```

זה בדיוק הסט שכובעי הרכס כבר משתמשים בו. מדוד על הגג שלך: 8 פאות בקבוצת
הגג, מהן 4 מישורי גג אמיתיים ו-4 פאות עזר (`tiles_report.txt`).
**אין צורך בבורר פאות בכלל.**

---

## 3. סיווג הקצוות — כמעט הכול כבר כתוב

| סוג קצה | מי מזהה אותו היום | מצב |
|---|---|---|
| **רכס + היפ** | `ridge_lines(faces)` — קטע xy ששתי פאות גג חולקות | **קיים ועובד מ-10.8** |
| היפ מזויף בקיר מקושת | `drop_facet_hips(lines, poly, wall_ids)` | **קיים** |
| **איב** | `eave_polygon(walls, overhang)` | **קיים** |
| **רייק / גמלון** | `gable_wall_ids` + `edge_profile_chain` → `gable_spans` | **קיים** — כבר מחשב איפה הפאסיה נעצרת והרייק מתחיל |

מה שחסר: **מקום אחד** שמחזיר את ארבעת הרשימות יחד. אני מציע
`RoofManager.roof_edges(grp, poly, wall_ids, zmap)` שמחזיר
`{ eave: [...], rake: [...], ridge: [...], hip: [...] }` — עטיפה דקה מעל
מה שכבר קיים, לא מנוע חדש. **אסור לכתוב מסווג שני**; שני מסווגים שלא
מסכימים ביניהם זו בדיוק המלכודת שתיקנתי היום בחלונות.

ההבחנה רכס↔היפ: קטע אופקי (`|za - zb| < 1"`) = רכס, קטע משופע = היפ.
`ridge_lines` כבר מחזיר `za`/`zb`, אז זו שורה אחת.

---

## 4. איך מחושב הכיוון של כל עותק — כבר בנוי ובדוק

`roof_tile_math.rb` בונה לכל מישור גג מערכת צירים משלו:

- `u` — **לרוחב** המדרון, אופקי, לאורך האיב
- `v` — **במעלה** המדרון, **באורך אמיתי** ולא היטל
- `n` — הנורמל, תמיד כלפי מעלה
- `v = 0` יושב על האיב

מדוד על הגג שלך (`tiles_report.txt`): ארבעת המישורים חזרו עם
`u.z = 0.0000` — כלומר `u` אופקי בדיוק, כנדרש.

הטרנספורמציה לעותק היא אז שורה אחת, בלי שום הנחה על X/Y/Z של העולם:

```ruby
Geom::Transformation.axes(origin_of_slot, u, v_or_n, ...)
```

וזה עונה על "must work from the roof's local coordinate system" שביקשת.
בדוק ב-`tests/rt72.rb`.

**ספייסינג:** `RoofTileMath.edge_slots(u1, u2, tile_w, margin:)` כבר מחזיר
את המרכזים של הרעפים **השלמים בלבד** בתוך הקטע — חצי רעף בקצה לא מקבל
עותק, בדיוק כמו שביקשת. הרוחב מגיע מטבלת `shapes` (`tile_w`), לא
מ-bounding box; אם להגדרה יש `coverage_w` באטריביוט הוא גובר.

---

## 5. איך נשאר קל — המספרים

זה החלק שבו הקוד הקיים **לא** עומד בדרישה שלך, ואני אומר את זה במפורש:

**`build_cap_piece!` בונה `grp.entities.add_group` לכל חתיכת כובע.**
כלומר על גג גדול מאות **קבוצות ייחודיות** — בדיוק ה-"thousands of unique
groups" שאתה אומר להימנע ממנו. זו הנקודה האמיתית לתקן.

| | היום | אחרי |
|---|---|---|
| כובעי רכס/היפ | ~250 קבוצות ייחודיות | הגדרה אחת + ~250 עותקים |
| רעפי איב | לא קיים | הגדרה אחת + ~225 עותקים |
| רעפי רייק | לא קיים | הגדרה אחת + ~40 עותקים |
| שדה | טקסטורה | טקסטורה (ללא שינוי) |
| **פאות ייחודיות בסך הכול** | אלפים | **~30** |

חישוב האיב לגג שלך: היקף 63+59 רגל ≈ 2,928", חלקי 13" לרעף ≈ **225 עותקים**.
זה אותו סדר גודל של 408 של Vali, על בית קטן יותר.

**תגים** — `InteriorPro.assign_tag(entity, name)` כבר קיים ב-`main.rb:211`.
נשתמש בו כמו שהוא:
`InteriorPro_RoofTiles_Edge` · `_Rake` · `_Ridge` · `_Hip`.

**בלם:** מעל תקציב עותקים הגג נשאר עם הטקסטורה בלבד ומדפיס למה — אותה
תבנית של `MAX_RIDGE_CAP_PIECES`.

---

## 6. סדר הצעדים — כל אחד נבדק ונשמר בנפרד

| # | מה | איך נדע שזה עובד |
|---|---|---|
| 1 | להוציא את `build_tile_courses!` של היום | הגג חוזר לשטוח+טקסטורה |
| 2 | `roof_tile_parts.rb` + `rt77` | 4 הגדרות, ≤8 פאות כל אחת, קריאה שנייה לא בונה כלום |
| 3 | `roof_edges` — העטיפה של הסיווג + `rt78` | איב/רייק/רכס/היפ נספרים נכון על גג היפ ועל גמלון |
| 4 | **רעפי איב בלבד** + תג | **אתה מסתכל ומצלם** |
| 5 | כובעי רכס+היפ → עותקים, מאחורי דגל | ספירת קבוצות יורדת, הגג נראה זהה |
| 6 | רעפי רייק | אתה מסתכל |

צעד 4 הוא הראשון שנראה. אני מציע לעצור שם ולצלם לפני שממשיכים.

---

## 7. שאלה אחת פתוחה לפני שמתחילים

בתמונות ששלחת רואים **רעף שלם** בולט מהקצה — לא כרטיס דק. ל-Vali יש
`va_BirdStop` בגודל 5.1" × 1.9" ובעובי **אפס** — כרטיס אחד, בלי נפח.

השאלה: רעף האיב אצלנו יהיה
**(א)** אף קצר — ~4" במעלה המדרון, 6 פאות, נראה כמו התמונות מזווית רגילה, או
**(ב)** רעף שלם — 14" במעלה המדרון, 8 פאות, נכון גם מזווית נמוכה מאוד?

ההפרש בעלות זניח (שניהם הגדרה אחת). ההפרש הוא איך זה נראה כשמסתכלים כמעט
מלמעלה. אני נוטה ל-(א) ומוסיף (ב) אחר כך אם יחסר.
